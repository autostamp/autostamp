// AUTOGENERATED by openapi-bindgen. Do not edit by hand.

use crate::runtime::{dispatch, AuthApply, AuthKind, FieldLocation, FieldSpec, OpSpec};
use crate::exports::autostamp::squareup::apple_pay as iface_apple_pay;
use serde_json::{Map, Value};

const OP_APPLE_PAY_REGISTER_DOMAIN: OpSpec = OpSpec {
    method: "POST",
    path_template: "/v2/apple-pay/domains",
    fields: &[
        FieldSpec { snake: "domain_name", wire: "domain_name", location: FieldLocation::Body },
    ],
    auth: &[
        AuthApply { secret_key: "oauth2", kind: AuthKind::Bearer },
    ],
};

fn iface_apple_pay__register_domain_response__to_json(p: &iface_apple_pay::RegisterDomainResponse) -> Value {
    let mut m = Map::new();
    m.insert("errors".into(), match (&p.errors) { Some(v) => Value::Array((v).iter().map(|v| iface_apple_pay__error__to_json(v)).collect()), None => Value::Null });
    m.insert("status".into(), match (&p.status) { Some(v) => Value::String((v).clone()), None => Value::Null });
    Value::Object(m)
}

fn iface_apple_pay__error__to_json(p: &iface_apple_pay::Error) -> Value {
    let mut m = Map::new();
    m.insert("category".into(), Value::String((&p.category).clone()));
    m.insert("code".into(), Value::String((&p.code).clone()));
    m.insert("detail".into(), match (&p.detail) { Some(v) => Value::String((v).clone()), None => Value::Null });
    m.insert("field".into(), match (&p.field) { Some(v) => Value::String((v).clone()), None => Value::Null });
    Value::Object(m)
}

fn iface_apple_pay__register_domain_params__to_json(p: &iface_apple_pay::RegisterDomainParams) -> Value {
    let mut m = Map::new();
    m.insert("domain_name".into(), Value::String((&p.domain_name).clone()));
    Value::Object(m)
}

fn iface_apple_pay__register_domain_response__from_json(v: &Value) -> Option<iface_apple_pay::RegisterDomainResponse> {
    let m = v.as_object()?;
    Some(iface_apple_pay::RegisterDomainResponse {
        errors: m.get("errors").filter(|v| !v.is_null()).and_then(|v| (v).as_array().map(|a| a.iter().filter_map(|x| iface_apple_pay__error__from_json(x)).collect())),
        status: m.get("status").filter(|v| !v.is_null()).and_then(|v| (v).as_str().map(|s| s.to_string())),
    })
}

fn iface_apple_pay__error__from_json(v: &Value) -> Option<iface_apple_pay::Error> {
    let m = v.as_object()?;
    Some(iface_apple_pay::Error {
        category: m.get("category").and_then(|v| (v).as_str().map(|s| s.to_string())).unwrap_or_default(),
        code: m.get("code").and_then(|v| (v).as_str().map(|s| s.to_string())).unwrap_or_default(),
        detail: m.get("detail").filter(|v| !v.is_null()).and_then(|v| (v).as_str().map(|s| s.to_string())),
        field: m.get("field").filter(|v| !v.is_null()).and_then(|v| (v).as_str().map(|s| s.to_string())),
    })
}

fn iface_apple_pay__register_domain__ok(body: String) -> Result<iface_apple_pay::RegisterDomainResponse, crate::runtime::DispatchError> {
    let v: Value = match serde_json::from_str(&body) {
        Ok(v) => v,
        Err(e) => return Err(crate::runtime::DispatchError::Transport(format!("failed to decode response body as JSON: {e}"))),
    };
    match iface_apple_pay__register_domain_response__from_json(&v) {
        Some(x) => Ok(x),
        None => Err(crate::runtime::DispatchError::Transport("response body did not match the expected schema".to_string())),
    }
}

fn iface_apple_pay__register_domain__err(e: crate::runtime::DispatchError) -> String {
    match e {
        crate::runtime::DispatchError::Http { status, body } => format!("HTTP {status}: {body}"),
        crate::runtime::DispatchError::Transport(m) => m,
    }
}

impl iface_apple_pay::Guest for crate::Component {
    fn register_domain(params: iface_apple_pay::RegisterDomainParams) -> Result<iface_apple_pay::RegisterDomainResponse, String> {
        let json = iface_apple_pay__register_domain_params__to_json(&params);
        match dispatch(&OP_APPLE_PAY_REGISTER_DOMAIN, json).and_then(iface_apple_pay__register_domain__ok) {
            Ok(v) => Ok(v),
            Err(e) => Err(iface_apple_pay__register_domain__err(e)),
        }
    }
}
