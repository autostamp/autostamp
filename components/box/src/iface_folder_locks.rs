// AUTOGENERATED by openapi-bindgen. Do not edit by hand.

use crate::runtime::{dispatch, AuthApply, AuthKind, FieldLocation, FieldSpec, OpSpec};
use crate::exports::autostamp::box_api::folder_locks as iface_folder_locks;
use serde_json::{Map, Value};

const OP_FOLDER_LOCKS_GET_FOLDER_LOCKS: OpSpec = OpSpec {
    method: "GET",
    path_template: "/folder_locks",
    fields: &[
        FieldSpec { snake: "folder_id", wire: "folder_id", location: FieldLocation::Query },
    ],
    auth: &[
        AuthApply { secret_key: "OAuth2Security", kind: AuthKind::Bearer },
    ],
};

const OP_FOLDER_LOCKS_POST_FOLDER_LOCKS: OpSpec = OpSpec {
    method: "POST",
    path_template: "/folder_locks",
    fields: &[
        FieldSpec { snake: "folder", wire: "folder", location: FieldLocation::Body },
        FieldSpec { snake: "locked_operations", wire: "locked_operations", location: FieldLocation::Body },
    ],
    auth: &[
        AuthApply { secret_key: "OAuth2Security", kind: AuthKind::Bearer },
    ],
};

const OP_FOLDER_LOCKS_DELETE_FOLDER_LOCKS_ID: OpSpec = OpSpec {
    method: "DELETE",
    path_template: "/folder_locks/{folder_lock_id}",
    fields: &[
        FieldSpec { snake: "folder_lock_id", wire: "folder_lock_id", location: FieldLocation::Path },
    ],
    auth: &[
        AuthApply { secret_key: "OAuth2Security", kind: AuthKind::Bearer },
    ],
};

fn iface_folder_locks__folder_lock_created_by_type_op_enum__to_str(e: &iface_folder_locks::FolderLockCreatedByTypeOpEnum) -> &'static str {
    match e {
        iface_folder_locks::FolderLockCreatedByTypeOpEnum::User => "user",
    }
}

fn iface_folder_locks__folder_locks__to_json(p: &iface_folder_locks::FolderLocks) -> Value {
    let mut m = Map::new();
    m.insert("data".into(), match (&p.data) { Some(v) => Value::String((v).clone()), None => Value::Null });
    Value::Object(m)
}

fn iface_folder_locks__post_folder_locks_body_folder__to_json(p: &iface_folder_locks::PostFolderLocksBodyFolder) -> Value {
    let mut m = Map::new();
    m.insert("id".into(), Value::String((&p.id).clone()));
    m.insert("type".into(), Value::String((&p.type_op).clone()));
    Value::Object(m)
}

fn iface_folder_locks__post_folder_locks_body_locked_operations__to_json(p: &iface_folder_locks::PostFolderLocksBodyLockedOperations) -> Value {
    let mut m = Map::new();
    m.insert("delete".into(), Value::Bool(*(&p.delete)));
    m.insert("move".into(), Value::Bool(*(&p.move_)));
    Value::Object(m)
}

fn iface_folder_locks__folder_lock__to_json(p: &iface_folder_locks::FolderLock) -> Value {
    let mut m = Map::new();
    m.insert("created_at".into(), match (&p.created_at) { Some(v) => Value::String((v).clone()), None => Value::Null });
    m.insert("created_by".into(), match (&p.created_by) { Some(v) => iface_folder_locks__folder_lock_created_by__to_json(v), None => Value::Null });
    m.insert("folder".into(), match (&p.folder) { Some(v) => iface_folder_locks__folder_lock_folder__to_json(v), None => Value::Null });
    m.insert("id".into(), match (&p.id) { Some(v) => Value::String((v).clone()), None => Value::Null });
    m.insert("lock_type".into(), match (&p.lock_type) { Some(v) => Value::String((v).clone()), None => Value::Null });
    m.insert("locked_operations".into(), match (&p.locked_operations) { Some(v) => iface_folder_locks__folder_lock_locked_operations__to_json(v), None => Value::Null });
    m.insert("type".into(), match (&p.type_op) { Some(v) => Value::String((v).clone()), None => Value::Null });
    Value::Object(m)
}

fn iface_folder_locks__folder_lock_created_by__to_json(p: &iface_folder_locks::FolderLockCreatedBy) -> Value {
    let mut m = Map::new();
    m.insert("id".into(), match (&p.id) { Some(v) => Value::String((v).clone()), None => Value::Null });
    m.insert("type".into(), Value::String(iface_folder_locks__folder_lock_created_by_type_op_enum__to_str(&p.type_op).into()));
    Value::Object(m)
}

fn iface_folder_locks__folder_lock_folder__to_json(p: &iface_folder_locks::FolderLockFolder) -> Value {
    let mut m = Map::new();
    m.insert("data".into(), match (&p.data) { Some(v) => Value::String((v).clone()), None => Value::Null });
    Value::Object(m)
}

fn iface_folder_locks__folder_lock_locked_operations__to_json(p: &iface_folder_locks::FolderLockLockedOperations) -> Value {
    let mut m = Map::new();
    m.insert("delete".into(), Value::Bool(*(&p.delete)));
    m.insert("move".into(), Value::Bool(*(&p.move_)));
    Value::Object(m)
}

fn iface_folder_locks__get_folder_locks_params__to_json(p: &iface_folder_locks::GetFolderLocksParams) -> Value {
    let mut m = Map::new();
    m.insert("folder_id".into(), Value::String((&p.folder_id).clone()));
    Value::Object(m)
}

fn iface_folder_locks__post_folder_locks_params__to_json(p: &iface_folder_locks::PostFolderLocksParams) -> Value {
    let mut m = Map::new();
    m.insert("folder".into(), iface_folder_locks__post_folder_locks_body_folder__to_json(&p.folder));
    m.insert("locked_operations".into(), match (&p.locked_operations) { Some(v) => iface_folder_locks__post_folder_locks_body_locked_operations__to_json(v), None => Value::Null });
    Value::Object(m)
}

fn iface_folder_locks__delete_folder_locks_id_params__to_json(p: &iface_folder_locks::DeleteFolderLocksIdParams) -> Value {
    let mut m = Map::new();
    m.insert("folder_lock_id".into(), Value::String((&p.folder_lock_id).clone()));
    Value::Object(m)
}

fn iface_folder_locks__folder_locks__from_json(v: &Value) -> Option<iface_folder_locks::FolderLocks> {
    let m = v.as_object()?;
    Some(iface_folder_locks::FolderLocks {
        data: m.get("data").filter(|v| !v.is_null()).and_then(|v| (v).as_str().map(|s| s.to_string())),
    })
}

fn iface_folder_locks__folder_lock__from_json(v: &Value) -> Option<iface_folder_locks::FolderLock> {
    let m = v.as_object()?;
    Some(iface_folder_locks::FolderLock {
        created_at: m.get("created_at").filter(|v| !v.is_null()).and_then(|v| (v).as_str().map(|s| s.to_string())),
        created_by: m.get("created_by").filter(|v| !v.is_null()).and_then(|v| iface_folder_locks__folder_lock_created_by__from_json(v)),
        folder: m.get("folder").filter(|v| !v.is_null()).and_then(|v| iface_folder_locks__folder_lock_folder__from_json(v)),
        id: m.get("id").filter(|v| !v.is_null()).and_then(|v| (v).as_str().map(|s| s.to_string())),
        lock_type: m.get("lock_type").filter(|v| !v.is_null()).and_then(|v| (v).as_str().map(|s| s.to_string())),
        locked_operations: m.get("locked_operations").filter(|v| !v.is_null()).and_then(|v| iface_folder_locks__folder_lock_locked_operations__from_json(v)),
        type_op: m.get("type").filter(|v| !v.is_null()).and_then(|v| (v).as_str().map(|s| s.to_string())),
    })
}

fn iface_folder_locks__folder_lock_created_by__from_json(v: &Value) -> Option<iface_folder_locks::FolderLockCreatedBy> {
    let m = v.as_object()?;
    Some(iface_folder_locks::FolderLockCreatedBy {
        id: m.get("id").filter(|v| !v.is_null()).and_then(|v| (v).as_str().map(|s| s.to_string())),
        type_op: match m.get("type").and_then(|v| (v).as_str().and_then(iface_folder_locks__folder_lock_created_by_type_op_enum__from_str)) { Some(x) => x, None => return None },
    })
}

fn iface_folder_locks__folder_lock_folder__from_json(v: &Value) -> Option<iface_folder_locks::FolderLockFolder> {
    let m = v.as_object()?;
    Some(iface_folder_locks::FolderLockFolder {
        data: m.get("data").filter(|v| !v.is_null()).and_then(|v| (v).as_str().map(|s| s.to_string())),
    })
}

fn iface_folder_locks__folder_lock_locked_operations__from_json(v: &Value) -> Option<iface_folder_locks::FolderLockLockedOperations> {
    let m = v.as_object()?;
    Some(iface_folder_locks::FolderLockLockedOperations {
        delete: m.get("delete").and_then(|v| (v).as_bool()).unwrap_or_default(),
        move_: m.get("move").and_then(|v| (v).as_bool()).unwrap_or_default(),
    })
}

fn iface_folder_locks__folder_lock_created_by_type_op_enum__from_str(s: &str) -> Option<iface_folder_locks::FolderLockCreatedByTypeOpEnum> {
    match s {
        "user" => Some(iface_folder_locks::FolderLockCreatedByTypeOpEnum::User),
        _ => None,
    }
}

fn iface_folder_locks__get_folder_locks__ok(body: String) -> Result<iface_folder_locks::FolderLocks, crate::runtime::DispatchError> {
    let v: Value = match serde_json::from_str(&body) {
        Ok(v) => v,
        Err(e) => return Err(crate::runtime::DispatchError::Transport(format!("failed to decode response body as JSON: {e}"))),
    };
    match iface_folder_locks__folder_locks__from_json(&v) {
        Some(x) => Ok(x),
        None => Err(crate::runtime::DispatchError::Transport("response body did not match the expected schema".to_string())),
    }
}

fn iface_folder_locks__get_folder_locks__err(e: crate::runtime::DispatchError) -> iface_folder_locks::GetFolderLocksError {
    match e {
        crate::runtime::DispatchError::Http { status, body } => match status {
            403u16 => iface_folder_locks::GetFolderLocksError::Forbidden(body),
            404u16 => iface_folder_locks::GetFolderLocksError::NotFound(body),
            405u16 => iface_folder_locks::GetFolderLocksError::MethodNotAllowed(body),
            _ => iface_folder_locks::GetFolderLocksError::Other(body),
        },
        crate::runtime::DispatchError::Transport(m) => iface_folder_locks::GetFolderLocksError::Other(m),
    }
}

fn iface_folder_locks__post_folder_locks__ok(body: String) -> Result<iface_folder_locks::FolderLock, crate::runtime::DispatchError> {
    let v: Value = match serde_json::from_str(&body) {
        Ok(v) => v,
        Err(e) => return Err(crate::runtime::DispatchError::Transport(format!("failed to decode response body as JSON: {e}"))),
    };
    match iface_folder_locks__folder_lock__from_json(&v) {
        Some(x) => Ok(x),
        None => Err(crate::runtime::DispatchError::Transport("response body did not match the expected schema".to_string())),
    }
}

fn iface_folder_locks__post_folder_locks__err(e: crate::runtime::DispatchError) -> iface_folder_locks::PostFolderLocksError {
    match e {
        crate::runtime::DispatchError::Http { status, body } => match status {
            400u16 => iface_folder_locks::PostFolderLocksError::BadRequest(body),
            404u16 => iface_folder_locks::PostFolderLocksError::NotFound(body),
            _ => iface_folder_locks::PostFolderLocksError::Other(body),
        },
        crate::runtime::DispatchError::Transport(m) => iface_folder_locks::PostFolderLocksError::Other(m),
    }
}

fn iface_folder_locks__delete_folder_locks_id__ok(body: String) -> Result<String, crate::runtime::DispatchError> {
    Ok(body)
}

fn iface_folder_locks__delete_folder_locks_id__err(e: crate::runtime::DispatchError) -> iface_folder_locks::DeleteFolderLocksIdError {
    match e {
        crate::runtime::DispatchError::Http { status, body } => match status {
            403u16 => iface_folder_locks::DeleteFolderLocksIdError::Forbidden(body),
            404u16 => iface_folder_locks::DeleteFolderLocksIdError::NotFound(body),
            _ => iface_folder_locks::DeleteFolderLocksIdError::Other(body),
        },
        crate::runtime::DispatchError::Transport(m) => iface_folder_locks::DeleteFolderLocksIdError::Other(m),
    }
}

impl iface_folder_locks::Guest for crate::Component {
    fn get_folder_locks(params: iface_folder_locks::GetFolderLocksParams) -> Result<iface_folder_locks::FolderLocks, iface_folder_locks::GetFolderLocksError> {
        let json = iface_folder_locks__get_folder_locks_params__to_json(&params);
        match dispatch(&OP_FOLDER_LOCKS_GET_FOLDER_LOCKS, json).and_then(iface_folder_locks__get_folder_locks__ok) {
            Ok(v) => Ok(v),
            Err(e) => Err(iface_folder_locks__get_folder_locks__err(e)),
        }
    }
    fn post_folder_locks(params: iface_folder_locks::PostFolderLocksParams) -> Result<iface_folder_locks::FolderLock, iface_folder_locks::PostFolderLocksError> {
        let json = iface_folder_locks__post_folder_locks_params__to_json(&params);
        match dispatch(&OP_FOLDER_LOCKS_POST_FOLDER_LOCKS, json).and_then(iface_folder_locks__post_folder_locks__ok) {
            Ok(v) => Ok(v),
            Err(e) => Err(iface_folder_locks__post_folder_locks__err(e)),
        }
    }
    fn delete_folder_locks_id(params: iface_folder_locks::DeleteFolderLocksIdParams) -> Result<String, iface_folder_locks::DeleteFolderLocksIdError> {
        let json = iface_folder_locks__delete_folder_locks_id_params__to_json(&params);
        match dispatch(&OP_FOLDER_LOCKS_DELETE_FOLDER_LOCKS_ID, json).and_then(iface_folder_locks__delete_folder_locks_id__ok) {
            Ok(v) => Ok(v),
            Err(e) => Err(iface_folder_locks__delete_folder_locks_id__err(e)),
        }
    }
}
