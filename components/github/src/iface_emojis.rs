// AUTOGENERATED by openapi-bindgen. Do not edit by hand.

use crate::runtime::{dispatch, AuthApply, AuthKind, FieldLocation, FieldSpec, OpSpec};
use crate::exports::autostamp::github::emojis as iface_emojis;
use serde_json::{Map, Value};

const OP_EMOJIS_GET: OpSpec = OpSpec {
    method: "GET",
    path_template: "/emojis",
    fields: &[
    ],
    auth: &[
    ],
};

fn iface_emojis__get_response_entry__to_json(p: &iface_emojis::GetResponseEntry) -> Value {
    let mut m = Map::new();
    m.insert("key".into(), Value::String((&p.key).clone()));
    m.insert("value".into(), Value::String((&p.value).clone()));
    Value::Object(m)
}

fn iface_emojis__get_response_entry__from_json(v: &Value) -> Option<iface_emojis::GetResponseEntry> {
    let m = v.as_object()?;
    Some(iface_emojis::GetResponseEntry {
        key: m.get("key").and_then(|v| (v).as_str().map(|s| s.to_string())).unwrap_or_default(),
        value: m.get("value").and_then(|v| (v).as_str().map(|s| s.to_string())).unwrap_or_default(),
    })
}

fn iface_emojis__get__ok(body: String) -> Result<Vec<iface_emojis::GetResponseEntry>, crate::runtime::DispatchError> {
    let v: Value = match serde_json::from_str(&body) {
        Ok(v) => v,
        Err(e) => return Err(crate::runtime::DispatchError::Transport(format!("failed to decode response body as JSON: {e}"))),
    };
    match (&v).as_object().map(|o| o.iter().filter_map(|(k, x)| ((x).as_str().map(|s| s.to_string())).map(|val| iface_emojis::GetResponseEntry { key: k.clone(), value: val })).collect()) {
        Some(x) => Ok(x),
        None => Err(crate::runtime::DispatchError::Transport("response body did not match the expected schema".to_string())),
    }
}

fn iface_emojis__get__err(e: crate::runtime::DispatchError) -> iface_emojis::GetError {
    match e {
        crate::runtime::DispatchError::Http { status, body } => match status {
            304u16 => iface_emojis::GetError::NotModified(body),
            _ => iface_emojis::GetError::Other(body),
        },
        crate::runtime::DispatchError::Transport(m) => iface_emojis::GetError::Other(m),
    }
}

impl iface_emojis::Guest for crate::Component {
    fn get() -> Result<Vec<iface_emojis::GetResponseEntry>, iface_emojis::GetError> {
        match dispatch(&OP_EMOJIS_GET, Value::Object(Map::new())).and_then(iface_emojis__get__ok) {
            Ok(v) => Ok(v),
            Err(e) => Err(iface_emojis__get__err(e)),
        }
    }
}
