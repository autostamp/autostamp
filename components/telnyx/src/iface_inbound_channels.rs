// AUTOGENERATED by openapi-bindgen. Do not edit by hand.

use crate::runtime::{dispatch, AuthApply, AuthKind, FieldLocation, FieldSpec, OpSpec};
use crate::exports::autostamp::telnyx::inbound_channels as iface_inbound_channels;
use serde_json::{Map, Value};

const OP_INBOUND_CHANNELS_LIST_OUTBOUND_CHANNELS: OpSpec = OpSpec {
    method: "GET",
    path_template: "/phone_numbers/inbound_channels",
    fields: &[
    ],
    auth: &[
        AuthApply { secret_key: "BearerAuth", kind: AuthKind::Bearer },
    ],
};

const OP_INBOUND_CHANNELS_UPDATE_OUTBOUND_CHANNELS: OpSpec = OpSpec {
    method: "PATCH",
    path_template: "/phone_numbers/inbound_channels",
    fields: &[
        FieldSpec { snake: "channels", wire: "channels", location: FieldLocation::Body },
    ],
    auth: &[
        AuthApply { secret_key: "BearerAuth", kind: AuthKind::Bearer },
    ],
};

fn iface_inbound_channels__list_outbound_channels_response__to_json(p: &iface_inbound_channels::ListOutboundChannelsResponse) -> Value {
    let mut m = Map::new();
    m.insert("data".into(), match (&p.data) { Some(v) => iface_inbound_channels__list_outbound_channels_response_data__to_json(v), None => Value::Null });
    Value::Object(m)
}

fn iface_inbound_channels__list_outbound_channels_response_data__to_json(p: &iface_inbound_channels::ListOutboundChannelsResponseData) -> Value {
    let mut m = Map::new();
    m.insert("channels".into(), match (&p.channels) { Some(v) => Value::Number(serde_json::Number::from(*(v))), None => Value::Null });
    m.insert("record_type".into(), match (&p.record_type) { Some(v) => Value::String((v).clone()), None => Value::Null });
    Value::Object(m)
}

fn iface_inbound_channels__update_outbound_channels_response__to_json(p: &iface_inbound_channels::UpdateOutboundChannelsResponse) -> Value {
    let mut m = Map::new();
    m.insert("data".into(), match (&p.data) { Some(v) => iface_inbound_channels__update_outbound_channels_response_data__to_json(v), None => Value::Null });
    Value::Object(m)
}

fn iface_inbound_channels__update_outbound_channels_response_data__to_json(p: &iface_inbound_channels::UpdateOutboundChannelsResponseData) -> Value {
    let mut m = Map::new();
    m.insert("channels".into(), match (&p.channels) { Some(v) => Value::Number(serde_json::Number::from(*(v))), None => Value::Null });
    m.insert("record_type".into(), match (&p.record_type) { Some(v) => Value::String((v).clone()), None => Value::Null });
    Value::Object(m)
}

fn iface_inbound_channels__update_outbound_channels_params__to_json(p: &iface_inbound_channels::UpdateOutboundChannelsParams) -> Value {
    let mut m = Map::new();
    m.insert("channels".into(), Value::Number(serde_json::Number::from(*(&p.channels))));
    Value::Object(m)
}

fn iface_inbound_channels__list_outbound_channels_response__from_json(v: &Value) -> Option<iface_inbound_channels::ListOutboundChannelsResponse> {
    let m = v.as_object()?;
    Some(iface_inbound_channels::ListOutboundChannelsResponse {
        data: m.get("data").filter(|v| !v.is_null()).and_then(|v| iface_inbound_channels__list_outbound_channels_response_data__from_json(v)),
    })
}

fn iface_inbound_channels__list_outbound_channels_response_data__from_json(v: &Value) -> Option<iface_inbound_channels::ListOutboundChannelsResponseData> {
    let m = v.as_object()?;
    Some(iface_inbound_channels::ListOutboundChannelsResponseData {
        channels: m.get("channels").filter(|v| !v.is_null()).and_then(|v| (v).as_i64().map(|n| n as i32)),
        record_type: m.get("record_type").filter(|v| !v.is_null()).and_then(|v| (v).as_str().map(|s| s.to_string())),
    })
}

fn iface_inbound_channels__update_outbound_channels_response__from_json(v: &Value) -> Option<iface_inbound_channels::UpdateOutboundChannelsResponse> {
    let m = v.as_object()?;
    Some(iface_inbound_channels::UpdateOutboundChannelsResponse {
        data: m.get("data").filter(|v| !v.is_null()).and_then(|v| iface_inbound_channels__update_outbound_channels_response_data__from_json(v)),
    })
}

fn iface_inbound_channels__update_outbound_channels_response_data__from_json(v: &Value) -> Option<iface_inbound_channels::UpdateOutboundChannelsResponseData> {
    let m = v.as_object()?;
    Some(iface_inbound_channels::UpdateOutboundChannelsResponseData {
        channels: m.get("channels").filter(|v| !v.is_null()).and_then(|v| (v).as_i64().map(|n| n as i32)),
        record_type: m.get("record_type").filter(|v| !v.is_null()).and_then(|v| (v).as_str().map(|s| s.to_string())),
    })
}

fn iface_inbound_channels__list_outbound_channels__ok(body: String) -> Result<iface_inbound_channels::ListOutboundChannelsResponse, crate::runtime::DispatchError> {
    let v: Value = match serde_json::from_str(&body) {
        Ok(v) => v,
        Err(e) => return Err(crate::runtime::DispatchError::Transport(format!("failed to decode response body as JSON: {e}"))),
    };
    match iface_inbound_channels__list_outbound_channels_response__from_json(&v) {
        Some(x) => Ok(x),
        None => Err(crate::runtime::DispatchError::Transport("response body did not match the expected schema".to_string())),
    }
}

fn iface_inbound_channels__list_outbound_channels__err(e: crate::runtime::DispatchError) -> iface_inbound_channels::ListOutboundChannelsError {
    match e {
        crate::runtime::DispatchError::Http { status, body } => match status {
            401u16 => iface_inbound_channels::ListOutboundChannelsError::Unauthorized(body),
            404u16 => iface_inbound_channels::ListOutboundChannelsError::NotFound(body),
            422u16 => iface_inbound_channels::ListOutboundChannelsError::UnprocessableEntity(body),
            _ => iface_inbound_channels::ListOutboundChannelsError::Other(body),
        },
        crate::runtime::DispatchError::Transport(m) => iface_inbound_channels::ListOutboundChannelsError::Other(m),
    }
}

fn iface_inbound_channels__update_outbound_channels__ok(body: String) -> Result<iface_inbound_channels::UpdateOutboundChannelsResponse, crate::runtime::DispatchError> {
    let v: Value = match serde_json::from_str(&body) {
        Ok(v) => v,
        Err(e) => return Err(crate::runtime::DispatchError::Transport(format!("failed to decode response body as JSON: {e}"))),
    };
    match iface_inbound_channels__update_outbound_channels_response__from_json(&v) {
        Some(x) => Ok(x),
        None => Err(crate::runtime::DispatchError::Transport("response body did not match the expected schema".to_string())),
    }
}

fn iface_inbound_channels__update_outbound_channels__err(e: crate::runtime::DispatchError) -> String {
    match e {
        crate::runtime::DispatchError::Http { status, body } => format!("HTTP {status}: {body}"),
        crate::runtime::DispatchError::Transport(m) => m,
    }
}

impl iface_inbound_channels::Guest for crate::Component {
    fn list_outbound_channels() -> Result<iface_inbound_channels::ListOutboundChannelsResponse, iface_inbound_channels::ListOutboundChannelsError> {
        match dispatch(&OP_INBOUND_CHANNELS_LIST_OUTBOUND_CHANNELS, Value::Object(Map::new())).and_then(iface_inbound_channels__list_outbound_channels__ok) {
            Ok(v) => Ok(v),
            Err(e) => Err(iface_inbound_channels__list_outbound_channels__err(e)),
        }
    }
    fn update_outbound_channels(params: iface_inbound_channels::UpdateOutboundChannelsParams) -> Result<iface_inbound_channels::UpdateOutboundChannelsResponse, String> {
        let json = iface_inbound_channels__update_outbound_channels_params__to_json(&params);
        match dispatch(&OP_INBOUND_CHANNELS_UPDATE_OUTBOUND_CHANNELS, json).and_then(iface_inbound_channels__update_outbound_channels__ok) {
            Ok(v) => Ok(v),
            Err(e) => Err(iface_inbound_channels__update_outbound_channels__err(e)),
        }
    }
}
