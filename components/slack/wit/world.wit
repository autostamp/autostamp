/// ## Authentication
///
/// Reads credentials from the host via `wasmcloud:secrets`; operations never take a token.
/// Provision these named secrets:
///
/// | Secret name | Applied to each request as |
/// | --- | --- |
/// | `slackAuth` | `Authorization: Bearer <secret>` |
package autostamp:slack@0.3.0;

interface admin-apps {
  record approve-params {
    /// Authentication token. Requires scope: `admin.apps:write`
    token: string
  }
  approve: func(params: approve-params) -> result<string, string>;

  record restrict-params {
    /// Authentication token. Requires scope: `admin.apps:write`
    token: string
  }
  restrict: func(params: restrict-params) -> result<string, string>;

}


interface admin-apps-approved {
  record list-op-params {
    /// Authentication token. Requires scope: `admin.apps:read`
    token: string,
    /// The maximum number of items to return. Must be between 1 - 1000 both inclusive.
    limit: option<s32>,
    /// Set `cursor` to `next_cursor` returned by the previous call to list items in the next page
    cursor: option<string>,
    team-id: option<string>,
    enterprise-id: option<string>
  }
  list-op: func(params: list-op-params) -> result<string, string>;

}

interface admin-apps-requests {
  record list-op-params {
    /// Authentication token. Requires scope: `admin.apps:read`
    token: string,
    /// The maximum number of items to return. Must be between 1 - 1000 both inclusive.
    limit: option<s32>,
    /// Set `cursor` to `next_cursor` returned by the previous call to list items in the next page
    cursor: option<string>,
    team-id: option<string>
  }
  list-op: func(params: list-op-params) -> result<string, string>;

}

interface admin-apps-restricted {
  record list-op-params {
    /// Authentication token. Requires scope: `admin.apps:read`
    token: string,
    /// The maximum number of items to return. Must be between 1 - 1000 both inclusive.
    limit: option<s32>,
    /// Set `cursor` to `next_cursor` returned by the previous call to list items in the next page
    cursor: option<string>,
    team-id: option<string>,
    enterprise-id: option<string>
  }
  list-op: func(params: list-op-params) -> result<string, string>;

}

interface admin-conversations {
  record archive-params {
    /// Authentication token. Requires scope: `admin.conversations:write`
    token: string
  }
  archive: func(params: archive-params) -> result<string, string>;

  record convert-to-private-params {
    /// Authentication token. Requires scope: `admin.conversations:write`
    token: string
  }
  convert-to-private: func(params: convert-to-private-params) -> result<string, string>;

  record create-params {
    /// Authentication token. Requires scope: `admin.conversations:write`
    token: string
  }
  create: func(params: create-params) -> result<string, string>;

  record delete-params {
    /// Authentication token. Requires scope: `admin.conversations:write`
    token: string
  }
  delete: func(params: delete-params) -> result<string, string>;

  record disconnect-shared-params {
    /// Authentication token. Requires scope: `admin.conversations:write`
    token: string
  }
  disconnect-shared: func(params: disconnect-shared-params) -> result<string, string>;

  record get-conversation-prefs-params {
    /// Authentication token. Requires scope: `admin.conversations:read`
    token: string,
    /// The channel to get preferences for.
    channel-id: string
  }
  get-conversation-prefs: func(params: get-conversation-prefs-params) -> result<string, string>;

  record get-teams-params {
    /// Authentication token. Requires scope: `admin.conversations:read`
    token: string,
    /// The channel to determine connected workspaces within the organization for.
    channel-id: string,
    /// Set `cursor` to `next_cursor` returned by the previous call to list items in the next page
    cursor: option<string>,
    /// The maximum number of items to return. Must be between 1 - 1000 both inclusive.
    limit: option<s32>
  }
  get-teams: func(params: get-teams-params) -> result<string, string>;

  record invite-params {
    /// Authentication token. Requires scope: `admin.conversations:write`
    token: string
  }
  invite: func(params: invite-params) -> result<string, string>;

  record rename-params {
    /// Authentication token. Requires scope: `admin.conversations:write`
    token: string
  }
  rename: func(params: rename-params) -> result<string, string>;

  record search-params {
    /// Authentication token. Requires scope: `admin.conversations:read`
    token: string,
    /// Comma separated string of team IDs, signifying the workspaces to search through.
    team-ids: option<string>,
    /// Name of the the channel to query by.
    query: option<string>,
    /// Maximum number of items to be returned. Must be between 1 - 20 both inclusive. Default is 10.
    limit: option<s32>,
    /// Set `cursor` to `next_cursor` returned by the previous call to list items in the next page.
    cursor: option<string>,
    /// The type of channel to include or exclude in the search. For example `private` will search private channels, while `private_exclude` will exclude them. For a full list of types, check the [Types section](#types).
    search-channel-types: option<string>,
    /// Possible values are `relevant` (search ranking based on what we think is closest), `name` (alphabetical), `member_count` (number of users in the channel), and `created` (date channel was created). You can optionally pair this with the `sort_dir` arg to change how it is sorted 
    sort: option<string>,
    /// Sort direction. Possible values are `asc` for ascending order like (1, 2, 3) or (a, b, c), and `desc` for descending order like (3, 2, 1) or (c, b, a)
    sort-dir: option<string>
  }
  search: func(params: search-params) -> result<string, string>;

  record set-conversation-prefs-params {
    /// Authentication token. Requires scope: `admin.conversations:write`
    token: string
  }
  set-conversation-prefs: func(params: set-conversation-prefs-params) -> result<string, string>;

  record set-teams-params {
    /// Authentication token. Requires scope: `admin.conversations:write`
    token: string
  }
  set-teams: func(params: set-teams-params) -> result<string, string>;

  record unarchive-params {
    /// Authentication token. Requires scope: `admin.conversations:write`
    token: string
  }
  unarchive: func(params: unarchive-params) -> result<string, string>;

}

interface admin-conversations-ekm {
  record list-original-connected-channel-info-params {
    /// Authentication token. Requires scope: `admin.conversations:read`
    token: string,
    /// A comma-separated list of channels to filter to.
    channel-ids: option<string>,
    /// A comma-separated list of the workspaces to which the channels you would like returned belong.
    team-ids: option<string>,
    /// The maximum number of items to return. Must be between 1 - 1000 both inclusive.
    limit: option<s32>,
    /// Set `cursor` to `next_cursor` returned by the previous call to list items in the next page.
    cursor: option<string>
  }
  list-original-connected-channel-info: func(params: list-original-connected-channel-info-params) -> result<string, string>;

}

interface admin-conversations-restrict-access {
  add-group: func() -> result<string, string>;

  record list-groups-params {
    /// Authentication token. Requires scope: `admin.conversations:read`
    token: string,
    channel-id: string,
    /// The workspace where the channel exists. This argument is required for channels only tied to one workspace, and optional for channels that are shared across an organization.
    team-id: option<string>
  }
  list-groups: func(params: list-groups-params) -> result<string, string>;

  remove-group: func() -> result<string, string>;

}

interface admin-emoji {
  add: func() -> result<string, string>;

  add-alias: func() -> result<string, string>;

  record list-op-params {
    /// Authentication token. Requires scope: `admin.teams:read`
    token: string,
    /// Set `cursor` to `next_cursor` returned by the previous call to list items in the next page
    cursor: option<string>,
    /// The maximum number of items to return. Must be between 1 - 1000 both inclusive.
    limit: option<s32>
  }
  list-op: func(params: list-op-params) -> result<string, string>;

  remove: func() -> result<string, string>;

  rename: func() -> result<string, string>;

}

interface admin-invite-requests {
  record approve-params {
    /// Authentication token. Requires scope: `admin.invites:write`
    token: string
  }
  approve: func(params: approve-params) -> result<string, string>;

  record deny-params {
    /// Authentication token. Requires scope: `admin.invites:write`
    token: string
  }
  deny: func(params: deny-params) -> result<string, string>;

  record list-op-params {
    /// Authentication token. Requires scope: `admin.invites:read`
    token: string,
    /// ID for the workspace where the invite requests were made.
    team-id: option<string>,
    /// Value of the `next_cursor` field sent as part of the previous API response
    cursor: option<string>,
    /// The number of results that will be returned by the API on each invocation. Must be between 1 - 1000, both inclusive
    limit: option<s32>
  }
  list-op: func(params: list-op-params) -> result<string, string>;

}

interface admin-invite-requests-approved {
  record list-op-params {
    /// Authentication token. Requires scope: `admin.invites:read`
    token: string,
    /// ID for the workspace where the invite requests were made.
    team-id: option<string>,
    /// Value of the `next_cursor` field sent as part of the previous API response
    cursor: option<string>,
    /// The number of results that will be returned by the API on each invocation. Must be between 1 - 1000, both inclusive
    limit: option<s32>
  }
  list-op: func(params: list-op-params) -> result<string, string>;

}

interface admin-invite-requests-denied {
  record list-op-params {
    /// Authentication token. Requires scope: `admin.invites:read`
    token: string,
    /// ID for the workspace where the invite requests were made.
    team-id: option<string>,
    /// Value of the `next_cursor` field sent as part of the previous api response
    cursor: option<string>,
    /// The number of results that will be returned by the API on each invocation. Must be between 1 - 1000 both inclusive
    limit: option<s32>
  }
  list-op: func(params: list-op-params) -> result<string, string>;

}

interface admin-teams-admins {
  record list-op-params {
    /// Authentication token. Requires scope: `admin.teams:read`
    token: string,
    /// The maximum number of items to return.
    limit: option<s32>,
    /// Set `cursor` to `next_cursor` returned by the previous call to list items in the next page.
    cursor: option<string>,
    team-id: string
  }
  list-op: func(params: list-op-params) -> result<string, string>;

}

interface admin-teams {
  record create-params {
    /// Authentication token. Requires scope: `admin.teams:write`
    token: string
  }
  create: func(params: create-params) -> result<string, string>;

  record list-op-params {
    /// Authentication token. Requires scope: `admin.teams:read`
    token: string,
    /// The maximum number of items to return. Must be between 1 - 100 both inclusive.
    limit: option<s32>,
    /// Set `cursor` to `next_cursor` returned by the previous call to list items in the next page.
    cursor: option<string>
  }
  list-op: func(params: list-op-params) -> result<string, string>;

}

interface admin-teams-owners {
  record list-op-params {
    /// Authentication token. Requires scope: `admin.teams:read`
    token: string,
    team-id: string,
    /// The maximum number of items to return. Must be between 1 - 1000 both inclusive.
    limit: option<s32>,
    /// Set `cursor` to `next_cursor` returned by the previous call to list items in the next page.
    cursor: option<string>
  }
  list-op: func(params: list-op-params) -> result<string, string>;

}

interface admin-teams-settings {
  record info-params {
    /// Authentication token. Requires scope: `admin.teams:read`
    token: string,
    team-id: string
  }
  info: func(params: info-params) -> result<string, string>;

  set-default-channels: func() -> result<string, string>;

  record set-description-params {
    /// Authentication token. Requires scope: `admin.teams:write`
    token: string
  }
  set-description: func(params: set-description-params) -> result<string, string>;

  record set-discoverability-params {
    /// Authentication token. Requires scope: `admin.teams:write`
    token: string
  }
  set-discoverability: func(params: set-discoverability-params) -> result<string, string>;

  set-icon: func() -> result<string, string>;

  record set-name-params {
    /// Authentication token. Requires scope: `admin.teams:write`
    token: string
  }
  set-name: func(params: set-name-params) -> result<string, string>;

}

interface admin-usergroups {
  record add-channels-params {
    /// Authentication token. Requires scope: `admin.usergroups:write`
    token: string
  }
  add-channels: func(params: add-channels-params) -> result<string, string>;

  record add-teams-params {
    /// Authentication token. Requires scope: `admin.teams:write`
    token: string
  }
  add-teams: func(params: add-teams-params) -> result<string, string>;

  record list-channels-params {
    /// Authentication token. Requires scope: `admin.usergroups:read`
    token: string,
    /// ID of the IDP group to list default channels for.
    usergroup-id: string,
    /// ID of the the workspace.
    team-id: option<string>,
    /// Flag to include or exclude the count of members per channel.
    include-num-members: option<bool>
  }
  list-channels: func(params: list-channels-params) -> result<string, string>;

  record remove-channels-params {
    /// Authentication token. Requires scope: `admin.usergroups:write`
    token: string
  }
  remove-channels: func(params: remove-channels-params) -> result<string, string>;

}

interface admin-users {
  record assign-params {
    /// Authentication token. Requires scope: `admin.users:write`
    token: string
  }
  assign: func(params: assign-params) -> result<string, string>;

  record invite-params {
    /// Authentication token. Requires scope: `admin.users:write`
    token: string
  }
  invite: func(params: invite-params) -> result<string, string>;

  record list-op-params {
    /// Authentication token. Requires scope: `admin.users:read`
    token: string,
    /// The ID (`T1234`) of the workspace.
    team-id: string,
    /// Set `cursor` to `next_cursor` returned by the previous call to list items in the next page.
    cursor: option<string>,
    /// Limit for how many users to be retrieved per page
    limit: option<s32>
  }
  list-op: func(params: list-op-params) -> result<string, string>;

  record remove-params {
    /// Authentication token. Requires scope: `admin.users:write`
    token: string
  }
  remove: func(params: remove-params) -> result<string, string>;

  record set-admin-params {
    /// Authentication token. Requires scope: `admin.users:write`
    token: string
  }
  set-admin: func(params: set-admin-params) -> result<string, string>;

  record set-expiration-params {
    /// Authentication token. Requires scope: `admin.users:write`
    token: string
  }
  set-expiration: func(params: set-expiration-params) -> result<string, string>;

  record set-owner-params {
    /// Authentication token. Requires scope: `admin.users:write`
    token: string
  }
  set-owner: func(params: set-owner-params) -> result<string, string>;

  record set-regular-params {
    /// Authentication token. Requires scope: `admin.users:write`
    token: string
  }
  set-regular: func(params: set-regular-params) -> result<string, string>;

}

interface admin-users-session {
  record invalidate-params {
    /// Authentication token. Requires scope: `admin.users:write`
    token: string
  }
  invalidate: func(params: invalidate-params) -> result<string, string>;

  record reset-params {
    /// Authentication token. Requires scope: `admin.users:write`
    token: string
  }
  reset: func(params: reset-params) -> result<string, string>;

}

interface api {
  record test-params {
    /// Error response to return
    error: option<string>,
    /// example property to return
    foo: option<string>
  }
  test: func(params: test-params) -> result<string, string>;

}

interface apps-event-authorizations {
  record list-op-params {
    /// Authentication token. Requires scope: `authorizations:read`
    token: string,
    event-context: string,
    cursor: option<string>,
    limit: option<s32>
  }
  list-op: func(params: list-op-params) -> result<string, string>;

}

interface apps-permissions {
  record info-params {
    /// Authentication token. Requires scope: `none`
    token: option<string>
  }
  info: func(params: info-params) -> result<string, string>;

  record request-params {
    /// Authentication token. Requires scope: `none`
    token: string,
    /// A comma separated list of scopes to request for
    scopes: string,
    /// Token used to trigger the permissions API
    trigger-id: string
  }
  request: func(params: request-params) -> result<string, string>;

}

interface apps-permissions-resources {
  record list-op-params {
    /// Authentication token. Requires scope: `none`
    token: string,
    /// Paginate through collections of data by setting the `cursor` parameter to a `next_cursor` attribute returned by a previous request's `response_metadata`. Default value fetches the first "page" of the collection. See [pagination](/docs/pagination) for more detail.
    cursor: option<string>,
    /// The maximum number of items to return.
    limit: option<s32>
  }
  list-op: func(params: list-op-params) -> result<string, string>;

}

interface apps-permissions-scopes {
  record list-op-params {
    /// Authentication token. Requires scope: `none`
    token: string
  }
  list-op: func(params: list-op-params) -> result<string, string>;

}

interface apps-permissions-users {
  record list-op-params {
    /// Authentication token. Requires scope: `none`
    token: string,
    /// Paginate through collections of data by setting the `cursor` parameter to a `next_cursor` attribute returned by a previous request's `response_metadata`. Default value fetches the first "page" of the collection. See [pagination](/docs/pagination) for more detail.
    cursor: option<string>,
    /// The maximum number of items to return.
    limit: option<s32>
  }
  list-op: func(params: list-op-params) -> result<string, string>;

  record request-params {
    /// Authentication token. Requires scope: `none`
    token: string,
    /// A comma separated list of user scopes to request for
    scopes: string,
    /// Token used to trigger the request
    trigger-id: string,
    /// The user this scope is being requested for
    user: string
  }
  request: func(params: request-params) -> result<string, string>;

}

interface apps {
  record uninstall-params {
    /// Authentication token. Requires scope: `none`
    token: option<string>,
    /// Issued when you created your application.
    client-id: option<string>,
    /// Issued when you created your application.
    client-secret: option<string>
  }
  uninstall: func(params: uninstall-params) -> result<string, string>;

}

interface auth {
  record revoke-params {
    /// Authentication token. Requires scope: `none`
    token: string,
    /// Setting this parameter to `1` triggers a _testing mode_ where the specified token will not actually be revoked.
    test: option<bool>
  }
  revoke: func(params: revoke-params) -> result<string, string>;

  record test-params {
    /// Authentication token. Requires scope: `none`
    token: string
  }
  test: func(params: test-params) -> result<string, string>;

}

interface bots {
  record info-params {
    /// Authentication token. Requires scope: `users:read`
    token: string,
    /// Bot user to get info on
    bot: option<string>
  }
  info: func(params: info-params) -> result<string, string>;

}

interface calls {
  record add-params {
    /// Authentication token. Requires scope: `calls:write`
    token: string
  }
  add: func(params: add-params) -> result<string, string>;

  record end-params {
    /// Authentication token. Requires scope: `calls:write`
    token: string
  }
  end: func(params: end-params) -> result<string, string>;

  record info-params {
    /// Authentication token. Requires scope: `calls:read`
    token: string,
    /// `id` of the Call returned by the [`calls.add`](/methods/calls.add) method.
    id: string
  }
  info: func(params: info-params) -> result<string, string>;

  record update-params {
    /// Authentication token. Requires scope: `calls:write`
    token: string
  }
  update: func(params: update-params) -> result<string, string>;

}

interface calls-participants {
  record add-params {
    /// Authentication token. Requires scope: `calls:write`
    token: string
  }
  add: func(params: add-params) -> result<string, string>;

  record remove-params {
    /// Authentication token. Requires scope: `calls:write`
    token: string
  }
  remove: func(params: remove-params) -> result<string, string>;

}

interface chat {
  record delete-params {
    /// Authentication token. Requires scope: `chat:write`
    token: option<string>
  }
  delete: func(params: delete-params) -> result<string, string>;

  record delete-scheduled-message-params {
    /// Authentication token. Requires scope: `chat:write`
    token: string
  }
  delete-scheduled-message: func(params: delete-scheduled-message-params) -> result<string, string>;

  record get-permalink-params {
    /// Authentication token. Requires scope: `none`
    token: string,
    /// The ID of the conversation or channel containing the message
    channel: string,
    /// A message's `ts` value, uniquely identifying it within a channel
    message-ts: string
  }
  get-permalink: func(params: get-permalink-params) -> result<string, string>;

  record me-message-params {
    /// Authentication token. Requires scope: `chat:write`
    token: option<string>
  }
  me-message: func(params: me-message-params) -> result<string, string>;

  record post-ephemeral-params {
    /// Authentication token. Requires scope: `chat:write`
    token: string
  }
  post-ephemeral: func(params: post-ephemeral-params) -> result<string, string>;

  record post-message-params {
    /// Authentication token. Requires scope: `chat:write`
    token: string
  }
  post-message: func(params: post-message-params) -> result<string, string>;

  record schedule-message-params {
    /// Authentication token. Requires scope: `chat:write`
    token: option<string>
  }
  schedule-message: func(params: schedule-message-params) -> result<string, string>;

  record unfurl-params {
    /// Authentication token. Requires scope: `links:write`
    token: string
  }
  unfurl: func(params: unfurl-params) -> result<string, string>;

  record update-params {
    /// Authentication token. Requires scope: `chat:write`
    token: string
  }
  update: func(params: update-params) -> result<string, string>;

}

interface chat-scheduled-messages {
  record list-op-params {
    /// Authentication token. Requires scope: `none`
    token: option<string>,
    /// The channel of the scheduled messages
    channel: option<string>,
    /// A UNIX timestamp of the latest value in the time range
    latest: option<f64>,
    /// A UNIX timestamp of the oldest value in the time range
    oldest: option<f64>,
    /// Maximum number of original entries to return.
    limit: option<s32>,
    /// For pagination purposes, this is the `cursor` value returned from a previous call to `chat.scheduledmessages.list` indicating where you want to start this call from.
    cursor: option<string>
  }
  list-op: func(params: list-op-params) -> result<string, string>;

}

interface conversations {
  record archive-params {
    /// Authentication token. Requires scope: `conversations:write`
    token: option<string>
  }
  archive: func(params: archive-params) -> result<string, string>;

  record close-params {
    /// Authentication token. Requires scope: `conversations:write`
    token: option<string>
  }
  close: func(params: close-params) -> result<string, string>;

  record create-params {
    /// Authentication token. Requires scope: `conversations:write`
    token: option<string>
  }
  create: func(params: create-params) -> result<string, string>;

  record history-params {
    /// Authentication token. Requires scope: `conversations:history`
    token: option<string>,
    /// Conversation ID to fetch history for.
    channel: option<string>,
    /// End of time range of messages to include in results.
    latest: option<f64>,
    /// Start of time range of messages to include in results.
    oldest: option<f64>,
    /// Include messages with latest or oldest timestamp in results only when either timestamp is specified.
    inclusive: option<bool>,
    /// The maximum number of items to return. Fewer than the requested number of items may be returned, even if the end of the users list hasn't been reached.
    limit: option<s32>,
    /// Paginate through collections of data by setting the `cursor` parameter to a `next_cursor` attribute returned by a previous request's `response_metadata`. Default value fetches the first "page" of the collection. See [pagination](/docs/pagination) for more detail.
    cursor: option<string>
  }
  history: func(params: history-params) -> result<string, string>;

  record info-params {
    /// Authentication token. Requires scope: `conversations:read`
    token: option<string>,
    /// Conversation ID to learn more about
    channel: option<string>,
    /// Set this to `true` to receive the locale for this conversation. Defaults to `false`
    include-locale: option<bool>,
    /// Set to `true` to include the member count for the specified conversation. Defaults to `false`
    include-num-members: option<bool>
  }
  info: func(params: info-params) -> result<string, string>;

  record invite-params {
    /// Authentication token. Requires scope: `conversations:write`
    token: option<string>
  }
  invite: func(params: invite-params) -> result<string, string>;

  record join-params {
    /// Authentication token. Requires scope: `channels:write`
    token: option<string>
  }
  join: func(params: join-params) -> result<string, string>;

  record kick-params {
    /// Authentication token. Requires scope: `conversations:write`
    token: option<string>
  }
  kick: func(params: kick-params) -> result<string, string>;

  record leave-params {
    /// Authentication token. Requires scope: `conversations:write`
    token: option<string>
  }
  leave: func(params: leave-params) -> result<string, string>;

  record list-op-params {
    /// Authentication token. Requires scope: `conversations:read`
    token: option<string>,
    /// Set to `true` to exclude archived channels from the list
    exclude-archived: option<bool>,
    /// Mix and match channel types by providing a comma-separated list of any combination of `public_channel`, `private_channel`, `mpim`, `im`
    types: option<string>,
    /// The maximum number of items to return. Fewer than the requested number of items may be returned, even if the end of the list hasn't been reached. Must be an integer no larger than 1000.
    limit: option<s32>,
    /// Paginate through collections of data by setting the `cursor` parameter to a `next_cursor` attribute returned by a previous request's `response_metadata`. Default value fetches the first "page" of the collection. See [pagination](/docs/pagination) for more detail.
    cursor: option<string>
  }
  list-op: func(params: list-op-params) -> result<string, string>;

  record mark-params {
    /// Authentication token. Requires scope: `conversations:write`
    token: option<string>
  }
  mark: func(params: mark-params) -> result<string, string>;

  record members-params {
    /// Authentication token. Requires scope: `conversations:read`
    token: option<string>,
    /// ID of the conversation to retrieve members for
    channel: option<string>,
    /// The maximum number of items to return. Fewer than the requested number of items may be returned, even if the end of the users list hasn't been reached.
    limit: option<s32>,
    /// Paginate through collections of data by setting the `cursor` parameter to a `next_cursor` attribute returned by a previous request's `response_metadata`. Default value fetches the first "page" of the collection. See [pagination](/docs/pagination) for more detail.
    cursor: option<string>
  }
  members: func(params: members-params) -> result<string, string>;

  record open-params {
    /// Authentication token. Requires scope: `conversations:write`
    token: option<string>
  }
  open: func(params: open-params) -> result<string, string>;

  record rename-params {
    /// Authentication token. Requires scope: `conversations:write`
    token: option<string>
  }
  rename: func(params: rename-params) -> result<string, string>;

  record replies-params {
    /// Authentication token. Requires scope: `conversations:history`
    token: option<string>,
    /// Conversation ID to fetch thread from.
    channel: option<string>,
    /// Unique identifier of a thread's parent message. `ts` must be the timestamp of an existing message with 0 or more replies. If there are no replies then just the single message referenced by `ts` will return - it is just an ordinary, unthreaded message.
    ts: option<f64>,
    /// End of time range of messages to include in results.
    latest: option<f64>,
    /// Start of time range of messages to include in results.
    oldest: option<f64>,
    /// Include messages with latest or oldest timestamp in results only when either timestamp is specified.
    inclusive: option<bool>,
    /// The maximum number of items to return. Fewer than the requested number of items may be returned, even if the end of the users list hasn't been reached.
    limit: option<s32>,
    /// Paginate through collections of data by setting the `cursor` parameter to a `next_cursor` attribute returned by a previous request's `response_metadata`. Default value fetches the first "page" of the collection. See [pagination](/docs/pagination) for more detail.
    cursor: option<string>
  }
  replies: func(params: replies-params) -> result<string, string>;

  record set-purpose-params {
    /// Authentication token. Requires scope: `conversations:write`
    token: option<string>
  }
  set-purpose: func(params: set-purpose-params) -> result<string, string>;

  record set-topic-params {
    /// Authentication token. Requires scope: `conversations:write`
    token: option<string>
  }
  set-topic: func(params: set-topic-params) -> result<string, string>;

  record unarchive-params {
    /// Authentication token. Requires scope: `conversations:write`
    token: option<string>
  }
  unarchive: func(params: unarchive-params) -> result<string, string>;

}

interface dialog {
  record open-params {
    /// Authentication token. Requires scope: `none`
    token: string,
    /// The dialog definition. This must be a JSON-encoded string.
    dialog: string,
    /// Exchange a trigger to post to the user.
    trigger-id: string
  }
  open: func(params: open-params) -> result<string, string>;

}

interface dnd {
  record end-dnd-params {
    /// Authentication token. Requires scope: `dnd:write`
    token: string
  }
  end-dnd: func(params: end-dnd-params) -> result<string, string>;

  record end-snooze-params {
    /// Authentication token. Requires scope: `dnd:write`
    token: string
  }
  end-snooze: func(params: end-snooze-params) -> result<string, string>;

  record info-params {
    /// Authentication token. Requires scope: `dnd:read`
    token: option<string>,
    /// User to fetch status for (defaults to current user)
    user: option<string>
  }
  info: func(params: info-params) -> result<string, string>;

  set-snooze: func() -> result<string, string>;

  record team-info-params {
    /// Authentication token. Requires scope: `dnd:read`
    token: option<string>,
    /// Comma-separated list of users to fetch Do Not Disturb status for
    users: option<string>
  }
  team-info: func(params: team-info-params) -> result<string, string>;

}

interface emoji {
  record list-op-params {
    /// Authentication token. Requires scope: `emoji:read`
    token: string
  }
  list-op: func(params: list-op-params) -> result<string, string>;

}

interface files-comments {
  record delete-params {
    /// Authentication token. Requires scope: `files:write:user`
    token: option<string>
  }
  delete: func(params: delete-params) -> result<string, string>;

}

interface files {
  record delete-params {
    /// Authentication token. Requires scope: `files:write:user`
    token: option<string>
  }
  delete: func(params: delete-params) -> result<string, string>;

  record info-params {
    /// Authentication token. Requires scope: `files:read`
    token: option<string>,
    /// Specify a file by providing its ID.
    file: option<string>,
    count: option<string>,
    page: option<string>,
    /// The maximum number of items to return. Fewer than the requested number of items may be returned, even if the end of the list hasn't been reached.
    limit: option<s32>,
    /// Parameter for pagination. File comments are paginated for a single file. Set `cursor` equal to the `next_cursor` attribute returned by the previous request's `response_metadata`. This parameter is optional, but pagination is mandatory: the default value simply fetches the first "page" of the collection of comments. See [pagination](/docs/pagination) for more details.
    cursor: option<string>
  }
  info: func(params: info-params) -> result<string, string>;

  record list-op-params {
    /// Authentication token. Requires scope: `files:read`
    token: option<string>,
    /// Filter files created by a single user.
    user: option<string>,
    /// Filter files appearing in a specific channel, indicated by its ID.
    channel: option<string>,
    /// Filter files created after this timestamp (inclusive).
    ts-from: option<f64>,
    /// Filter files created before this timestamp (inclusive).
    ts-to: option<f64>,
    /// Filter files by type ([see below](#file_types)). You can pass multiple values in the types argument, like `types=spaces,snippets`.The default value is `all`, which does not filter the list.
    types: option<string>,
    count: option<string>,
    page: option<string>,
    /// Show truncated file info for files hidden due to being too old, and the team who owns the file being over the file limit.
    show-files-hidden-by-limit: option<bool>
  }
  list-op: func(params: list-op-params) -> result<string, string>;

  record revoke-public-url-params {
    /// Authentication token. Requires scope: `files:write:user`
    token: option<string>
  }
  revoke-public-url: func(params: revoke-public-url-params) -> result<string, string>;

  record shared-public-url-params {
    /// Authentication token. Requires scope: `files:write:user`
    token: option<string>
  }
  shared-public-url: func(params: shared-public-url-params) -> result<string, string>;

  upload: func() -> result<string, string>;

}

interface files-remote {
  add: func() -> result<string, string>;

  record info-params {
    /// Authentication token. Requires scope: `remote_files:read`
    token: option<string>,
    /// Specify a file by providing its ID.
    file: option<string>,
    /// Creator defined GUID for the file.
    external-id: option<string>
  }
  info: func(params: info-params) -> result<string, string>;

  record list-op-params {
    /// Authentication token. Requires scope: `remote_files:read`
    token: option<string>,
    /// Filter files appearing in a specific channel, indicated by its ID.
    channel: option<string>,
    /// Filter files created after this timestamp (inclusive).
    ts-from: option<f64>,
    /// Filter files created before this timestamp (inclusive).
    ts-to: option<f64>,
    /// The maximum number of items to return.
    limit: option<s32>,
    /// Paginate through collections of data by setting the `cursor` parameter to a `next_cursor` attribute returned by a previous request's `response_metadata`. Default value fetches the first "page" of the collection. See [pagination](/docs/pagination) for more detail.
    cursor: option<string>
  }
  list-op: func(params: list-op-params) -> result<string, string>;

  remove: func() -> result<string, string>;

  record share-params {
    /// Authentication token. Requires scope: `remote_files:share`
    token: option<string>,
    /// Specify a file registered with Slack by providing its ID. Either this field or `external_id` or both are required.
    file: option<string>,
    /// The globally unique identifier (GUID) for the file, as set by the app registering the file with Slack.  Either this field or `file` or both are required.
    external-id: option<string>,
    /// Comma-separated list of channel IDs where the file will be shared.
    channels: option<string>
  }
  share: func(params: share-params) -> result<string, string>;

  update: func() -> result<string, string>;

}

interface migration {
  record exchange-params {
    /// Authentication token. Requires scope: `tokens.basic`
    token: string,
    /// A comma-separated list of user ids, up to 400 per request
    users: string,
    /// Specify team_id starts with `T` in case of Org Token
    team-id: option<string>,
    /// Specify `true` to convert `W` global user IDs to workspace-specific `U` IDs. Defaults to `false`.
    to-old: option<bool>
  }
  exchange: func(params: exchange-params) -> result<string, string>;

}

interface oauth {
  record access-params {
    /// Issued when you created your application.
    client-id: option<string>,
    /// Issued when you created your application.
    client-secret: option<string>,
    /// The `code` param returned via the OAuth callback.
    code: option<string>,
    /// This must match the originally submitted URI (if one was sent).
    redirect-uri: option<string>,
    /// Request the user to add your app only to a single channel. Only valid with a [legacy workspace app](https://api.slack.com/legacy-workspace-apps).
    single-channel: option<bool>
  }
  access: func(params: access-params) -> result<string, string>;

  record token-params {
    /// Issued when you created your application.
    client-id: option<string>,
    /// Issued when you created your application.
    client-secret: option<string>,
    /// The `code` param returned via the OAuth callback.
    code: option<string>,
    /// This must match the originally submitted URI (if one was sent).
    redirect-uri: option<string>,
    /// Request the user to add your app only to a single channel.
    single-channel: option<bool>
  }
  token: func(params: token-params) -> result<string, string>;

}

interface oauth-v2 {
  record access-params {
    /// Issued when you created your application.
    client-id: option<string>,
    /// Issued when you created your application.
    client-secret: option<string>,
    /// The `code` param returned via the OAuth callback.
    code: string,
    /// This must match the originally submitted URI (if one was sent).
    redirect-uri: option<string>
  }
  access: func(params: access-params) -> result<string, string>;

}

interface pins {
  record add-params {
    /// Authentication token. Requires scope: `pins:write`
    token: string
  }
  add: func(params: add-params) -> result<string, string>;

  record list-op-params {
    /// Authentication token. Requires scope: `pins:read`
    token: string,
    /// Channel to get pinned items for.
    channel: string
  }
  list-op: func(params: list-op-params) -> result<string, string>;

  record remove-params {
    /// Authentication token. Requires scope: `pins:write`
    token: string
  }
  remove: func(params: remove-params) -> result<string, string>;

}

interface reactions {
  record add-params {
    /// Authentication token. Requires scope: `reactions:write`
    token: string
  }
  add: func(params: add-params) -> result<string, string>;

  record get-params {
    /// Authentication token. Requires scope: `reactions:read`
    token: string,
    /// Channel where the message to get reactions for was posted.
    channel: option<string>,
    /// File to get reactions for.
    file: option<string>,
    /// File comment to get reactions for.
    file-comment: option<string>,
    /// If true always return the complete reaction list.
    full: option<bool>,
    /// Timestamp of the message to get reactions for.
    timestamp: option<string>
  }
  get: func(params: get-params) -> result<string, string>;

  record list-op-params {
    /// Authentication token. Requires scope: `reactions:read`
    token: string,
    /// Show reactions made by this user. Defaults to the authed user.
    user: option<string>,
    /// If true always return the complete reaction list.
    full: option<bool>,
    count: option<s32>,
    page: option<s32>,
    /// Parameter for pagination. Set `cursor` equal to the `next_cursor` attribute returned by the previous request's `response_metadata`. This parameter is optional, but pagination is mandatory: the default value simply fetches the first "page" of the collection. See [pagination](/docs/pagination) for more details.
    cursor: option<string>,
    /// The maximum number of items to return. Fewer than the requested number of items may be returned, even if the end of the list hasn't been reached.
    limit: option<s32>
  }
  list-op: func(params: list-op-params) -> result<string, string>;

  record remove-params {
    /// Authentication token. Requires scope: `reactions:write`
    token: string
  }
  remove: func(params: remove-params) -> result<string, string>;

}

interface reminders {
  record add-params {
    /// Authentication token. Requires scope: `reminders:write`
    token: string
  }
  add: func(params: add-params) -> result<string, string>;

  record complete-params {
    /// Authentication token. Requires scope: `reminders:write`
    token: option<string>
  }
  complete: func(params: complete-params) -> result<string, string>;

  record delete-params {
    /// Authentication token. Requires scope: `reminders:write`
    token: option<string>
  }
  delete: func(params: delete-params) -> result<string, string>;

  record info-params {
    /// Authentication token. Requires scope: `reminders:read`
    token: option<string>,
    /// The ID of the reminder
    reminder: option<string>
  }
  info: func(params: info-params) -> result<string, string>;

  record list-op-params {
    /// Authentication token. Requires scope: `reminders:read`
    token: option<string>
  }
  list-op: func(params: list-op-params) -> result<string, string>;

}

interface rtm {
  record connect-params {
    /// Authentication token. Requires scope: `rtm:stream`
    token: string,
    /// Batch presence deliveries via subscription. Enabling changes the shape of `presence_change` events. See [batch presence](/docs/presence-and-status#batching).
    batch-presence-aware: option<bool>,
    /// Only deliver presence events when requested by subscription. See [presence subscriptions](/docs/presence-and-status#subscriptions).
    presence-sub: option<bool>
  }
  connect: func(params: connect-params) -> result<string, string>;

}

interface search {
  record messages-params {
    /// Authentication token. Requires scope: `search:read`
    token: string,
    /// Pass the number of results you want per "page". Maximum of `100`.
    count: option<s32>,
    /// Pass a value of `true` to enable query highlight markers (see below).
    highlight: option<bool>,
    page: option<s32>,
    /// Search query.
    query: string,
    /// Return matches sorted by either `score` or `timestamp`.
    sort: option<string>,
    /// Change sort direction to ascending (`asc`) or descending (`desc`).
    sort-dir: option<string>
  }
  messages: func(params: messages-params) -> result<string, string>;

}

interface stars {
  record add-params {
    /// Authentication token. Requires scope: `stars:write`
    token: string
  }
  add: func(params: add-params) -> result<string, string>;

  record list-op-params {
    /// Authentication token. Requires scope: `stars:read`
    token: option<string>,
    count: option<string>,
    page: option<string>,
    /// Parameter for pagination. Set `cursor` equal to the `next_cursor` attribute returned by the previous request's `response_metadata`. This parameter is optional, but pagination is mandatory: the default value simply fetches the first "page" of the collection. See [pagination](/docs/pagination) for more details.
    cursor: option<string>,
    /// The maximum number of items to return. Fewer than the requested number of items may be returned, even if the end of the list hasn't been reached.
    limit: option<s32>
  }
  list-op: func(params: list-op-params) -> result<string, string>;

  record remove-params {
    /// Authentication token. Requires scope: `stars:write`
    token: string
  }
  remove: func(params: remove-params) -> result<string, string>;

}

interface team {
  record access-logs-params {
    /// Authentication token. Requires scope: `admin`
    token: string,
    /// End of time range of logs to include in results (inclusive).
    before: option<string>,
    count: option<string>,
    page: option<string>
  }
  access-logs: func(params: access-logs-params) -> result<string, string>;

  record billable-info-params {
    /// Authentication token. Requires scope: `admin`
    token: string,
    /// A user to retrieve the billable information for. Defaults to all users.
    user: option<string>
  }
  billable-info: func(params: billable-info-params) -> result<string, string>;

  record info-params {
    /// Authentication token. Requires scope: `team:read`
    token: string,
    /// Team to get info on, if omitted, will return information about the current team. Will only return team that the authenticated token is allowed to see through external shared channels
    team: option<string>
  }
  info: func(params: info-params) -> result<string, string>;

  record integration-logs-params {
    /// Authentication token. Requires scope: `admin`
    token: string,
    /// Filter logs to this Slack app. Defaults to all logs.
    app-id: option<string>,
    /// Filter logs with this change type. Defaults to all logs.
    change-type: option<string>,
    count: option<string>,
    page: option<string>,
    /// Filter logs to this service. Defaults to all logs.
    service-id: option<string>,
    /// Filter logs generated by this user’s actions. Defaults to all logs.
    user: option<string>
  }
  integration-logs: func(params: integration-logs-params) -> result<string, string>;

}

interface team-profile {
  record get-params {
    /// Authentication token. Requires scope: `users.profile:read`
    token: string,
    /// Filter by visibility.
    visibility: option<string>
  }
  get: func(params: get-params) -> result<string, string>;

}

interface usergroups {
  record create-params {
    /// Authentication token. Requires scope: `usergroups:write`
    token: string
  }
  create: func(params: create-params) -> result<string, string>;

  record disable-params {
    /// Authentication token. Requires scope: `usergroups:write`
    token: string
  }
  disable: func(params: disable-params) -> result<string, string>;

  record enable-params {
    /// Authentication token. Requires scope: `usergroups:write`
    token: string
  }
  enable: func(params: enable-params) -> result<string, string>;

  record list-op-params {
    /// Include the list of users for each User Group.
    include-users: option<bool>,
    /// Authentication token. Requires scope: `usergroups:read`
    token: string,
    /// Include the number of users in each User Group.
    include-count: option<bool>,
    /// Include disabled User Groups.
    include-disabled: option<bool>
  }
  list-op: func(params: list-op-params) -> result<string, string>;

  record update-params {
    /// Authentication token. Requires scope: `usergroups:write`
    token: string
  }
  update: func(params: update-params) -> result<string, string>;

}

interface usergroups-users {
  record list-op-params {
    /// Authentication token. Requires scope: `usergroups:read`
    token: string,
    /// Allow results that involve disabled User Groups.
    include-disabled: option<bool>,
    /// The encoded ID of the User Group to update.
    usergroup: string
  }
  list-op: func(params: list-op-params) -> result<string, string>;

  record update-params {
    /// Authentication token. Requires scope: `usergroups:write`
    token: string
  }
  update: func(params: update-params) -> result<string, string>;

}

interface users {
  record conversations-params {
    /// Authentication token. Requires scope: `conversations:read`
    token: option<string>,
    /// Browse conversations by a specific user ID's membership. Non-public channels are restricted to those where the calling user shares membership.
    user: option<string>,
    /// Mix and match channel types by providing a comma-separated list of any combination of `public_channel`, `private_channel`, `mpim`, `im`
    types: option<string>,
    /// Set to `true` to exclude archived channels from the list
    exclude-archived: option<bool>,
    /// The maximum number of items to return. Fewer than the requested number of items may be returned, even if the end of the list hasn't been reached. Must be an integer no larger than 1000.
    limit: option<s32>,
    /// Paginate through collections of data by setting the `cursor` parameter to a `next_cursor` attribute returned by a previous request's `response_metadata`. Default value fetches the first "page" of the collection. See [pagination](/docs/pagination) for more detail.
    cursor: option<string>
  }
  conversations: func(params: conversations-params) -> result<string, string>;

  delete-photo: func() -> result<string, string>;

  record get-presence-params {
    /// Authentication token. Requires scope: `users:read`
    token: string,
    /// User to get presence info on. Defaults to the authed user.
    user: option<string>
  }
  get-presence: func(params: get-presence-params) -> result<string, string>;

  record identity-params {
    /// Authentication token. Requires scope: `identity.basic`
    token: option<string>
  }
  identity: func(params: identity-params) -> result<string, string>;

  record info-params {
    /// Authentication token. Requires scope: `users:read`
    token: string,
    /// Set this to `true` to receive the locale for this user. Defaults to `false`
    include-locale: option<bool>,
    /// User to get info on
    user: option<string>
  }
  info: func(params: info-params) -> result<string, string>;

  record list-op-params {
    /// Authentication token. Requires scope: `users:read`
    token: option<string>,
    /// The maximum number of items to return. Fewer than the requested number of items may be returned, even if the end of the users list hasn't been reached. Providing no `limit` value will result in Slack attempting to deliver you the entire result set. If the collection is too large you may experience `limit_required` or HTTP 500 errors.
    limit: option<s32>,
    /// Paginate through collections of data by setting the `cursor` parameter to a `next_cursor` attribute returned by a previous request's `response_metadata`. Default value fetches the first "page" of the collection. See [pagination](/docs/pagination) for more detail.
    cursor: option<string>,
    /// Set this to `true` to receive the locale for users. Defaults to `false`
    include-locale: option<bool>
  }
  list-op: func(params: list-op-params) -> result<string, string>;

  record lookup-by-email-params {
    /// Authentication token. Requires scope: `users:read.email`
    token: string,
    /// An email address belonging to a user in the workspace
    email: string
  }
  lookup-by-email: func(params: lookup-by-email-params) -> result<string, string>;

  record set-active-params {
    /// Authentication token. Requires scope: `users:write`
    token: string
  }
  set-active: func(params: set-active-params) -> result<string, string>;

  set-photo: func() -> result<string, string>;

  record set-presence-params {
    /// Authentication token. Requires scope: `users:write`
    token: string
  }
  set-presence: func(params: set-presence-params) -> result<string, string>;

}

interface users-profile {
  record get-params {
    /// Authentication token. Requires scope: `users.profile:read`
    token: string,
    /// Include labels for each ID in custom profile fields
    include-labels: option<bool>,
    /// User to retrieve profile info for
    user: option<string>
  }
  get: func(params: get-params) -> result<string, string>;

  record set-params {
    /// Authentication token. Requires scope: `users.profile:write`
    token: string
  }
  set: func(params: set-params) -> result<string, string>;

}

interface views {
  record open-params {
    /// Authentication token. Requires scope: `none`
    token: string,
    /// Exchange a trigger to post to the user.
    trigger-id: string,
    /// A [view payload](/reference/surfaces/views). This must be a JSON-encoded string.
    view: string
  }
  open: func(params: open-params) -> result<string, string>;

  record publish-params {
    /// Authentication token. Requires scope: `none`
    token: string,
    /// `id` of the user you want publish a view to.
    user-id: string,
    /// A [view payload](/reference/surfaces/views). This must be a JSON-encoded string.
    view: string,
    /// A string that represents view state to protect against possible race conditions.
    hash: option<string>
  }
  publish: func(params: publish-params) -> result<string, string>;

  record push-params {
    /// Authentication token. Requires scope: `none`
    token: string,
    /// Exchange a trigger to post to the user.
    trigger-id: string,
    /// A [view payload](/reference/surfaces/views). This must be a JSON-encoded string.
    view: string
  }
  push: func(params: push-params) -> result<string, string>;

  record update-params {
    /// Authentication token. Requires scope: `none`
    token: string,
    /// A unique identifier of the view to be updated. Either `view_id` or `external_id` is required.
    view-id: option<string>,
    /// A unique identifier of the view set by the developer. Must be unique for all views on a team. Max length of 255 characters. Either `view_id` or `external_id` is required.
    external-id: option<string>,
    /// A [view object](/reference/surfaces/views). This must be a JSON-encoded string.
    view: option<string>,
    /// A string that represents view state to protect against possible race conditions.
    hash: option<string>
  }
  update: func(params: update-params) -> result<string, string>;

}

interface workflows {
  record step-completed-params {
    /// Authentication token. Requires scope: `workflow.steps:execute`
    token: string,
    /// Context identifier that maps to the correct workflow step execution.
    workflow-step-execute-id: string,
    /// Key-value object of outputs from your step. Keys of this object reflect the configured `key` properties of your [`outputs`](/reference/workflows/workflow_step#output) array from your `workflow_step` object.
    outputs: option<string>
  }
  step-completed: func(params: step-completed-params) -> result<string, string>;

  record step-failed-params {
    /// Authentication token. Requires scope: `workflow.steps:execute`
    token: string,
    /// Context identifier that maps to the correct workflow step execution.
    workflow-step-execute-id: string,
    /// A JSON-based object with a `message` property that should contain a human readable error message.
    error: string
  }
  step-failed: func(params: step-failed-params) -> result<string, string>;

  record update-step-params {
    /// Authentication token. Requires scope: `workflow.steps:execute`
    token: string,
    /// A context identifier provided with `view_submission` payloads used to call back to `workflows.updateStep`.
    workflow-step-edit-id: string,
    /// A JSON key-value map of inputs required from a user during configuration. This is the data your app expects to receive when the workflow step starts. **Please note**: the embedded variable format is set and replaced by the workflow system. You cannot create custom variables that will be replaced at runtime. [Read more about variables in workflow steps here](/workflows/steps#variables).
    inputs: option<string>,
    /// An JSON array of output objects used during step execution. This is the data your app agrees to provide when your workflow step was executed.
    outputs: option<string>,
    /// An optional field that can be used to override the step name that is shown in the Workflow Builder.
    step-name: option<string>,
    /// An optional field that can be used to override app image that is shown in the Workflow Builder.
    step-image-url: option<string>
  }
  update-step: func(params: update-step-params) -> result<string, string>;

}
/// The component world: imports the secrets host capability
/// the runtime uses and exports the generated interfaces. The HTTP host
/// capability is imported by the `wstd` crate's bindings at build time.
world client {
  import wasmcloud:secrets/store@1.0.0;
  import wasmcloud:secrets/reveal@1.0.0;
  export admin-apps;
  export admin-apps-approved;
  export admin-apps-requests;
  export admin-apps-restricted;
  export admin-conversations;
  export admin-conversations-ekm;
  export admin-conversations-restrict-access;
  export admin-emoji;
  export admin-invite-requests;
  export admin-invite-requests-approved;
  export admin-invite-requests-denied;
  export admin-teams-admins;
  export admin-teams;
  export admin-teams-owners;
  export admin-teams-settings;
  export admin-usergroups;
  export admin-users;
  export admin-users-session;
  export api;
  export apps-event-authorizations;
  export apps-permissions;
  export apps-permissions-resources;
  export apps-permissions-scopes;
  export apps-permissions-users;
  export apps;
  export auth;
  export bots;
  export calls;
  export calls-participants;
  export chat;
  export chat-scheduled-messages;
  export conversations;
  export dialog;
  export dnd;
  export emoji;
  export files-comments;
  export files;
  export files-remote;
  export migration;
  export oauth;
  export oauth-v2;
  export pins;
  export reactions;
  export reminders;
  export rtm;
  export search;
  export stars;
  export team;
  export team-profile;
  export usergroups;
  export usergroups-users;
  export users;
  export users-profile;
  export views;
  export workflows;
}
