/// ## Authentication
///
/// Reads credentials from the host via `wasmcloud:secrets`; operations never take a token.
/// Provision these named secrets:
///
/// | Secret name | Applied to each request as |
/// | --- | --- |
/// | `basicAuth` | `Authorization: Basic base64(<secret>)` |
package autostamp:stripe@0.2.1;

interface account {
  record get-account-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-account: func(params: get-account-params) -> result<string, string>;

}


interface account-links {
  post-account-links: func() -> result<string, string>;

}

interface accounts {
  record get-accounts-params {
    created: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-accounts: func(params: get-accounts-params) -> result<string, string>;

  post-accounts: func() -> result<string, string>;

  record get-accounts-account-params {
    account: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-accounts-account: func(params: get-accounts-account-params) -> result<string, string>;

  record post-accounts-account-params {
    account: string
  }
  post-accounts-account: func(params: post-accounts-account-params) -> result<string, string>;

  record delete-accounts-account-params {
    account: string
  }
  delete-accounts-account: func(params: delete-accounts-account-params) -> result<string, string>;

  record post-accounts-account-bank-accounts-params {
    account: string
  }
  post-accounts-account-bank-accounts: func(params: post-accounts-account-bank-accounts-params) -> result<string, string>;

  record get-accounts-account-bank-accounts-id-params {
    account: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    id: string
  }
  get-accounts-account-bank-accounts-id: func(params: get-accounts-account-bank-accounts-id-params) -> result<string, string>;

  record post-accounts-account-bank-accounts-id-params {
    account: string,
    id: string
  }
  post-accounts-account-bank-accounts-id: func(params: post-accounts-account-bank-accounts-id-params) -> result<string, string>;

  record delete-accounts-account-bank-accounts-id-params {
    account: string,
    id: string
  }
  delete-accounts-account-bank-accounts-id: func(params: delete-accounts-account-bank-accounts-id-params) -> result<string, string>;

  record get-accounts-account-capabilities-params {
    account: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-accounts-account-capabilities: func(params: get-accounts-account-capabilities-params) -> result<string, string>;

  record get-accounts-account-capabilities-capability-params {
    account: string,
    capability: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-accounts-account-capabilities-capability: func(params: get-accounts-account-capabilities-capability-params) -> result<string, string>;

  record post-accounts-account-capabilities-capability-params {
    account: string,
    capability: string
  }
  post-accounts-account-capabilities-capability: func(params: post-accounts-account-capabilities-capability-params) -> result<string, string>;

  record get-accounts-account-external-accounts-params {
    account: string,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-accounts-account-external-accounts: func(params: get-accounts-account-external-accounts-params) -> result<string, string>;

  record post-accounts-account-external-accounts-params {
    account: string
  }
  post-accounts-account-external-accounts: func(params: post-accounts-account-external-accounts-params) -> result<string, string>;

  record get-accounts-account-external-accounts-id-params {
    account: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    id: string
  }
  get-accounts-account-external-accounts-id: func(params: get-accounts-account-external-accounts-id-params) -> result<string, string>;

  record post-accounts-account-external-accounts-id-params {
    account: string,
    id: string
  }
  post-accounts-account-external-accounts-id: func(params: post-accounts-account-external-accounts-id-params) -> result<string, string>;

  record delete-accounts-account-external-accounts-id-params {
    account: string,
    id: string
  }
  delete-accounts-account-external-accounts-id: func(params: delete-accounts-account-external-accounts-id-params) -> result<string, string>;

  record post-accounts-account-login-links-params {
    account: string
  }
  post-accounts-account-login-links: func(params: post-accounts-account-login-links-params) -> result<string, string>;

  record get-accounts-account-people-params {
    account: string,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// Filters on the list of people returned based on the person's relationship to the account's company.
    relationship: option<string>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-accounts-account-people: func(params: get-accounts-account-people-params) -> result<string, string>;

  record post-accounts-account-people-params {
    account: string
  }
  post-accounts-account-people: func(params: post-accounts-account-people-params) -> result<string, string>;

  record get-accounts-account-people-person-params {
    account: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    person: string
  }
  get-accounts-account-people-person: func(params: get-accounts-account-people-person-params) -> result<string, string>;

  record post-accounts-account-people-person-params {
    account: string,
    person: string
  }
  post-accounts-account-people-person: func(params: post-accounts-account-people-person-params) -> result<string, string>;

  record delete-accounts-account-people-person-params {
    account: string,
    person: string
  }
  delete-accounts-account-people-person: func(params: delete-accounts-account-people-person-params) -> result<string, string>;

  record get-accounts-account-persons-params {
    account: string,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// Filters on the list of people returned based on the person's relationship to the account's company.
    relationship: option<string>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-accounts-account-persons: func(params: get-accounts-account-persons-params) -> result<string, string>;

  record post-accounts-account-persons-params {
    account: string
  }
  post-accounts-account-persons: func(params: post-accounts-account-persons-params) -> result<string, string>;

  record get-accounts-account-persons-person-params {
    account: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    person: string
  }
  get-accounts-account-persons-person: func(params: get-accounts-account-persons-person-params) -> result<string, string>;

  record post-accounts-account-persons-person-params {
    account: string,
    person: string
  }
  post-accounts-account-persons-person: func(params: post-accounts-account-persons-person-params) -> result<string, string>;

  record delete-accounts-account-persons-person-params {
    account: string,
    person: string
  }
  delete-accounts-account-persons-person: func(params: delete-accounts-account-persons-person-params) -> result<string, string>;

  record post-accounts-account-reject-params {
    account: string
  }
  post-accounts-account-reject: func(params: post-accounts-account-reject-params) -> result<string, string>;

}

interface apple-pay {
  record get-apple-pay-domains-params {
    domain-name: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-apple-pay-domains: func(params: get-apple-pay-domains-params) -> result<string, string>;

  post-apple-pay-domains: func() -> result<string, string>;

  record get-apple-pay-domains-domain-params {
    domain: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-apple-pay-domains-domain: func(params: get-apple-pay-domains-domain-params) -> result<string, string>;

  record delete-apple-pay-domains-domain-params {
    domain: string
  }
  delete-apple-pay-domains-domain: func(params: delete-apple-pay-domains-domain-params) -> result<string, string>;

}

interface application-fees {
  record get-application-fees-params {
    /// Only return application fees for the charge specified by this charge ID.
    charge: option<string>,
    created: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-application-fees: func(params: get-application-fees-params) -> result<string, string>;

  record get-application-fees-fee-refunds-id-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    fee: string,
    id: string
  }
  get-application-fees-fee-refunds-id: func(params: get-application-fees-fee-refunds-id-params) -> result<string, string>;

  record post-application-fees-fee-refunds-id-params {
    fee: string,
    id: string
  }
  post-application-fees-fee-refunds-id: func(params: post-application-fees-fee-refunds-id-params) -> result<string, string>;

  record get-application-fees-id-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    id: string
  }
  get-application-fees-id: func(params: get-application-fees-id-params) -> result<string, string>;

  record post-application-fees-id-refund-params {
    id: string
  }
  post-application-fees-id-refund: func(params: post-application-fees-id-refund-params) -> result<string, string>;

  record get-application-fees-id-refunds-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    id: string,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-application-fees-id-refunds: func(params: get-application-fees-id-refunds-params) -> result<string, string>;

  record post-application-fees-id-refunds-params {
    id: string
  }
  post-application-fees-id-refunds: func(params: post-application-fees-id-refunds-params) -> result<string, string>;

}

interface apps {
  record get-apps-secrets-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// Specifies the scoping of the secret. Requests originating from UI extensions can only access account-scoped secrets or secrets scoped to their own user.
    scope: string,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-apps-secrets: func(params: get-apps-secrets-params) -> result<string, string>;

  post-apps-secrets: func() -> result<string, string>;

  post-apps-secrets-delete: func() -> result<string, string>;

  record get-apps-secrets-find-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A name for the secret that's unique within the scope.
    name: string,
    /// Specifies the scoping of the secret. Requests originating from UI extensions can only access account-scoped secrets or secrets scoped to their own user.
    scope: string
  }
  get-apps-secrets-find: func(params: get-apps-secrets-find-params) -> result<string, string>;

}

interface balance {
  record get-balance-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-balance: func(params: get-balance-params) -> result<string, string>;

  record get-balance-history-params {
    created: option<string>,
    /// Only return transactions in a certain currency. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
    currency: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// For automatic Stripe payouts only, only returns transactions that were paid out on the specified payout ID.
    payout: option<string>,
    /// Only returns the original transaction.
    source: option<string>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Only returns transactions of the given type. One of: `adjustment`, `advance`, `advance_funding`, `anticipation_repayment`, `application_fee`, `application_fee_refund`, `charge`, `connect_collection_transfer`, `contribution`, `issuing_authorization_hold`, `issuing_authorization_release`, `issuing_dispute`, `issuing_transaction`, `payment`, `payment_failure_refund`, `payment_refund`, `payout`, `payout_cancel`, `payout_failure`, `refund`, `refund_failure`, `reserve_transaction`, `reserved_funds`, `stripe_fee`, `stripe_fx_fee`, `tax_fee`, `topup`, `topup_reversal`, `transfer`, `transfer_cancel`, `transfer_failure`, or `transfer_refund`.
    type-op: option<string>
  }
  get-balance-history: func(params: get-balance-history-params) -> result<string, string>;

  record get-balance-history-id-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    id: string
  }
  get-balance-history-id: func(params: get-balance-history-id-params) -> result<string, string>;

}

interface balance-transactions {
  record get-balance-transactions-params {
    created: option<string>,
    /// Only return transactions in a certain currency. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
    currency: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// For automatic Stripe payouts only, only returns transactions that were paid out on the specified payout ID.
    payout: option<string>,
    /// Only returns the original transaction.
    source: option<string>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Only returns transactions of the given type. One of: `adjustment`, `advance`, `advance_funding`, `anticipation_repayment`, `application_fee`, `application_fee_refund`, `charge`, `connect_collection_transfer`, `contribution`, `issuing_authorization_hold`, `issuing_authorization_release`, `issuing_dispute`, `issuing_transaction`, `payment`, `payment_failure_refund`, `payment_refund`, `payout`, `payout_cancel`, `payout_failure`, `refund`, `refund_failure`, `reserve_transaction`, `reserved_funds`, `stripe_fee`, `stripe_fx_fee`, `tax_fee`, `topup`, `topup_reversal`, `transfer`, `transfer_cancel`, `transfer_failure`, or `transfer_refund`.
    type-op: option<string>
  }
  get-balance-transactions: func(params: get-balance-transactions-params) -> result<string, string>;

  record get-balance-transactions-id-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    id: string
  }
  get-balance-transactions-id: func(params: get-balance-transactions-id-params) -> result<string, string>;

}

interface billing-portal {
  record get-billing-portal-configurations-params {
    /// Only return configurations that are active or inactive (e.g., pass `true` to only list active configurations).
    active: option<bool>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// Only return the default or non-default configurations (e.g., pass `true` to only list the default configuration).
    is-default: option<bool>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-billing-portal-configurations: func(params: get-billing-portal-configurations-params) -> result<string, string>;

  post-billing-portal-configurations: func() -> result<string, string>;

  record get-billing-portal-configurations-configuration-params {
    configuration: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-billing-portal-configurations-configuration: func(params: get-billing-portal-configurations-configuration-params) -> result<string, string>;

  record post-billing-portal-configurations-configuration-params {
    configuration: string
  }
  post-billing-portal-configurations-configuration: func(params: post-billing-portal-configurations-configuration-params) -> result<string, string>;

  post-billing-portal-sessions: func() -> result<string, string>;

}

interface charges {
  record get-charges-params {
    created: option<string>,
    /// Only return charges for the customer specified by this customer ID.
    customer: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// Only return charges that were created by the PaymentIntent specified by this PaymentIntent ID.
    payment-intent: option<string>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Only return charges for this transfer group.
    transfer-group: option<string>
  }
  get-charges: func(params: get-charges-params) -> result<string, string>;

  post-charges: func() -> result<string, string>;

  record get-charges-search-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
    page: option<string>,
    /// The search query string. See [search query language](https://stripe.com/docs/search#search-query-language) and the list of supported [query fields for charges](https://stripe.com/docs/search#query-fields-for-charges).
    query: string
  }
  get-charges-search: func(params: get-charges-search-params) -> result<string, string>;

  record get-charges-charge-params {
    charge: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-charges-charge: func(params: get-charges-charge-params) -> result<string, string>;

  record post-charges-charge-params {
    charge: string
  }
  post-charges-charge: func(params: post-charges-charge-params) -> result<string, string>;

  record post-charges-charge-capture-params {
    charge: string
  }
  post-charges-charge-capture: func(params: post-charges-charge-capture-params) -> result<string, string>;

  record get-charges-charge-dispute-params {
    charge: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-charges-charge-dispute: func(params: get-charges-charge-dispute-params) -> result<string, string>;

  record post-charges-charge-dispute-params {
    charge: string
  }
  post-charges-charge-dispute: func(params: post-charges-charge-dispute-params) -> result<string, string>;

  record post-charges-charge-dispute-close-params {
    charge: string
  }
  post-charges-charge-dispute-close: func(params: post-charges-charge-dispute-close-params) -> result<string, string>;

  record post-charges-charge-refund-params {
    charge: string
  }
  post-charges-charge-refund: func(params: post-charges-charge-refund-params) -> result<string, string>;

  record get-charges-charge-refunds-params {
    charge: string,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-charges-charge-refunds: func(params: get-charges-charge-refunds-params) -> result<string, string>;

  record post-charges-charge-refunds-params {
    charge: string
  }
  post-charges-charge-refunds: func(params: post-charges-charge-refunds-params) -> result<string, string>;

  record get-charges-charge-refunds-refund-params {
    charge: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    refund: string
  }
  get-charges-charge-refunds-refund: func(params: get-charges-charge-refunds-refund-params) -> result<string, string>;

  record post-charges-charge-refunds-refund-params {
    charge: string,
    refund: string
  }
  post-charges-charge-refunds-refund: func(params: post-charges-charge-refunds-refund-params) -> result<string, string>;

}

interface checkout {
  record get-checkout-sessions-params {
    /// Only return the Checkout Sessions for the Customer specified.
    customer: option<string>,
    /// Only return the Checkout Sessions for the Customer details specified.
    customer-details: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// Only return the Checkout Session for the PaymentIntent specified.
    payment-intent: option<string>,
    /// Only return the Checkout Sessions for the Payment Link specified.
    payment-link: option<string>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Only return the Checkout Session for the subscription specified.
    subscription: option<string>
  }
  get-checkout-sessions: func(params: get-checkout-sessions-params) -> result<string, string>;

  post-checkout-sessions: func() -> result<string, string>;

  record get-checkout-sessions-session-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    session: string
  }
  get-checkout-sessions-session: func(params: get-checkout-sessions-session-params) -> result<string, string>;

  record post-checkout-sessions-session-expire-params {
    session: string
  }
  post-checkout-sessions-session-expire: func(params: post-checkout-sessions-session-expire-params) -> result<string, string>;

  record get-checkout-sessions-session-line-items-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    session: string,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-checkout-sessions-session-line-items: func(params: get-checkout-sessions-session-line-items-params) -> result<string, string>;

}

interface country-specs {
  record get-country-specs-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-country-specs: func(params: get-country-specs-params) -> result<string, string>;

  record get-country-specs-country-params {
    country: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-country-specs-country: func(params: get-country-specs-country-params) -> result<string, string>;

}

interface coupons {
  record get-coupons-params {
    /// A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
    created: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-coupons: func(params: get-coupons-params) -> result<string, string>;

  post-coupons: func() -> result<string, string>;

  record get-coupons-coupon-params {
    coupon: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-coupons-coupon: func(params: get-coupons-coupon-params) -> result<string, string>;

  record post-coupons-coupon-params {
    coupon: string
  }
  post-coupons-coupon: func(params: post-coupons-coupon-params) -> result<string, string>;

  record delete-coupons-coupon-params {
    coupon: string
  }
  delete-coupons-coupon: func(params: delete-coupons-coupon-params) -> result<string, string>;

}

interface credit-notes {
  enum get-credit-notes-preview-lines-item-type-op-enum {
    custom-line-item,
    invoice-line-item,
  }

  enum get-credit-notes-preview-reason-enum {
    duplicate,
    fraudulent,
    order-change,
    product-unsatisfactory,
  }

  record get-credit-notes-preview-lines-item {
    amount: option<s32>,
    description: option<string>,
    invoice-line-item: option<string>,
    quantity: option<s32>,
    tax-rates: option<string>,
    type-op: get-credit-notes-preview-lines-item-type-op-enum,
    unit-amount: option<s32>,
    unit-amount-decimal: option<string>
  }

  record get-credit-notes-preview-lines-lines-item {
    amount: option<s32>,
    description: option<string>,
    invoice-line-item: option<string>,
    quantity: option<s32>,
    tax-rates: option<string>,
    type-op: get-credit-notes-preview-lines-item-type-op-enum,
    unit-amount: option<s32>,
    unit-amount-decimal: option<string>
  }

  record get-credit-notes-params {
    /// Only return credit notes for the customer specified by this customer ID.
    customer: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// Only return credit notes for the invoice specified by this invoice ID.
    invoice: option<string>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-credit-notes: func(params: get-credit-notes-params) -> result<string, string>;

  post-credit-notes: func() -> result<string, string>;

  record get-credit-notes-preview-params {
    /// The integer amount in cents (or local equivalent) representing the total amount of the credit note.
    amount: option<s32>,
    /// The integer amount in cents (or local equivalent) representing the amount to credit the customer's balance, which will be automatically applied to their next invoice.
    credit-amount: option<s32>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// ID of the invoice.
    invoice: string,
    /// Line items that make up the credit note.
    lines: option<list<get-credit-notes-preview-lines-item>>,
    /// The credit note's memo appears on the credit note PDF.
    memo: option<string>,
    /// Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
    metadata: option<string>,
    /// The integer amount in cents (or local equivalent) representing the amount that is credited outside of Stripe.
    out-of-band-amount: option<s32>,
    /// Reason for issuing this credit note, one of `duplicate`, `fraudulent`, `order_change`, or `product_unsatisfactory`
    reason: option<get-credit-notes-preview-reason-enum>,
    /// ID of an existing refund to link this credit note to.
    refund: option<string>,
    /// The integer amount in cents (or local equivalent) representing the amount to refund. If set, a refund will be created for the charge associated with the invoice.
    refund-amount: option<s32>,
    /// When shipping_cost contains the shipping_rate from the invoice, the shipping_cost is included in the credit note.
    shipping-cost: option<string>
  }
  get-credit-notes-preview: func(params: get-credit-notes-preview-params) -> result<string, string>;

  record get-credit-notes-preview-lines-params {
    /// The integer amount in cents (or local equivalent) representing the total amount of the credit note.
    amount: option<s32>,
    /// The integer amount in cents (or local equivalent) representing the amount to credit the customer's balance, which will be automatically applied to their next invoice.
    credit-amount: option<s32>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// ID of the invoice.
    invoice: string,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// Line items that make up the credit note.
    lines: option<list<get-credit-notes-preview-lines-lines-item>>,
    /// The credit note's memo appears on the credit note PDF.
    memo: option<string>,
    /// Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
    metadata: option<string>,
    /// The integer amount in cents (or local equivalent) representing the amount that is credited outside of Stripe.
    out-of-band-amount: option<s32>,
    /// Reason for issuing this credit note, one of `duplicate`, `fraudulent`, `order_change`, or `product_unsatisfactory`
    reason: option<get-credit-notes-preview-reason-enum>,
    /// ID of an existing refund to link this credit note to.
    refund: option<string>,
    /// The integer amount in cents (or local equivalent) representing the amount to refund. If set, a refund will be created for the charge associated with the invoice.
    refund-amount: option<s32>,
    /// When shipping_cost contains the shipping_rate from the invoice, the shipping_cost is included in the credit note.
    shipping-cost: option<string>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-credit-notes-preview-lines: func(params: get-credit-notes-preview-lines-params) -> result<string, string>;

  record get-credit-notes-credit-note-lines-params {
    credit-note: string,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-credit-notes-credit-note-lines: func(params: get-credit-notes-credit-note-lines-params) -> result<string, string>;

  record get-credit-notes-id-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    id: string
  }
  get-credit-notes-id: func(params: get-credit-notes-id-params) -> result<string, string>;

  record post-credit-notes-id-params {
    id: string
  }
  post-credit-notes-id: func(params: post-credit-notes-id-params) -> result<string, string>;

  record post-credit-notes-id-void-params {
    id: string
  }
  post-credit-notes-id-void: func(params: post-credit-notes-id-void-params) -> result<string, string>;

}

interface customers {
  enum get-customers-customer-payment-methods-type-op-enum {
    acss-debit,
    affirm,
    afterpay-clearpay,
    alipay,
    au-becs-debit,
    bacs-debit,
    bancontact,
    blik,
    boleto,
    card,
    cashapp,
    customer-balance,
    eps,
    fpx,
    giropay,
    grabpay,
    ideal,
    klarna,
    konbini,
    link,
    oxxo,
    p24,
    paynow,
    pix,
    promptpay,
    sepa-debit,
    sofort,
    us-bank-account,
    wechat-pay,
  }

  record get-customers-params {
    created: option<string>,
    /// A case-sensitive filter on the list based on the customer's `email` field. The value must be a string.
    email: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Provides a list of customers that are associated with the specified test clock. The response will not include customers with test clocks if this parameter is not set.
    test-clock: option<string>
  }
  get-customers: func(params: get-customers-params) -> result<string, string>;

  post-customers: func() -> result<string, string>;

  record get-customers-search-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
    page: option<string>,
    /// The search query string. See [search query language](https://stripe.com/docs/search#search-query-language) and the list of supported [query fields for customers](https://stripe.com/docs/search#query-fields-for-customers).
    query: string
  }
  get-customers-search: func(params: get-customers-search-params) -> result<string, string>;

  record get-customers-customer-params {
    customer: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-customers-customer: func(params: get-customers-customer-params) -> result<string, string>;

  record post-customers-customer-params {
    customer: string
  }
  post-customers-customer: func(params: post-customers-customer-params) -> result<string, string>;

  record delete-customers-customer-params {
    customer: string
  }
  delete-customers-customer: func(params: delete-customers-customer-params) -> result<string, string>;

  record get-customers-customer-balance-transactions-params {
    customer: string,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-customers-customer-balance-transactions: func(params: get-customers-customer-balance-transactions-params) -> result<string, string>;

  record post-customers-customer-balance-transactions-params {
    customer: string
  }
  post-customers-customer-balance-transactions: func(params: post-customers-customer-balance-transactions-params) -> result<string, string>;

  record get-customers-customer-balance-transactions-transaction-params {
    customer: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    transaction: string
  }
  get-customers-customer-balance-transactions-transaction: func(params: get-customers-customer-balance-transactions-transaction-params) -> result<string, string>;

  record post-customers-customer-balance-transactions-transaction-params {
    customer: string,
    transaction: string
  }
  post-customers-customer-balance-transactions-transaction: func(params: post-customers-customer-balance-transactions-transaction-params) -> result<string, string>;

  record get-customers-customer-bank-accounts-params {
    customer: string,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-customers-customer-bank-accounts: func(params: get-customers-customer-bank-accounts-params) -> result<string, string>;

  record post-customers-customer-bank-accounts-params {
    customer: string
  }
  post-customers-customer-bank-accounts: func(params: post-customers-customer-bank-accounts-params) -> result<string, string>;

  record get-customers-customer-bank-accounts-id-params {
    customer: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    id: string
  }
  get-customers-customer-bank-accounts-id: func(params: get-customers-customer-bank-accounts-id-params) -> result<string, string>;

  record post-customers-customer-bank-accounts-id-params {
    customer: string,
    id: string
  }
  post-customers-customer-bank-accounts-id: func(params: post-customers-customer-bank-accounts-id-params) -> result<string, string>;

  record delete-customers-customer-bank-accounts-id-params {
    customer: string,
    id: string
  }
  delete-customers-customer-bank-accounts-id: func(params: delete-customers-customer-bank-accounts-id-params) -> result<string, string>;

  record post-customers-customer-bank-accounts-id-verify-params {
    customer: string,
    id: string
  }
  post-customers-customer-bank-accounts-id-verify: func(params: post-customers-customer-bank-accounts-id-verify-params) -> result<string, string>;

  record get-customers-customer-cards-params {
    customer: string,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-customers-customer-cards: func(params: get-customers-customer-cards-params) -> result<string, string>;

  record post-customers-customer-cards-params {
    customer: string
  }
  post-customers-customer-cards: func(params: post-customers-customer-cards-params) -> result<string, string>;

  record get-customers-customer-cards-id-params {
    customer: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    id: string
  }
  get-customers-customer-cards-id: func(params: get-customers-customer-cards-id-params) -> result<string, string>;

  record post-customers-customer-cards-id-params {
    customer: string,
    id: string
  }
  post-customers-customer-cards-id: func(params: post-customers-customer-cards-id-params) -> result<string, string>;

  record delete-customers-customer-cards-id-params {
    customer: string,
    id: string
  }
  delete-customers-customer-cards-id: func(params: delete-customers-customer-cards-id-params) -> result<string, string>;

  record get-customers-customer-cash-balance-params {
    customer: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-customers-customer-cash-balance: func(params: get-customers-customer-cash-balance-params) -> result<string, string>;

  record post-customers-customer-cash-balance-params {
    customer: string
  }
  post-customers-customer-cash-balance: func(params: post-customers-customer-cash-balance-params) -> result<string, string>;

  record get-customers-customer-cash-balance-transactions-params {
    customer: string,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-customers-customer-cash-balance-transactions: func(params: get-customers-customer-cash-balance-transactions-params) -> result<string, string>;

  record get-customers-customer-cash-balance-transactions-transaction-params {
    customer: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    transaction: string
  }
  get-customers-customer-cash-balance-transactions-transaction: func(params: get-customers-customer-cash-balance-transactions-transaction-params) -> result<string, string>;

  record get-customers-customer-discount-params {
    customer: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-customers-customer-discount: func(params: get-customers-customer-discount-params) -> result<string, string>;

  record delete-customers-customer-discount-params {
    customer: string
  }
  delete-customers-customer-discount: func(params: delete-customers-customer-discount-params) -> result<string, string>;

  record post-customers-customer-funding-instructions-params {
    customer: string
  }
  post-customers-customer-funding-instructions: func(params: post-customers-customer-funding-instructions-params) -> result<string, string>;

  record get-customers-customer-payment-methods-params {
    customer: string,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// An optional filter on the list, based on the object `type` field. Without the filter, the list includes all current and future payment method types. If your integration expects only one type of payment method in the response, make sure to provide a type value in the request.
    type-op: option<get-customers-customer-payment-methods-type-op-enum>
  }
  get-customers-customer-payment-methods: func(params: get-customers-customer-payment-methods-params) -> result<string, string>;

  record get-customers-customer-payment-methods-payment-method-params {
    customer: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    payment-method: string
  }
  get-customers-customer-payment-methods-payment-method: func(params: get-customers-customer-payment-methods-payment-method-params) -> result<string, string>;

  record get-customers-customer-sources-params {
    customer: string,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// Filter sources according to a particular object type.
    object: option<string>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-customers-customer-sources: func(params: get-customers-customer-sources-params) -> result<string, string>;

  record post-customers-customer-sources-params {
    customer: string
  }
  post-customers-customer-sources: func(params: post-customers-customer-sources-params) -> result<string, string>;

  record get-customers-customer-sources-id-params {
    customer: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    id: string
  }
  get-customers-customer-sources-id: func(params: get-customers-customer-sources-id-params) -> result<string, string>;

  record post-customers-customer-sources-id-params {
    customer: string,
    id: string
  }
  post-customers-customer-sources-id: func(params: post-customers-customer-sources-id-params) -> result<string, string>;

  record delete-customers-customer-sources-id-params {
    customer: string,
    id: string
  }
  delete-customers-customer-sources-id: func(params: delete-customers-customer-sources-id-params) -> result<string, string>;

  record post-customers-customer-sources-id-verify-params {
    customer: string,
    id: string
  }
  post-customers-customer-sources-id-verify: func(params: post-customers-customer-sources-id-verify-params) -> result<string, string>;

  record get-customers-customer-subscriptions-params {
    customer: string,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-customers-customer-subscriptions: func(params: get-customers-customer-subscriptions-params) -> result<string, string>;

  record post-customers-customer-subscriptions-params {
    customer: string
  }
  post-customers-customer-subscriptions: func(params: post-customers-customer-subscriptions-params) -> result<string, string>;

  record get-customers-customer-subscriptions-subscription-exposed-id-params {
    customer: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    subscription-exposed-id: string
  }
  get-customers-customer-subscriptions-subscription-exposed-id: func(params: get-customers-customer-subscriptions-subscription-exposed-id-params) -> result<string, string>;

  record post-customers-customer-subscriptions-subscription-exposed-id-params {
    customer: string,
    subscription-exposed-id: string
  }
  post-customers-customer-subscriptions-subscription-exposed-id: func(params: post-customers-customer-subscriptions-subscription-exposed-id-params) -> result<string, string>;

  record delete-customers-customer-subscriptions-subscription-exposed-id-params {
    customer: string,
    subscription-exposed-id: string
  }
  delete-customers-customer-subscriptions-subscription-exposed-id: func(params: delete-customers-customer-subscriptions-subscription-exposed-id-params) -> result<string, string>;

  record get-customers-customer-subscriptions-subscription-exposed-id-discount-params {
    customer: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    subscription-exposed-id: string
  }
  get-customers-customer-subscriptions-subscription-exposed-id-discount: func(params: get-customers-customer-subscriptions-subscription-exposed-id-discount-params) -> result<string, string>;

  record delete-customers-customer-subscriptions-subscription-exposed-id-discount-params {
    customer: string,
    subscription-exposed-id: string
  }
  delete-customers-customer-subscriptions-subscription-exposed-id-discount: func(params: delete-customers-customer-subscriptions-subscription-exposed-id-discount-params) -> result<string, string>;

  record get-customers-customer-tax-ids-params {
    customer: string,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-customers-customer-tax-ids: func(params: get-customers-customer-tax-ids-params) -> result<string, string>;

  record post-customers-customer-tax-ids-params {
    customer: string
  }
  post-customers-customer-tax-ids: func(params: post-customers-customer-tax-ids-params) -> result<string, string>;

  record get-customers-customer-tax-ids-id-params {
    customer: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    id: string
  }
  get-customers-customer-tax-ids-id: func(params: get-customers-customer-tax-ids-id-params) -> result<string, string>;

  record delete-customers-customer-tax-ids-id-params {
    customer: string,
    id: string
  }
  delete-customers-customer-tax-ids-id: func(params: delete-customers-customer-tax-ids-id-params) -> result<string, string>;

}

interface disputes {
  record get-disputes-params {
    /// Only return disputes associated to the charge specified by this charge ID.
    charge: option<string>,
    created: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// Only return disputes associated to the PaymentIntent specified by this PaymentIntent ID.
    payment-intent: option<string>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-disputes: func(params: get-disputes-params) -> result<string, string>;

  record get-disputes-dispute-params {
    dispute: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-disputes-dispute: func(params: get-disputes-dispute-params) -> result<string, string>;

  record post-disputes-dispute-params {
    dispute: string
  }
  post-disputes-dispute: func(params: post-disputes-dispute-params) -> result<string, string>;

  record post-disputes-dispute-close-params {
    dispute: string
  }
  post-disputes-dispute-close: func(params: post-disputes-dispute-close-params) -> result<string, string>;

}

interface ephemeral-keys {
  post-ephemeral-keys: func() -> result<string, string>;

  record delete-ephemeral-keys-key-params {
    key: string
  }
  delete-ephemeral-keys-key: func(params: delete-ephemeral-keys-key-params) -> result<string, string>;

}

interface events {
  record get-events-params {
    created: option<string>,
    /// Filter events by whether all webhooks were successfully delivered. If false, events which are still pending or have failed all delivery attempts to a webhook endpoint will be returned.
    delivery-success: option<bool>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// A string containing a specific event name, or group of events using * as a wildcard. The list will be filtered to include only events with a matching event property.
    type-op: option<string>,
    /// An array of up to 20 strings containing specific event names. The list will be filtered to include only events with a matching event property. You may pass either `type` or `types`, but not both.
    types: option<list<string>>
  }
  get-events: func(params: get-events-params) -> result<string, string>;

  record get-events-id-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    id: string
  }
  get-events-id: func(params: get-events-id-params) -> result<string, string>;

}

interface exchange-rates {
  record get-exchange-rates-params {
    /// A cursor for use in pagination. `ending_before` is the currency that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with the exchange rate for currency X your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and total number of supported payout currencies, and the default is the max.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is the currency that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with the exchange rate for currency X, your subsequent call can include `starting_after=X` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-exchange-rates: func(params: get-exchange-rates-params) -> result<string, string>;

  record get-exchange-rates-rate-id-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    rate-id: string
  }
  get-exchange-rates-rate-id: func(params: get-exchange-rates-rate-id-params) -> result<string, string>;

}

interface file-links {
  record get-file-links-params {
    created: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// Filter links by their expiration status. By default, all links are returned.
    expired: option<bool>,
    /// Only return links for the given file.
    file: option<string>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-file-links: func(params: get-file-links-params) -> result<string, string>;

  post-file-links: func() -> result<string, string>;

  record get-file-links-link-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    link: string
  }
  get-file-links-link: func(params: get-file-links-link-params) -> result<string, string>;

  record post-file-links-link-params {
    link: string
  }
  post-file-links-link: func(params: post-file-links-link-params) -> result<string, string>;

}

interface files {
  enum get-files-purpose-enum {
    account-requirement,
    additional-verification,
    business-icon,
    business-logo,
    customer-signature,
    dispute-evidence,
    document-provider-identity-document,
    finance-report-run,
    identity-document,
    identity-document-downloadable,
    pci-document,
    selfie,
    sigma-scheduled-query,
    tax-document-user-upload,
    terminal-reader-splashscreen,
  }

  record get-files-params {
    created: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// The file purpose to filter queries by. If none is provided, files will not be filtered by purpose.
    purpose: option<get-files-purpose-enum>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-files: func(params: get-files-params) -> result<string, string>;

  post-files: func() -> result<string, string>;

  record get-files-file-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    file: string
  }
  get-files-file: func(params: get-files-file-params) -> result<string, string>;

}

interface financial-connections {
  record get-financial-connections-accounts-params {
    /// If present, only return accounts that belong to the specified account holder. `account_holder[customer]` and `account_holder[account]` are mutually exclusive.
    account-holder: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// If present, only return accounts that were collected as part of the given session.
    session: option<string>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-financial-connections-accounts: func(params: get-financial-connections-accounts-params) -> result<string, string>;

  record get-financial-connections-accounts-account-params {
    account: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-financial-connections-accounts-account: func(params: get-financial-connections-accounts-account-params) -> result<string, string>;

  record post-financial-connections-accounts-account-disconnect-params {
    account: string
  }
  post-financial-connections-accounts-account-disconnect: func(params: post-financial-connections-accounts-account-disconnect-params) -> result<string, string>;

  record get-financial-connections-accounts-account-owners-params {
    account: string,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// The ID of the ownership object to fetch owners from.
    ownership: string,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-financial-connections-accounts-account-owners: func(params: get-financial-connections-accounts-account-owners-params) -> result<string, string>;

  record post-financial-connections-accounts-account-refresh-params {
    account: string
  }
  post-financial-connections-accounts-account-refresh: func(params: post-financial-connections-accounts-account-refresh-params) -> result<string, string>;

  post-financial-connections-sessions: func() -> result<string, string>;

  record get-financial-connections-sessions-session-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    session: string
  }
  get-financial-connections-sessions-session: func(params: get-financial-connections-sessions-session-params) -> result<string, string>;

}

interface identity {
  enum get-identity-verification-reports-type-op-enum {
    document,
    id-number,
  }

  enum get-identity-verification-sessions-status-enum {
    canceled,
    processing,
    requires-input,
    verified,
  }

  record get-identity-verification-reports-params {
    created: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Only return VerificationReports of this type
    type-op: option<get-identity-verification-reports-type-op-enum>,
    /// Only return VerificationReports created by this VerificationSession ID. It is allowed to provide a VerificationIntent ID.
    verification-session: option<string>
  }
  get-identity-verification-reports: func(params: get-identity-verification-reports-params) -> result<string, string>;

  record get-identity-verification-reports-report-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    report: string
  }
  get-identity-verification-reports-report: func(params: get-identity-verification-reports-report-params) -> result<string, string>;

  record get-identity-verification-sessions-params {
    created: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Only return VerificationSessions with this status. [Learn more about the lifecycle of sessions](https://stripe.com/docs/identity/how-sessions-work).
    status: option<get-identity-verification-sessions-status-enum>
  }
  get-identity-verification-sessions: func(params: get-identity-verification-sessions-params) -> result<string, string>;

  post-identity-verification-sessions: func() -> result<string, string>;

  record get-identity-verification-sessions-session-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    session: string
  }
  get-identity-verification-sessions-session: func(params: get-identity-verification-sessions-session-params) -> result<string, string>;

  record post-identity-verification-sessions-session-params {
    session: string
  }
  post-identity-verification-sessions-session: func(params: post-identity-verification-sessions-session-params) -> result<string, string>;

  record post-identity-verification-sessions-session-cancel-params {
    session: string
  }
  post-identity-verification-sessions-session-cancel: func(params: post-identity-verification-sessions-session-cancel-params) -> result<string, string>;

  record post-identity-verification-sessions-session-redact-params {
    session: string
  }
  post-identity-verification-sessions-session-redact: func(params: post-identity-verification-sessions-session-redact-params) -> result<string, string>;

}

interface invoiceitems {
  record get-invoiceitems-params {
    created: option<string>,
    /// The identifier of the customer whose invoice items to return. If none is provided, all invoice items will be returned.
    customer: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// Only return invoice items belonging to this invoice. If none is provided, all invoice items will be returned. If specifying an invoice, no customer identifier is needed.
    invoice: option<string>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// Set to `true` to only show pending invoice items, which are not yet attached to any invoices. Set to `false` to only show invoice items already attached to invoices. If unspecified, no filter is applied.
    pending: option<bool>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-invoiceitems: func(params: get-invoiceitems-params) -> result<string, string>;

  post-invoiceitems: func() -> result<string, string>;

  record get-invoiceitems-invoiceitem-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    invoiceitem: string
  }
  get-invoiceitems-invoiceitem: func(params: get-invoiceitems-invoiceitem-params) -> result<string, string>;

  record post-invoiceitems-invoiceitem-params {
    invoiceitem: string
  }
  post-invoiceitems-invoiceitem: func(params: post-invoiceitems-invoiceitem-params) -> result<string, string>;

  record delete-invoiceitems-invoiceitem-params {
    invoiceitem: string
  }
  delete-invoiceitems-invoiceitem: func(params: delete-invoiceitems-invoiceitem-params) -> result<string, string>;

}

interface invoices {
  enum get-invoices-collection-method-enum {
    charge-automatically,
    send-invoice,
  }

  enum get-invoices-status-enum {
    draft,
    open,
    paid,
    uncollectible,
    void,
  }

  enum get-invoices-upcoming-invoice-items-item-price-data-tax-behavior-enum {
    exclusive,
    inclusive,
    unspecified,
  }

  enum get-invoices-upcoming-subscription-items-item-price-data-recurring-interval-enum {
    day,
    month,
    week,
    year,
  }

  enum get-invoices-upcoming-subscription-proration-behavior-enum {
    always-invoice,
    create-prorations,
    none,
  }

  enum get-invoices-upcoming-subscription-resume-at-enum {
    now,
  }

  record get-invoices-upcoming-invoice-items-item {
    amount: option<s32>,
    currency: option<string>,
    description: option<string>,
    discountable: option<bool>,
    discounts: option<string>,
    invoiceitem: option<string>,
    metadata: option<string>,
    period: option<get-invoices-upcoming-invoice-items-item-period>,
    price: option<string>,
    price-data: option<get-invoices-upcoming-invoice-items-item-price-data>,
    quantity: option<s32>,
    tax-behavior: option<get-invoices-upcoming-invoice-items-item-price-data-tax-behavior-enum>,
    tax-code: option<string>,
    tax-rates: option<string>,
    unit-amount: option<s32>,
    unit-amount-decimal: option<string>
  }

  record get-invoices-upcoming-invoice-items-item-period {
    end: s32,
    start: s32
  }

  record get-invoices-upcoming-invoice-items-item-price-data {
    currency: string,
    product: string,
    tax-behavior: option<get-invoices-upcoming-invoice-items-item-price-data-tax-behavior-enum>,
    unit-amount: option<s32>,
    unit-amount-decimal: option<string>
  }

  record get-invoices-upcoming-subscription-items-item {
    billing-thresholds: option<string>,
    clear-usage: option<bool>,
    deleted: option<bool>,
    id: option<string>,
    metadata: option<string>,
    price: option<string>,
    price-data: option<get-invoices-upcoming-subscription-items-item-price-data>,
    quantity: option<s32>,
    tax-rates: option<string>
  }

  record get-invoices-upcoming-subscription-items-item-price-data {
    currency: string,
    product: string,
    recurring: get-invoices-upcoming-subscription-items-item-price-data-recurring,
    tax-behavior: option<get-invoices-upcoming-invoice-items-item-price-data-tax-behavior-enum>,
    unit-amount: option<s32>,
    unit-amount-decimal: option<string>
  }

  record get-invoices-upcoming-subscription-items-item-price-data-recurring {
    interval: get-invoices-upcoming-subscription-items-item-price-data-recurring-interval-enum,
    interval-count: option<s32>
  }

  record get-invoices-upcoming-lines-invoice-items-item {
    amount: option<s32>,
    currency: option<string>,
    description: option<string>,
    discountable: option<bool>,
    discounts: option<string>,
    invoiceitem: option<string>,
    metadata: option<string>,
    period: option<get-invoices-upcoming-lines-invoice-items-item-period>,
    price: option<string>,
    price-data: option<get-invoices-upcoming-lines-invoice-items-item-price-data>,
    quantity: option<s32>,
    tax-behavior: option<get-invoices-upcoming-invoice-items-item-price-data-tax-behavior-enum>,
    tax-code: option<string>,
    tax-rates: option<string>,
    unit-amount: option<s32>,
    unit-amount-decimal: option<string>
  }

  record get-invoices-upcoming-lines-invoice-items-item-period {
    end: s32,
    start: s32
  }

  record get-invoices-upcoming-lines-invoice-items-item-price-data {
    currency: string,
    product: string,
    tax-behavior: option<get-invoices-upcoming-invoice-items-item-price-data-tax-behavior-enum>,
    unit-amount: option<s32>,
    unit-amount-decimal: option<string>
  }

  record get-invoices-upcoming-lines-subscription-items-item {
    billing-thresholds: option<string>,
    clear-usage: option<bool>,
    deleted: option<bool>,
    id: option<string>,
    metadata: option<string>,
    price: option<string>,
    price-data: option<get-invoices-upcoming-lines-subscription-items-item-price-data>,
    quantity: option<s32>,
    tax-rates: option<string>
  }

  record get-invoices-upcoming-lines-subscription-items-item-price-data {
    currency: string,
    product: string,
    recurring: get-invoices-upcoming-lines-subscription-items-item-price-data-recurring,
    tax-behavior: option<get-invoices-upcoming-invoice-items-item-price-data-tax-behavior-enum>,
    unit-amount: option<s32>,
    unit-amount-decimal: option<string>
  }

  record get-invoices-upcoming-lines-subscription-items-item-price-data-recurring {
    interval: get-invoices-upcoming-subscription-items-item-price-data-recurring-interval-enum,
    interval-count: option<s32>
  }

  record get-invoices-params {
    /// The collection method of the invoice to retrieve. Either `charge_automatically` or `send_invoice`.
    collection-method: option<get-invoices-collection-method-enum>,
    created: option<string>,
    /// Only return invoices for the customer specified by this customer ID.
    customer: option<string>,
    due-date: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// The status of the invoice, one of `draft`, `open`, `paid`, `uncollectible`, or `void`. [Learn more](https://stripe.com/docs/billing/invoices/workflow#workflow-overview)
    status: option<get-invoices-status-enum>,
    /// Only return invoices for the subscription specified by this subscription ID.
    subscription: option<string>
  }
  get-invoices: func(params: get-invoices-params) -> result<string, string>;

  post-invoices: func() -> result<string, string>;

  record get-invoices-search-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
    page: option<string>,
    /// The search query string. See [search query language](https://stripe.com/docs/search#search-query-language) and the list of supported [query fields for invoices](https://stripe.com/docs/search#query-fields-for-invoices).
    query: string
  }
  get-invoices-search: func(params: get-invoices-search-params) -> result<string, string>;

  record get-invoices-upcoming-params {
    /// Settings for automatic tax lookup for this invoice preview.
    automatic-tax: option<string>,
    /// The code of the coupon to apply. If `subscription` or `subscription_items` is provided, the invoice returned will preview updating or creating a subscription with that coupon. Otherwise, it will preview applying that coupon to the customer for the next upcoming invoice from among the customer's subscriptions. The invoice can be previewed without a coupon by passing this value as an empty string.
    coupon: option<string>,
    /// The currency to preview this invoice in. Defaults to that of `customer` if not specified.
    currency: option<string>,
    /// The identifier of the customer whose upcoming invoice you'd like to retrieve.
    customer: option<string>,
    /// Details about the customer you want to invoice or overrides for an existing customer.
    customer-details: option<string>,
    /// The coupons to redeem into discounts for the invoice preview. If not specified, inherits the discount from the customer or subscription. This only works for coupons directly applied to the invoice. To apply a coupon to a subscription, you must use the `coupon` parameter instead. Pass an empty string to avoid inheriting any discounts. To preview the upcoming invoice for a subscription that hasn't been created, use `coupon` instead.
    discounts: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// List of invoice items to add or update in the upcoming invoice preview.
    invoice-items: option<list<get-invoices-upcoming-invoice-items-item>>,
    /// The identifier of the unstarted schedule whose upcoming invoice you'd like to retrieve. Cannot be used with subscription or subscription fields.
    schedule: option<string>,
    /// The identifier of the subscription for which you'd like to retrieve the upcoming invoice. If not provided, but a `subscription_items` is provided, you will preview creating a subscription with those items. If neither `subscription` nor `subscription_items` is provided, you will retrieve the next upcoming invoice from among the customer's subscriptions.
    subscription: option<string>,
    /// For new subscriptions, a future timestamp to anchor the subscription's [billing cycle](https://stripe.com/docs/subscriptions/billing-cycle). This is used to determine the date of the first full invoice, and, for plans with `month` or `year` intervals, the day of the month for subsequent invoices. For existing subscriptions, the value can only be set to `now` or `unchanged`.
    subscription-billing-cycle-anchor: option<string>,
    /// Timestamp indicating when the subscription should be scheduled to cancel. Will prorate if within the current period and prorations have been enabled using `proration_behavior`.
    subscription-cancel-at: option<string>,
    /// Boolean indicating whether this subscription should cancel at the end of the current period.
    subscription-cancel-at-period-end: option<bool>,
    /// This simulates the subscription being canceled or expired immediately.
    subscription-cancel-now: option<bool>,
    /// If provided, the invoice returned will preview updating or creating a subscription with these default tax rates. The default tax rates will apply to any line item that does not have `tax_rates` set.
    subscription-default-tax-rates: option<string>,
    /// A list of up to 20 subscription items, each with an attached price.
    subscription-items: option<list<get-invoices-upcoming-subscription-items-item>>,
    /// Determines how to handle [prorations](https://stripe.com/docs/subscriptions/billing-cycle#prorations) when the billing cycle changes (e.g., when switching plans, resetting `billing_cycle_anchor=now`, or starting a trial), or if an item's `quantity` changes. The default value is `create_prorations`.
    subscription-proration-behavior: option<get-invoices-upcoming-subscription-proration-behavior-enum>,
    /// If previewing an update to a subscription, and doing proration, `subscription_proration_date` forces the proration to be calculated as though the update was done at the specified time. The time given must be within the current subscription period and within the current phase of the schedule backing this subscription, if the schedule exists. If set, `subscription`, and one of `subscription_items`, or `subscription_trial_end` are required. Also, `subscription_proration_behavior` cannot be set to 'none'.
    subscription-proration-date: option<s32>,
    /// For paused subscriptions, setting `subscription_resume_at` to `now` will preview the invoice that will be generated if the subscription is resumed.
    subscription-resume-at: option<get-invoices-upcoming-subscription-resume-at-enum>,
    /// Date a subscription is intended to start (can be future or past)
    subscription-start-date: option<s32>,
    /// If provided, the invoice returned will preview updating or creating a subscription with that trial end. If set, one of `subscription_items` or `subscription` is required.
    subscription-trial-end: option<string>,
    /// Indicates if a plan's `trial_period_days` should be applied to the subscription. Setting `subscription_trial_end` per subscription is preferred, and this defaults to `false`. Setting this flag to `true` together with `subscription_trial_end` is not allowed. See [Using trial periods on subscriptions](https://stripe.com/docs/billing/subscriptions/trials) to learn more.
    subscription-trial-from-plan: option<bool>
  }
  get-invoices-upcoming: func(params: get-invoices-upcoming-params) -> result<string, string>;

  record get-invoices-upcoming-lines-params {
    /// Settings for automatic tax lookup for this invoice preview.
    automatic-tax: option<string>,
    /// The code of the coupon to apply. If `subscription` or `subscription_items` is provided, the invoice returned will preview updating or creating a subscription with that coupon. Otherwise, it will preview applying that coupon to the customer for the next upcoming invoice from among the customer's subscriptions. The invoice can be previewed without a coupon by passing this value as an empty string.
    coupon: option<string>,
    /// The currency to preview this invoice in. Defaults to that of `customer` if not specified.
    currency: option<string>,
    /// The identifier of the customer whose upcoming invoice you'd like to retrieve.
    customer: option<string>,
    /// Details about the customer you want to invoice or overrides for an existing customer.
    customer-details: option<string>,
    /// The coupons to redeem into discounts for the invoice preview. If not specified, inherits the discount from the customer or subscription. This only works for coupons directly applied to the invoice. To apply a coupon to a subscription, you must use the `coupon` parameter instead. Pass an empty string to avoid inheriting any discounts. To preview the upcoming invoice for a subscription that hasn't been created, use `coupon` instead.
    discounts: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// List of invoice items to add or update in the upcoming invoice preview.
    invoice-items: option<list<get-invoices-upcoming-lines-invoice-items-item>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// The identifier of the unstarted schedule whose upcoming invoice you'd like to retrieve. Cannot be used with subscription or subscription fields.
    schedule: option<string>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// The identifier of the subscription for which you'd like to retrieve the upcoming invoice. If not provided, but a `subscription_items` is provided, you will preview creating a subscription with those items. If neither `subscription` nor `subscription_items` is provided, you will retrieve the next upcoming invoice from among the customer's subscriptions.
    subscription: option<string>,
    /// For new subscriptions, a future timestamp to anchor the subscription's [billing cycle](https://stripe.com/docs/subscriptions/billing-cycle). This is used to determine the date of the first full invoice, and, for plans with `month` or `year` intervals, the day of the month for subsequent invoices. For existing subscriptions, the value can only be set to `now` or `unchanged`.
    subscription-billing-cycle-anchor: option<string>,
    /// Timestamp indicating when the subscription should be scheduled to cancel. Will prorate if within the current period and prorations have been enabled using `proration_behavior`.
    subscription-cancel-at: option<string>,
    /// Boolean indicating whether this subscription should cancel at the end of the current period.
    subscription-cancel-at-period-end: option<bool>,
    /// This simulates the subscription being canceled or expired immediately.
    subscription-cancel-now: option<bool>,
    /// If provided, the invoice returned will preview updating or creating a subscription with these default tax rates. The default tax rates will apply to any line item that does not have `tax_rates` set.
    subscription-default-tax-rates: option<string>,
    /// A list of up to 20 subscription items, each with an attached price.
    subscription-items: option<list<get-invoices-upcoming-lines-subscription-items-item>>,
    /// Determines how to handle [prorations](https://stripe.com/docs/subscriptions/billing-cycle#prorations) when the billing cycle changes (e.g., when switching plans, resetting `billing_cycle_anchor=now`, or starting a trial), or if an item's `quantity` changes. The default value is `create_prorations`.
    subscription-proration-behavior: option<get-invoices-upcoming-subscription-proration-behavior-enum>,
    /// If previewing an update to a subscription, and doing proration, `subscription_proration_date` forces the proration to be calculated as though the update was done at the specified time. The time given must be within the current subscription period and within the current phase of the schedule backing this subscription, if the schedule exists. If set, `subscription`, and one of `subscription_items`, or `subscription_trial_end` are required. Also, `subscription_proration_behavior` cannot be set to 'none'.
    subscription-proration-date: option<s32>,
    /// For paused subscriptions, setting `subscription_resume_at` to `now` will preview the invoice that will be generated if the subscription is resumed.
    subscription-resume-at: option<get-invoices-upcoming-subscription-resume-at-enum>,
    /// Date a subscription is intended to start (can be future or past)
    subscription-start-date: option<s32>,
    /// If provided, the invoice returned will preview updating or creating a subscription with that trial end. If set, one of `subscription_items` or `subscription` is required.
    subscription-trial-end: option<string>,
    /// Indicates if a plan's `trial_period_days` should be applied to the subscription. Setting `subscription_trial_end` per subscription is preferred, and this defaults to `false`. Setting this flag to `true` together with `subscription_trial_end` is not allowed. See [Using trial periods on subscriptions](https://stripe.com/docs/billing/subscriptions/trials) to learn more.
    subscription-trial-from-plan: option<bool>
  }
  get-invoices-upcoming-lines: func(params: get-invoices-upcoming-lines-params) -> result<string, string>;

  record get-invoices-invoice-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    invoice: string
  }
  get-invoices-invoice: func(params: get-invoices-invoice-params) -> result<string, string>;

  record post-invoices-invoice-params {
    invoice: string
  }
  post-invoices-invoice: func(params: post-invoices-invoice-params) -> result<string, string>;

  record delete-invoices-invoice-params {
    invoice: string
  }
  delete-invoices-invoice: func(params: delete-invoices-invoice-params) -> result<string, string>;

  record post-invoices-invoice-finalize-params {
    invoice: string
  }
  post-invoices-invoice-finalize: func(params: post-invoices-invoice-finalize-params) -> result<string, string>;

  record get-invoices-invoice-lines-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    invoice: string,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-invoices-invoice-lines: func(params: get-invoices-invoice-lines-params) -> result<string, string>;

  record post-invoices-invoice-mark-uncollectible-params {
    invoice: string
  }
  post-invoices-invoice-mark-uncollectible: func(params: post-invoices-invoice-mark-uncollectible-params) -> result<string, string>;

  record post-invoices-invoice-pay-params {
    invoice: string
  }
  post-invoices-invoice-pay: func(params: post-invoices-invoice-pay-params) -> result<string, string>;

  record post-invoices-invoice-send-params {
    invoice: string
  }
  post-invoices-invoice-send: func(params: post-invoices-invoice-send-params) -> result<string, string>;

  record post-invoices-invoice-void-params {
    invoice: string
  }
  post-invoices-invoice-void: func(params: post-invoices-invoice-void-params) -> result<string, string>;

}

interface issuing {
  enum get-issuing-authorizations-status-enum {
    closed,
    pending,
    reversed,
  }

  enum get-issuing-cardholders-status-enum {
    active,
    blocked,
    inactive,
  }

  enum get-issuing-cardholders-type-op-enum {
    company,
    individual,
  }

  enum get-issuing-cards-status-enum {
    active,
    canceled,
    inactive,
  }

  enum get-issuing-cards-type-op-enum {
    physical,
    virtual,
  }

  enum get-issuing-disputes-status-enum {
    expired,
    lost,
    submitted,
    unsubmitted,
    won,
  }

  enum get-issuing-transactions-type-op-enum {
    capture,
    refund,
  }

  record get-issuing-authorizations-params {
    /// Only return authorizations that belong to the given card.
    card: option<string>,
    /// Only return authorizations that belong to the given cardholder.
    cardholder: option<string>,
    /// Only return authorizations that were created during the given date interval.
    created: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Only return authorizations with the given status. One of `pending`, `closed`, or `reversed`.
    status: option<get-issuing-authorizations-status-enum>
  }
  get-issuing-authorizations: func(params: get-issuing-authorizations-params) -> result<string, string>;

  record get-issuing-authorizations-authorization-params {
    authorization: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-issuing-authorizations-authorization: func(params: get-issuing-authorizations-authorization-params) -> result<string, string>;

  record post-issuing-authorizations-authorization-params {
    authorization: string
  }
  post-issuing-authorizations-authorization: func(params: post-issuing-authorizations-authorization-params) -> result<string, string>;

  record post-issuing-authorizations-authorization-approve-params {
    authorization: string
  }
  post-issuing-authorizations-authorization-approve: func(params: post-issuing-authorizations-authorization-approve-params) -> result<string, string>;

  record post-issuing-authorizations-authorization-decline-params {
    authorization: string
  }
  post-issuing-authorizations-authorization-decline: func(params: post-issuing-authorizations-authorization-decline-params) -> result<string, string>;

  record get-issuing-cardholders-params {
    /// Only return cardholders that were created during the given date interval.
    created: option<string>,
    /// Only return cardholders that have the given email address.
    email: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// Only return cardholders that have the given phone number.
    phone-number: option<string>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Only return cardholders that have the given status. One of `active`, `inactive`, or `blocked`.
    status: option<get-issuing-cardholders-status-enum>,
    /// Only return cardholders that have the given type. One of `individual` or `company`.
    type-op: option<get-issuing-cardholders-type-op-enum>
  }
  get-issuing-cardholders: func(params: get-issuing-cardholders-params) -> result<string, string>;

  post-issuing-cardholders: func() -> result<string, string>;

  record get-issuing-cardholders-cardholder-params {
    cardholder: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-issuing-cardholders-cardholder: func(params: get-issuing-cardholders-cardholder-params) -> result<string, string>;

  record post-issuing-cardholders-cardholder-params {
    cardholder: string
  }
  post-issuing-cardholders-cardholder: func(params: post-issuing-cardholders-cardholder-params) -> result<string, string>;

  record get-issuing-cards-params {
    /// Only return cards belonging to the Cardholder with the provided ID.
    cardholder: option<string>,
    /// Only return cards that were issued during the given date interval.
    created: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Only return cards that have the given expiration month.
    exp-month: option<s32>,
    /// Only return cards that have the given expiration year.
    exp-year: option<s32>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// Only return cards that have the given last four digits.
    last4: option<string>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Only return cards that have the given status. One of `active`, `inactive`, or `canceled`.
    status: option<get-issuing-cards-status-enum>,
    /// Only return cards that have the given type. One of `virtual` or `physical`.
    type-op: option<get-issuing-cards-type-op-enum>
  }
  get-issuing-cards: func(params: get-issuing-cards-params) -> result<string, string>;

  post-issuing-cards: func() -> result<string, string>;

  record get-issuing-cards-card-params {
    card: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-issuing-cards-card: func(params: get-issuing-cards-card-params) -> result<string, string>;

  record post-issuing-cards-card-params {
    card: string
  }
  post-issuing-cards-card: func(params: post-issuing-cards-card-params) -> result<string, string>;

  record get-issuing-disputes-params {
    /// Select Issuing disputes that were created during the given date interval.
    created: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Select Issuing disputes with the given status.
    status: option<get-issuing-disputes-status-enum>,
    /// Select the Issuing dispute for the given transaction.
    transaction: option<string>
  }
  get-issuing-disputes: func(params: get-issuing-disputes-params) -> result<string, string>;

  post-issuing-disputes: func() -> result<string, string>;

  record get-issuing-disputes-dispute-params {
    dispute: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-issuing-disputes-dispute: func(params: get-issuing-disputes-dispute-params) -> result<string, string>;

  record post-issuing-disputes-dispute-params {
    dispute: string
  }
  post-issuing-disputes-dispute: func(params: post-issuing-disputes-dispute-params) -> result<string, string>;

  record post-issuing-disputes-dispute-submit-params {
    dispute: string
  }
  post-issuing-disputes-dispute-submit: func(params: post-issuing-disputes-dispute-submit-params) -> result<string, string>;

  record get-issuing-settlements-params {
    /// Only return issuing settlements that were created during the given date interval.
    created: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-issuing-settlements: func(params: get-issuing-settlements-params) -> result<string, string>;

  record get-issuing-settlements-settlement-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    settlement: string
  }
  get-issuing-settlements-settlement: func(params: get-issuing-settlements-settlement-params) -> result<string, string>;

  record post-issuing-settlements-settlement-params {
    settlement: string
  }
  post-issuing-settlements-settlement: func(params: post-issuing-settlements-settlement-params) -> result<string, string>;

  record get-issuing-transactions-params {
    /// Only return transactions that belong to the given card.
    card: option<string>,
    /// Only return transactions that belong to the given cardholder.
    cardholder: option<string>,
    /// Only return transactions that were created during the given date interval.
    created: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Only return transactions that have the given type. One of `capture` or `refund`.
    type-op: option<get-issuing-transactions-type-op-enum>
  }
  get-issuing-transactions: func(params: get-issuing-transactions-params) -> result<string, string>;

  record get-issuing-transactions-transaction-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    transaction: string
  }
  get-issuing-transactions-transaction: func(params: get-issuing-transactions-transaction-params) -> result<string, string>;

  record post-issuing-transactions-transaction-params {
    transaction: string
  }
  post-issuing-transactions-transaction: func(params: post-issuing-transactions-transaction-params) -> result<string, string>;

}

interface link-account-sessions {
  post-link-account-sessions: func() -> result<string, string>;

  record get-link-account-sessions-session-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    session: string
  }
  get-link-account-sessions-session: func(params: get-link-account-sessions-session-params) -> result<string, string>;

}

interface linked-accounts {
  record get-linked-accounts-params {
    /// If present, only return accounts that belong to the specified account holder. `account_holder[customer]` and `account_holder[account]` are mutually exclusive.
    account-holder: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// If present, only return accounts that were collected as part of the given session.
    session: option<string>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-linked-accounts: func(params: get-linked-accounts-params) -> result<string, string>;

  record get-linked-accounts-account-params {
    account: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-linked-accounts-account: func(params: get-linked-accounts-account-params) -> result<string, string>;

  record post-linked-accounts-account-disconnect-params {
    account: string
  }
  post-linked-accounts-account-disconnect: func(params: post-linked-accounts-account-disconnect-params) -> result<string, string>;

  record get-linked-accounts-account-owners-params {
    account: string,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// The ID of the ownership object to fetch owners from.
    ownership: string,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-linked-accounts-account-owners: func(params: get-linked-accounts-account-owners-params) -> result<string, string>;

  record post-linked-accounts-account-refresh-params {
    account: string
  }
  post-linked-accounts-account-refresh: func(params: post-linked-accounts-account-refresh-params) -> result<string, string>;

}

interface mandates {
  record get-mandates-mandate-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    mandate: string
  }
  get-mandates-mandate: func(params: get-mandates-mandate-params) -> result<string, string>;

}

interface payment-intents {
  record get-payment-intents-params {
    /// A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
    created: option<string>,
    /// Only return PaymentIntents for the customer specified by this customer ID.
    customer: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-payment-intents: func(params: get-payment-intents-params) -> result<string, string>;

  post-payment-intents: func() -> result<string, string>;

  record get-payment-intents-search-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
    page: option<string>,
    /// The search query string. See [search query language](https://stripe.com/docs/search#search-query-language) and the list of supported [query fields for payment intents](https://stripe.com/docs/search#query-fields-for-payment-intents).
    query: string
  }
  get-payment-intents-search: func(params: get-payment-intents-search-params) -> result<string, string>;

  record get-payment-intents-intent-params {
    /// The client secret of the PaymentIntent. Required if a publishable key is used to retrieve the source.
    client-secret: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    intent: string
  }
  get-payment-intents-intent: func(params: get-payment-intents-intent-params) -> result<string, string>;

  record post-payment-intents-intent-params {
    intent: string
  }
  post-payment-intents-intent: func(params: post-payment-intents-intent-params) -> result<string, string>;

  record post-payment-intents-intent-apply-customer-balance-params {
    intent: string
  }
  post-payment-intents-intent-apply-customer-balance: func(params: post-payment-intents-intent-apply-customer-balance-params) -> result<string, string>;

  record post-payment-intents-intent-cancel-params {
    intent: string
  }
  post-payment-intents-intent-cancel: func(params: post-payment-intents-intent-cancel-params) -> result<string, string>;

  record post-payment-intents-intent-capture-params {
    intent: string
  }
  post-payment-intents-intent-capture: func(params: post-payment-intents-intent-capture-params) -> result<string, string>;

  record post-payment-intents-intent-confirm-params {
    intent: string
  }
  post-payment-intents-intent-confirm: func(params: post-payment-intents-intent-confirm-params) -> result<string, string>;

  record post-payment-intents-intent-increment-authorization-params {
    intent: string
  }
  post-payment-intents-intent-increment-authorization: func(params: post-payment-intents-intent-increment-authorization-params) -> result<string, string>;

  record post-payment-intents-intent-verify-microdeposits-params {
    intent: string
  }
  post-payment-intents-intent-verify-microdeposits: func(params: post-payment-intents-intent-verify-microdeposits-params) -> result<string, string>;

}

interface payment-links {
  record get-payment-links-params {
    /// Only return payment links that are active or inactive (e.g., pass `false` to list all inactive payment links).
    active: option<bool>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-payment-links: func(params: get-payment-links-params) -> result<string, string>;

  post-payment-links: func() -> result<string, string>;

  record get-payment-links-payment-link-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    payment-link: string
  }
  get-payment-links-payment-link: func(params: get-payment-links-payment-link-params) -> result<string, string>;

  record post-payment-links-payment-link-params {
    payment-link: string
  }
  post-payment-links-payment-link: func(params: post-payment-links-payment-link-params) -> result<string, string>;

  record get-payment-links-payment-link-line-items-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    payment-link: string,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-payment-links-payment-link-line-items: func(params: get-payment-links-payment-link-line-items-params) -> result<string, string>;

}

interface payment-methods {
  enum get-payment-methods-type-op-enum {
    acss-debit,
    affirm,
    afterpay-clearpay,
    alipay,
    au-becs-debit,
    bacs-debit,
    bancontact,
    blik,
    boleto,
    card,
    cashapp,
    customer-balance,
    eps,
    fpx,
    giropay,
    grabpay,
    ideal,
    klarna,
    konbini,
    link,
    oxxo,
    p24,
    paynow,
    pix,
    promptpay,
    sepa-debit,
    sofort,
    us-bank-account,
    wechat-pay,
  }

  record get-payment-methods-params {
    /// The ID of the customer whose PaymentMethods will be retrieved.
    customer: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// An optional filter on the list, based on the object `type` field. Without the filter, the list includes all current and future payment method types. If your integration expects only one type of payment method in the response, make sure to provide a type value in the request.
    type-op: option<get-payment-methods-type-op-enum>
  }
  get-payment-methods: func(params: get-payment-methods-params) -> result<string, string>;

  post-payment-methods: func() -> result<string, string>;

  record get-payment-methods-payment-method-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    payment-method: string
  }
  get-payment-methods-payment-method: func(params: get-payment-methods-payment-method-params) -> result<string, string>;

  record post-payment-methods-payment-method-params {
    payment-method: string
  }
  post-payment-methods-payment-method: func(params: post-payment-methods-payment-method-params) -> result<string, string>;

  record post-payment-methods-payment-method-attach-params {
    payment-method: string
  }
  post-payment-methods-payment-method-attach: func(params: post-payment-methods-payment-method-attach-params) -> result<string, string>;

  record post-payment-methods-payment-method-detach-params {
    payment-method: string
  }
  post-payment-methods-payment-method-detach: func(params: post-payment-methods-payment-method-detach-params) -> result<string, string>;

}

interface payouts {
  record get-payouts-params {
    arrival-date: option<string>,
    created: option<string>,
    /// The ID of an external account - only return payouts sent to this external account.
    destination: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Only return payouts that have the given status: `pending`, `paid`, `failed`, or `canceled`.
    status: option<string>
  }
  get-payouts: func(params: get-payouts-params) -> result<string, string>;

  post-payouts: func() -> result<string, string>;

  record get-payouts-payout-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    payout: string
  }
  get-payouts-payout: func(params: get-payouts-payout-params) -> result<string, string>;

  record post-payouts-payout-params {
    payout: string
  }
  post-payouts-payout: func(params: post-payouts-payout-params) -> result<string, string>;

  record post-payouts-payout-cancel-params {
    payout: string
  }
  post-payouts-payout-cancel: func(params: post-payouts-payout-cancel-params) -> result<string, string>;

  record post-payouts-payout-reverse-params {
    payout: string
  }
  post-payouts-payout-reverse: func(params: post-payouts-payout-reverse-params) -> result<string, string>;

}

interface plans {
  record get-plans-params {
    /// Only return plans that are active or inactive (e.g., pass `false` to list all inactive plans).
    active: option<bool>,
    /// A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
    created: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// Only return plans for the given product.
    product: option<string>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-plans: func(params: get-plans-params) -> result<string, string>;

  post-plans: func() -> result<string, string>;

  record get-plans-plan-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    plan: string
  }
  get-plans-plan: func(params: get-plans-plan-params) -> result<string, string>;

  record post-plans-plan-params {
    plan: string
  }
  post-plans-plan: func(params: post-plans-plan-params) -> result<string, string>;

  record delete-plans-plan-params {
    plan: string
  }
  delete-plans-plan: func(params: delete-plans-plan-params) -> result<string, string>;

}

interface prices {
  enum get-prices-type-op-enum {
    one-time,
    recurring,
  }

  record get-prices-params {
    /// Only return prices that are active or inactive (e.g., pass `false` to list all inactive prices).
    active: option<bool>,
    /// A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
    created: option<string>,
    /// Only return prices for the given currency.
    currency: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// Only return the price with these lookup_keys, if any exist.
    lookup-keys: option<list<string>>,
    /// Only return prices for the given product.
    product: option<string>,
    /// Only return prices with these recurring fields.
    recurring: option<string>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Only return prices of type `recurring` or `one_time`.
    type-op: option<get-prices-type-op-enum>
  }
  get-prices: func(params: get-prices-params) -> result<string, string>;

  post-prices: func() -> result<string, string>;

  record get-prices-search-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
    page: option<string>,
    /// The search query string. See [search query language](https://stripe.com/docs/search#search-query-language) and the list of supported [query fields for prices](https://stripe.com/docs/search#query-fields-for-prices).
    query: string
  }
  get-prices-search: func(params: get-prices-search-params) -> result<string, string>;

  record get-prices-price-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    price: string
  }
  get-prices-price: func(params: get-prices-price-params) -> result<string, string>;

  record post-prices-price-params {
    price: string
  }
  post-prices-price: func(params: post-prices-price-params) -> result<string, string>;

}

interface products {
  record get-products-params {
    /// Only return products that are active or inactive (e.g., pass `false` to list all inactive products).
    active: option<bool>,
    /// Only return products that were created during the given date interval.
    created: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// Only return products with the given IDs. Cannot be used with [starting_after](https://stripe.com/docs/api#list_products-starting_after) or [ending_before](https://stripe.com/docs/api#list_products-ending_before).
    ids: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// Only return products that can be shipped (i.e., physical, not digital products).
    shippable: option<bool>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Only return products with the given url.
    url: option<string>
  }
  get-products: func(params: get-products-params) -> result<string, string>;

  post-products: func() -> result<string, string>;

  record get-products-search-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
    page: option<string>,
    /// The search query string. See [search query language](https://stripe.com/docs/search#search-query-language) and the list of supported [query fields for products](https://stripe.com/docs/search#query-fields-for-products).
    query: string
  }
  get-products-search: func(params: get-products-search-params) -> result<string, string>;

  record get-products-id-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    id: string
  }
  get-products-id: func(params: get-products-id-params) -> result<string, string>;

  record post-products-id-params {
    id: string
  }
  post-products-id: func(params: post-products-id-params) -> result<string, string>;

  record delete-products-id-params {
    id: string
  }
  delete-products-id: func(params: delete-products-id-params) -> result<string, string>;

}

interface promotion-codes {
  record get-promotion-codes-params {
    /// Filter promotion codes by whether they are active.
    active: option<bool>,
    /// Only return promotion codes that have this case-insensitive code.
    code: option<string>,
    /// Only return promotion codes for this coupon.
    coupon: option<string>,
    /// A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
    created: option<string>,
    /// Only return promotion codes that are restricted to this customer.
    customer: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-promotion-codes: func(params: get-promotion-codes-params) -> result<string, string>;

  post-promotion-codes: func() -> result<string, string>;

  record get-promotion-codes-promotion-code-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    promotion-code: string
  }
  get-promotion-codes-promotion-code: func(params: get-promotion-codes-promotion-code-params) -> result<string, string>;

  record post-promotion-codes-promotion-code-params {
    promotion-code: string
  }
  post-promotion-codes-promotion-code: func(params: post-promotion-codes-promotion-code-params) -> result<string, string>;

}

interface quotes {
  enum get-quotes-status-enum {
    accepted,
    canceled,
    draft,
    open,
  }

  record get-quotes-params {
    /// The ID of the customer whose quotes will be retrieved.
    customer: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// The status of the quote.
    status: option<get-quotes-status-enum>,
    /// Provides a list of quotes that are associated with the specified test clock. The response will not include quotes with test clocks if this and the customer parameter is not set.
    test-clock: option<string>
  }
  get-quotes: func(params: get-quotes-params) -> result<string, string>;

  post-quotes: func() -> result<string, string>;

  record get-quotes-quote-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    quote: string
  }
  get-quotes-quote: func(params: get-quotes-quote-params) -> result<string, string>;

  record post-quotes-quote-params {
    quote: string
  }
  post-quotes-quote: func(params: post-quotes-quote-params) -> result<string, string>;

  record post-quotes-quote-accept-params {
    quote: string
  }
  post-quotes-quote-accept: func(params: post-quotes-quote-accept-params) -> result<string, string>;

  record post-quotes-quote-cancel-params {
    quote: string
  }
  post-quotes-quote-cancel: func(params: post-quotes-quote-cancel-params) -> result<string, string>;

  record get-quotes-quote-computed-upfront-line-items-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    quote: string,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-quotes-quote-computed-upfront-line-items: func(params: get-quotes-quote-computed-upfront-line-items-params) -> result<string, string>;

  record post-quotes-quote-finalize-params {
    quote: string
  }
  post-quotes-quote-finalize: func(params: post-quotes-quote-finalize-params) -> result<string, string>;

  record get-quotes-quote-line-items-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    quote: string,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-quotes-quote-line-items: func(params: get-quotes-quote-line-items-params) -> result<string, string>;

  record get-quotes-quote-pdf-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    quote: string
  }
  get-quotes-quote-pdf: func(params: get-quotes-quote-pdf-params) -> result<string, string>;

}

interface radar {
  record get-radar-early-fraud-warnings-params {
    /// Only return early fraud warnings for the charge specified by this charge ID.
    charge: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// Only return early fraud warnings for charges that were created by the PaymentIntent specified by this PaymentIntent ID.
    payment-intent: option<string>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-radar-early-fraud-warnings: func(params: get-radar-early-fraud-warnings-params) -> result<string, string>;

  record get-radar-early-fraud-warnings-early-fraud-warning-params {
    early-fraud-warning: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-radar-early-fraud-warnings-early-fraud-warning: func(params: get-radar-early-fraud-warnings-early-fraud-warning-params) -> result<string, string>;

  record get-radar-value-list-items-params {
    created: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Return items belonging to the parent list whose value matches the specified value (using an "is like" match).
    value: option<string>,
    /// Identifier for the parent value list this item belongs to.
    value-list: string
  }
  get-radar-value-list-items: func(params: get-radar-value-list-items-params) -> result<string, string>;

  post-radar-value-list-items: func() -> result<string, string>;

  record get-radar-value-list-items-item-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    item: string
  }
  get-radar-value-list-items-item: func(params: get-radar-value-list-items-item-params) -> result<string, string>;

  record delete-radar-value-list-items-item-params {
    item: string
  }
  delete-radar-value-list-items-item: func(params: delete-radar-value-list-items-item-params) -> result<string, string>;

  record get-radar-value-lists-params {
    /// The alias used to reference the value list when writing rules.
    alias: option<string>,
    /// A value contained within a value list - returns all value lists containing this value.
    contains: option<string>,
    created: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-radar-value-lists: func(params: get-radar-value-lists-params) -> result<string, string>;

  post-radar-value-lists: func() -> result<string, string>;

  record get-radar-value-lists-value-list-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    value-list: string
  }
  get-radar-value-lists-value-list: func(params: get-radar-value-lists-value-list-params) -> result<string, string>;

  record post-radar-value-lists-value-list-params {
    value-list: string
  }
  post-radar-value-lists-value-list: func(params: post-radar-value-lists-value-list-params) -> result<string, string>;

  record delete-radar-value-lists-value-list-params {
    value-list: string
  }
  delete-radar-value-lists-value-list: func(params: delete-radar-value-lists-value-list-params) -> result<string, string>;

}

interface refunds {
  record get-refunds-params {
    /// Only return refunds for the charge specified by this charge ID.
    charge: option<string>,
    created: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// Only return refunds for the PaymentIntent specified by this ID.
    payment-intent: option<string>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-refunds: func(params: get-refunds-params) -> result<string, string>;

  post-refunds: func() -> result<string, string>;

  record get-refunds-refund-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    refund: string
  }
  get-refunds-refund: func(params: get-refunds-refund-params) -> result<string, string>;

  record post-refunds-refund-params {
    refund: string
  }
  post-refunds-refund: func(params: post-refunds-refund-params) -> result<string, string>;

  record post-refunds-refund-cancel-params {
    refund: string
  }
  post-refunds-refund-cancel: func(params: post-refunds-refund-cancel-params) -> result<string, string>;

}

interface reporting {
  record get-reporting-report-runs-params {
    created: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-reporting-report-runs: func(params: get-reporting-report-runs-params) -> result<string, string>;

  post-reporting-report-runs: func() -> result<string, string>;

  record get-reporting-report-runs-report-run-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    report-run: string
  }
  get-reporting-report-runs-report-run: func(params: get-reporting-report-runs-report-run-params) -> result<string, string>;

  record get-reporting-report-types-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-reporting-report-types: func(params: get-reporting-report-types-params) -> result<string, string>;

  record get-reporting-report-types-report-type-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    report-type: string
  }
  get-reporting-report-types-report-type: func(params: get-reporting-report-types-report-type-params) -> result<string, string>;

}

interface reviews {
  record get-reviews-params {
    created: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-reviews: func(params: get-reviews-params) -> result<string, string>;

  record get-reviews-review-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    review: string
  }
  get-reviews-review: func(params: get-reviews-review-params) -> result<string, string>;

  record post-reviews-review-approve-params {
    review: string
  }
  post-reviews-review-approve: func(params: post-reviews-review-approve-params) -> result<string, string>;

}

interface setup-attempts {
  record get-setup-attempts-params {
    /// A filter on the list, based on the object `created` field. The value
    /// can be a string with an integer Unix timestamp, or it can be a
    created: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// Only return SetupAttempts created by the SetupIntent specified by
    /// this ID.
    setup-intent: string,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-setup-attempts: func(params: get-setup-attempts-params) -> result<string, string>;

}

interface setup-intents {
  record get-setup-intents-params {
    /// If present, the SetupIntent's payment method will be attached to the in-context Stripe Account.
    /// 
    attach-to-self: option<bool>,
    /// A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
    created: option<string>,
    /// Only return SetupIntents for the customer specified by this customer ID.
    customer: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// Only return SetupIntents associated with the specified payment method.
    payment-method: option<string>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-setup-intents: func(params: get-setup-intents-params) -> result<string, string>;

  post-setup-intents: func() -> result<string, string>;

  record get-setup-intents-intent-params {
    /// The client secret of the SetupIntent. Required if a publishable key is used to retrieve the SetupIntent.
    client-secret: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    intent: string
  }
  get-setup-intents-intent: func(params: get-setup-intents-intent-params) -> result<string, string>;

  record post-setup-intents-intent-params {
    intent: string
  }
  post-setup-intents-intent: func(params: post-setup-intents-intent-params) -> result<string, string>;

  record post-setup-intents-intent-cancel-params {
    intent: string
  }
  post-setup-intents-intent-cancel: func(params: post-setup-intents-intent-cancel-params) -> result<string, string>;

  record post-setup-intents-intent-confirm-params {
    intent: string
  }
  post-setup-intents-intent-confirm: func(params: post-setup-intents-intent-confirm-params) -> result<string, string>;

  record post-setup-intents-intent-verify-microdeposits-params {
    intent: string
  }
  post-setup-intents-intent-verify-microdeposits: func(params: post-setup-intents-intent-verify-microdeposits-params) -> result<string, string>;

}

interface shipping-rates {
  record get-shipping-rates-params {
    /// Only return shipping rates that are active or inactive.
    active: option<bool>,
    /// A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
    created: option<string>,
    /// Only return shipping rates for the given currency.
    currency: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-shipping-rates: func(params: get-shipping-rates-params) -> result<string, string>;

  post-shipping-rates: func() -> result<string, string>;

  record get-shipping-rates-shipping-rate-token-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    shipping-rate-token: string
  }
  get-shipping-rates-shipping-rate-token: func(params: get-shipping-rates-shipping-rate-token-params) -> result<string, string>;

  record post-shipping-rates-shipping-rate-token-params {
    shipping-rate-token: string
  }
  post-shipping-rates-shipping-rate-token: func(params: post-shipping-rates-shipping-rate-token-params) -> result<string, string>;

}

interface sigma {
  record get-sigma-scheduled-query-runs-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-sigma-scheduled-query-runs: func(params: get-sigma-scheduled-query-runs-params) -> result<string, string>;

  record get-sigma-scheduled-query-runs-scheduled-query-run-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    scheduled-query-run: string
  }
  get-sigma-scheduled-query-runs-scheduled-query-run: func(params: get-sigma-scheduled-query-runs-scheduled-query-run-params) -> result<string, string>;

}

interface sources {
  post-sources: func() -> result<string, string>;

  record get-sources-source-params {
    /// The client secret of the source. Required if a publishable key is used to retrieve the source.
    client-secret: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    source: string
  }
  get-sources-source: func(params: get-sources-source-params) -> result<string, string>;

  record post-sources-source-params {
    source: string
  }
  post-sources-source: func(params: post-sources-source-params) -> result<string, string>;

  record get-sources-source-mandate-notifications-mandate-notification-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    mandate-notification: string,
    source: string
  }
  get-sources-source-mandate-notifications-mandate-notification: func(params: get-sources-source-mandate-notifications-mandate-notification-params) -> result<string, string>;

  record get-sources-source-source-transactions-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    source: string,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-sources-source-source-transactions: func(params: get-sources-source-source-transactions-params) -> result<string, string>;

  record get-sources-source-source-transactions-source-transaction-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    source: string,
    source-transaction: string
  }
  get-sources-source-source-transactions-source-transaction: func(params: get-sources-source-source-transactions-source-transaction-params) -> result<string, string>;

  record post-sources-source-verify-params {
    source: string
  }
  post-sources-source-verify: func(params: post-sources-source-verify-params) -> result<string, string>;

}

interface subscription-items {
  record get-subscription-items-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// The ID of the subscription whose items will be retrieved.
    subscription: string
  }
  get-subscription-items: func(params: get-subscription-items-params) -> result<string, string>;

  post-subscription-items: func() -> result<string, string>;

  record get-subscription-items-item-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    item: string
  }
  get-subscription-items-item: func(params: get-subscription-items-item-params) -> result<string, string>;

  record post-subscription-items-item-params {
    item: string
  }
  post-subscription-items-item: func(params: post-subscription-items-item-params) -> result<string, string>;

  record delete-subscription-items-item-params {
    item: string
  }
  delete-subscription-items-item: func(params: delete-subscription-items-item-params) -> result<string, string>;

  record get-subscription-items-subscription-item-usage-record-summaries-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    subscription-item: string
  }
  get-subscription-items-subscription-item-usage-record-summaries: func(params: get-subscription-items-subscription-item-usage-record-summaries-params) -> result<string, string>;

  record post-subscription-items-subscription-item-usage-records-params {
    subscription-item: string
  }
  post-subscription-items-subscription-item-usage-records: func(params: post-subscription-items-subscription-item-usage-records-params) -> result<string, string>;

}

interface subscription-schedules {
  record get-subscription-schedules-params {
    /// Only return subscription schedules that were created canceled the given date interval.
    canceled-at: option<string>,
    /// Only return subscription schedules that completed during the given date interval.
    completed-at: option<string>,
    /// Only return subscription schedules that were created during the given date interval.
    created: option<string>,
    /// Only return subscription schedules for the given customer.
    customer: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// Only return subscription schedules that were released during the given date interval.
    released-at: option<string>,
    /// Only return subscription schedules that have not started yet.
    scheduled: option<bool>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-subscription-schedules: func(params: get-subscription-schedules-params) -> result<string, string>;

  post-subscription-schedules: func() -> result<string, string>;

  record get-subscription-schedules-schedule-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    schedule: string
  }
  get-subscription-schedules-schedule: func(params: get-subscription-schedules-schedule-params) -> result<string, string>;

  record post-subscription-schedules-schedule-params {
    schedule: string
  }
  post-subscription-schedules-schedule: func(params: post-subscription-schedules-schedule-params) -> result<string, string>;

  record post-subscription-schedules-schedule-cancel-params {
    schedule: string
  }
  post-subscription-schedules-schedule-cancel: func(params: post-subscription-schedules-schedule-cancel-params) -> result<string, string>;

  record post-subscription-schedules-schedule-release-params {
    schedule: string
  }
  post-subscription-schedules-schedule-release: func(params: post-subscription-schedules-schedule-release-params) -> result<string, string>;

}

interface subscriptions {
  enum get-subscriptions-collection-method-enum {
    charge-automatically,
    send-invoice,
  }

  enum get-subscriptions-status-enum {
    active,
    all,
    canceled,
    ended,
    incomplete,
    incomplete-expired,
    past-due,
    paused,
    trialing,
    unpaid,
  }

  record get-subscriptions-params {
    /// The collection method of the subscriptions to retrieve. Either `charge_automatically` or `send_invoice`.
    collection-method: option<get-subscriptions-collection-method-enum>,
    created: option<string>,
    current-period-end: option<string>,
    current-period-start: option<string>,
    /// The ID of the customer whose subscriptions will be retrieved.
    customer: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// Filter for subscriptions that contain this recurring price ID.
    price: option<string>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// The status of the subscriptions to retrieve. Passing in a value of `canceled` will return all canceled subscriptions, including those belonging to deleted customers. Pass `ended` to find subscriptions that are canceled and subscriptions that are expired due to [incomplete payment](https://stripe.com/docs/billing/subscriptions/overview#subscription-statuses). Passing in a value of `all` will return subscriptions of all statuses. If no value is supplied, all subscriptions that have not been canceled are returned.
    status: option<get-subscriptions-status-enum>,
    /// Filter for subscriptions that are associated with the specified test clock. The response will not include subscriptions with test clocks if this and the customer parameter is not set.
    test-clock: option<string>
  }
  get-subscriptions: func(params: get-subscriptions-params) -> result<string, string>;

  post-subscriptions: func() -> result<string, string>;

  record get-subscriptions-search-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
    page: option<string>,
    /// The search query string. See [search query language](https://stripe.com/docs/search#search-query-language) and the list of supported [query fields for subscriptions](https://stripe.com/docs/search#query-fields-for-subscriptions).
    query: string
  }
  get-subscriptions-search: func(params: get-subscriptions-search-params) -> result<string, string>;

  record get-subscriptions-subscription-exposed-id-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    subscription-exposed-id: string
  }
  get-subscriptions-subscription-exposed-id: func(params: get-subscriptions-subscription-exposed-id-params) -> result<string, string>;

  record post-subscriptions-subscription-exposed-id-params {
    subscription-exposed-id: string
  }
  post-subscriptions-subscription-exposed-id: func(params: post-subscriptions-subscription-exposed-id-params) -> result<string, string>;

  record delete-subscriptions-subscription-exposed-id-params {
    subscription-exposed-id: string
  }
  delete-subscriptions-subscription-exposed-id: func(params: delete-subscriptions-subscription-exposed-id-params) -> result<string, string>;

  record delete-subscriptions-subscription-exposed-id-discount-params {
    subscription-exposed-id: string
  }
  delete-subscriptions-subscription-exposed-id-discount: func(params: delete-subscriptions-subscription-exposed-id-discount-params) -> result<string, string>;

  record post-subscriptions-subscription-resume-params {
    subscription: string
  }
  post-subscriptions-subscription-resume: func(params: post-subscriptions-subscription-resume-params) -> result<string, string>;

}

interface tax {
  post-tax-calculations: func() -> result<string, string>;

  record get-tax-calculations-calculation-line-items-params {
    calculation: string,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-tax-calculations-calculation-line-items: func(params: get-tax-calculations-calculation-line-items-params) -> result<string, string>;

  post-tax-transactions-create-from-calculation: func() -> result<string, string>;

  post-tax-transactions-create-reversal: func() -> result<string, string>;

  record get-tax-transactions-transaction-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    transaction: string
  }
  get-tax-transactions-transaction: func(params: get-tax-transactions-transaction-params) -> result<string, string>;

  record get-tax-transactions-transaction-line-items-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    transaction: string
  }
  get-tax-transactions-transaction-line-items: func(params: get-tax-transactions-transaction-line-items-params) -> result<string, string>;

}

interface tax-codes {
  record get-tax-codes-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-tax-codes: func(params: get-tax-codes-params) -> result<string, string>;

  record get-tax-codes-id-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    id: string
  }
  get-tax-codes-id: func(params: get-tax-codes-id-params) -> result<string, string>;

}

interface tax-rates {
  record get-tax-rates-params {
    /// Optional flag to filter by tax rates that are either active or inactive (archived).
    active: option<bool>,
    /// Optional range for filtering created date.
    created: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// Optional flag to filter by tax rates that are inclusive (or those that are not inclusive).
    inclusive: option<bool>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-tax-rates: func(params: get-tax-rates-params) -> result<string, string>;

  post-tax-rates: func() -> result<string, string>;

  record get-tax-rates-tax-rate-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    tax-rate: string
  }
  get-tax-rates-tax-rate: func(params: get-tax-rates-tax-rate-params) -> result<string, string>;

  record post-tax-rates-tax-rate-params {
    tax-rate: string
  }
  post-tax-rates-tax-rate: func(params: post-tax-rates-tax-rate-params) -> result<string, string>;

}

interface terminal {
  enum get-terminal-readers-device-type-enum {
    bbpos-chipper2x,
    bbpos-wisepad3,
    bbpos-wisepos-e,
    simulated-wisepos-e,
    stripe-m2,
    verifone-p400,
  }

  enum get-terminal-readers-status-enum {
    offline,
    online,
  }

  record get-terminal-configurations-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// if present, only return the account default or non-default configurations.
    is-account-default: option<bool>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-terminal-configurations: func(params: get-terminal-configurations-params) -> result<string, string>;

  post-terminal-configurations: func() -> result<string, string>;

  record get-terminal-configurations-configuration-params {
    configuration: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-terminal-configurations-configuration: func(params: get-terminal-configurations-configuration-params) -> result<string, string>;

  record post-terminal-configurations-configuration-params {
    configuration: string
  }
  post-terminal-configurations-configuration: func(params: post-terminal-configurations-configuration-params) -> result<string, string>;

  record delete-terminal-configurations-configuration-params {
    configuration: string
  }
  delete-terminal-configurations-configuration: func(params: delete-terminal-configurations-configuration-params) -> result<string, string>;

  post-terminal-connection-tokens: func() -> result<string, string>;

  record get-terminal-locations-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-terminal-locations: func(params: get-terminal-locations-params) -> result<string, string>;

  post-terminal-locations: func() -> result<string, string>;

  record get-terminal-locations-location-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    location: string
  }
  get-terminal-locations-location: func(params: get-terminal-locations-location-params) -> result<string, string>;

  record post-terminal-locations-location-params {
    location: string
  }
  post-terminal-locations-location: func(params: post-terminal-locations-location-params) -> result<string, string>;

  record delete-terminal-locations-location-params {
    location: string
  }
  delete-terminal-locations-location: func(params: delete-terminal-locations-location-params) -> result<string, string>;

  record get-terminal-readers-params {
    /// Filters readers by device type
    device-type: option<get-terminal-readers-device-type-enum>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A location ID to filter the response list to only readers at the specific location
    location: option<string>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// A status filter to filter readers to only offline or online readers
    status: option<get-terminal-readers-status-enum>
  }
  get-terminal-readers: func(params: get-terminal-readers-params) -> result<string, string>;

  post-terminal-readers: func() -> result<string, string>;

  record get-terminal-readers-reader-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    reader: string
  }
  get-terminal-readers-reader: func(params: get-terminal-readers-reader-params) -> result<string, string>;

  record post-terminal-readers-reader-params {
    reader: string
  }
  post-terminal-readers-reader: func(params: post-terminal-readers-reader-params) -> result<string, string>;

  record delete-terminal-readers-reader-params {
    reader: string
  }
  delete-terminal-readers-reader: func(params: delete-terminal-readers-reader-params) -> result<string, string>;

  record post-terminal-readers-reader-cancel-action-params {
    reader: string
  }
  post-terminal-readers-reader-cancel-action: func(params: post-terminal-readers-reader-cancel-action-params) -> result<string, string>;

  record post-terminal-readers-reader-process-payment-intent-params {
    reader: string
  }
  post-terminal-readers-reader-process-payment-intent: func(params: post-terminal-readers-reader-process-payment-intent-params) -> result<string, string>;

  record post-terminal-readers-reader-process-setup-intent-params {
    reader: string
  }
  post-terminal-readers-reader-process-setup-intent: func(params: post-terminal-readers-reader-process-setup-intent-params) -> result<string, string>;

  record post-terminal-readers-reader-refund-payment-params {
    reader: string
  }
  post-terminal-readers-reader-refund-payment: func(params: post-terminal-readers-reader-refund-payment-params) -> result<string, string>;

  record post-terminal-readers-reader-set-reader-display-params {
    reader: string
  }
  post-terminal-readers-reader-set-reader-display: func(params: post-terminal-readers-reader-set-reader-display-params) -> result<string, string>;

}

interface test-helpers {
  record post-test-helpers-customers-customer-fund-cash-balance-params {
    customer: string
  }
  post-test-helpers-customers-customer-fund-cash-balance: func(params: post-test-helpers-customers-customer-fund-cash-balance-params) -> result<string, string>;

  record post-test-helpers-issuing-cards-card-shipping-deliver-params {
    card: string
  }
  post-test-helpers-issuing-cards-card-shipping-deliver: func(params: post-test-helpers-issuing-cards-card-shipping-deliver-params) -> result<string, string>;

  record post-test-helpers-issuing-cards-card-shipping-fail-params {
    card: string
  }
  post-test-helpers-issuing-cards-card-shipping-fail: func(params: post-test-helpers-issuing-cards-card-shipping-fail-params) -> result<string, string>;

  record post-test-helpers-issuing-cards-card-shipping-return-params {
    card: string
  }
  post-test-helpers-issuing-cards-card-shipping-return: func(params: post-test-helpers-issuing-cards-card-shipping-return-params) -> result<string, string>;

  record post-test-helpers-issuing-cards-card-shipping-ship-params {
    card: string
  }
  post-test-helpers-issuing-cards-card-shipping-ship: func(params: post-test-helpers-issuing-cards-card-shipping-ship-params) -> result<string, string>;

  record post-test-helpers-refunds-refund-expire-params {
    refund: string
  }
  post-test-helpers-refunds-refund-expire: func(params: post-test-helpers-refunds-refund-expire-params) -> result<string, string>;

  record post-test-helpers-terminal-readers-reader-present-payment-method-params {
    reader: string
  }
  post-test-helpers-terminal-readers-reader-present-payment-method: func(params: post-test-helpers-terminal-readers-reader-present-payment-method-params) -> result<string, string>;

  record get-test-helpers-test-clocks-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-test-helpers-test-clocks: func(params: get-test-helpers-test-clocks-params) -> result<string, string>;

  post-test-helpers-test-clocks: func() -> result<string, string>;

  record get-test-helpers-test-clocks-test-clock-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    test-clock: string
  }
  get-test-helpers-test-clocks-test-clock: func(params: get-test-helpers-test-clocks-test-clock-params) -> result<string, string>;

  record delete-test-helpers-test-clocks-test-clock-params {
    test-clock: string
  }
  delete-test-helpers-test-clocks-test-clock: func(params: delete-test-helpers-test-clocks-test-clock-params) -> result<string, string>;

  record post-test-helpers-test-clocks-test-clock-advance-params {
    test-clock: string
  }
  post-test-helpers-test-clocks-test-clock-advance: func(params: post-test-helpers-test-clocks-test-clock-advance-params) -> result<string, string>;

  record post-test-helpers-treasury-inbound-transfers-id-fail-params {
    id: string
  }
  post-test-helpers-treasury-inbound-transfers-id-fail: func(params: post-test-helpers-treasury-inbound-transfers-id-fail-params) -> result<string, string>;

  record post-test-helpers-treasury-inbound-transfers-id-return-params {
    id: string
  }
  post-test-helpers-treasury-inbound-transfers-id-return: func(params: post-test-helpers-treasury-inbound-transfers-id-return-params) -> result<string, string>;

  record post-test-helpers-treasury-inbound-transfers-id-succeed-params {
    id: string
  }
  post-test-helpers-treasury-inbound-transfers-id-succeed: func(params: post-test-helpers-treasury-inbound-transfers-id-succeed-params) -> result<string, string>;

  record post-test-helpers-treasury-outbound-payments-id-fail-params {
    id: string
  }
  post-test-helpers-treasury-outbound-payments-id-fail: func(params: post-test-helpers-treasury-outbound-payments-id-fail-params) -> result<string, string>;

  record post-test-helpers-treasury-outbound-payments-id-post-params {
    id: string
  }
  post-test-helpers-treasury-outbound-payments-id-post: func(params: post-test-helpers-treasury-outbound-payments-id-post-params) -> result<string, string>;

  record post-test-helpers-treasury-outbound-payments-id-return-params {
    id: string
  }
  post-test-helpers-treasury-outbound-payments-id-return: func(params: post-test-helpers-treasury-outbound-payments-id-return-params) -> result<string, string>;

  record post-test-helpers-treasury-outbound-transfers-outbound-transfer-fail-params {
    outbound-transfer: string
  }
  post-test-helpers-treasury-outbound-transfers-outbound-transfer-fail: func(params: post-test-helpers-treasury-outbound-transfers-outbound-transfer-fail-params) -> result<string, string>;

  record post-test-helpers-treasury-outbound-transfers-outbound-transfer-post-params {
    outbound-transfer: string
  }
  post-test-helpers-treasury-outbound-transfers-outbound-transfer-post: func(params: post-test-helpers-treasury-outbound-transfers-outbound-transfer-post-params) -> result<string, string>;

  record post-test-helpers-treasury-outbound-transfers-outbound-transfer-return-params {
    outbound-transfer: string
  }
  post-test-helpers-treasury-outbound-transfers-outbound-transfer-return: func(params: post-test-helpers-treasury-outbound-transfers-outbound-transfer-return-params) -> result<string, string>;

  post-test-helpers-treasury-received-credits: func() -> result<string, string>;

  post-test-helpers-treasury-received-debits: func() -> result<string, string>;

}

interface tokens {
  post-tokens: func() -> result<string, string>;

  record get-tokens-token-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    token: string
  }
  get-tokens-token: func(params: get-tokens-token-params) -> result<string, string>;

}

interface topups {
  enum get-topups-status-enum {
    canceled,
    failed,
    pending,
    succeeded,
  }

  record get-topups-params {
    /// A positive integer representing how much to transfer.
    amount: option<string>,
    /// A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
    created: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Only return top-ups that have the given status. One of `canceled`, `failed`, `pending` or `succeeded`.
    status: option<get-topups-status-enum>
  }
  get-topups: func(params: get-topups-params) -> result<string, string>;

  post-topups: func() -> result<string, string>;

  record get-topups-topup-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    topup: string
  }
  get-topups-topup: func(params: get-topups-topup-params) -> result<string, string>;

  record post-topups-topup-params {
    topup: string
  }
  post-topups-topup: func(params: post-topups-topup-params) -> result<string, string>;

  record post-topups-topup-cancel-params {
    topup: string
  }
  post-topups-topup-cancel: func(params: post-topups-topup-cancel-params) -> result<string, string>;

}

interface transfers {
  record get-transfers-params {
    created: option<string>,
    /// Only return transfers for the destination specified by this account ID.
    destination: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Only return transfers with the specified transfer group.
    transfer-group: option<string>
  }
  get-transfers: func(params: get-transfers-params) -> result<string, string>;

  post-transfers: func() -> result<string, string>;

  record get-transfers-id-reversals-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    id: string,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-transfers-id-reversals: func(params: get-transfers-id-reversals-params) -> result<string, string>;

  record post-transfers-id-reversals-params {
    id: string
  }
  post-transfers-id-reversals: func(params: post-transfers-id-reversals-params) -> result<string, string>;

  record get-transfers-transfer-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    transfer: string
  }
  get-transfers-transfer: func(params: get-transfers-transfer-params) -> result<string, string>;

  record post-transfers-transfer-params {
    transfer: string
  }
  post-transfers-transfer: func(params: post-transfers-transfer-params) -> result<string, string>;

  record get-transfers-transfer-reversals-id-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    id: string,
    transfer: string
  }
  get-transfers-transfer-reversals-id: func(params: get-transfers-transfer-reversals-id-params) -> result<string, string>;

  record post-transfers-transfer-reversals-id-params {
    id: string,
    transfer: string
  }
  post-transfers-transfer-reversals-id: func(params: post-transfers-transfer-reversals-id-params) -> result<string, string>;

}

interface treasury {
  enum get-treasury-credit-reversals-status-enum {
    canceled,
    posted,
    processing,
  }

  enum get-treasury-debit-reversals-resolution-enum {
    lost,
    won,
  }

  enum get-treasury-debit-reversals-status-enum {
    canceled,
    completed,
    processing,
  }

  enum get-treasury-inbound-transfers-status-enum {
    canceled,
    failed,
    processing,
    succeeded,
  }

  enum get-treasury-outbound-payments-status-enum {
    canceled,
    failed,
    posted,
    processing,
    returned,
  }

  enum get-treasury-received-credits-status-enum {
    failed,
    succeeded,
  }

  enum get-treasury-transaction-entries-order-by-enum {
    created,
    effective-at,
  }

  enum get-treasury-transactions-order-by-enum {
    created,
    posted-at,
  }

  enum get-treasury-transactions-status-enum {
    open,
    posted,
    void,
  }

  record get-treasury-credit-reversals-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// Returns objects associated with this FinancialAccount.
    financial-account: string,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// Only return CreditReversals for the ReceivedCredit ID.
    received-credit: option<string>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Only return CreditReversals for a given status.
    status: option<get-treasury-credit-reversals-status-enum>
  }
  get-treasury-credit-reversals: func(params: get-treasury-credit-reversals-params) -> result<string, string>;

  post-treasury-credit-reversals: func() -> result<string, string>;

  record get-treasury-credit-reversals-credit-reversal-params {
    credit-reversal: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-treasury-credit-reversals-credit-reversal: func(params: get-treasury-credit-reversals-credit-reversal-params) -> result<string, string>;

  record get-treasury-debit-reversals-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// Returns objects associated with this FinancialAccount.
    financial-account: string,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// Only return DebitReversals for the ReceivedDebit ID.
    received-debit: option<string>,
    /// Only return DebitReversals for a given resolution.
    resolution: option<get-treasury-debit-reversals-resolution-enum>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Only return DebitReversals for a given status.
    status: option<get-treasury-debit-reversals-status-enum>
  }
  get-treasury-debit-reversals: func(params: get-treasury-debit-reversals-params) -> result<string, string>;

  post-treasury-debit-reversals: func() -> result<string, string>;

  record get-treasury-debit-reversals-debit-reversal-params {
    debit-reversal: string,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>
  }
  get-treasury-debit-reversals-debit-reversal: func(params: get-treasury-debit-reversals-debit-reversal-params) -> result<string, string>;

  record get-treasury-financial-accounts-params {
    created: option<string>,
    /// An object ID cursor for use in pagination.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit ranging from 1 to 100 (defaults to 10).
    limit: option<s32>,
    /// An object ID cursor for use in pagination.
    starting-after: option<string>
  }
  get-treasury-financial-accounts: func(params: get-treasury-financial-accounts-params) -> result<string, string>;

  post-treasury-financial-accounts: func() -> result<string, string>;

  record get-treasury-financial-accounts-financial-account-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    financial-account: string
  }
  get-treasury-financial-accounts-financial-account: func(params: get-treasury-financial-accounts-financial-account-params) -> result<string, string>;

  record post-treasury-financial-accounts-financial-account-params {
    financial-account: string
  }
  post-treasury-financial-accounts-financial-account: func(params: post-treasury-financial-accounts-financial-account-params) -> result<string, string>;

  record get-treasury-financial-accounts-financial-account-features-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    financial-account: string
  }
  get-treasury-financial-accounts-financial-account-features: func(params: get-treasury-financial-accounts-financial-account-features-params) -> result<string, string>;

  record post-treasury-financial-accounts-financial-account-features-params {
    financial-account: string
  }
  post-treasury-financial-accounts-financial-account-features: func(params: post-treasury-financial-accounts-financial-account-features-params) -> result<string, string>;

  record get-treasury-inbound-transfers-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// Returns objects associated with this FinancialAccount.
    financial-account: string,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Only return InboundTransfers that have the given status: `processing`, `succeeded`, `failed` or `canceled`.
    status: option<get-treasury-inbound-transfers-status-enum>
  }
  get-treasury-inbound-transfers: func(params: get-treasury-inbound-transfers-params) -> result<string, string>;

  post-treasury-inbound-transfers: func() -> result<string, string>;

  record get-treasury-inbound-transfers-id-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    id: string
  }
  get-treasury-inbound-transfers-id: func(params: get-treasury-inbound-transfers-id-params) -> result<string, string>;

  record post-treasury-inbound-transfers-inbound-transfer-cancel-params {
    inbound-transfer: string
  }
  post-treasury-inbound-transfers-inbound-transfer-cancel: func(params: post-treasury-inbound-transfers-inbound-transfer-cancel-params) -> result<string, string>;

  record get-treasury-outbound-payments-params {
    /// Only return OutboundPayments sent to this customer.
    customer: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// Returns objects associated with this FinancialAccount.
    financial-account: string,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Only return OutboundPayments that have the given status: `processing`, `failed`, `posted`, `returned`, or `canceled`.
    status: option<get-treasury-outbound-payments-status-enum>
  }
  get-treasury-outbound-payments: func(params: get-treasury-outbound-payments-params) -> result<string, string>;

  post-treasury-outbound-payments: func() -> result<string, string>;

  record get-treasury-outbound-payments-id-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    id: string
  }
  get-treasury-outbound-payments-id: func(params: get-treasury-outbound-payments-id-params) -> result<string, string>;

  record post-treasury-outbound-payments-id-cancel-params {
    id: string
  }
  post-treasury-outbound-payments-id-cancel: func(params: post-treasury-outbound-payments-id-cancel-params) -> result<string, string>;

  record get-treasury-outbound-transfers-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// Returns objects associated with this FinancialAccount.
    financial-account: string,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Only return OutboundTransfers that have the given status: `processing`, `canceled`, `failed`, `posted`, or `returned`.
    status: option<get-treasury-outbound-payments-status-enum>
  }
  get-treasury-outbound-transfers: func(params: get-treasury-outbound-transfers-params) -> result<string, string>;

  post-treasury-outbound-transfers: func() -> result<string, string>;

  record get-treasury-outbound-transfers-outbound-transfer-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    outbound-transfer: string
  }
  get-treasury-outbound-transfers-outbound-transfer: func(params: get-treasury-outbound-transfers-outbound-transfer-params) -> result<string, string>;

  record post-treasury-outbound-transfers-outbound-transfer-cancel-params {
    outbound-transfer: string
  }
  post-treasury-outbound-transfers-outbound-transfer-cancel: func(params: post-treasury-outbound-transfers-outbound-transfer-cancel-params) -> result<string, string>;

  record get-treasury-received-credits-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// The FinancialAccount that received the funds.
    financial-account: string,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// Only return ReceivedCredits described by the flow.
    linked-flows: option<string>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Only return ReceivedCredits that have the given status: `succeeded` or `failed`.
    status: option<get-treasury-received-credits-status-enum>
  }
  get-treasury-received-credits: func(params: get-treasury-received-credits-params) -> result<string, string>;

  record get-treasury-received-credits-id-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    id: string
  }
  get-treasury-received-credits-id: func(params: get-treasury-received-credits-id-params) -> result<string, string>;

  record get-treasury-received-debits-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// The FinancialAccount that funds were pulled from.
    financial-account: string,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Only return ReceivedDebits that have the given status: `succeeded` or `failed`.
    status: option<get-treasury-received-credits-status-enum>
  }
  get-treasury-received-debits: func(params: get-treasury-received-debits-params) -> result<string, string>;

  record get-treasury-received-debits-id-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    id: string
  }
  get-treasury-received-debits-id: func(params: get-treasury-received-debits-id-params) -> result<string, string>;

  record get-treasury-transaction-entries-params {
    created: option<string>,
    effective-at: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// Returns objects associated with this FinancialAccount.
    financial-account: string,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// The results are in reverse chronological order by `created` or `effective_at`. The default is `created`.
    order-by: option<get-treasury-transaction-entries-order-by-enum>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Only return TransactionEntries associated with this Transaction.
    transaction: option<string>
  }
  get-treasury-transaction-entries: func(params: get-treasury-transaction-entries-params) -> result<string, string>;

  record get-treasury-transaction-entries-id-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    id: string
  }
  get-treasury-transaction-entries-id: func(params: get-treasury-transaction-entries-id-params) -> result<string, string>;

  record get-treasury-transactions-params {
    created: option<string>,
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// Returns objects associated with this FinancialAccount.
    financial-account: string,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// The results are in reverse chronological order by `created` or `posted_at`. The default is `created`.
    order-by: option<get-treasury-transactions-order-by-enum>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>,
    /// Only return Transactions that have the given status: `open`, `posted`, or `void`.
    status: option<get-treasury-transactions-status-enum>,
    /// A filter for the `status_transitions.posted_at` timestamp. When using this filter, `status=posted` and `order_by=posted_at` must also be specified.
    status-transitions: option<string>
  }
  get-treasury-transactions: func(params: get-treasury-transactions-params) -> result<string, string>;

  record get-treasury-transactions-id-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    id: string
  }
  get-treasury-transactions-id: func(params: get-treasury-transactions-id-params) -> result<string, string>;

}

interface webhook-endpoints {
  record get-webhook-endpoints-params {
    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
    ending-before: option<string>,
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    limit: option<s32>,
    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
    starting-after: option<string>
  }
  get-webhook-endpoints: func(params: get-webhook-endpoints-params) -> result<string, string>;

  post-webhook-endpoints: func() -> result<string, string>;

  record get-webhook-endpoints-webhook-endpoint-params {
    /// Specifies which fields in the response should be expanded.
    expand: option<list<string>>,
    webhook-endpoint: string
  }
  get-webhook-endpoints-webhook-endpoint: func(params: get-webhook-endpoints-webhook-endpoint-params) -> result<string, string>;

  record post-webhook-endpoints-webhook-endpoint-params {
    webhook-endpoint: string
  }
  post-webhook-endpoints-webhook-endpoint: func(params: post-webhook-endpoints-webhook-endpoint-params) -> result<string, string>;

  record delete-webhook-endpoints-webhook-endpoint-params {
    webhook-endpoint: string
  }
  delete-webhook-endpoints-webhook-endpoint: func(params: delete-webhook-endpoints-webhook-endpoint-params) -> result<string, string>;

}
/// The component world: imports the secrets host capability
/// the runtime uses and exports the generated interfaces. The HTTP host
/// capability is imported by the `wstd` crate's bindings at build time.
world client {
  import wasmcloud:secrets/store@1.0.0;
  import wasmcloud:secrets/reveal@1.0.0;
  export account;
  export account-links;
  export accounts;
  export apple-pay;
  export application-fees;
  export apps;
  export balance;
  export balance-transactions;
  export billing-portal;
  export charges;
  export checkout;
  export country-specs;
  export coupons;
  export credit-notes;
  export customers;
  export disputes;
  export ephemeral-keys;
  export events;
  export exchange-rates;
  export file-links;
  export files;
  export financial-connections;
  export identity;
  export invoiceitems;
  export invoices;
  export issuing;
  export link-account-sessions;
  export linked-accounts;
  export mandates;
  export payment-intents;
  export payment-links;
  export payment-methods;
  export payouts;
  export plans;
  export prices;
  export products;
  export promotion-codes;
  export quotes;
  export radar;
  export refunds;
  export reporting;
  export reviews;
  export setup-attempts;
  export setup-intents;
  export shipping-rates;
  export sigma;
  export sources;
  export subscription-items;
  export subscription-schedules;
  export subscriptions;
  export tax;
  export tax-codes;
  export tax-rates;
  export terminal;
  export test-helpers;
  export tokens;
  export topups;
  export transfers;
  export treasury;
  export webhook-endpoints;
}
