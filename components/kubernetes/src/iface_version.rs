// AUTOGENERATED by openapi-bindgen. Do not edit by hand.

use crate::runtime::{dispatch, AuthApply, AuthKind, FieldLocation, FieldSpec, OpSpec};
use crate::exports::autostamp::kubernetes::version as iface_version;
use serde_json::{Map, Value};

const OP_VERSION_GET_CODE_VERSION: OpSpec = OpSpec {
    method: "GET",
    path_template: "/version/",
    fields: &[
    ],
    auth: &[
    ],
};

fn iface_version__io_k8s_apimachinery_pkg_version_info__to_json(p: &iface_version::IoK8sApimachineryPkgVersionInfo) -> Value {
    let mut m = Map::new();
    m.insert("buildDate".into(), Value::String((&p.build_date).clone()));
    m.insert("compiler".into(), Value::String((&p.compiler).clone()));
    m.insert("gitCommit".into(), Value::String((&p.git_commit).clone()));
    m.insert("gitTreeState".into(), Value::String((&p.git_tree_state).clone()));
    m.insert("gitVersion".into(), Value::String((&p.git_version).clone()));
    m.insert("goVersion".into(), Value::String((&p.go_version).clone()));
    m.insert("major".into(), Value::String((&p.major).clone()));
    m.insert("minor".into(), Value::String((&p.minor).clone()));
    m.insert("platform".into(), Value::String((&p.platform).clone()));
    Value::Object(m)
}

fn iface_version__io_k8s_apimachinery_pkg_version_info__from_json(v: &Value) -> Option<iface_version::IoK8sApimachineryPkgVersionInfo> {
    let m = v.as_object()?;
    Some(iface_version::IoK8sApimachineryPkgVersionInfo {
        build_date: m.get("buildDate").and_then(|v| (v).as_str().map(|s| s.to_string())).unwrap_or_default(),
        compiler: m.get("compiler").and_then(|v| (v).as_str().map(|s| s.to_string())).unwrap_or_default(),
        git_commit: m.get("gitCommit").and_then(|v| (v).as_str().map(|s| s.to_string())).unwrap_or_default(),
        git_tree_state: m.get("gitTreeState").and_then(|v| (v).as_str().map(|s| s.to_string())).unwrap_or_default(),
        git_version: m.get("gitVersion").and_then(|v| (v).as_str().map(|s| s.to_string())).unwrap_or_default(),
        go_version: m.get("goVersion").and_then(|v| (v).as_str().map(|s| s.to_string())).unwrap_or_default(),
        major: m.get("major").and_then(|v| (v).as_str().map(|s| s.to_string())).unwrap_or_default(),
        minor: m.get("minor").and_then(|v| (v).as_str().map(|s| s.to_string())).unwrap_or_default(),
        platform: m.get("platform").and_then(|v| (v).as_str().map(|s| s.to_string())).unwrap_or_default(),
    })
}

fn iface_version__get_code_version__ok(body: String) -> Result<iface_version::IoK8sApimachineryPkgVersionInfo, crate::runtime::DispatchError> {
    let v: Value = match serde_json::from_str(&body) {
        Ok(v) => v,
        Err(e) => return Err(crate::runtime::DispatchError::Transport(format!("failed to decode response body as JSON: {e}"))),
    };
    match iface_version__io_k8s_apimachinery_pkg_version_info__from_json(&v) {
        Some(x) => Ok(x),
        None => Err(crate::runtime::DispatchError::Transport("response body did not match the expected schema".to_string())),
    }
}

fn iface_version__get_code_version__err(e: crate::runtime::DispatchError) -> iface_version::GetCodeVersionError {
    match e {
        crate::runtime::DispatchError::Http { status, body } => match status {
            401u16 => iface_version::GetCodeVersionError::Unauthorized(body),
            _ => iface_version::GetCodeVersionError::Other(body),
        },
        crate::runtime::DispatchError::Transport(m) => iface_version::GetCodeVersionError::Other(m),
    }
}

impl iface_version::Guest for crate::Component {
    fn get_code_version() -> Result<iface_version::IoK8sApimachineryPkgVersionInfo, iface_version::GetCodeVersionError> {
        match dispatch(&OP_VERSION_GET_CODE_VERSION, Value::Object(Map::new())).and_then(iface_version__get_code_version__ok) {
            Ok(v) => Ok(v),
            Err(e) => Err(iface_version__get_code_version__err(e)),
        }
    }
}
