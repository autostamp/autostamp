// AUTOGENERATED by openapi-bindgen. Do not edit by hand.

use crate::runtime::{dispatch, AuthApply, AuthKind, FieldLocation, FieldSpec, OpSpec};
use crate::exports::autostamp::tfl::air_quality as iface_air_quality;
use serde_json::{Map, Value};

const OP_AIR_QUALITY_GET: OpSpec = OpSpec {
    method: "GET",
    path_template: "/AirQuality",
    fields: &[
    ],
    auth: &[
    ],
};

fn iface_air_quality__system_object_entry__to_json(p: &iface_air_quality::SystemObjectEntry) -> Value {
    let mut m = Map::new();
    m.insert("key".into(), Value::String((&p.key).clone()));
    m.insert("value".into(), Value::String((&p.value).clone()));
    Value::Object(m)
}

fn iface_air_quality__system_object_entry__from_json(v: &Value) -> Option<iface_air_quality::SystemObjectEntry> {
    let m = v.as_object()?;
    Some(iface_air_quality::SystemObjectEntry {
        key: m.get("key").and_then(|v| (v).as_str().map(|s| s.to_string())).unwrap_or_default(),
        value: m.get("value").and_then(|v| (v).as_str().map(|s| s.to_string())).unwrap_or_default(),
    })
}

fn iface_air_quality__get__ok(body: String) -> Result<Vec<iface_air_quality::SystemObjectEntry>, crate::runtime::DispatchError> {
    let v: Value = match serde_json::from_str(&body) {
        Ok(v) => v,
        Err(e) => return Err(crate::runtime::DispatchError::Transport(format!("failed to decode response body as JSON: {e}"))),
    };
    match (&v).as_object().map(|o| o.iter().filter_map(|(k, x)| ((x).as_str().map(|s| s.to_string())).map(|val| iface_air_quality::SystemObjectEntry { key: k.clone(), value: val })).collect()) {
        Some(x) => Ok(x),
        None => Err(crate::runtime::DispatchError::Transport("response body did not match the expected schema".to_string())),
    }
}

fn iface_air_quality__get__err(e: crate::runtime::DispatchError) -> String {
    match e {
        crate::runtime::DispatchError::Http { status, body } => format!("HTTP {status}: {body}"),
        crate::runtime::DispatchError::Transport(m) => m,
    }
}

impl iface_air_quality::Guest for crate::Component {
    fn get() -> Result<Vec<iface_air_quality::SystemObjectEntry>, String> {
        match dispatch(&OP_AIR_QUALITY_GET, Value::Object(Map::new())).and_then(iface_air_quality__get__ok) {
            Ok(v) => Ok(v),
            Err(e) => Err(iface_air_quality__get__err(e)),
        }
    }
}
