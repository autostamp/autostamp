// AUTOGENERATED by openapi-bindgen. Do not edit by hand.
#![allow(unused, clippy::all, non_snake_case)]

wit_bindgen::generate!({
    world: "client",
    path: "wit",
    generate_all,
});

/// The API base URL, resolved from the OpenAPI `servers` list.
const BASE_URL: &str = "https://example.com/rest/default";

/// The component entry point implementing the generated interfaces.
struct Component;

mod runtime {
// AUTOGENERATED by openapi-bindgen. Do not edit by hand.
//
// The generic request runtime shared by every generated component. Codegen emits one
// `OpSpec` per operation (an HTTP method, a path template, the parameter fields, and the
// resolved auth schemes) and each `Guest` method calls `dispatch`. The runtime builds the
// outgoing request with the `wstd` HTTP client (which talks to the host over `wasi:http`),
// attaches credentials fetched from `wasmcloud:secrets`, and returns the response body.
//
// The generated guest exports are synchronous, so the async `wstd` client is driven to
// completion on a local reactor via `wstd::runtime::block_on`.
//
// Credentials are never passed as operation arguments: for each `AuthApply` the runtime
// looks up the secret by `secret_key` (the OpenAPI security-scheme name) via
// `wasmcloud:secrets/store`, reveals it via `wasmcloud:secrets/reveal`, and applies it to
// the request according to its `AuthKind`.

use serde_json::Value;
use wstd::http::{Body, Client, Method, Request};

use crate::wasmcloud::secrets::reveal;
use crate::wasmcloud::secrets::store;

/// Where a parameter field belongs in the outgoing request.
pub(crate) enum FieldLocation {
    Path,
    Query,
    Header,
    Body,
}

/// A single operation parameter. `snake` keys the field in the internal params JSON (and, for a
/// path field, matches the `{snake}` placeholder in the path template); `wire` is the verbatim name
/// the field takes on the HTTP wire (query key, header name, or body property). The two differ
/// whenever the source name isn't already snake_case (e.g. `dryRun` → snake `dry_run`, wire
/// `dryRun`), and let distinct inputs that share a snake key (`DateCreated`, `DateCreated<`) still
/// travel under their true names.
pub(crate) struct FieldSpec {
    pub(crate) snake: &'static str,
    pub(crate) wire: &'static str,
    pub(crate) location: FieldLocation,
}

/// How a resolved security scheme is attached to the request.
pub(crate) enum AuthKind {
    /// `Authorization: Bearer <secret>`.
    Bearer,
    /// `Authorization: Basic base64(<secret>)`, secret holding `user:pass`.
    Basic,
    /// A header named by the inner string carrying the secret verbatim.
    ApiKeyHeader(&'static str),
    /// A query parameter named by the inner string carrying the secret verbatim.
    ApiKeyQuery(&'static str),
    /// A `Cookie: <name>=<secret>` entry named by the inner string.
    ApiKeyCookie(&'static str),
}

/// A security scheme to apply, paired with the `wasmcloud:secrets` key holding its value.
pub(crate) struct AuthApply {
    pub(crate) secret_key: &'static str,
    pub(crate) kind: AuthKind,
}

/// The failure modes of [`dispatch`], kept structured so each generated operation can map them
/// onto its own typed error: an `Http` failure carries the response status and body (routed to
/// the matching error-variant case), while a `Transport` failure carries a message for the
/// catch-all case (a secret lookup, connection, or decode failure that never produced an HTTP
/// status).
pub(crate) enum DispatchError {
    Http { status: u16, body: String },
    Transport(String),
}

/// A single API operation: HTTP method, path template (with `{snake}` placeholders), the
/// parameter fields, and the security schemes to apply.
pub(crate) struct OpSpec {
    pub(crate) method: &'static str,
    pub(crate) path_template: &'static str,
    pub(crate) fields: &'static [FieldSpec],
    pub(crate) auth: &'static [AuthApply],
}

/// Execute `op` with the supplied `params` (a JSON object keyed by snake-case field names)
/// and return the raw 2xx response body, or a structured [`DispatchError`] the caller maps
/// onto the operation's typed error.
pub(crate) fn dispatch(op: &OpSpec, params: Value) -> Result<String, DispatchError> {
    let obj = match &params {
        Value::Object(map) => Some(map),
        _ => None,
    };

    // Substitute `{snake}` placeholders in the path with their field values.
    let mut path = op.path_template.to_string();
    let mut query: Vec<(String, String)> = Vec::new();
    let mut headers: Vec<(String, String)> = Vec::new();
    let mut body = serde_json::Map::new();

    for field in op.fields {
        let value = obj.and_then(|m| m.get(field.snake));
        let Some(value) = value else { continue };
        if value.is_null() {
            continue;
        }
        match field.location {
            FieldLocation::Path => {
                let placeholder = format!("{{{}}}", field.snake);
                path = path.replace(&placeholder, &percent_encode(&scalar(value)));
            }
            FieldLocation::Query => query.push((field.wire.to_string(), scalar(value))),
            FieldLocation::Header => headers.push((field.wire.to_string(), scalar(value))),
            FieldLocation::Body => {
                body.insert(field.wire.to_string(), value.clone());
            }
        }
    }

    // Apply each resolved security scheme, sourcing its value from wasmcloud:secrets.
    let mut cookies: Vec<String> = Vec::new();
    for apply in op.auth {
        let secret = fetch_secret(apply.secret_key).map_err(DispatchError::Transport)?;
        match &apply.kind {
            AuthKind::Bearer => headers.push(("authorization".into(), format!("Bearer {secret}"))),
            AuthKind::Basic => {
                headers.push(("authorization".into(), format!("Basic {}", base64(secret.as_bytes()))))
            }
            AuthKind::ApiKeyHeader(name) => headers.push((name.to_string(), secret)),
            AuthKind::ApiKeyQuery(name) => query.push((name.to_string(), secret)),
            AuthKind::ApiKeyCookie(name) => cookies.push(format!("{name}={secret}")),
        }
    }
    if !cookies.is_empty() {
        headers.push(("cookie".into(), cookies.join("; ")));
    }

    // Assemble the path + query string.
    if !query.is_empty() {
        let joined = query
            .iter()
            .map(|(k, v)| format!("{}={}", percent_encode(k), percent_encode(v)))
            .collect::<Vec<_>>()
            .join("&");
        path.push('?');
        path.push_str(&joined);
    }

    let body_bytes = if body.is_empty() {
        Vec::new()
    } else {
        Value::Object(body).to_string().into_bytes()
    };

    perform(op.method, &path, &headers, &body_bytes)
}

/// Render a JSON scalar as the string that should appear in a path, query, or header.
fn scalar(value: &Value) -> String {
    match value {
        Value::String(s) => s.clone(),
        Value::Bool(b) => b.to_string(),
        Value::Number(n) => n.to_string(),
        other => other.to_string(),
    }
}

/// Fetch and reveal a secret value by key from `wasmcloud:secrets`.
fn fetch_secret(key: &str) -> Result<String, String> {
    let secret = store::get(key).map_err(|err| match err {
        store::SecretsError::Upstream(msg) => format!("secret `{key}`: upstream error: {msg}"),
        store::SecretsError::Io(msg) => format!("secret `{key}`: io error: {msg}"),
        store::SecretsError::NotFound => format!("secret `{key}`: not found"),
    })?;
    match reveal::reveal(&secret) {
        store::SecretValue::String(s) => Ok(s),
        store::SecretValue::Bytes(bytes) => {
            String::from_utf8(bytes).map_err(|_| format!("secret `{key}` is not valid UTF-8"))
        }
    }
}

/// Build and send the outgoing request with `wstd`, returning the response body on a 2xx
/// status and a structured [`DispatchError`] otherwise. The generated guest exports are
/// synchronous, so the async `wstd` client is driven to completion on a local reactor via
/// `wstd::runtime::block_on`.
fn perform(
    method: &str,
    path_with_query: &str,
    headers: &[(String, String)],
    body: &[u8],
) -> Result<String, DispatchError> {
    let url = join_url(crate::BASE_URL, path_with_query);
    let method = Method::from_bytes(method.as_bytes())
        .map_err(|err| DispatchError::Transport(format!("invalid HTTP method `{method}`: {err}")))?;

    let mut builder = Request::builder().method(method).uri(url);
    for (name, value) in headers {
        builder = builder.header(name.as_str(), value.as_str());
    }
    let request = builder
        .body(if body.is_empty() {
            Body::empty()
        } else {
            Body::from(body.to_vec())
        })
        .map_err(|err| DispatchError::Transport(format!("failed to build request: {err}")))?;

    wstd::runtime::block_on(async move {
        let client = Client::new();
        let response = client
            .send(request)
            .await
            .map_err(|err| DispatchError::Transport(format!("request failed: {err:#}")))?;

        let status = response.status().as_u16();
        let mut response_body = response.into_body();
        let bytes = response_body.contents().await.map_err(|err| {
            DispatchError::Transport(format!("failed to read response body: {err:#}"))
        })?;
        let body_text = String::from_utf8_lossy(bytes).into_owned();

        if (200..300).contains(&status) {
            Ok(body_text)
        } else {
            Err(DispatchError::Http {
                status,
                body: body_text,
            })
        }
    })
}

/// Join the API base URL with an operation path (which may include a query string) into a
/// single absolute URL. `wstd` parses the scheme, authority, and path from it.
fn join_url(base: &str, path_with_query: &str) -> String {
    let base = base.trim_end_matches('/');
    if base.is_empty() {
        return path_with_query.to_string();
    }
    if path_with_query.starts_with('/') {
        format!("{base}{path_with_query}")
    } else {
        format!("{base}/{path_with_query}")
    }
}

/// Percent-encode a string for use in a path segment or query value, leaving the RFC 3986
/// unreserved characters untouched.
fn percent_encode(input: &str) -> String {
    let mut out = String::with_capacity(input.len());
    for byte in input.bytes() {
        match byte {
            b'A'..=b'Z' | b'a'..=b'z' | b'0'..=b'9' | b'-' | b'_' | b'.' | b'~' => {
                out.push(byte as char)
            }
            _ => out.push_str(&format!("%{byte:02X}")),
        }
    }
    out
}

/// Standard base64 encoding (RFC 4648) without padding omission.
fn base64(input: &[u8]) -> String {
    const ALPHABET: &[u8; 64] = b"ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
    let mut out = String::with_capacity(input.len().div_ceil(3) * 4);
    for chunk in input.chunks(3) {
        let b0 = chunk[0] as u32;
        let b1 = *chunk.get(1).unwrap_or(&0) as u32;
        let b2 = *chunk.get(2).unwrap_or(&0) as u32;
        let triple = (b0 << 16) | (b1 << 8) | b2;
        out.push(ALPHABET[((triple >> 18) & 0x3f) as usize] as char);
        out.push(ALPHABET[((triple >> 12) & 0x3f) as usize] as char);
        if chunk.len() > 1 {
            out.push(ALPHABET[((triple >> 6) & 0x3f) as usize] as char);
        } else {
            out.push('=');
        }
        if chunk.len() > 2 {
            out.push(ALPHABET[(triple & 0x3f) as usize] as char);
        } else {
            out.push('=');
        }
    }
    out
}
}

mod iface_addresses_address_id;
mod iface_amazon_billing_address_amazon_order_reference_id;
mod iface_amazon_shipping_address_amazon_order_reference_id;
mod iface_amazon_order_ref;
mod iface_analytics_link;
mod iface_attribute_metadata_customer;
mod iface_attribute_metadata_customer_attribute_attribute_code;
mod iface_attribute_metadata_customer_custom;
mod iface_attribute_metadata_customer_form_form_code;
mod iface_attribute_metadata_customer_address;
mod iface_attribute_metadata_customer_address_attribute_attribute_code;
mod iface_attribute_metadata_customer_address_custom;
mod iface_attribute_metadata_customer_address_form_form_code;
mod iface_bulk_bulk_uuid_detailed_status;
mod iface_bulk_bulk_uuid_operation_status_status;
mod iface_bulk_bulk_uuid_status;
mod iface_bundle_products_options_add;
mod iface_bundle_products_options_types;
mod iface_bundle_products_options_option_id;
mod iface_bundle_products_product_sku_children;
mod iface_bundle_products_sku_links_id;
mod iface_bundle_products_sku_links_option_id;
mod iface_bundle_products_sku_options_all;
mod iface_bundle_products_sku_options_option_id;
mod iface_bundle_products_sku_options_option_id_children_child_sku;
mod iface_carts;
mod iface_carts_guest_carts_cart_id_check_gift_card_gift_card_code;
mod iface_carts_guest_carts_cart_id_gift_cards;
mod iface_carts_guest_carts_cart_id_gift_cards_gift_card_code;
mod iface_carts_licence;
mod iface_carts_mine;
mod iface_carts_mine_balance_apply;
mod iface_carts_mine_balance_unapply;
mod iface_carts_mine_billing_address;
mod iface_carts_mine_check_gift_card_gift_card_code;
mod iface_carts_mine_checkout_fields;
mod iface_carts_mine_collect_totals;
mod iface_carts_mine_collection_point_search_request;
mod iface_carts_mine_collection_point_search_result;
mod iface_carts_mine_collection_point_select;
mod iface_carts_mine_coupons;
mod iface_carts_mine_coupons_coupon_code;
mod iface_carts_mine_delivery_option;
mod iface_carts_mine_estimate_shipping_methods;
mod iface_carts_mine_estimate_shipping_methods_by_address_id;
mod iface_carts_mine_gift_message;
mod iface_carts_mine_gift_message_item_id;
mod iface_carts_mine_gift_cards;
mod iface_carts_mine_gift_cards_gift_card_code;
mod iface_carts_mine_items;
mod iface_carts_mine_items_item_id;
mod iface_carts_mine_order;
mod iface_carts_mine_payment_information;
mod iface_carts_mine_payment_methods;
mod iface_carts_mine_selected_payment_method;
mod iface_carts_mine_set_payment_information;
mod iface_carts_mine_shipping_information;
mod iface_carts_mine_shipping_methods;
mod iface_carts_mine_totals;
mod iface_carts_mine_totals_information;
mod iface_carts_search;
mod iface_carts_cart_id;
mod iface_carts_cart_id_billing_address;
mod iface_carts_cart_id_coupons;
mod iface_carts_cart_id_coupons_coupon_code;
mod iface_carts_cart_id_estimate_shipping_methods;
mod iface_carts_cart_id_estimate_shipping_methods_by_address_id;
mod iface_carts_cart_id_gift_message;
mod iface_carts_cart_id_gift_message_item_id;
mod iface_carts_cart_id_gift_cards;
mod iface_carts_cart_id_gift_cards_gift_card_code;
mod iface_carts_cart_id_items;
mod iface_carts_cart_id_items_item_id;
mod iface_carts_cart_id_order;
mod iface_carts_cart_id_payment_methods;
mod iface_carts_cart_id_selected_payment_method;
mod iface_carts_cart_id_shipping_information;
mod iface_carts_cart_id_shipping_methods;
mod iface_carts_cart_id_totals;
mod iface_carts_cart_id_totals_information;
mod iface_carts_quote_id_gift_cards;
mod iface_carts_quote_id_items;
mod iface_categories;
mod iface_categories_attributes;
mod iface_categories_attributes_attribute_code;
mod iface_categories_attributes_attribute_code_options;
mod iface_categories_list;
mod iface_categories_category_id;
mod iface_categories_category_id_move;
mod iface_categories_category_id_products;
mod iface_categories_category_id_products_sku;
mod iface_categories_id;
mod iface_cms_block;
mod iface_cms_block_search;
mod iface_cms_block_block_id;
mod iface_cms_block_id;
mod iface_cms_page;
mod iface_cms_page_search;
mod iface_cms_page_id;
mod iface_cms_page_page_id;
mod iface_company;
mod iface_company_assign_roles;
mod iface_company_role;
mod iface_company_role_id;
mod iface_company_role_role_id;
mod iface_company_role_role_id_users;
mod iface_company_company_id;
mod iface_company_credits;
mod iface_company_credits_company_company_id;
mod iface_company_credits_history;
mod iface_company_credits_history_history_id;
mod iface_company_credits_credit_id;
mod iface_company_credits_credit_id_decrease_balance;
mod iface_company_credits_credit_id_increase_balance;
mod iface_company_credits_id;
mod iface_configurable_products_variation;
mod iface_configurable_products_sku_child;
mod iface_configurable_products_sku_children;
mod iface_configurable_products_sku_children_child_sku;
mod iface_configurable_products_sku_options;
mod iface_configurable_products_sku_options_all;
mod iface_configurable_products_sku_options_id;
mod iface_coupons;
mod iface_coupons_delete_by_codes;
mod iface_coupons_delete_by_ids;
mod iface_coupons_generate;
mod iface_coupons_search;
mod iface_coupons_coupon_id;
mod iface_creditmemo;
mod iface_creditmemo_refund;
mod iface_creditmemo_id;
mod iface_creditmemo_id_comments;
mod iface_creditmemo_id_emails;
mod iface_creditmemos;
mod iface_customer_groups;
mod iface_customer_groups_default;
mod iface_customer_groups_default_id;
mod iface_customer_groups_default_store_id;
mod iface_customer_groups_search;
mod iface_customer_groups_id;
mod iface_customer_groups_id_permissions;
mod iface_customers;
mod iface_customers_addresses_address_id;
mod iface_customers_confirm;
mod iface_customers_is_email_available;
mod iface_customers_me;
mod iface_customers_me_activate;
mod iface_customers_me_billing_address;
mod iface_customers_me_password;
mod iface_customers_me_shipping_address;
mod iface_customers_password;
mod iface_customers_reset_password;
mod iface_customers_search;
mod iface_customers_validate;
mod iface_customers_customer_id;
mod iface_customers_customer_id_billing_address;
mod iface_customers_customer_id_carts;
mod iface_customers_customer_id_confirm;
mod iface_customers_customer_id_password_reset_link_token_reset_password_link_token;
mod iface_customers_customer_id_permissions_readonly;
mod iface_customers_customer_id_shipping_address;
mod iface_customers_email_activate;
mod iface_directory_countries;
mod iface_directory_countries_country_id;
mod iface_directory_currency;
mod iface_eav_attribute_sets;
mod iface_eav_attribute_sets_list;
mod iface_eav_attribute_sets_attribute_set_id;
mod iface_gift_wrappings;
mod iface_gift_wrappings_id;
mod iface_gift_wrappings_wrapping_id;
mod iface_giftregistry_mine_estimate_shipping_methods;
mod iface_guest_carts;
mod iface_guest_carts_cart_id;
mod iface_guest_carts_cart_id_billing_address;
mod iface_guest_carts_cart_id_checkout_fields;
mod iface_guest_carts_cart_id_collect_totals;
mod iface_guest_carts_cart_id_collection_point_search_request;
mod iface_guest_carts_cart_id_collection_point_search_result;
mod iface_guest_carts_cart_id_collection_point_select;
mod iface_guest_carts_cart_id_coupons;
mod iface_guest_carts_cart_id_coupons_coupon_code;
mod iface_guest_carts_cart_id_delivery_option;
mod iface_guest_carts_cart_id_estimate_shipping_methods;
mod iface_guest_carts_cart_id_gift_message;
mod iface_guest_carts_cart_id_gift_message_item_id;
mod iface_guest_carts_cart_id_items;
mod iface_guest_carts_cart_id_items_item_id;
mod iface_guest_carts_cart_id_order;
mod iface_guest_carts_cart_id_payment_information;
mod iface_guest_carts_cart_id_payment_methods;
mod iface_guest_carts_cart_id_selected_payment_method;
mod iface_guest_carts_cart_id_set_payment_information;
mod iface_guest_carts_cart_id_shipping_information;
mod iface_guest_carts_cart_id_shipping_methods;
mod iface_guest_carts_cart_id_totals;
mod iface_guest_carts_cart_id_totals_information;
mod iface_guest_giftregistry_cart_id_estimate_shipping_methods;
mod iface_hierarchy_move_id;
mod iface_hierarchy_id;
mod iface_integration_admin_token;
mod iface_integration_customer_token;
mod iface_invoice_invoice_id_refund;
mod iface_invoices;
mod iface_invoices_comments;
mod iface_invoices_id;
mod iface_invoices_id_capture;
mod iface_invoices_id_comments;
mod iface_invoices_id_emails;
mod iface_invoices_id_void;
mod iface_modules;
mod iface_negotiable_carts_cart_id_billing_address;
mod iface_negotiable_carts_cart_id_coupons;
mod iface_negotiable_carts_cart_id_coupons_coupon_code;
mod iface_negotiable_carts_cart_id_estimate_shipping_methods;
mod iface_negotiable_carts_cart_id_estimate_shipping_methods_by_address_id;
mod iface_negotiable_carts_cart_id_gift_cards;
mod iface_negotiable_carts_cart_id_gift_cards_gift_card_code;
mod iface_negotiable_carts_cart_id_payment_information;
mod iface_negotiable_carts_cart_id_set_payment_information;
mod iface_negotiable_carts_cart_id_shipping_information;
mod iface_negotiable_carts_cart_id_totals;
mod iface_negotiable_quote_attachment_content;
mod iface_negotiable_quote_decline;
mod iface_negotiable_quote_prices_updated;
mod iface_negotiable_quote_request;
mod iface_negotiable_quote_submit_to_customer;
mod iface_negotiable_quote_quote_id;
mod iface_negotiable_quote_quote_id_comments;
mod iface_negotiable_quote_quote_id_shipping_method;
mod iface_order_order_id_invoice;
mod iface_order_order_id_refund;
mod iface_order_order_id_ship;
mod iface_orders;
mod iface_orders_create;
mod iface_orders_items;
mod iface_orders_items_id;
mod iface_orders_id;
mod iface_orders_id_cancel;
mod iface_orders_id_comments;
mod iface_orders_id_emails;
mod iface_orders_id_hold;
mod iface_orders_id_statuses;
mod iface_orders_id_unhold;
mod iface_orders_parent_id;
mod iface_products;
mod iface_products_render_info;
mod iface_products_attribute_sets;
mod iface_products_attribute_sets_attributes;
mod iface_products_attribute_sets_groups;
mod iface_products_attribute_sets_groups_list;
mod iface_products_attribute_sets_groups_group_id;
mod iface_products_attribute_sets_sets_list;
mod iface_products_attribute_sets_attribute_set_id;
mod iface_products_attribute_sets_attribute_set_id_attributes;
mod iface_products_attribute_sets_attribute_set_id_attributes_attribute_code;
mod iface_products_attribute_sets_attribute_set_id_groups;
mod iface_products_attributes;
mod iface_products_attributes_types;
mod iface_products_attributes_attribute_code;
mod iface_products_attributes_attribute_code_options;
mod iface_products_attributes_attribute_code_options_option_id;
mod iface_products_base_prices;
mod iface_products_base_prices_information;
mod iface_products_cost;
mod iface_products_cost_delete;
mod iface_products_cost_information;
mod iface_products_downloadable_links_samples_id;
mod iface_products_downloadable_links_id;
mod iface_products_links_types;
mod iface_products_links_type_attributes;
mod iface_products_media_types_attribute_set_name;
mod iface_products_options;
mod iface_products_options_types;
mod iface_products_options_option_id;
mod iface_products_special_price;
mod iface_products_special_price_delete;
mod iface_products_special_price_information;
mod iface_products_tier_prices;
mod iface_products_tier_prices_delete;
mod iface_products_tier_prices_information;
mod iface_products_types;
mod iface_products_product_sku_stock_items_item_id;
mod iface_products_sku;
mod iface_products_sku_downloadable_links;
mod iface_products_sku_downloadable_links_samples;
mod iface_products_sku_downloadable_links_samples_id;
mod iface_products_sku_downloadable_links_id;
mod iface_products_sku_group_prices_customer_group_id_tiers;
mod iface_products_sku_group_prices_customer_group_id_tiers_qty;
mod iface_products_sku_group_prices_customer_group_id_tiers_qty_price_price;
mod iface_products_sku_links;
mod iface_products_sku_links_type;
mod iface_products_sku_links_type_linked_product_sku;
mod iface_products_sku_media;
mod iface_products_sku_media_entry_id;
mod iface_products_sku_options;
mod iface_products_sku_options_option_id;
mod iface_products_sku_websites;
mod iface_products_sku_websites_website_id;
mod iface_requisition_lists;
mod iface_returns;
mod iface_returns_id;
mod iface_returns_id_comments;
mod iface_returns_id_labels;
mod iface_returns_id_tracking_numbers;
mod iface_returns_id_tracking_numbers_track_id;
mod iface_returns_attribute_metadata;
mod iface_returns_attribute_metadata_custom;
mod iface_returns_attribute_metadata_form_form_code;
mod iface_returns_attribute_metadata_attribute_code;
mod iface_reward_mine_use_reward;
mod iface_sales_rules;
mod iface_sales_rules_search;
mod iface_sales_rules_rule_id;
mod iface_search;
mod iface_shared_catalog;
mod iface_shared_catalog_id;
mod iface_shared_catalog_id_assign_categories;
mod iface_shared_catalog_id_assign_products;
mod iface_shared_catalog_id_categories;
mod iface_shared_catalog_id_products;
mod iface_shared_catalog_id_unassign_categories;
mod iface_shared_catalog_id_unassign_products;
mod iface_shared_catalog_shared_catalog_id;
mod iface_shared_catalog_shared_catalog_id_assign_companies;
mod iface_shared_catalog_shared_catalog_id_companies;
mod iface_shared_catalog_shared_catalog_id_unassign_companies;
mod iface_shipment;
mod iface_shipment_track;
mod iface_shipment_track_id;
mod iface_shipment_id;
mod iface_shipment_id_comments;
mod iface_shipment_id_emails;
mod iface_shipment_id_label;
mod iface_shipments;
mod iface_stock_items_low_stock;
mod iface_stock_items_product_sku;
mod iface_stock_statuses_product_sku;
mod iface_store_store_configs;
mod iface_store_store_groups;
mod iface_store_store_views;
mod iface_store_websites;
mod iface_tax_classes;
mod iface_tax_classes_search;
mod iface_tax_classes_class_id;
mod iface_tax_classes_tax_class_id;
mod iface_tax_rates;
mod iface_tax_rates_search;
mod iface_tax_rates_rate_id;
mod iface_tax_rules;
mod iface_tax_rules_search;
mod iface_tax_rules_rule_id;
mod iface_team;
mod iface_team_company_id;
mod iface_team_team_id;
mod iface_temando_rma_rma_id_shipments;
mod iface_transactions;
mod iface_transactions_id;
mod iface_worldpay_guest_carts_cart_id_payment_information;

export!(Component);
