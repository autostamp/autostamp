// AUTOGENERATED by openapi-bindgen. Do not edit by hand.

use crate::runtime::{dispatch, AuthApply, AuthKind, FieldLocation, FieldSpec, OpSpec};
use crate::exports::autostamp::xero::identity as iface_identity;
use serde_json::{Map, Value};

const OP_IDENTITY_GET_CONNECTIONS: OpSpec = OpSpec {
    method: "GET",
    path_template: "/Connections",
    fields: &[
        FieldSpec { snake: "auth_event_id", wire: "authEventId", location: FieldLocation::Query },
    ],
    auth: &[
        AuthApply { secret_key: "OAuth2", kind: AuthKind::Bearer },
    ],
};

const OP_IDENTITY_DELETE_CONNECTION: OpSpec = OpSpec {
    method: "DELETE",
    path_template: "/Connections/{id}",
    fields: &[
        FieldSpec { snake: "id", wire: "id", location: FieldLocation::Path },
    ],
    auth: &[
        AuthApply { secret_key: "OAuth2", kind: AuthKind::Bearer },
    ],
};

fn iface_identity__connection__to_json(p: &iface_identity::Connection) -> Value {
    let mut m = Map::new();
    m.insert("authEventId".into(), match (&p.auth_event_id) { Some(v) => Value::String((v).clone()), None => Value::Null });
    m.insert("createdDateUtc".into(), match (&p.created_date_utc) { Some(v) => Value::String((v).clone()), None => Value::Null });
    m.insert("id".into(), match (&p.id) { Some(v) => Value::String((v).clone()), None => Value::Null });
    m.insert("tenantId".into(), match (&p.tenant_id) { Some(v) => Value::String((v).clone()), None => Value::Null });
    m.insert("tenantName".into(), match (&p.tenant_name) { Some(v) => Value::String((v).clone()), None => Value::Null });
    m.insert("tenantType".into(), match (&p.tenant_type) { Some(v) => Value::String((v).clone()), None => Value::Null });
    m.insert("updatedDateUtc".into(), match (&p.updated_date_utc) { Some(v) => Value::String((v).clone()), None => Value::Null });
    Value::Object(m)
}

fn iface_identity__get_connections_params__to_json(p: &iface_identity::GetConnectionsParams) -> Value {
    let mut m = Map::new();
    m.insert("auth_event_id".into(), match (&p.auth_event_id) { Some(v) => Value::String((v).clone()), None => Value::Null });
    Value::Object(m)
}

fn iface_identity__delete_connection_params__to_json(p: &iface_identity::DeleteConnectionParams) -> Value {
    let mut m = Map::new();
    m.insert("id".into(), Value::String((&p.id).clone()));
    Value::Object(m)
}

fn iface_identity__connection__from_json(v: &Value) -> Option<iface_identity::Connection> {
    let m = v.as_object()?;
    Some(iface_identity::Connection {
        auth_event_id: m.get("authEventId").filter(|v| !v.is_null()).and_then(|v| (v).as_str().map(|s| s.to_string())),
        created_date_utc: m.get("createdDateUtc").filter(|v| !v.is_null()).and_then(|v| (v).as_str().map(|s| s.to_string())),
        id: m.get("id").filter(|v| !v.is_null()).and_then(|v| (v).as_str().map(|s| s.to_string())),
        tenant_id: m.get("tenantId").filter(|v| !v.is_null()).and_then(|v| (v).as_str().map(|s| s.to_string())),
        tenant_name: m.get("tenantName").filter(|v| !v.is_null()).and_then(|v| (v).as_str().map(|s| s.to_string())),
        tenant_type: m.get("tenantType").filter(|v| !v.is_null()).and_then(|v| (v).as_str().map(|s| s.to_string())),
        updated_date_utc: m.get("updatedDateUtc").filter(|v| !v.is_null()).and_then(|v| (v).as_str().map(|s| s.to_string())),
    })
}

fn iface_identity__get_connections__ok(body: String) -> Result<Vec<iface_identity::Connection>, crate::runtime::DispatchError> {
    let v: Value = match serde_json::from_str(&body) {
        Ok(v) => v,
        Err(e) => return Err(crate::runtime::DispatchError::Transport(format!("failed to decode response body as JSON: {e}"))),
    };
    match (&v).as_array().map(|a| a.iter().filter_map(|x| iface_identity__connection__from_json(x)).collect()) {
        Some(x) => Ok(x),
        None => Err(crate::runtime::DispatchError::Transport("response body did not match the expected schema".to_string())),
    }
}

fn iface_identity__get_connections__err(e: crate::runtime::DispatchError) -> String {
    match e {
        crate::runtime::DispatchError::Http { status, body } => format!("HTTP {status}: {body}"),
        crate::runtime::DispatchError::Transport(m) => m,
    }
}

fn iface_identity__delete_connection__ok(body: String) -> Result<String, crate::runtime::DispatchError> {
    Ok(body)
}

fn iface_identity__delete_connection__err(e: crate::runtime::DispatchError) -> iface_identity::DeleteConnectionError {
    match e {
        crate::runtime::DispatchError::Http { status, body } => match status {
            404u16 => iface_identity::DeleteConnectionError::NotFound(body),
            _ => iface_identity::DeleteConnectionError::Other(body),
        },
        crate::runtime::DispatchError::Transport(m) => iface_identity::DeleteConnectionError::Other(m),
    }
}

impl iface_identity::Guest for crate::Component {
    fn get_connections(params: iface_identity::GetConnectionsParams) -> Result<Vec<iface_identity::Connection>, String> {
        let json = iface_identity__get_connections_params__to_json(&params);
        match dispatch(&OP_IDENTITY_GET_CONNECTIONS, json).and_then(iface_identity__get_connections__ok) {
            Ok(v) => Ok(v),
            Err(e) => Err(iface_identity__get_connections__err(e)),
        }
    }
    fn delete_connection(params: iface_identity::DeleteConnectionParams) -> Result<String, iface_identity::DeleteConnectionError> {
        let json = iface_identity__delete_connection_params__to_json(&params);
        match dispatch(&OP_IDENTITY_DELETE_CONNECTION, json).and_then(iface_identity__delete_connection__ok) {
            Ok(v) => Ok(v),
            Err(e) => Err(iface_identity__delete_connection__err(e)),
        }
    }
}
