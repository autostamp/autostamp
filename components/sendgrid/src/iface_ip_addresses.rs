// AUTOGENERATED by openapi-bindgen. Do not edit by hand.

use crate::runtime::{dispatch, AuthApply, AuthKind, FieldLocation, FieldSpec, OpSpec};
use crate::exports::autostamp::sendgrid::ip_addresses as iface_ip_addresses;
use serde_json::{Map, Value};

const OP_IP_ADDRESSES_GET_IPS: OpSpec = OpSpec {
    method: "GET",
    path_template: "/ips",
    fields: &[
        FieldSpec { snake: "ip", wire: "ip", location: FieldLocation::Query },
        FieldSpec { snake: "exclude_whitelabels", wire: "exclude_whitelabels", location: FieldLocation::Query },
        FieldSpec { snake: "limit", wire: "limit", location: FieldLocation::Query },
        FieldSpec { snake: "offset", wire: "offset", location: FieldLocation::Query },
        FieldSpec { snake: "subuser", wire: "subuser", location: FieldLocation::Query },
        FieldSpec { snake: "sort_by_direction", wire: "sort_by_direction", location: FieldLocation::Query },
    ],
    auth: &[
        AuthApply { secret_key: "Authorization", kind: AuthKind::ApiKeyHeader("Authorization") },
    ],
};

const OP_IP_ADDRESSES_POST_IPS: OpSpec = OpSpec {
    method: "POST",
    path_template: "/ips",
    fields: &[
        FieldSpec { snake: "count", wire: "count", location: FieldLocation::Body },
        FieldSpec { snake: "subusers", wire: "subusers", location: FieldLocation::Body },
        FieldSpec { snake: "warmup", wire: "warmup", location: FieldLocation::Body },
    ],
    auth: &[
        AuthApply { secret_key: "Authorization", kind: AuthKind::ApiKeyHeader("Authorization") },
    ],
};

const OP_IP_ADDRESSES_GET_IPS_ASSIGNED: OpSpec = OpSpec {
    method: "GET",
    path_template: "/ips/assigned",
    fields: &[
    ],
    auth: &[
        AuthApply { secret_key: "Authorization", kind: AuthKind::ApiKeyHeader("Authorization") },
    ],
};

const OP_IP_ADDRESSES_GET_IPS_REMAINING: OpSpec = OpSpec {
    method: "GET",
    path_template: "/ips/remaining",
    fields: &[
    ],
    auth: &[
        AuthApply { secret_key: "Authorization", kind: AuthKind::ApiKeyHeader("Authorization") },
    ],
};

const OP_IP_ADDRESSES_GET_IPS_IP_ADDRESS: OpSpec = OpSpec {
    method: "GET",
    path_template: "/ips/{ip_address}",
    fields: &[
        FieldSpec { snake: "ip_address", wire: "ip_address", location: FieldLocation::Path },
    ],
    auth: &[
        AuthApply { secret_key: "Authorization", kind: AuthKind::ApiKeyHeader("Authorization") },
    ],
};

fn iface_ip_addresses__get_ips_sort_by_direction_enum__to_str(e: &iface_ip_addresses::GetIpsSortByDirectionEnum) -> &'static str {
    match e {
        iface_ip_addresses::GetIpsSortByDirectionEnum::Desc => "desc",
        iface_ip_addresses::GetIpsSortByDirectionEnum::Asc => "asc",
    }
}

fn iface_ip_addresses__get_ips_response_item__to_json(p: &iface_ip_addresses::GetIpsResponseItem) -> Value {
    let mut m = Map::new();
    m.insert("assigned_at".into(), Value::Number(serde_json::Number::from(*(&p.assigned_at))));
    m.insert("ip".into(), Value::String((&p.ip).clone()));
    m.insert("pools".into(), Value::Array((&p.pools).iter().map(|v| Value::String((v).clone())).collect()));
    m.insert("rdns".into(), match (&p.rdns) { Some(v) => Value::String((v).clone()), None => Value::Null });
    m.insert("start_date".into(), serde_json::Number::from_f64(*(&p.start_date)).map(Value::Number).unwrap_or(Value::Null));
    m.insert("subusers".into(), Value::Array((&p.subusers).iter().map(|v| Value::String((v).clone())).collect()));
    m.insert("warmup".into(), Value::Bool(*(&p.warmup)));
    m.insert("whitelabeled".into(), Value::Bool(*(&p.whitelabeled)));
    Value::Object(m)
}

fn iface_ip_addresses__post_ips_response__to_json(p: &iface_ip_addresses::PostIpsResponse) -> Value {
    let mut m = Map::new();
    m.insert("ips".into(), Value::Array((&p.ips).iter().map(|v| iface_ip_addresses__post_ips_response_ips_item__to_json(v)).collect()));
    m.insert("remaining_ips".into(), Value::Number(serde_json::Number::from(*(&p.remaining_ips))));
    m.insert("warmup".into(), Value::Bool(*(&p.warmup)));
    Value::Object(m)
}

fn iface_ip_addresses__post_ips_response_ips_item__to_json(p: &iface_ip_addresses::PostIpsResponseIpsItem) -> Value {
    let mut m = Map::new();
    m.insert("ip".into(), Value::String((&p.ip).clone()));
    m.insert("subusers".into(), Value::Array((&p.subusers).iter().map(|v| Value::String((v).clone())).collect()));
    Value::Object(m)
}

fn iface_ip_addresses__get_ips_assigned_response_item__to_json(p: &iface_ip_addresses::GetIpsAssignedResponseItem) -> Value {
    let mut m = Map::new();
    m.insert("ip".into(), Value::String((&p.ip).clone()));
    m.insert("pools".into(), Value::Array((&p.pools).iter().map(|v| Value::String((v).clone())).collect()));
    m.insert("start_date".into(), Value::Number(serde_json::Number::from(*(&p.start_date))));
    m.insert("warmup".into(), Value::Bool(*(&p.warmup)));
    Value::Object(m)
}

fn iface_ip_addresses__get_ips_remaining_response__to_json(p: &iface_ip_addresses::GetIpsRemainingResponse) -> Value {
    let mut m = Map::new();
    m.insert("results".into(), Value::Array((&p.results).iter().map(|v| iface_ip_addresses__get_ips_remaining_response_results_item__to_json(v)).collect()));
    Value::Object(m)
}

fn iface_ip_addresses__get_ips_remaining_response_results_item__to_json(p: &iface_ip_addresses::GetIpsRemainingResponseResultsItem) -> Value {
    let mut m = Map::new();
    m.insert("period".into(), Value::String((&p.period).clone()));
    m.insert("price_per_ip".into(), serde_json::Number::from_f64(*(&p.price_per_ip)).map(Value::Number).unwrap_or(Value::Null));
    m.insert("remaining".into(), Value::Number(serde_json::Number::from(*(&p.remaining))));
    Value::Object(m)
}

fn iface_ip_addresses__get_ips_ip_address_response__to_json(p: &iface_ip_addresses::GetIpsIpAddressResponse) -> Value {
    let mut m = Map::new();
    m.insert("ip".into(), Value::String((&p.ip).clone()));
    m.insert("pools".into(), Value::Array((&p.pools).iter().map(|v| Value::String((v).clone())).collect()));
    m.insert("rdns".into(), Value::String((&p.rdns).clone()));
    m.insert("start_date".into(), Value::Number(serde_json::Number::from(*(&p.start_date))));
    m.insert("subusers".into(), Value::Array((&p.subusers).iter().map(|v| Value::String((v).clone())).collect()));
    m.insert("warmup".into(), Value::Bool(*(&p.warmup)));
    m.insert("whitelabeled".into(), Value::Bool(*(&p.whitelabeled)));
    Value::Object(m)
}

fn iface_ip_addresses__get_ips_params__to_json(p: &iface_ip_addresses::GetIpsParams) -> Value {
    let mut m = Map::new();
    m.insert("ip".into(), match (&p.ip) { Some(v) => Value::String((v).clone()), None => Value::Null });
    m.insert("exclude_whitelabels".into(), match (&p.exclude_whitelabels) { Some(v) => Value::Bool(*(v)), None => Value::Null });
    m.insert("limit".into(), match (&p.limit) { Some(v) => Value::Number(serde_json::Number::from(*(v))), None => Value::Null });
    m.insert("offset".into(), match (&p.offset) { Some(v) => Value::Number(serde_json::Number::from(*(v))), None => Value::Null });
    m.insert("subuser".into(), match (&p.subuser) { Some(v) => Value::String((v).clone()), None => Value::Null });
    m.insert("sort_by_direction".into(), match (&p.sort_by_direction) { Some(v) => Value::String(iface_ip_addresses__get_ips_sort_by_direction_enum__to_str(v).into()), None => Value::Null });
    Value::Object(m)
}

fn iface_ip_addresses__post_ips_params__to_json(p: &iface_ip_addresses::PostIpsParams) -> Value {
    let mut m = Map::new();
    m.insert("count".into(), Value::Number(serde_json::Number::from(*(&p.count))));
    m.insert("subusers".into(), match (&p.subusers) { Some(v) => Value::Array((v).iter().map(|v| Value::String((v).clone())).collect()), None => Value::Null });
    m.insert("warmup".into(), match (&p.warmup) { Some(v) => Value::Bool(*(v)), None => Value::Null });
    Value::Object(m)
}

fn iface_ip_addresses__get_ips_ip_address_params__to_json(p: &iface_ip_addresses::GetIpsIpAddressParams) -> Value {
    let mut m = Map::new();
    m.insert("ip_address".into(), Value::String((&p.ip_address).clone()));
    Value::Object(m)
}

fn iface_ip_addresses__get_ips_response_item__from_json(v: &Value) -> Option<iface_ip_addresses::GetIpsResponseItem> {
    let m = v.as_object()?;
    Some(iface_ip_addresses::GetIpsResponseItem {
        assigned_at: m.get("assigned_at").and_then(|v| (v).as_i64().map(|n| n as i32)).unwrap_or_default(),
        ip: m.get("ip").and_then(|v| (v).as_str().map(|s| s.to_string())).unwrap_or_default(),
        pools: m.get("pools").and_then(|v| (v).as_array().map(|a| a.iter().filter_map(|x| (x).as_str().map(|s| s.to_string())).collect())).unwrap_or_default(),
        rdns: m.get("rdns").filter(|v| !v.is_null()).and_then(|v| (v).as_str().map(|s| s.to_string())),
        start_date: m.get("start_date").and_then(|v| (v).as_f64()).unwrap_or_default(),
        subusers: m.get("subusers").and_then(|v| (v).as_array().map(|a| a.iter().filter_map(|x| (x).as_str().map(|s| s.to_string())).collect())).unwrap_or_default(),
        warmup: m.get("warmup").and_then(|v| (v).as_bool()).unwrap_or_default(),
        whitelabeled: m.get("whitelabeled").and_then(|v| (v).as_bool()).unwrap_or_default(),
    })
}

fn iface_ip_addresses__post_ips_response__from_json(v: &Value) -> Option<iface_ip_addresses::PostIpsResponse> {
    let m = v.as_object()?;
    Some(iface_ip_addresses::PostIpsResponse {
        ips: m.get("ips").and_then(|v| (v).as_array().map(|a| a.iter().filter_map(|x| iface_ip_addresses__post_ips_response_ips_item__from_json(x)).collect())).unwrap_or_default(),
        remaining_ips: m.get("remaining_ips").and_then(|v| (v).as_i64().map(|n| n as i32)).unwrap_or_default(),
        warmup: m.get("warmup").and_then(|v| (v).as_bool()).unwrap_or_default(),
    })
}

fn iface_ip_addresses__post_ips_response_ips_item__from_json(v: &Value) -> Option<iface_ip_addresses::PostIpsResponseIpsItem> {
    let m = v.as_object()?;
    Some(iface_ip_addresses::PostIpsResponseIpsItem {
        ip: m.get("ip").and_then(|v| (v).as_str().map(|s| s.to_string())).unwrap_or_default(),
        subusers: m.get("subusers").and_then(|v| (v).as_array().map(|a| a.iter().filter_map(|x| (x).as_str().map(|s| s.to_string())).collect())).unwrap_or_default(),
    })
}

fn iface_ip_addresses__get_ips_assigned_response_item__from_json(v: &Value) -> Option<iface_ip_addresses::GetIpsAssignedResponseItem> {
    let m = v.as_object()?;
    Some(iface_ip_addresses::GetIpsAssignedResponseItem {
        ip: m.get("ip").and_then(|v| (v).as_str().map(|s| s.to_string())).unwrap_or_default(),
        pools: m.get("pools").and_then(|v| (v).as_array().map(|a| a.iter().filter_map(|x| (x).as_str().map(|s| s.to_string())).collect())).unwrap_or_default(),
        start_date: m.get("start_date").and_then(|v| (v).as_i64().map(|n| n as i32)).unwrap_or_default(),
        warmup: m.get("warmup").and_then(|v| (v).as_bool()).unwrap_or_default(),
    })
}

fn iface_ip_addresses__get_ips_remaining_response__from_json(v: &Value) -> Option<iface_ip_addresses::GetIpsRemainingResponse> {
    let m = v.as_object()?;
    Some(iface_ip_addresses::GetIpsRemainingResponse {
        results: m.get("results").and_then(|v| (v).as_array().map(|a| a.iter().filter_map(|x| iface_ip_addresses__get_ips_remaining_response_results_item__from_json(x)).collect())).unwrap_or_default(),
    })
}

fn iface_ip_addresses__get_ips_remaining_response_results_item__from_json(v: &Value) -> Option<iface_ip_addresses::GetIpsRemainingResponseResultsItem> {
    let m = v.as_object()?;
    Some(iface_ip_addresses::GetIpsRemainingResponseResultsItem {
        period: m.get("period").and_then(|v| (v).as_str().map(|s| s.to_string())).unwrap_or_default(),
        price_per_ip: m.get("price_per_ip").and_then(|v| (v).as_f64()).unwrap_or_default(),
        remaining: m.get("remaining").and_then(|v| (v).as_i64().map(|n| n as i32)).unwrap_or_default(),
    })
}

fn iface_ip_addresses__get_ips_ip_address_response__from_json(v: &Value) -> Option<iface_ip_addresses::GetIpsIpAddressResponse> {
    let m = v.as_object()?;
    Some(iface_ip_addresses::GetIpsIpAddressResponse {
        ip: m.get("ip").and_then(|v| (v).as_str().map(|s| s.to_string())).unwrap_or_default(),
        pools: m.get("pools").and_then(|v| (v).as_array().map(|a| a.iter().filter_map(|x| (x).as_str().map(|s| s.to_string())).collect())).unwrap_or_default(),
        rdns: m.get("rdns").and_then(|v| (v).as_str().map(|s| s.to_string())).unwrap_or_default(),
        start_date: m.get("start_date").and_then(|v| (v).as_i64().map(|n| n as i32)).unwrap_or_default(),
        subusers: m.get("subusers").and_then(|v| (v).as_array().map(|a| a.iter().filter_map(|x| (x).as_str().map(|s| s.to_string())).collect())).unwrap_or_default(),
        warmup: m.get("warmup").and_then(|v| (v).as_bool()).unwrap_or_default(),
        whitelabeled: m.get("whitelabeled").and_then(|v| (v).as_bool()).unwrap_or_default(),
    })
}

fn iface_ip_addresses__get_ips__ok(body: String) -> Result<Vec<iface_ip_addresses::GetIpsResponseItem>, crate::runtime::DispatchError> {
    let v: Value = match serde_json::from_str(&body) {
        Ok(v) => v,
        Err(e) => return Err(crate::runtime::DispatchError::Transport(format!("failed to decode response body as JSON: {e}"))),
    };
    match (&v).as_array().map(|a| a.iter().filter_map(|x| iface_ip_addresses__get_ips_response_item__from_json(x)).collect()) {
        Some(x) => Ok(x),
        None => Err(crate::runtime::DispatchError::Transport("response body did not match the expected schema".to_string())),
    }
}

fn iface_ip_addresses__get_ips__err(e: crate::runtime::DispatchError) -> String {
    match e {
        crate::runtime::DispatchError::Http { status, body } => format!("HTTP {status}: {body}"),
        crate::runtime::DispatchError::Transport(m) => m,
    }
}

fn iface_ip_addresses__post_ips__ok(body: String) -> Result<iface_ip_addresses::PostIpsResponse, crate::runtime::DispatchError> {
    let v: Value = match serde_json::from_str(&body) {
        Ok(v) => v,
        Err(e) => return Err(crate::runtime::DispatchError::Transport(format!("failed to decode response body as JSON: {e}"))),
    };
    match iface_ip_addresses__post_ips_response__from_json(&v) {
        Some(x) => Ok(x),
        None => Err(crate::runtime::DispatchError::Transport("response body did not match the expected schema".to_string())),
    }
}

fn iface_ip_addresses__post_ips__err(e: crate::runtime::DispatchError) -> iface_ip_addresses::PostIpsError {
    match e {
        crate::runtime::DispatchError::Http { status, body } => match status {
            400u16 => iface_ip_addresses::PostIpsError::BadRequest(body),
            _ => iface_ip_addresses::PostIpsError::Other(body),
        },
        crate::runtime::DispatchError::Transport(m) => iface_ip_addresses::PostIpsError::Other(m),
    }
}

fn iface_ip_addresses__get_ips_assigned__ok(body: String) -> Result<Vec<iface_ip_addresses::GetIpsAssignedResponseItem>, crate::runtime::DispatchError> {
    let v: Value = match serde_json::from_str(&body) {
        Ok(v) => v,
        Err(e) => return Err(crate::runtime::DispatchError::Transport(format!("failed to decode response body as JSON: {e}"))),
    };
    match (&v).as_array().map(|a| a.iter().filter_map(|x| iface_ip_addresses__get_ips_assigned_response_item__from_json(x)).collect()) {
        Some(x) => Ok(x),
        None => Err(crate::runtime::DispatchError::Transport("response body did not match the expected schema".to_string())),
    }
}

fn iface_ip_addresses__get_ips_assigned__err(e: crate::runtime::DispatchError) -> String {
    match e {
        crate::runtime::DispatchError::Http { status, body } => format!("HTTP {status}: {body}"),
        crate::runtime::DispatchError::Transport(m) => m,
    }
}

fn iface_ip_addresses__get_ips_remaining__ok(body: String) -> Result<iface_ip_addresses::GetIpsRemainingResponse, crate::runtime::DispatchError> {
    let v: Value = match serde_json::from_str(&body) {
        Ok(v) => v,
        Err(e) => return Err(crate::runtime::DispatchError::Transport(format!("failed to decode response body as JSON: {e}"))),
    };
    match iface_ip_addresses__get_ips_remaining_response__from_json(&v) {
        Some(x) => Ok(x),
        None => Err(crate::runtime::DispatchError::Transport("response body did not match the expected schema".to_string())),
    }
}

fn iface_ip_addresses__get_ips_remaining__err(e: crate::runtime::DispatchError) -> String {
    match e {
        crate::runtime::DispatchError::Http { status, body } => format!("HTTP {status}: {body}"),
        crate::runtime::DispatchError::Transport(m) => m,
    }
}

fn iface_ip_addresses__get_ips_ip_address__ok(body: String) -> Result<iface_ip_addresses::GetIpsIpAddressResponse, crate::runtime::DispatchError> {
    let v: Value = match serde_json::from_str(&body) {
        Ok(v) => v,
        Err(e) => return Err(crate::runtime::DispatchError::Transport(format!("failed to decode response body as JSON: {e}"))),
    };
    match iface_ip_addresses__get_ips_ip_address_response__from_json(&v) {
        Some(x) => Ok(x),
        None => Err(crate::runtime::DispatchError::Transport("response body did not match the expected schema".to_string())),
    }
}

fn iface_ip_addresses__get_ips_ip_address__err(e: crate::runtime::DispatchError) -> String {
    match e {
        crate::runtime::DispatchError::Http { status, body } => format!("HTTP {status}: {body}"),
        crate::runtime::DispatchError::Transport(m) => m,
    }
}

impl iface_ip_addresses::Guest for crate::Component {
    fn get_ips(params: iface_ip_addresses::GetIpsParams) -> Result<Vec<iface_ip_addresses::GetIpsResponseItem>, String> {
        let json = iface_ip_addresses__get_ips_params__to_json(&params);
        match dispatch(&OP_IP_ADDRESSES_GET_IPS, json).and_then(iface_ip_addresses__get_ips__ok) {
            Ok(v) => Ok(v),
            Err(e) => Err(iface_ip_addresses__get_ips__err(e)),
        }
    }
    fn post_ips(params: iface_ip_addresses::PostIpsParams) -> Result<iface_ip_addresses::PostIpsResponse, iface_ip_addresses::PostIpsError> {
        let json = iface_ip_addresses__post_ips_params__to_json(&params);
        match dispatch(&OP_IP_ADDRESSES_POST_IPS, json).and_then(iface_ip_addresses__post_ips__ok) {
            Ok(v) => Ok(v),
            Err(e) => Err(iface_ip_addresses__post_ips__err(e)),
        }
    }
    fn get_ips_assigned() -> Result<Vec<iface_ip_addresses::GetIpsAssignedResponseItem>, String> {
        match dispatch(&OP_IP_ADDRESSES_GET_IPS_ASSIGNED, Value::Object(Map::new())).and_then(iface_ip_addresses__get_ips_assigned__ok) {
            Ok(v) => Ok(v),
            Err(e) => Err(iface_ip_addresses__get_ips_assigned__err(e)),
        }
    }
    fn get_ips_remaining() -> Result<iface_ip_addresses::GetIpsRemainingResponse, String> {
        match dispatch(&OP_IP_ADDRESSES_GET_IPS_REMAINING, Value::Object(Map::new())).and_then(iface_ip_addresses__get_ips_remaining__ok) {
            Ok(v) => Ok(v),
            Err(e) => Err(iface_ip_addresses__get_ips_remaining__err(e)),
        }
    }
    fn get_ips_ip_address(params: iface_ip_addresses::GetIpsIpAddressParams) -> Result<iface_ip_addresses::GetIpsIpAddressResponse, String> {
        let json = iface_ip_addresses__get_ips_ip_address_params__to_json(&params);
        match dispatch(&OP_IP_ADDRESSES_GET_IPS_IP_ADDRESS, json).and_then(iface_ip_addresses__get_ips_ip_address__ok) {
            Ok(v) => Ok(v),
            Err(e) => Err(iface_ip_addresses__get_ips_ip_address__err(e)),
        }
    }
}
