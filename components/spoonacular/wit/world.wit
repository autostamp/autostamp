/// ## Authentication
///
/// Reads credentials from the host via `wasmcloud:secrets`; operations never take a token.
/// Provision these named secrets:
///
/// | Secret name | Applied to each request as |
/// | --- | --- |
/// | `apiKeyScheme` | header `x-api-key` |
package autostamp:spoonacular@0.5.0;

interface misc {
  enum detect-food-in-text-content-type-enum {
    application-x-www-form-urlencoded,
    application-json,
    multipart-form-data,
  }

  record talk-to-chatbot-response {
    answer-text: string,
    media: list<string>
  }

  record get-conversation-suggests-response {
    suggests: get-conversation-suggests-response-suggests,
    words: list<string>
  }

  record get-conversation-suggests-response-suggests {
    x: list<get-conversation-suggests-response-suggests-x-item>
  }

  record get-conversation-suggests-response-suggests-x-item {
    name: string
  }

  record search-custom-foods-response {
    custom-foods: list<search-custom-foods-response-custom-foods-item>,
    number: s32,
    offset: s32,
    type-op: string
  }

  record search-custom-foods-response-custom-foods-item {
    id: s32,
    image-url: string,
    price: f64,
    servings: f64,
    title: string
  }

  record detect-food-in-text-response {
    annotations: list<detect-food-in-text-response-annotations-item>
  }

  record detect-food-in-text-response-annotations-item {
    annotation: string,
    image: string,
    tag: string
  }

  record image-analysis-by-url-response {
    category: image-analysis-by-url-response-category,
    nutrition: image-analysis-by-url-response-nutrition,
    recipes: list<image-analysis-by-url-response-recipes-item>
  }

  record image-analysis-by-url-response-category {
    name: string,
    probability: f64
  }

  record image-analysis-by-url-response-nutrition {
    calories: image-analysis-by-url-response-nutrition-calories,
    carbs: image-analysis-by-url-response-nutrition-carbs,
    fat: image-analysis-by-url-response-nutrition-fat,
    protein: image-analysis-by-url-response-nutrition-protein,
    recipes-used: s32
  }

  record image-analysis-by-url-response-nutrition-calories {
    confidence-range95-percent: image-analysis-by-url-response-nutrition-calories-confidence-range95-percent,
    standard-deviation: f64,
    unit: string,
    value: f64
  }

  record image-analysis-by-url-response-nutrition-calories-confidence-range95-percent {
    max: f64,
    min: f64
  }

  record image-analysis-by-url-response-nutrition-carbs {
    confidence-range95-percent: image-analysis-by-url-response-nutrition-carbs-confidence-range95-percent,
    standard-deviation: f64,
    unit: string,
    value: f64
  }

  record image-analysis-by-url-response-nutrition-carbs-confidence-range95-percent {
    max: f64,
    min: f64
  }

  record image-analysis-by-url-response-nutrition-fat {
    confidence-range95-percent: image-analysis-by-url-response-nutrition-fat-confidence-range95-percent,
    standard-deviation: f64,
    unit: string,
    value: f64
  }

  record image-analysis-by-url-response-nutrition-fat-confidence-range95-percent {
    max: f64,
    min: f64
  }

  record image-analysis-by-url-response-nutrition-protein {
    confidence-range95-percent: image-analysis-by-url-response-nutrition-protein-confidence-range95-percent,
    standard-deviation: f64,
    unit: string,
    value: f64
  }

  record image-analysis-by-url-response-nutrition-protein-confidence-range95-percent {
    max: f64,
    min: f64
  }

  record image-analysis-by-url-response-recipes-item {
    id: s32,
    image-type: string,
    title: string,
    url: string
  }

  record image-classification-by-url-response {
    category: string,
    probability: f64
  }

  record get-a-random-food-joke-response {
    text: string
  }

  record search-all-food-response {
    limit: s32,
    offset: s32,
    query: string,
    search-results: list<search-all-food-response-search-results-item>,
    total-results: s32
  }

  record search-all-food-response-search-results-item {
    name: string,
    results: option<list<search-all-food-response-search-results-item-results-item>>,
    total-results: s32
  }

  record search-all-food-response-search-results-item-results-item {
    content: string,
    id: string,
    image: string,
    link: string,
    name: string,
    relevance: f64,
    type-op: string
  }

  record search-site-content-response {
    articles: list<search-site-content-response-articles-item>,
    grocery-products: list<search-site-content-response-grocery-products-item>,
    menu-items: list<search-site-content-response-menu-items-item>,
    recipes: list<search-site-content-response-recipes-item>
  }

  record search-site-content-response-articles-item {
    data-points: option<list<string>>,
    image: string,
    link: string,
    name: string
  }

  record search-site-content-response-grocery-products-item {
    data-points: option<list<search-site-content-response-grocery-products-item-data-points-item>>,
    image: string,
    link: string,
    name: string
  }

  record search-site-content-response-grocery-products-item-data-points-item {
    key: string,
    value: string
  }

  record search-site-content-response-menu-items-item {
    data-points: option<list<search-site-content-response-menu-items-item-data-points-item>>,
    image: string,
    link: string,
    name: string
  }

  record search-site-content-response-menu-items-item-data-points-item {
    key: string,
    value: string
  }

  record search-site-content-response-recipes-item {
    data-points: option<list<search-site-content-response-recipes-item-data-points-item>>,
    image: string,
    link: string,
    name: string
  }

  record search-site-content-response-recipes-item-data-points-item {
    key: string,
    value: string
  }

  record get-random-food-trivia-response {
    text: string
  }

  record search-food-videos-response {
    total-results: s32,
    videos: list<search-food-videos-response-videos-item>
  }

  record search-food-videos-response-videos-item {
    length: s32,
    rating: f64,
    short-title: string,
    thumbnail: string,
    title: string,
    views: s32,
    you-tube-id: string
  }

  variant talk-to-chatbot-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-conversation-suggests-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant search-custom-foods-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant detect-food-in-text-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant image-analysis-by-url-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant image-classification-by-url-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-a-random-food-joke-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant search-all-food-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant search-site-content-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-random-food-trivia-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant search-food-videos-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  /// Talk to Chatbot
  record talk-to-chatbot-params {
    /// The request / question / answer from the user to the chatbot.
    text: string,
    /// An arbitrary globally unique id for your conversation. The conversation can contain states so you should pass your context id if you want the bot to be able to remember the conversation.
    context-id: option<string>
  }
  talk-to-chatbot: func(params: talk-to-chatbot-params) -> result<talk-to-chatbot-response, talk-to-chatbot-error>;

  /// Conversation Suggests
  record get-conversation-suggests-params {
    /// A (partial) query from the user. The endpoint will return if it matches topics it can talk about.
    query: string,
    /// The number of suggestions to return (between 1 and 25).
    number: option<f64>
  }
  get-conversation-suggests: func(params: get-conversation-suggests-params) -> result<get-conversation-suggests-response, get-conversation-suggests-error>;

  /// Search Custom Foods
  record search-custom-foods-params {
    /// The (natural language) search query.
    query: option<string>,
    /// The username.
    username: string,
    /// The private hash for the username.
    hash: string,
    /// The number of results to skip (between 0 and 900).
    offset: option<s32>,
    /// The maximum number of items to return (between 1 and 100). Defaults to 10.
    number: option<s32>
  }
  search-custom-foods: func(params: search-custom-foods-params) -> result<search-custom-foods-response, search-custom-foods-error>;

  /// Detect Food in Text
  record detect-food-in-text-params {
    /// The content type.
    content-type: option<detect-food-in-text-content-type-enum>,
    body: string
  }
  detect-food-in-text: func(params: detect-food-in-text-params) -> result<detect-food-in-text-response, detect-food-in-text-error>;

  /// Image Analysis by URL
  record image-analysis-by-url-params {
    /// The URL of the image to be analyzed.
    image-url: string
  }
  image-analysis-by-url: func(params: image-analysis-by-url-params) -> result<image-analysis-by-url-response, image-analysis-by-url-error>;

  /// Image Classification by URL
  record image-classification-by-url-params {
    /// The URL of the image to be classified.
    image-url: string
  }
  image-classification-by-url: func(params: image-classification-by-url-params) -> result<image-classification-by-url-response, image-classification-by-url-error>;

  /// Random Food Joke
  get-a-random-food-joke: func() -> result<get-a-random-food-joke-response, get-a-random-food-joke-error>;

  /// Search All Food
  record search-all-food-params {
    /// The search query.
    query: string,
    /// The number of results to skip (between 0 and 900).
    offset: option<s32>,
    /// The maximum number of items to return (between 1 and 100). Defaults to 10.
    number: option<s32>
  }
  search-all-food: func(params: search-all-food-params) -> result<search-all-food-response, search-all-food-error>;

  /// Search Site Content
  record search-site-content-params {
    /// The query to search for. You can also use partial queries such as "spagh" to already find spaghetti recipes, articles, grocery products, and other content.
    query: string
  }
  search-site-content: func(params: search-site-content-params) -> result<search-site-content-response, search-site-content-error>;

  /// Random Food Trivia
  get-random-food-trivia: func() -> result<get-random-food-trivia-response, get-random-food-trivia-error>;

  /// Search Food Videos
  record search-food-videos-params {
    /// The (natural language) search query.
    query: option<string>,
    /// The type of the recipes. See a full list of supported meal types.
    type-op: option<string>,
    /// The cuisine(s) of the recipes. One or more, comma separated. See a full list of supported cuisines.
    cuisine: option<string>,
    /// The diet for which the recipes must be suitable. See a full list of supported diets.
    diet: option<string>,
    /// A comma-separated list of ingredients that the recipes should contain.
    include-ingredients: option<string>,
    /// A comma-separated list of ingredients or ingredient types that the recipes must not contain.
    exclude-ingredients: option<string>,
    /// Minimum video length in seconds.
    min-length: option<f64>,
    /// Maximum video length in seconds.
    max-length: option<f64>,
    /// The number of results to skip (between 0 and 900).
    offset: option<s32>,
    /// The maximum number of items to return (between 1 and 100). Defaults to 10.
    number: option<s32>
  }
  search-food-videos: func(params: search-food-videos-params) -> result<search-food-videos-response, search-food-videos-error>;

}


interface ingredients {
  enum autocomplete-ingredient-search-language-enum {
    en,
    de,
  }

  enum visualize-ingredients-content-type-enum {
    application-x-www-form-urlencoded,
    application-json,
    multipart-form-data,
  }

  enum visualize-ingredients-accept-enum {
    application-json,
    text-html,
    media,
  }

  enum by-id-image-measure-enum {
    us,
    metric,
  }

  record autocomplete-ingredient-search-response-item {
    aisle: option<string>,
    id: option<s32>,
    image: string,
    name: string,
    possible-units: option<list<string>>
  }

  record map-ingredients-to-grocery-products-response-item {
    ingredient-image: string,
    meta: list<string>,
    original: string,
    original-name: string,
    products: list<map-ingredients-to-grocery-products-response-item-products-item>
  }

  record map-ingredients-to-grocery-products-response-item-products-item {
    id: s32,
    title: string,
    upc: string
  }

  record ingredient-search-response {
    number: s32,
    offset: s32,
    results: list<ingredient-search-response-results-item>,
    total-results: s32
  }

  record ingredient-search-response-results-item {
    id: s32,
    image: string,
    name: string
  }

  record get-ingredient-substitutes-response {
    ingredient: string,
    message: string,
    substitutes: list<string>
  }

  record compute-ingredient-amount-response {
    amount: f64,
    unit: string
  }

  record get-ingredient-information-response {
    aisle: string,
    amount: f64,
    category-path: list<string>,
    consistency: string,
    estimated-cost: get-ingredient-information-response-estimated-cost,
    id: s32,
    image: string,
    meta: list<get-ingredient-information-response-meta-item>,
    name: string,
    name-clean: string,
    nutrition: get-ingredient-information-response-nutrition,
    original: string,
    original-name: string,
    possible-units: list<string>,
    shopping-list-units: list<string>,
    unit: string,
    unit-long: string,
    unit-short: string
  }

  record get-ingredient-information-response-estimated-cost {
    unit: string,
    value: f64
  }

  record get-ingredient-information-response-meta-item {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record get-ingredient-information-response-nutrition {
    caloric-breakdown: get-ingredient-information-response-nutrition-caloric-breakdown,
    nutrients: list<get-ingredient-information-response-nutrition-nutrients-item>,
    properties: list<get-ingredient-information-response-nutrition-properties-item>,
    weight-per-serving: get-ingredient-information-response-nutrition-weight-per-serving
  }

  record get-ingredient-information-response-nutrition-caloric-breakdown {
    percent-carbs: f64,
    percent-fat: f64,
    percent-protein: f64
  }

  record get-ingredient-information-response-nutrition-nutrients-item {
    amount: f64,
    name: string,
    percent-of-daily-needs: f64,
    unit: string
  }

  record get-ingredient-information-response-nutrition-properties-item {
    amount: f64,
    name: string,
    unit: string
  }

  record get-ingredient-information-response-nutrition-weight-per-serving {
    amount: f64,
    unit: string
  }

  record get-ingredient-substitutes-by-id-response {
    ingredient: string,
    message: string,
    substitutes: list<string>
  }

  record by-id-image-response {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  variant autocomplete-ingredient-search-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant map-ingredients-to-grocery-products-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant ingredient-search-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-ingredient-substitutes-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant compute-ingredient-amount-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-ingredient-information-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-ingredient-substitutes-by-id-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant visualize-ingredients-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant by-id-image-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  /// Autocomplete Ingredient Search
  record autocomplete-ingredient-search-params {
    /// The (natural language) search query.
    query: option<string>,
    /// The maximum number of items to return (between 1 and 100). Defaults to 10.
    number: option<s32>,
    /// Whether to return more meta information about the ingredients.
    meta-information: option<bool>,
    /// A comma-separated list of intolerances. All recipes returned must not contain ingredients that are not suitable for people with the intolerances entered. See a full list of supported intolerances.
    intolerances: option<string>,
    /// The language of the input. Either 'en' or 'de'.
    language: option<autocomplete-ingredient-search-language-enum>
  }
  autocomplete-ingredient-search: func(params: autocomplete-ingredient-search-params) -> result<list<autocomplete-ingredient-search-response-item>, autocomplete-ingredient-search-error>;

  /// Map Ingredients to Grocery Products
  record map-ingredients-to-grocery-products-params {
    ingredients: list<string>,
    servings: f64
  }
  map-ingredients-to-grocery-products: func(params: map-ingredients-to-grocery-products-params) -> result<list<map-ingredients-to-grocery-products-response-item>, map-ingredients-to-grocery-products-error>;

  /// Ingredient Search
  record ingredient-search-params {
    /// The (natural language) search query.
    query: option<string>,
    /// Whether to add children of found foods.
    add-children: option<bool>,
    /// The minimum percentage of protein the food must have (between 0 and 100).
    min-protein-percent: option<f64>,
    /// The maximum percentage of protein the food can have (between 0 and 100).
    max-protein-percent: option<f64>,
    /// The minimum percentage of fat the food must have (between 0 and 100).
    min-fat-percent: option<f64>,
    /// The maximum percentage of fat the food can have (between 0 and 100).
    max-fat-percent: option<f64>,
    /// The minimum percentage of carbs the food must have (between 0 and 100).
    min-carbs-percent: option<f64>,
    /// The maximum percentage of carbs the food can have (between 0 and 100).
    max-carbs-percent: option<f64>,
    /// Whether to return more meta information about the ingredients.
    meta-information: option<bool>,
    /// A comma-separated list of intolerances. All recipes returned must not contain ingredients that are not suitable for people with the intolerances entered. See a full list of supported intolerances.
    intolerances: option<string>,
    /// The strategy to sort recipes by. See a full list of supported sorting options.
    sort: option<string>,
    /// The direction in which to sort. Must be either 'asc' (ascending) or 'desc' (descending).
    sort-direction: option<string>,
    /// The number of results to skip (between 0 and 900).
    offset: option<s32>,
    /// The maximum number of items to return (between 1 and 100). Defaults to 10.
    number: option<s32>,
    /// The language of the input. Either 'en' or 'de'.
    language: option<autocomplete-ingredient-search-language-enum>
  }
  ingredient-search: func(params: ingredient-search-params) -> result<ingredient-search-response, ingredient-search-error>;

  /// Get Ingredient Substitutes
  record get-ingredient-substitutes-params {
    /// The name of the ingredient you want to replace.
    ingredient-name: string
  }
  get-ingredient-substitutes: func(params: get-ingredient-substitutes-params) -> result<get-ingredient-substitutes-response, get-ingredient-substitutes-error>;

  /// Compute Ingredient Amount
  record compute-ingredient-amount-params {
    /// The id of the ingredient you want the amount for.
    id: string,
    /// The target nutrient. See a list of supported nutrients.
    nutrient: string,
    /// The target number of the given nutrient.
    target: f64,
    /// The target unit.
    unit: option<string>
  }
  compute-ingredient-amount: func(params: compute-ingredient-amount-params) -> result<compute-ingredient-amount-response, compute-ingredient-amount-error>;

  /// Get Ingredient Information
  record get-ingredient-information-params {
    /// The ingredient id.
    id: string,
    /// The amount of this ingredient.
    amount: option<f64>,
    /// The unit for the given amount.
    unit: option<string>
  }
  get-ingredient-information: func(params: get-ingredient-information-params) -> result<get-ingredient-information-response, get-ingredient-information-error>;

  /// Get Ingredient Substitutes by ID
  record get-ingredient-substitutes-by-id-params {
    /// The id of the ingredient you want substitutes for.
    id: string
  }
  get-ingredient-substitutes-by-id: func(params: get-ingredient-substitutes-by-id-params) -> result<get-ingredient-substitutes-by-id-response, get-ingredient-substitutes-by-id-error>;

  /// Ingredients Widget
  record visualize-ingredients-params {
    /// The content type.
    content-type: option<visualize-ingredients-content-type-enum>,
    /// The language of the input. Either 'en' or 'de'.
    language: option<autocomplete-ingredient-search-language-enum>,
    /// Accept header.
    accept: option<visualize-ingredients-accept-enum>,
    body: string
  }
  visualize-ingredients: func(params: visualize-ingredients-params) -> result<string, visualize-ingredients-error>;

  /// Ingredients by ID Image
  record by-id-image-params {
    /// The recipe id.
    id: string,
    /// Whether the the measures should be 'us' or 'metric'.
    measure: option<by-id-image-measure-enum>
  }
  by-id-image: func(params: by-id-image-params) -> result<by-id-image-response, by-id-image-error>;

}

interface recipes {
  enum compute-glycemic-load-language-enum {
    en,
    de,
  }

  enum analyze-recipe-instructions-content-type-enum {
    application-x-www-form-urlencoded,
    application-json,
    multipart-form-data,
  }

  enum visualize-equipment-accept-enum {
    application-json,
    text-html,
    media,
  }

  enum visualize-recipe-ingredients-by-id-measure-enum {
    us,
    metric,
  }

  record compute-glycemic-load-response {
    ingredients: list<compute-glycemic-load-response-ingredients-item>,
    total-glycemic-load: f64
  }

  record compute-glycemic-load-response-ingredients-item {
    glycemic-index: f64,
    glycemic-load: f64,
    id: s32,
    original: string
  }

  record analyze-recipe-response {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record analyze-recipe-instructions-response {
    equipment: list<analyze-recipe-instructions-response-equipment-item>,
    ingredients: list<analyze-recipe-instructions-response-ingredients-item>,
    parsed-instructions: list<analyze-recipe-instructions-response-parsed-instructions-item>
  }

  record analyze-recipe-instructions-response-equipment-item {
    id: f64,
    name: string
  }

  record analyze-recipe-instructions-response-ingredients-item {
    id: f64,
    name: string
  }

  record analyze-recipe-instructions-response-parsed-instructions-item {
    name: string,
    steps: option<list<analyze-recipe-instructions-response-parsed-instructions-item-steps-item>>
  }

  record analyze-recipe-instructions-response-parsed-instructions-item-steps-item {
    equipment: option<list<analyze-recipe-instructions-response-parsed-instructions-item-steps-item-equipment-item>>,
    ingredients: option<list<analyze-recipe-instructions-response-parsed-instructions-item-steps-item-ingredients-item>>,
    number: f64,
    step: string
  }

  record analyze-recipe-instructions-response-parsed-instructions-item-steps-item-equipment-item {
    id: f64,
    image: string,
    localized-name: string,
    name: string
  }

  record analyze-recipe-instructions-response-parsed-instructions-item-steps-item-ingredients-item {
    id: f64,
    image: string,
    localized-name: string,
    name: string
  }

  record autocomplete-recipe-search-response-item {
    id: s32,
    image-type: string,
    title: string
  }

  record search-recipes-response {
    number: s32,
    offset: s32,
    results: list<search-recipes-response-results-item>,
    total-results: s32
  }

  record search-recipes-response-results-item {
    calories: f64,
    carbs: string,
    fat: string,
    id: s32,
    image: string,
    image-type: string,
    protein: string,
    title: string
  }

  record convert-amounts-response {
    answer: string,
    source-amount: f64,
    source-unit: string,
    target-amount: f64,
    target-unit: string
  }

  record classify-cuisine-response {
    confidence: f64,
    cuisine: string,
    cuisines: list<string>
  }

  record extract-recipe-from-website-response {
    aggregate-likes: s32,
    analyzed-instructions: list<extract-recipe-from-website-response-analyzed-instructions-item>,
    cheap: bool,
    credits-text: string,
    cuisines: list<string>,
    dairy-free: bool,
    diets: list<string>,
    dish-types: list<string>,
    extended-ingredients: list<extract-recipe-from-website-response-extended-ingredients-item>,
    gaps: string,
    gluten-free: bool,
    health-score: f64,
    id: s32,
    image: string,
    image-type: string,
    instructions: string,
    ketogenic: bool,
    license: string,
    low-fodmap: bool,
    occasions: list<string>,
    price-per-serving: f64,
    ready-in-minutes: s32,
    servings: f64,
    source-name: string,
    source-url: string,
    spoonacular-score: f64,
    spoonacular-source-url: string,
    summary: string,
    sustainable: bool,
    title: string,
    vegan: bool,
    vegetarian: bool,
    very-healthy: bool,
    very-popular: bool,
    weight-watcher-smart-points: f64,
    whole30: bool,
    wine-pairing: extract-recipe-from-website-response-wine-pairing
  }

  record extract-recipe-from-website-response-analyzed-instructions-item {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record extract-recipe-from-website-response-extended-ingredients-item {
    aisle: string,
    amount: f64,
    consitency: string,
    id: s32,
    image: string,
    measures: option<extract-recipe-from-website-response-extended-ingredients-item-measures>,
    meta: option<list<string>>,
    name: string,
    original: string,
    original-name: string,
    unit: string
  }

  record extract-recipe-from-website-response-extended-ingredients-item-measures {
    metric: extract-recipe-from-website-response-extended-ingredients-item-measures-metric,
    us: extract-recipe-from-website-response-extended-ingredients-item-measures-us
  }

  record extract-recipe-from-website-response-extended-ingredients-item-measures-metric {
    amount: f64,
    unit-long: string,
    unit-short: string
  }

  record extract-recipe-from-website-response-extended-ingredients-item-measures-us {
    amount: f64,
    unit-long: string,
    unit-short: string
  }

  record extract-recipe-from-website-response-wine-pairing {
    paired-wines: list<string>,
    pairing-text: string,
    product-matches: list<extract-recipe-from-website-response-wine-pairing-product-matches-item>
  }

  record extract-recipe-from-website-response-wine-pairing-product-matches-item {
    average-rating: f64,
    description: string,
    id: s32,
    image-url: string,
    link: string,
    price: string,
    rating-count: s32,
    score: f64,
    title: string
  }

  record search-recipes-by-ingredients-response-item {
    id: s32,
    image: string,
    image-type: string,
    likes: s32,
    missed-ingredient-count: s32,
    missed-ingredients: list<search-recipes-by-ingredients-response-item-missed-ingredients-item>,
    title: string,
    unused-ingredients: list<search-recipes-by-ingredients-response-item-unused-ingredients-item>,
    used-ingredient-count: f64,
    used-ingredients: list<search-recipes-by-ingredients-response-item-used-ingredients-item>
  }

  record search-recipes-by-ingredients-response-item-missed-ingredients-item {
    aisle: string,
    amount: f64,
    id: s32,
    image: string,
    meta: option<list<string>>,
    name: string,
    original: string,
    original-name: string,
    unit: string,
    unit-long: string,
    unit-short: string
  }

  record search-recipes-by-ingredients-response-item-unused-ingredients-item {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record search-recipes-by-ingredients-response-item-used-ingredients-item {
    aisle: string,
    amount: f64,
    id: s32,
    image: string,
    meta: option<list<string>>,
    name: string,
    original: string,
    original-name: string,
    unit: string,
    unit-long: string,
    unit-short: string
  }

  record search-recipes-by-nutrients-response-item {
    calories: f64,
    carbs: string,
    fat: string,
    id: s32,
    image: string,
    image-type: string,
    protein: string,
    title: string
  }

  record guess-nutrition-by-dish-name-response {
    calories: guess-nutrition-by-dish-name-response-calories,
    carbs: guess-nutrition-by-dish-name-response-carbs,
    fat: guess-nutrition-by-dish-name-response-fat,
    protein: guess-nutrition-by-dish-name-response-protein,
    recipes-used: s32
  }

  record guess-nutrition-by-dish-name-response-calories {
    confidence-range95-percent: guess-nutrition-by-dish-name-response-calories-confidence-range95-percent,
    standard-deviation: f64,
    unit: string,
    value: f64
  }

  record guess-nutrition-by-dish-name-response-calories-confidence-range95-percent {
    max: f64,
    min: f64
  }

  record guess-nutrition-by-dish-name-response-carbs {
    confidence-range95-percent: guess-nutrition-by-dish-name-response-carbs-confidence-range95-percent,
    standard-deviation: f64,
    unit: string,
    value: f64
  }

  record guess-nutrition-by-dish-name-response-carbs-confidence-range95-percent {
    max: f64,
    min: f64
  }

  record guess-nutrition-by-dish-name-response-fat {
    confidence-range95-percent: guess-nutrition-by-dish-name-response-fat-confidence-range95-percent,
    standard-deviation: f64,
    unit: string,
    value: f64
  }

  record guess-nutrition-by-dish-name-response-fat-confidence-range95-percent {
    max: f64,
    min: f64
  }

  record guess-nutrition-by-dish-name-response-protein {
    confidence-range95-percent: guess-nutrition-by-dish-name-response-protein-confidence-range95-percent,
    standard-deviation: f64,
    unit: string,
    value: f64
  }

  record guess-nutrition-by-dish-name-response-protein-confidence-range95-percent {
    max: f64,
    min: f64
  }

  record get-recipe-information-bulk-response-item {
    aggregate-likes: s32,
    analyzed-instructions: list<string>,
    cheap: bool,
    credits-text: string,
    cuisines: list<string>,
    dairy-free: bool,
    diets: list<string>,
    dish-types: list<string>,
    extended-ingredients: list<get-recipe-information-bulk-response-item-extended-ingredients-item>,
    gaps: string,
    gluten-free: bool,
    health-score: f64,
    id: s32,
    image: string,
    image-type: string,
    instructions: string,
    ketogenic: bool,
    license: string,
    low-fodmap: bool,
    occasions: list<string>,
    price-per-serving: f64,
    ready-in-minutes: s32,
    servings: f64,
    source-name: string,
    source-url: string,
    spoonacular-score: f64,
    spoonacular-source-url: string,
    summary: string,
    sustainable: bool,
    title: string,
    vegan: bool,
    vegetarian: bool,
    very-healthy: bool,
    very-popular: bool,
    weight-watcher-smart-points: f64,
    whole30: bool,
    wine-pairing: get-recipe-information-bulk-response-item-wine-pairing
  }

  record get-recipe-information-bulk-response-item-extended-ingredients-item {
    aisle: string,
    amount: f64,
    consitency: string,
    id: s32,
    image: string,
    measures: option<get-recipe-information-bulk-response-item-extended-ingredients-item-measures>,
    meta: option<list<string>>,
    name: string,
    original: string,
    original-name: string,
    unit: string
  }

  record get-recipe-information-bulk-response-item-extended-ingredients-item-measures {
    metric: get-recipe-information-bulk-response-item-extended-ingredients-item-measures-metric,
    us: get-recipe-information-bulk-response-item-extended-ingredients-item-measures-us
  }

  record get-recipe-information-bulk-response-item-extended-ingredients-item-measures-metric {
    amount: f64,
    unit-long: string,
    unit-short: string
  }

  record get-recipe-information-bulk-response-item-extended-ingredients-item-measures-us {
    amount: f64,
    unit-long: string,
    unit-short: string
  }

  record get-recipe-information-bulk-response-item-wine-pairing {
    paired-wines: list<string>,
    pairing-text: string,
    product-matches: list<get-recipe-information-bulk-response-item-wine-pairing-product-matches-item>
  }

  record get-recipe-information-bulk-response-item-wine-pairing-product-matches-item {
    average-rating: f64,
    description: string,
    id: s32,
    image-url: string,
    link: string,
    price: string,
    rating-count: s32,
    score: f64,
    title: string
  }

  record parse-ingredients-response-item {
    aisle: string,
    amount: f64,
    consistency: string,
    estimated-cost: parse-ingredients-response-item-estimated-cost,
    id: s32,
    image: string,
    meta: list<string>,
    name: string,
    name-clean: string,
    nutrition: parse-ingredients-response-item-nutrition,
    original: string,
    original-name: string,
    possible-units: list<string>,
    unit: string,
    unit-long: string,
    unit-short: string
  }

  record parse-ingredients-response-item-estimated-cost {
    unit: string,
    value: f64
  }

  record parse-ingredients-response-item-nutrition {
    caloric-breakdown: parse-ingredients-response-item-nutrition-caloric-breakdown,
    flavonoids: list<parse-ingredients-response-item-nutrition-flavonoids-item>,
    nutrients: list<parse-ingredients-response-item-nutrition-nutrients-item>,
    properties: list<parse-ingredients-response-item-nutrition-properties-item>,
    weight-per-serving: parse-ingredients-response-item-nutrition-weight-per-serving
  }

  record parse-ingredients-response-item-nutrition-caloric-breakdown {
    percent-carbs: f64,
    percent-fat: f64,
    percent-protein: f64
  }

  record parse-ingredients-response-item-nutrition-flavonoids-item {
    amount: f64,
    name: string,
    unit: string
  }

  record parse-ingredients-response-item-nutrition-nutrients-item {
    amount: f64,
    name: string,
    percent-of-daily-needs: f64,
    unit: string
  }

  record parse-ingredients-response-item-nutrition-properties-item {
    amount: f64,
    name: string,
    unit: string
  }

  record parse-ingredients-response-item-nutrition-weight-per-serving {
    amount: f64,
    unit: string
  }

  record analyze-a-recipe-search-query-response {
    cuisines: list<string>,
    dishes: list<analyze-a-recipe-search-query-response-dishes-item>,
    ingredients: list<analyze-a-recipe-search-query-response-ingredients-item>,
    modifiers: list<string>
  }

  record analyze-a-recipe-search-query-response-dishes-item {
    image: string,
    name: string
  }

  record analyze-a-recipe-search-query-response-ingredients-item {
    image: string,
    include-op: bool,
    name: string
  }

  record quick-answer-response {
    answer: string,
    image: string
  }

  record get-random-recipes-response {
    recipes: list<get-random-recipes-response-recipes-item>
  }

  record get-random-recipes-response-recipes-item {
    aggregate-likes: f64,
    analyzed-instructions: option<list<get-random-recipes-response-recipes-item-analyzed-instructions-item>>,
    cheap: bool,
    credits-text: string,
    cuisines: option<list<string>>,
    dairy-free: bool,
    diets: option<list<string>>,
    dish-types: option<list<string>>,
    extended-ingredients: option<list<get-random-recipes-response-recipes-item-extended-ingredients-item>>,
    gaps: string,
    gluten-free: bool,
    health-score: f64,
    id: s32,
    image: string,
    image-type: string,
    instructions: string,
    ketogenic: bool,
    license: string,
    low-fodmap: bool,
    occasions: option<list<string>>,
    price-per-serving: f64,
    ready-in-minutes: s32,
    servings: f64,
    source-name: string,
    source-url: string,
    spoonacular-score: f64,
    spoonacular-source-url: string,
    summary: string,
    sustainable: bool,
    title: string,
    vegan: bool,
    vegetarian: bool,
    very-healthy: bool,
    very-popular: bool,
    weight-watcher-smart-points: f64,
    whole30: bool,
    wine-pairing: option<get-random-recipes-response-recipes-item-wine-pairing>
  }

  record get-random-recipes-response-recipes-item-analyzed-instructions-item {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record get-random-recipes-response-recipes-item-extended-ingredients-item {
    aisle: string,
    amount: f64,
    consitency: string,
    id: s32,
    image: string,
    measures: option<get-random-recipes-response-recipes-item-extended-ingredients-item-measures>,
    meta: option<list<string>>,
    name: string,
    original: string,
    original-name: string,
    unit: string
  }

  record get-random-recipes-response-recipes-item-extended-ingredients-item-measures {
    metric: get-random-recipes-response-recipes-item-extended-ingredients-item-measures-metric,
    us: get-random-recipes-response-recipes-item-extended-ingredients-item-measures-us
  }

  record get-random-recipes-response-recipes-item-extended-ingredients-item-measures-metric {
    amount: f64,
    unit-long: string,
    unit-short: string
  }

  record get-random-recipes-response-recipes-item-extended-ingredients-item-measures-us {
    amount: f64,
    unit-long: string,
    unit-short: string
  }

  record get-random-recipes-response-recipes-item-wine-pairing {
    paired-wines: list<string>,
    pairing-text: string,
    product-matches: list<get-random-recipes-response-recipes-item-wine-pairing-product-matches-item>
  }

  record get-random-recipes-response-recipes-item-wine-pairing-product-matches-item {
    average-rating: f64,
    description: string,
    id: s32,
    image-url: string,
    link: string,
    price: string,
    rating-count: s32,
    score: f64,
    title: string
  }

  record create-recipe-card-response {
    url: string
  }

  record get-analyzed-recipe-instructions-response {
    equipment: list<get-analyzed-recipe-instructions-response-equipment-item>,
    ingredients: list<get-analyzed-recipe-instructions-response-ingredients-item>,
    parsed-instructions: list<get-analyzed-recipe-instructions-response-parsed-instructions-item>
  }

  record get-analyzed-recipe-instructions-response-equipment-item {
    id: s32,
    name: string
  }

  record get-analyzed-recipe-instructions-response-ingredients-item {
    id: s32,
    name: string
  }

  record get-analyzed-recipe-instructions-response-parsed-instructions-item {
    name: string,
    steps: option<list<get-analyzed-recipe-instructions-response-parsed-instructions-item-steps-item>>
  }

  record get-analyzed-recipe-instructions-response-parsed-instructions-item-steps-item {
    equipment: option<list<get-analyzed-recipe-instructions-response-parsed-instructions-item-steps-item-equipment-item>>,
    ingredients: option<list<get-analyzed-recipe-instructions-response-parsed-instructions-item-steps-item-ingredients-item>>,
    number: f64,
    step: string
  }

  record get-analyzed-recipe-instructions-response-parsed-instructions-item-steps-item-equipment-item {
    id: s32,
    image: string,
    localized-name: string,
    name: string
  }

  record get-analyzed-recipe-instructions-response-parsed-instructions-item-steps-item-ingredients-item {
    id: s32,
    image: string,
    localized-name: string,
    name: string
  }

  record create-recipe-card-get-response {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record get-recipe-equipment-by-id-response {
    equipment: list<get-recipe-equipment-by-id-response-equipment-item>
  }

  record get-recipe-equipment-by-id-response-equipment-item {
    image: string,
    name: string
  }

  record equipment-by-id-image-response {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record get-recipe-information-response {
    aggregate-likes: s32,
    analyzed-instructions: list<get-recipe-information-response-analyzed-instructions-item>,
    cheap: bool,
    credits-text: string,
    cuisines: list<string>,
    dairy-free: bool,
    diets: list<string>,
    dish-types: list<string>,
    extended-ingredients: list<get-recipe-information-response-extended-ingredients-item>,
    gaps: string,
    gluten-free: bool,
    health-score: f64,
    id: s32,
    image: string,
    image-type: string,
    instructions: string,
    ketogenic: bool,
    license: string,
    low-fodmap: bool,
    occasions: list<string>,
    price-per-serving: f64,
    ready-in-minutes: s32,
    servings: f64,
    source-name: string,
    source-url: string,
    spoonacular-score: f64,
    spoonacular-source-url: string,
    summary: string,
    sustainable: bool,
    title: string,
    vegan: bool,
    vegetarian: bool,
    very-healthy: bool,
    very-popular: bool,
    weight-watcher-smart-points: f64,
    whole30: bool,
    wine-pairing: get-recipe-information-response-wine-pairing
  }

  record get-recipe-information-response-analyzed-instructions-item {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record get-recipe-information-response-extended-ingredients-item {
    aisle: string,
    amount: f64,
    consitency: string,
    id: s32,
    image: string,
    measures: option<get-recipe-information-response-extended-ingredients-item-measures>,
    meta: option<list<string>>,
    name: string,
    original: string,
    original-name: string,
    unit: string
  }

  record get-recipe-information-response-extended-ingredients-item-measures {
    metric: get-recipe-information-response-extended-ingredients-item-measures-metric,
    us: get-recipe-information-response-extended-ingredients-item-measures-us
  }

  record get-recipe-information-response-extended-ingredients-item-measures-metric {
    amount: f64,
    unit-long: string,
    unit-short: string
  }

  record get-recipe-information-response-extended-ingredients-item-measures-us {
    amount: f64,
    unit-long: string,
    unit-short: string
  }

  record get-recipe-information-response-wine-pairing {
    paired-wines: list<string>,
    pairing-text: string,
    product-matches: list<get-recipe-information-response-wine-pairing-product-matches-item>
  }

  record get-recipe-information-response-wine-pairing-product-matches-item {
    average-rating: f64,
    description: string,
    id: s32,
    image-url: string,
    link: string,
    price: string,
    rating-count: s32,
    score: f64,
    title: string
  }

  record get-recipe-ingredients-by-id-response {
    ingredients: list<get-recipe-ingredients-by-id-response-ingredients-item>
  }

  record get-recipe-ingredients-by-id-response-ingredients-item {
    amount: option<get-recipe-ingredients-by-id-response-ingredients-item-amount>,
    image: string,
    name: string
  }

  record get-recipe-ingredients-by-id-response-ingredients-item-amount {
    metric: get-recipe-ingredients-by-id-response-ingredients-item-amount-metric,
    us: get-recipe-ingredients-by-id-response-ingredients-item-amount-us
  }

  record get-recipe-ingredients-by-id-response-ingredients-item-amount-metric {
    unit: string,
    value: f64
  }

  record get-recipe-ingredients-by-id-response-ingredients-item-amount-us {
    unit: string,
    value: f64
  }

  record recipe-nutrition-label-image-response {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record get-recipe-nutrition-widget-by-id-response {
    bad: list<get-recipe-nutrition-widget-by-id-response-bad-item>,
    calories: string,
    carbs: string,
    fat: string,
    good: list<get-recipe-nutrition-widget-by-id-response-good-item>,
    protein: string
  }

  record get-recipe-nutrition-widget-by-id-response-bad-item {
    amount: string,
    indented: bool,
    name: string,
    percent-of-daily-needs: f64
  }

  record get-recipe-nutrition-widget-by-id-response-good-item {
    amount: string,
    indented: bool,
    name: string,
    percent-of-daily-needs: f64
  }

  record recipe-nutrition-by-id-image-response {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record get-recipe-price-breakdown-by-id-response {
    ingredients: list<get-recipe-price-breakdown-by-id-response-ingredients-item>,
    total-cost: f64,
    total-cost-per-serving: f64
  }

  record get-recipe-price-breakdown-by-id-response-ingredients-item {
    amount: option<get-recipe-price-breakdown-by-id-response-ingredients-item-amount>,
    image: string,
    name: string,
    price: f64
  }

  record get-recipe-price-breakdown-by-id-response-ingredients-item-amount {
    metric: get-recipe-price-breakdown-by-id-response-ingredients-item-amount-metric,
    us: get-recipe-price-breakdown-by-id-response-ingredients-item-amount-us
  }

  record get-recipe-price-breakdown-by-id-response-ingredients-item-amount-metric {
    unit: string,
    value: f64
  }

  record get-recipe-price-breakdown-by-id-response-ingredients-item-amount-us {
    unit: string,
    value: f64
  }

  record price-breakdown-by-id-image-response {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record get-similar-recipes-response-item {
    id: s32,
    image-type: string,
    ready-in-minutes: s32,
    servings: f64,
    source-url: string,
    title: string
  }

  record summarize-recipe-response {
    id: s32,
    summary: string,
    title: string
  }

  record get-recipe-taste-by-id-response {
    bitterness: f64,
    fattiness: f64,
    saltiness: f64,
    savoriness: f64,
    sourness: f64,
    spiciness: f64,
    sweetness: f64
  }

  record recipe-taste-by-id-image-response {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  variant compute-glycemic-load-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant analyze-recipe-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant analyze-recipe-instructions-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant autocomplete-recipe-search-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant search-recipes-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant convert-amounts-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant classify-cuisine-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant extract-recipe-from-website-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant search-recipes-by-ingredients-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant search-recipes-by-nutrients-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant guess-nutrition-by-dish-name-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-recipe-information-bulk-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant parse-ingredients-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant analyze-a-recipe-search-query-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant quick-answer-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-random-recipes-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant visualize-equipment-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant visualize-recipe-nutrition-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant visualize-price-breakdown-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant create-recipe-card-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant visualize-recipe-taste-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-analyzed-recipe-instructions-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant create-recipe-card-get-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant visualize-recipe-equipment-by-id-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-recipe-equipment-by-id-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant equipment-by-id-image-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-recipe-information-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant visualize-recipe-ingredients-by-id-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-recipe-ingredients-by-id-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant recipe-nutrition-label-widget-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant recipe-nutrition-label-image-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant visualize-recipe-nutrition-by-id-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-recipe-nutrition-widget-by-id-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant recipe-nutrition-by-id-image-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant visualize-recipe-price-breakdown-by-id-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-recipe-price-breakdown-by-id-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant price-breakdown-by-id-image-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-similar-recipes-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant summarize-recipe-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant visualize-recipe-taste-by-id-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-recipe-taste-by-id-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant recipe-taste-by-id-image-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  /// Compute Glycemic Load
  record compute-glycemic-load-params {
    /// The language of the input. Either 'en' or 'de'.
    language: option<compute-glycemic-load-language-enum>,
    ingredients: list<string>
  }
  compute-glycemic-load: func(params: compute-glycemic-load-params) -> result<compute-glycemic-load-response, compute-glycemic-load-error>;

  /// Analyze Recipe
  record analyze-recipe-params {
    /// The input language, either "en" or "de".
    language: option<string>,
    /// Whether nutrition data should be added to correctly parsed ingredients.
    include-nutrition: option<bool>,
    /// Whether taste data should be added to correctly parsed ingredients.
    include-taste: option<bool>,
    ingredients: option<list<string>>,
    instructions: option<string>,
    servings: option<s32>,
    title: option<string>
  }
  analyze-recipe: func(params: analyze-recipe-params) -> result<analyze-recipe-response, analyze-recipe-error>;

  /// Analyze Recipe Instructions
  record analyze-recipe-instructions-params {
    /// The content type.
    content-type: option<analyze-recipe-instructions-content-type-enum>,
    body: string
  }
  analyze-recipe-instructions: func(params: analyze-recipe-instructions-params) -> result<analyze-recipe-instructions-response, analyze-recipe-instructions-error>;

  /// Autocomplete Recipe Search
  record autocomplete-recipe-search-params {
    /// The (natural language) search query.
    query: option<string>,
    /// The maximum number of items to return (between 1 and 100). Defaults to 10.
    number: option<s32>
  }
  autocomplete-recipe-search: func(params: autocomplete-recipe-search-params) -> result<list<autocomplete-recipe-search-response-item>, autocomplete-recipe-search-error>;

  /// Search Recipes
  record search-recipes-params {
    /// The (natural language) search query.
    query: option<string>,
    /// The cuisine(s) of the recipes. One or more, comma separated (will be interpreted as 'OR'). See a full list of supported cuisines.
    cuisine: option<string>,
    /// The cuisine(s) the recipes must not match. One or more, comma separated (will be interpreted as 'AND'). See a full list of supported cuisines.
    exclude-cuisine: option<string>,
    /// The diet for which the recipes must be suitable. See a full list of supported diets.
    diet: option<string>,
    /// A comma-separated list of intolerances. All recipes returned must not contain ingredients that are not suitable for people with the intolerances entered. See a full list of supported intolerances.
    intolerances: option<string>,
    /// The equipment required. Multiple values will be interpreted as 'or'. For example, value could be "blender, frying pan, bowl".
    equipment: option<string>,
    /// A comma-separated list of ingredients that should/must be used in the recipes.
    include-ingredients: option<string>,
    /// A comma-separated list of ingredients or ingredient types that the recipes must not contain.
    exclude-ingredients: option<string>,
    /// The type of recipe. See a full list of supported meal types.
    type-op: option<string>,
    /// Whether the recipes must have instructions.
    instructions-required: option<bool>,
    /// Add information about the ingredients and whether they are used or missing in relation to the query.
    fill-ingredients: option<bool>,
    /// If set to true, you get more information about the recipes returned.
    add-recipe-information: option<bool>,
    /// If set to true, you get nutritional information about each recipes returned.
    add-recipe-nutrition: option<bool>,
    /// The username of the recipe author.
    author: option<string>,
    /// The tags (can be diets, meal types, cuisines, or intolerances) that the recipe must have.
    tags: option<string>,
    /// The id of the recipe box to which the search should be limited to.
    recipe-box-id: option<f64>,
    /// Enter text that must be found in the title of the recipes.
    title-match: option<string>,
    /// The maximum time in minutes it should take to prepare and cook the recipe.
    max-ready-time: option<f64>,
    /// Whether to ignore typical pantry items, such as water, salt, flour, etc.
    ignore-pantry: option<bool>,
    /// The strategy to sort recipes by. See a full list of supported sorting options.
    sort: option<string>,
    /// The direction in which to sort. Must be either 'asc' (ascending) or 'desc' (descending).
    sort-direction: option<string>,
    /// The minimum amount of carbohydrates in grams the recipe must have.
    min-carbs: option<f64>,
    /// The maximum amount of carbohydrates in grams the recipe can have.
    max-carbs: option<f64>,
    /// The minimum amount of protein in grams the recipe must have.
    min-protein: option<f64>,
    /// The maximum amount of protein in grams the recipe can have.
    max-protein: option<f64>,
    /// The minimum amount of calories the recipe must have.
    min-calories: option<f64>,
    /// The maximum amount of calories the recipe can have.
    max-calories: option<f64>,
    /// The minimum amount of fat in grams the recipe must have.
    min-fat: option<f64>,
    /// The maximum amount of fat in grams the recipe can have.
    max-fat: option<f64>,
    /// The minimum amount of alcohol in grams the recipe must have.
    min-alcohol: option<f64>,
    /// The maximum amount of alcohol in grams the recipe can have.
    max-alcohol: option<f64>,
    /// The minimum amount of caffeine in milligrams the recipe must have.
    min-caffeine: option<f64>,
    /// The maximum amount of caffeine in milligrams the recipe can have.
    max-caffeine: option<f64>,
    /// The minimum amount of copper in milligrams the recipe must have.
    min-copper: option<f64>,
    /// The maximum amount of copper in milligrams the recipe can have.
    max-copper: option<f64>,
    /// The minimum amount of calcium in milligrams the recipe must have.
    min-calcium: option<f64>,
    /// The maximum amount of calcium in milligrams the recipe can have.
    max-calcium: option<f64>,
    /// The minimum amount of choline in milligrams the recipe must have.
    min-choline: option<f64>,
    /// The maximum amount of choline in milligrams the recipe can have.
    max-choline: option<f64>,
    /// The minimum amount of cholesterol in milligrams the recipe must have.
    min-cholesterol: option<f64>,
    /// The maximum amount of cholesterol in milligrams the recipe can have.
    max-cholesterol: option<f64>,
    /// The minimum amount of fluoride in milligrams the recipe must have.
    min-fluoride: option<f64>,
    /// The maximum amount of fluoride in milligrams the recipe can have.
    max-fluoride: option<f64>,
    /// The minimum amount of saturated fat in grams the recipe must have.
    min-saturated-fat: option<f64>,
    /// The maximum amount of saturated fat in grams the recipe can have.
    max-saturated-fat: option<f64>,
    /// The minimum amount of Vitamin A in IU the recipe must have.
    min-vitamin-a: option<f64>,
    /// The maximum amount of Vitamin A in IU the recipe can have.
    max-vitamin-a: option<f64>,
    /// The minimum amount of Vitamin C milligrams the recipe must have.
    min-vitamin-c: option<f64>,
    /// The maximum amount of Vitamin C in milligrams the recipe can have.
    max-vitamin-c: option<f64>,
    /// The minimum amount of Vitamin D in micrograms the recipe must have.
    min-vitamin-d: option<f64>,
    /// The maximum amount of Vitamin D in micrograms the recipe can have.
    max-vitamin-d: option<f64>,
    /// The minimum amount of Vitamin E in milligrams the recipe must have.
    min-vitamin-e: option<f64>,
    /// The maximum amount of Vitamin E in milligrams the recipe can have.
    max-vitamin-e: option<f64>,
    /// The minimum amount of Vitamin K in micrograms the recipe must have.
    min-vitamin-k: option<f64>,
    /// The maximum amount of Vitamin K in micrograms the recipe can have.
    max-vitamin-k: option<f64>,
    /// The minimum amount of Vitamin B1 in milligrams the recipe must have.
    min-vitamin-b1: option<f64>,
    /// The maximum amount of Vitamin B1 in milligrams the recipe can have.
    max-vitamin-b1: option<f64>,
    /// The minimum amount of Vitamin B2 in milligrams the recipe must have.
    min-vitamin-b2: option<f64>,
    /// The maximum amount of Vitamin B2 in milligrams the recipe can have.
    max-vitamin-b2: option<f64>,
    /// The minimum amount of Vitamin B5 in milligrams the recipe must have.
    min-vitamin-b5: option<f64>,
    /// The maximum amount of Vitamin B5 in milligrams the recipe can have.
    max-vitamin-b5: option<f64>,
    /// The minimum amount of Vitamin B3 in milligrams the recipe must have.
    min-vitamin-b3: option<f64>,
    /// The maximum amount of Vitamin B3 in milligrams the recipe can have.
    max-vitamin-b3: option<f64>,
    /// The minimum amount of Vitamin B6 in milligrams the recipe must have.
    min-vitamin-b6: option<f64>,
    /// The maximum amount of Vitamin B6 in milligrams the recipe can have.
    max-vitamin-b6: option<f64>,
    /// The minimum amount of Vitamin B12 in micrograms the recipe must have.
    min-vitamin-b12: option<f64>,
    /// The maximum amount of Vitamin B12 in micrograms the recipe can have.
    max-vitamin-b12: option<f64>,
    /// The minimum amount of fiber in grams the recipe must have.
    min-fiber: option<f64>,
    /// The maximum amount of fiber in grams the recipe can have.
    max-fiber: option<f64>,
    /// The minimum amount of folate in micrograms the recipe must have.
    min-folate: option<f64>,
    /// The maximum amount of folate in micrograms the recipe can have.
    max-folate: option<f64>,
    /// The minimum amount of folic acid in micrograms the recipe must have.
    min-folic-acid: option<f64>,
    /// The maximum amount of folic acid in micrograms the recipe can have.
    max-folic-acid: option<f64>,
    /// The minimum amount of iodine in micrograms the recipe must have.
    min-iodine: option<f64>,
    /// The maximum amount of iodine in micrograms the recipe can have.
    max-iodine: option<f64>,
    /// The minimum amount of iron in milligrams the recipe must have.
    min-iron: option<f64>,
    /// The maximum amount of iron in milligrams the recipe can have.
    max-iron: option<f64>,
    /// The minimum amount of magnesium in milligrams the recipe must have.
    min-magnesium: option<f64>,
    /// The maximum amount of magnesium in milligrams the recipe can have.
    max-magnesium: option<f64>,
    /// The minimum amount of manganese in milligrams the recipe must have.
    min-manganese: option<f64>,
    /// The maximum amount of manganese in milligrams the recipe can have.
    max-manganese: option<f64>,
    /// The minimum amount of phosphorus in milligrams the recipe must have.
    min-phosphorus: option<f64>,
    /// The maximum amount of phosphorus in milligrams the recipe can have.
    max-phosphorus: option<f64>,
    /// The minimum amount of potassium in milligrams the recipe must have.
    min-potassium: option<f64>,
    /// The maximum amount of potassium in milligrams the recipe can have.
    max-potassium: option<f64>,
    /// The minimum amount of selenium in micrograms the recipe must have.
    min-selenium: option<f64>,
    /// The maximum amount of selenium in micrograms the recipe can have.
    max-selenium: option<f64>,
    /// The minimum amount of sodium in milligrams the recipe must have.
    min-sodium: option<f64>,
    /// The maximum amount of sodium in milligrams the recipe can have.
    max-sodium: option<f64>,
    /// The minimum amount of sugar in grams the recipe must have.
    min-sugar: option<f64>,
    /// The maximum amount of sugar in grams the recipe can have.
    max-sugar: option<f64>,
    /// The minimum amount of zinc in milligrams the recipe must have.
    min-zinc: option<f64>,
    /// The maximum amount of zinc in milligrams the recipe can have.
    max-zinc: option<f64>,
    /// The number of results to skip (between 0 and 900).
    offset: option<s32>,
    /// The maximum number of items to return (between 1 and 100). Defaults to 10.
    number: option<s32>,
    /// Whether the recipes should have an open license that allows display with proper attribution.
    limit-license: option<bool>
  }
  search-recipes: func(params: search-recipes-params) -> result<search-recipes-response, search-recipes-error>;

  /// Convert Amounts
  record convert-amounts-params {
    /// The ingredient which you want to convert.
    ingredient-name: string,
    /// The amount from which you want to convert, e.g. the 2.5 in "2.5 cups of flour to grams".
    source-amount: f64,
    /// The unit from which you want to convert, e.g. the grams in "2.5 cups of flour to grams". You can also use "piece", e.g. "3.4 oz tomatoes to piece"
    source-unit: string,
    /// The unit to which you want to convert, e.g. the grams in "2.5 cups of flour to grams". You can also use "piece", e.g. "3.4 oz tomatoes to piece"
    target-unit: string
  }
  convert-amounts: func(params: convert-amounts-params) -> result<convert-amounts-response, convert-amounts-error>;

  /// Classify Cuisine
  record classify-cuisine-params {
    /// The content type.
    content-type: option<analyze-recipe-instructions-content-type-enum>,
    body: string
  }
  classify-cuisine: func(params: classify-cuisine-params) -> result<classify-cuisine-response, classify-cuisine-error>;

  /// Extract Recipe from Website
  record extract-recipe-from-website-params {
    /// The URL of the recipe page.
    url: string,
    /// If true, the extraction will be triggered whether we already know the recipe or not. Use this only if information is missing as this operation is slower.
    force-extraction: option<bool>,
    /// If true, the recipe will be analyzed and classified resolving in more data such as cuisines, dish types, and more.
    analyze: option<bool>,
    /// Include nutrition data in the recipe information. Nutrition data is per serving. If you want the nutrition data for the entire recipe, just multiply by the number of servings.
    include-nutrition: option<bool>,
    /// Whether taste data should be added to correctly parsed ingredients.
    include-taste: option<bool>
  }
  extract-recipe-from-website: func(params: extract-recipe-from-website-params) -> result<extract-recipe-from-website-response, extract-recipe-from-website-error>;

  /// Search Recipes by Ingredients
  record search-recipes-by-ingredients-params {
    /// A comma-separated list of ingredients that the recipes should contain.
    ingredients: option<string>,
    /// The maximum number of items to return (between 1 and 100). Defaults to 10.
    number: option<s32>,
    /// Whether the recipes should have an open license that allows display with proper attribution.
    limit-license: option<bool>,
    /// Whether to maximize used ingredients (1) or minimize missing ingredients (2) first.
    ranking: option<f64>,
    /// Whether to ignore typical pantry items, such as water, salt, flour, etc.
    ignore-pantry: option<bool>
  }
  search-recipes-by-ingredients: func(params: search-recipes-by-ingredients-params) -> result<list<search-recipes-by-ingredients-response-item>, search-recipes-by-ingredients-error>;

  /// Search Recipes by Nutrients
  record search-recipes-by-nutrients-params {
    /// The minimum amount of carbohydrates in grams the recipe must have.
    min-carbs: option<f64>,
    /// The maximum amount of carbohydrates in grams the recipe can have.
    max-carbs: option<f64>,
    /// The minimum amount of protein in grams the recipe must have.
    min-protein: option<f64>,
    /// The maximum amount of protein in grams the recipe can have.
    max-protein: option<f64>,
    /// The minimum amount of calories the recipe must have.
    min-calories: option<f64>,
    /// The maximum amount of calories the recipe can have.
    max-calories: option<f64>,
    /// The minimum amount of fat in grams the recipe must have.
    min-fat: option<f64>,
    /// The maximum amount of fat in grams the recipe can have.
    max-fat: option<f64>,
    /// The minimum amount of alcohol in grams the recipe must have.
    min-alcohol: option<f64>,
    /// The maximum amount of alcohol in grams the recipe can have.
    max-alcohol: option<f64>,
    /// The minimum amount of caffeine in milligrams the recipe must have.
    min-caffeine: option<f64>,
    /// The maximum amount of caffeine in milligrams the recipe can have.
    max-caffeine: option<f64>,
    /// The minimum amount of copper in milligrams the recipe must have.
    min-copper: option<f64>,
    /// The maximum amount of copper in milligrams the recipe can have.
    max-copper: option<f64>,
    /// The minimum amount of calcium in milligrams the recipe must have.
    min-calcium: option<f64>,
    /// The maximum amount of calcium in milligrams the recipe can have.
    max-calcium: option<f64>,
    /// The minimum amount of choline in milligrams the recipe must have.
    min-choline: option<f64>,
    /// The maximum amount of choline in milligrams the recipe can have.
    max-choline: option<f64>,
    /// The minimum amount of cholesterol in milligrams the recipe must have.
    min-cholesterol: option<f64>,
    /// The maximum amount of cholesterol in milligrams the recipe can have.
    max-cholesterol: option<f64>,
    /// The minimum amount of fluoride in milligrams the recipe must have.
    min-fluoride: option<f64>,
    /// The maximum amount of fluoride in milligrams the recipe can have.
    max-fluoride: option<f64>,
    /// The minimum amount of saturated fat in grams the recipe must have.
    min-saturated-fat: option<f64>,
    /// The maximum amount of saturated fat in grams the recipe can have.
    max-saturated-fat: option<f64>,
    /// The minimum amount of Vitamin A in IU the recipe must have.
    min-vitamin-a: option<f64>,
    /// The maximum amount of Vitamin A in IU the recipe can have.
    max-vitamin-a: option<f64>,
    /// The minimum amount of Vitamin C in milligrams the recipe must have.
    min-vitamin-c: option<f64>,
    /// The maximum amount of Vitamin C in milligrams the recipe can have.
    max-vitamin-c: option<f64>,
    /// The minimum amount of Vitamin D in micrograms the recipe must have.
    min-vitamin-d: option<f64>,
    /// The maximum amount of Vitamin D in micrograms the recipe can have.
    max-vitamin-d: option<f64>,
    /// The minimum amount of Vitamin E in milligrams the recipe must have.
    min-vitamin-e: option<f64>,
    /// The maximum amount of Vitamin E in milligrams the recipe can have.
    max-vitamin-e: option<f64>,
    /// The minimum amount of Vitamin K in micrograms the recipe must have.
    min-vitamin-k: option<f64>,
    /// The maximum amount of Vitamin K in micrograms the recipe can have.
    max-vitamin-k: option<f64>,
    /// The minimum amount of Vitamin B1 in milligrams the recipe must have.
    min-vitamin-b1: option<f64>,
    /// The maximum amount of Vitamin B1 in milligrams the recipe can have.
    max-vitamin-b1: option<f64>,
    /// The minimum amount of Vitamin B2 in milligrams the recipe must have.
    min-vitamin-b2: option<f64>,
    /// The maximum amount of Vitamin B2 in milligrams the recipe can have.
    max-vitamin-b2: option<f64>,
    /// The minimum amount of Vitamin B5 in milligrams the recipe must have.
    min-vitamin-b5: option<f64>,
    /// The maximum amount of Vitamin B5 in milligrams the recipe can have.
    max-vitamin-b5: option<f64>,
    /// The minimum amount of Vitamin B3 in milligrams the recipe must have.
    min-vitamin-b3: option<f64>,
    /// The maximum amount of Vitamin B3 in milligrams the recipe can have.
    max-vitamin-b3: option<f64>,
    /// The minimum amount of Vitamin B6 in milligrams the recipe must have.
    min-vitamin-b6: option<f64>,
    /// The maximum amount of Vitamin B6 in milligrams the recipe can have.
    max-vitamin-b6: option<f64>,
    /// The minimum amount of Vitamin B12 in micrograms the recipe must have.
    min-vitamin-b12: option<f64>,
    /// The maximum amount of Vitamin B12 in micrograms the recipe can have.
    max-vitamin-b12: option<f64>,
    /// The minimum amount of fiber in grams the recipe must have.
    min-fiber: option<f64>,
    /// The maximum amount of fiber in grams the recipe can have.
    max-fiber: option<f64>,
    /// The minimum amount of folate in micrograms the recipe must have.
    min-folate: option<f64>,
    /// The maximum amount of folate in micrograms the recipe can have.
    max-folate: option<f64>,
    /// The minimum amount of folic acid in micrograms the recipe must have.
    min-folic-acid: option<f64>,
    /// The maximum amount of folic acid in micrograms the recipe can have.
    max-folic-acid: option<f64>,
    /// The minimum amount of iodine in micrograms the recipe must have.
    min-iodine: option<f64>,
    /// The maximum amount of iodine in micrograms the recipe can have.
    max-iodine: option<f64>,
    /// The minimum amount of iron in milligrams the recipe must have.
    min-iron: option<f64>,
    /// The maximum amount of iron in milligrams the recipe can have.
    max-iron: option<f64>,
    /// The minimum amount of magnesium in milligrams the recipe must have.
    min-magnesium: option<f64>,
    /// The maximum amount of magnesium in milligrams the recipe can have.
    max-magnesium: option<f64>,
    /// The minimum amount of manganese in milligrams the recipe must have.
    min-manganese: option<f64>,
    /// The maximum amount of manganese in milligrams the recipe can have.
    max-manganese: option<f64>,
    /// The minimum amount of phosphorus in milligrams the recipe must have.
    min-phosphorus: option<f64>,
    /// The maximum amount of phosphorus in milligrams the recipe can have.
    max-phosphorus: option<f64>,
    /// The minimum amount of potassium in milligrams the recipe must have.
    min-potassium: option<f64>,
    /// The maximum amount of potassium in milligrams the recipe can have.
    max-potassium: option<f64>,
    /// The minimum amount of selenium in micrograms the recipe must have.
    min-selenium: option<f64>,
    /// The maximum amount of selenium in micrograms the recipe can have.
    max-selenium: option<f64>,
    /// The minimum amount of sodium in milligrams the recipe must have.
    min-sodium: option<f64>,
    /// The maximum amount of sodium in milligrams the recipe can have.
    max-sodium: option<f64>,
    /// The minimum amount of sugar in grams the recipe must have.
    min-sugar: option<f64>,
    /// The maximum amount of sugar in grams the recipe can have.
    max-sugar: option<f64>,
    /// The minimum amount of zinc in milligrams the recipe must have.
    min-zinc: option<f64>,
    /// The maximum amount of zinc in milligrams the recipe can have.
    max-zinc: option<f64>,
    /// The number of results to skip (between 0 and 900).
    offset: option<s32>,
    /// The maximum number of items to return (between 1 and 100). Defaults to 10.
    number: option<s32>,
    /// If true, every request will give you a random set of recipes within the requested limits.
    random: option<bool>,
    /// Whether the recipes should have an open license that allows display with proper attribution.
    limit-license: option<bool>
  }
  search-recipes-by-nutrients: func(params: search-recipes-by-nutrients-params) -> result<list<search-recipes-by-nutrients-response-item>, search-recipes-by-nutrients-error>;

  /// Guess Nutrition by Dish Name
  record guess-nutrition-by-dish-name-params {
    /// The title of the dish.
    title: string
  }
  guess-nutrition-by-dish-name: func(params: guess-nutrition-by-dish-name-params) -> result<guess-nutrition-by-dish-name-response, guess-nutrition-by-dish-name-error>;

  /// Get Recipe Information Bulk
  record get-recipe-information-bulk-params {
    /// A comma-separated list of recipe ids.
    ids: string,
    /// Include nutrition data in the recipe information. Nutrition data is per serving. If you want the nutrition data for the entire recipe, just multiply by the number of servings.
    include-nutrition: option<bool>
  }
  get-recipe-information-bulk: func(params: get-recipe-information-bulk-params) -> result<list<get-recipe-information-bulk-response-item>, get-recipe-information-bulk-error>;

  /// Parse Ingredients
  record parse-ingredients-params {
    /// The content type.
    content-type: option<analyze-recipe-instructions-content-type-enum>,
    /// The language of the input. Either 'en' or 'de'.
    language: option<compute-glycemic-load-language-enum>,
    body: string
  }
  parse-ingredients: func(params: parse-ingredients-params) -> result<list<parse-ingredients-response-item>, parse-ingredients-error>;

  /// Analyze a Recipe Search Query
  record analyze-a-recipe-search-query-params {
    /// The recipe search query.
    q: string
  }
  analyze-a-recipe-search-query: func(params: analyze-a-recipe-search-query-params) -> result<analyze-a-recipe-search-query-response, analyze-a-recipe-search-query-error>;

  /// Quick Answer
  record quick-answer-params {
    /// The nutrition related question.
    q: string
  }
  quick-answer: func(params: quick-answer-params) -> result<quick-answer-response, quick-answer-error>;

  /// Get Random Recipes
  record get-random-recipes-params {
    /// Whether the recipes should have an open license that allows display with proper attribution.
    limit-license: option<bool>,
    /// The tags (can be diets, meal types, cuisines, or intolerances) that the recipe must have.
    tags: option<string>,
    /// The maximum number of items to return (between 1 and 100). Defaults to 10.
    number: option<s32>
  }
  get-random-recipes: func(params: get-random-recipes-params) -> result<get-random-recipes-response, get-random-recipes-error>;

  /// Equipment Widget
  record visualize-equipment-params {
    /// The content type.
    content-type: option<analyze-recipe-instructions-content-type-enum>,
    /// Accept header.
    accept: option<visualize-equipment-accept-enum>,
    body: string
  }
  visualize-equipment: func(params: visualize-equipment-params) -> result<string, visualize-equipment-error>;

  /// Recipe Nutrition Widget
  record visualize-recipe-nutrition-params {
    /// The content type.
    content-type: option<analyze-recipe-instructions-content-type-enum>,
    /// Accept header.
    accept: option<visualize-equipment-accept-enum>,
    /// The language of the input. Either 'en' or 'de'.
    language: option<compute-glycemic-load-language-enum>,
    body: string
  }
  visualize-recipe-nutrition: func(params: visualize-recipe-nutrition-params) -> result<string, visualize-recipe-nutrition-error>;

  /// Price Breakdown Widget
  record visualize-price-breakdown-params {
    /// The content type.
    content-type: option<analyze-recipe-instructions-content-type-enum>,
    /// Accept header.
    accept: option<visualize-equipment-accept-enum>,
    /// The language of the input. Either 'en' or 'de'.
    language: option<compute-glycemic-load-language-enum>,
    body: string
  }
  visualize-price-breakdown: func(params: visualize-price-breakdown-params) -> result<string, visualize-price-breakdown-error>;

  /// Create Recipe Card
  record create-recipe-card-params {
    /// The content type.
    content-type: option<analyze-recipe-instructions-content-type-enum>,
    body: string
  }
  create-recipe-card: func(params: create-recipe-card-params) -> result<create-recipe-card-response, create-recipe-card-error>;

  /// Recipe Taste Widget
  record visualize-recipe-taste-params {
    /// The language of the input. Either 'en' or 'de'.
    language: option<compute-glycemic-load-language-enum>,
    /// The content type.
    content-type: option<analyze-recipe-instructions-content-type-enum>,
    /// Accept header.
    accept: option<visualize-equipment-accept-enum>,
    /// Whether to normalize to the strongest taste.
    normalize: option<bool>,
    /// Red, green, blue values for the chart color.
    rgb: option<string>,
    body: string
  }
  visualize-recipe-taste: func(params: visualize-recipe-taste-params) -> result<string, visualize-recipe-taste-error>;

  /// Get Analyzed Recipe Instructions
  record get-analyzed-recipe-instructions-params {
    /// The recipe id.
    id: string,
    /// Whether to break down the recipe steps even more.
    step-breakdown: option<bool>
  }
  get-analyzed-recipe-instructions: func(params: get-analyzed-recipe-instructions-params) -> result<get-analyzed-recipe-instructions-response, get-analyzed-recipe-instructions-error>;

  /// Create Recipe Card
  record create-recipe-card-get-params {
    /// The recipe id.
    id: string,
    /// The mask to put over the recipe image ("ellipseMask", "diamondMask", "starMask", "heartMask", "potMask", "fishMask").
    mask: option<string>,
    /// The background image ("none","background1", or "background2").
    background-image: option<string>,
    /// The background color for the recipe card as a hex-string.
    background-color: option<string>,
    /// The font color for the recipe card as a hex-string.
    font-color: option<string>
  }
  create-recipe-card-get: func(params: create-recipe-card-get-params) -> result<create-recipe-card-get-response, create-recipe-card-get-error>;

  /// Equipment by ID Widget
  record visualize-recipe-equipment-by-id-params {
    /// The recipe id.
    id: string,
    /// Whether the default CSS should be added to the response.
    default-css: option<bool>
  }
  visualize-recipe-equipment-by-id: func(params: visualize-recipe-equipment-by-id-params) -> result<string, visualize-recipe-equipment-by-id-error>;

  /// Equipment by ID
  record get-recipe-equipment-by-id-params {
    /// The recipe id.
    id: string
  }
  get-recipe-equipment-by-id: func(params: get-recipe-equipment-by-id-params) -> result<get-recipe-equipment-by-id-response, get-recipe-equipment-by-id-error>;

  /// Equipment by ID Image
  record equipment-by-id-image-params {
    /// The recipe id.
    id: string
  }
  equipment-by-id-image: func(params: equipment-by-id-image-params) -> result<equipment-by-id-image-response, equipment-by-id-image-error>;

  /// Get Recipe Information
  record get-recipe-information-params {
    /// The id of the recipe.
    id: string,
    /// Include nutrition data in the recipe information. Nutrition data is per serving. If you want the nutrition data for the entire recipe, just multiply by the number of servings.
    include-nutrition: option<bool>
  }
  get-recipe-information: func(params: get-recipe-information-params) -> result<get-recipe-information-response, get-recipe-information-error>;

  /// Ingredients by ID Widget
  record visualize-recipe-ingredients-by-id-params {
    /// The recipe id.
    id: string,
    /// Whether the default CSS should be added to the response.
    default-css: option<bool>,
    /// Whether the the measures should be 'us' or 'metric'.
    measure: option<visualize-recipe-ingredients-by-id-measure-enum>
  }
  visualize-recipe-ingredients-by-id: func(params: visualize-recipe-ingredients-by-id-params) -> result<string, visualize-recipe-ingredients-by-id-error>;

  /// Ingredients by ID
  record get-recipe-ingredients-by-id-params {
    /// The recipe id.
    id: string
  }
  get-recipe-ingredients-by-id: func(params: get-recipe-ingredients-by-id-params) -> result<get-recipe-ingredients-by-id-response, get-recipe-ingredients-by-id-error>;

  /// Recipe Nutrition Label Widget
  record recipe-nutrition-label-widget-params {
    /// The recipe id.
    id: string,
    /// Whether the default CSS should be added to the response.
    default-css: option<bool>,
    /// Whether to show optional nutrients.
    show-optional-nutrients: option<bool>,
    /// Whether to show zero values.
    show-zero-values: option<bool>,
    /// Whether to show a list of ingredients.
    show-ingredients: option<bool>
  }
  recipe-nutrition-label-widget: func(params: recipe-nutrition-label-widget-params) -> result<string, recipe-nutrition-label-widget-error>;

  /// Recipe Nutrition Label Image
  record recipe-nutrition-label-image-params {
    /// The recipe id.
    id: string,
    /// Whether to show optional nutrients.
    show-optional-nutrients: option<bool>,
    /// Whether to show zero values.
    show-zero-values: option<bool>,
    /// Whether to show a list of ingredients.
    show-ingredients: option<bool>
  }
  recipe-nutrition-label-image: func(params: recipe-nutrition-label-image-params) -> result<recipe-nutrition-label-image-response, recipe-nutrition-label-image-error>;

  /// Recipe Nutrition by ID Widget
  record visualize-recipe-nutrition-by-id-params {
    /// The recipe id.
    id: string,
    /// Whether the default CSS should be added to the response.
    default-css: option<bool>,
    /// Accept header.
    accept: option<visualize-equipment-accept-enum>
  }
  visualize-recipe-nutrition-by-id: func(params: visualize-recipe-nutrition-by-id-params) -> result<string, visualize-recipe-nutrition-by-id-error>;

  /// Nutrition by ID
  record get-recipe-nutrition-widget-by-id-params {
    /// The recipe id.
    id: string
  }
  get-recipe-nutrition-widget-by-id: func(params: get-recipe-nutrition-widget-by-id-params) -> result<get-recipe-nutrition-widget-by-id-response, get-recipe-nutrition-widget-by-id-error>;

  /// Recipe Nutrition by ID Image
  record recipe-nutrition-by-id-image-params {
    /// The recipe id.
    id: string
  }
  recipe-nutrition-by-id-image: func(params: recipe-nutrition-by-id-image-params) -> result<recipe-nutrition-by-id-image-response, recipe-nutrition-by-id-image-error>;

  /// Price Breakdown by ID Widget
  record visualize-recipe-price-breakdown-by-id-params {
    /// The recipe id.
    id: string,
    /// Whether the default CSS should be added to the response.
    default-css: option<bool>
  }
  visualize-recipe-price-breakdown-by-id: func(params: visualize-recipe-price-breakdown-by-id-params) -> result<string, visualize-recipe-price-breakdown-by-id-error>;

  /// Price Breakdown by ID
  record get-recipe-price-breakdown-by-id-params {
    /// The recipe id.
    id: string
  }
  get-recipe-price-breakdown-by-id: func(params: get-recipe-price-breakdown-by-id-params) -> result<get-recipe-price-breakdown-by-id-response, get-recipe-price-breakdown-by-id-error>;

  /// Price Breakdown by ID Image
  record price-breakdown-by-id-image-params {
    /// The recipe id.
    id: string
  }
  price-breakdown-by-id-image: func(params: price-breakdown-by-id-image-params) -> result<price-breakdown-by-id-image-response, price-breakdown-by-id-image-error>;

  /// Get Similar Recipes
  record get-similar-recipes-params {
    /// The id of the source recipe for which similar recipes should be found.
    id: string,
    /// The maximum number of items to return (between 1 and 100). Defaults to 10.
    number: option<s32>,
    /// Whether the recipes should have an open license that allows display with proper attribution.
    limit-license: option<bool>
  }
  get-similar-recipes: func(params: get-similar-recipes-params) -> result<list<get-similar-recipes-response-item>, get-similar-recipes-error>;

  /// Summarize Recipe
  record summarize-recipe-params {
    /// The recipe id.
    id: string
  }
  summarize-recipe: func(params: summarize-recipe-params) -> result<summarize-recipe-response, summarize-recipe-error>;

  /// Recipe Taste by ID Widget
  record visualize-recipe-taste-by-id-params {
    /// The recipe id.
    id: string,
    /// Whether to normalize to the strongest taste.
    normalize: option<bool>,
    /// Red, green, blue values for the chart color.
    rgb: option<string>
  }
  visualize-recipe-taste-by-id: func(params: visualize-recipe-taste-by-id-params) -> result<string, visualize-recipe-taste-by-id-error>;

  /// Taste by ID
  record get-recipe-taste-by-id-params {
    /// The recipe id.
    id: string,
    /// Normalize to the strongest taste.
    normalize: option<bool>
  }
  get-recipe-taste-by-id: func(params: get-recipe-taste-by-id-params) -> result<get-recipe-taste-by-id-response, get-recipe-taste-by-id-error>;

  /// Recipe Taste by ID Image
  record recipe-taste-by-id-image-params {
    /// The recipe id.
    id: string,
    /// Normalize to the strongest taste.
    normalize: option<bool>,
    /// Red, green, blue values for the chart color.
    rgb: option<string>
  }
  recipe-taste-by-id-image: func(params: recipe-taste-by-id-image-params) -> result<recipe-taste-by-id-image-response, recipe-taste-by-id-image-error>;

}

interface menu-items {
  enum visualize-menu-item-nutrition-by-id-accept-enum {
    application-json,
    text-html,
    media,
  }

  record search-menu-items-response {
    menu-items: list<search-menu-items-response-menu-items-item>,
    number: s32,
    offset: s32,
    total-menu-items: s32,
    type-op: string
  }

  record search-menu-items-response-menu-items-item {
    id: s32,
    image: string,
    image-type: string,
    restaurant-chain: string,
    servings: option<search-menu-items-response-menu-items-item-servings>,
    title: string
  }

  record search-menu-items-response-menu-items-item-servings {
    number: f64,
    size: f64,
    unit: string
  }

  record autocomplete-menu-item-search-response {
    results: list<autocomplete-menu-item-search-response-results-item>
  }

  record autocomplete-menu-item-search-response-results-item {
    id: s32,
    title: string
  }

  record get-menu-item-information-response {
    badges: list<string>,
    breadcrumbs: list<string>,
    generated-text: option<string>,
    id: s32,
    image-type: string,
    likes: f64,
    nutrition: get-menu-item-information-response-nutrition,
    price: option<f64>,
    restaurant-chain: string,
    servings: get-menu-item-information-response-servings,
    spoonacular-score: option<f64>,
    title: string
  }

  record get-menu-item-information-response-nutrition {
    caloric-breakdown: get-menu-item-information-response-nutrition-caloric-breakdown,
    nutrients: list<get-menu-item-information-response-nutrition-nutrients-item>
  }

  record get-menu-item-information-response-nutrition-caloric-breakdown {
    percent-carbs: f64,
    percent-fat: f64,
    percent-protein: f64
  }

  record get-menu-item-information-response-nutrition-nutrients-item {
    amount: f64,
    name: string,
    percent-of-daily-needs: f64,
    unit: string
  }

  record get-menu-item-information-response-servings {
    number: f64,
    size: f64,
    unit: string
  }

  record menu-item-nutrition-label-image-response {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record menu-item-nutrition-by-id-image-response {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  variant search-menu-items-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant autocomplete-menu-item-search-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-menu-item-information-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant menu-item-nutrition-label-widget-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant menu-item-nutrition-label-image-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant visualize-menu-item-nutrition-by-id-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant menu-item-nutrition-by-id-image-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  /// Search Menu Items
  record search-menu-items-params {
    /// The (natural language) search query.
    query: option<string>,
    /// The minimum amount of calories the menu item must have.
    min-calories: option<f64>,
    /// The maximum amount of calories the menu item can have.
    max-calories: option<f64>,
    /// The minimum amount of carbohydrates in grams the menu item must have.
    min-carbs: option<f64>,
    /// The maximum amount of carbohydrates in grams the menu item can have.
    max-carbs: option<f64>,
    /// The minimum amount of protein in grams the menu item must have.
    min-protein: option<f64>,
    /// The maximum amount of protein in grams the menu item can have.
    max-protein: option<f64>,
    /// The minimum amount of fat in grams the menu item must have.
    min-fat: option<f64>,
    /// The maximum amount of fat in grams the menu item can have.
    max-fat: option<f64>,
    /// If set to true, you get more information about the menu items returned.
    add-menu-item-information: option<bool>,
    /// The number of results to skip (between 0 and 900).
    offset: option<s32>,
    /// The maximum number of items to return (between 1 and 100). Defaults to 10.
    number: option<s32>
  }
  search-menu-items: func(params: search-menu-items-params) -> result<search-menu-items-response, search-menu-items-error>;

  /// Autocomplete Menu Item Search
  record autocomplete-menu-item-search-params {
    /// The (partial) search query.
    query: string,
    /// The number of results to return (between 1 and 25).
    number: option<f64>
  }
  autocomplete-menu-item-search: func(params: autocomplete-menu-item-search-params) -> result<autocomplete-menu-item-search-response, autocomplete-menu-item-search-error>;

  /// Get Menu Item Information
  record get-menu-item-information-params {
    /// The menu item id.
    id: string
  }
  get-menu-item-information: func(params: get-menu-item-information-params) -> result<get-menu-item-information-response, get-menu-item-information-error>;

  /// Menu Item Nutrition Label Widget
  record menu-item-nutrition-label-widget-params {
    /// The menu item id.
    id: string,
    /// Whether the default CSS should be added to the response.
    default-css: option<bool>,
    /// Whether to show optional nutrients.
    show-optional-nutrients: option<bool>,
    /// Whether to show zero values.
    show-zero-values: option<bool>,
    /// Whether to show a list of ingredients.
    show-ingredients: option<bool>
  }
  menu-item-nutrition-label-widget: func(params: menu-item-nutrition-label-widget-params) -> result<string, menu-item-nutrition-label-widget-error>;

  /// Menu Item Nutrition Label Image
  record menu-item-nutrition-label-image-params {
    /// The menu item id.
    id: string,
    /// Whether to show optional nutrients.
    show-optional-nutrients: option<bool>,
    /// Whether to show zero values.
    show-zero-values: option<bool>,
    /// Whether to show a list of ingredients.
    show-ingredients: option<bool>
  }
  menu-item-nutrition-label-image: func(params: menu-item-nutrition-label-image-params) -> result<menu-item-nutrition-label-image-response, menu-item-nutrition-label-image-error>;

  /// Menu Item Nutrition by ID Widget
  record visualize-menu-item-nutrition-by-id-params {
    /// The menu item id.
    id: string,
    /// Whether the default CSS should be added to the response.
    default-css: option<bool>,
    /// Accept header.
    accept: option<visualize-menu-item-nutrition-by-id-accept-enum>
  }
  visualize-menu-item-nutrition-by-id: func(params: visualize-menu-item-nutrition-by-id-params) -> result<string, visualize-menu-item-nutrition-by-id-error>;

  /// Menu Item Nutrition by ID Image
  record menu-item-nutrition-by-id-image-params {
    /// The menu item id.
    id: string
  }
  menu-item-nutrition-by-id-image: func(params: menu-item-nutrition-by-id-image-params) -> result<menu-item-nutrition-by-id-image-response, menu-item-nutrition-by-id-image-error>;

}

interface products {
  enum classify-grocery-product-locale-enum {
    en-us,
    en-gb,
  }

  enum visualize-product-nutrition-by-id-accept-enum {
    application-json,
    text-html,
    media,
  }

  record classify-grocery-product-response {
    breadcrumbs: list<string>,
    category: string,
    clean-title: string,
    image: string,
    usda-code: s32
  }

  record classify-grocery-product-bulk-body-item {
    plu-code: string,
    title: string,
    upc: string
  }

  record classify-grocery-product-bulk-response-item {
    breadcrumbs: list<string>,
    category: string,
    clean-title: string,
    image: string,
    usda-code: s32
  }

  record search-grocery-products-response {
    number: s32,
    offset: s32,
    products: list<search-grocery-products-response-products-item>,
    total-products: s32,
    type-op: string
  }

  record search-grocery-products-response-products-item {
    id: s32,
    image-type: string,
    title: string
  }

  record autocomplete-product-search-response {
    results: list<autocomplete-product-search-response-results-item>
  }

  record autocomplete-product-search-response-results-item {
    id: s32,
    title: string
  }

  record search-grocery-products-by-upc-response {
    badges: list<string>,
    breadcrumbs: list<string>,
    generated-text: string,
    id: s32,
    image-type: string,
    important-badges: list<string>,
    ingredient-count: option<s32>,
    ingredient-list: string,
    ingredients: list<search-grocery-products-by-upc-response-ingredients-item>,
    likes: f64,
    nutrition: search-grocery-products-by-upc-response-nutrition,
    price: f64,
    servings: search-grocery-products-by-upc-response-servings,
    spoonacular-score: f64,
    title: string
  }

  record search-grocery-products-by-upc-response-ingredients-item {
    description: option<string>,
    name: string,
    safety-level: option<string>
  }

  record search-grocery-products-by-upc-response-nutrition {
    caloric-breakdown: search-grocery-products-by-upc-response-nutrition-caloric-breakdown,
    nutrients: list<search-grocery-products-by-upc-response-nutrition-nutrients-item>
  }

  record search-grocery-products-by-upc-response-nutrition-caloric-breakdown {
    percent-carbs: f64,
    percent-fat: f64,
    percent-protein: f64
  }

  record search-grocery-products-by-upc-response-nutrition-nutrients-item {
    amount: f64,
    name: string,
    percent-of-daily-needs: f64,
    unit: string
  }

  record search-grocery-products-by-upc-response-servings {
    number: f64,
    size: f64,
    unit: string
  }

  record get-comparable-products-response {
    comparable-products: get-comparable-products-response-comparable-products
  }

  record get-comparable-products-response-comparable-products {
    calories: list<get-comparable-products-response-comparable-products-calories-item>,
    likes: list<get-comparable-products-response-comparable-products-likes-item>,
    price: list<get-comparable-products-response-comparable-products-price-item>,
    protein: list<get-comparable-products-response-comparable-products-protein-item>,
    spoonacular-score: list<get-comparable-products-response-comparable-products-spoonacular-score-item>,
    sugar: list<get-comparable-products-response-comparable-products-sugar-item>
  }

  record get-comparable-products-response-comparable-products-calories-item {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record get-comparable-products-response-comparable-products-likes-item {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record get-comparable-products-response-comparable-products-price-item {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record get-comparable-products-response-comparable-products-protein-item {
    difference: f64,
    id: s32,
    image: string,
    title: string
  }

  record get-comparable-products-response-comparable-products-spoonacular-score-item {
    difference: f64,
    id: s32,
    image: string,
    title: string
  }

  record get-comparable-products-response-comparable-products-sugar-item {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record get-product-information-response {
    aisle: string,
    badges: list<string>,
    breadcrumbs: list<string>,
    generated-text: option<string>,
    id: s32,
    image-type: string,
    important-badges: list<string>,
    ingredient-count: s32,
    ingredient-list: string,
    ingredients: list<get-product-information-response-ingredients-item>,
    likes: f64,
    nutrition: get-product-information-response-nutrition,
    price: f64,
    servings: get-product-information-response-servings,
    spoonacular-score: f64,
    title: string
  }

  record get-product-information-response-ingredients-item {
    description: option<string>,
    name: string,
    safety-level: option<string>
  }

  record get-product-information-response-nutrition {
    caloric-breakdown: get-product-information-response-nutrition-caloric-breakdown,
    nutrients: list<get-product-information-response-nutrition-nutrients-item>
  }

  record get-product-information-response-nutrition-caloric-breakdown {
    percent-carbs: f64,
    percent-fat: f64,
    percent-protein: f64
  }

  record get-product-information-response-nutrition-nutrients-item {
    amount: f64,
    name: string,
    percent-of-daily-needs: f64,
    unit: string
  }

  record get-product-information-response-servings {
    number: f64,
    size: f64,
    unit: string
  }

  record product-nutrition-label-image-response {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record product-nutrition-by-id-image-response {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  variant classify-grocery-product-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant classify-grocery-product-bulk-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant search-grocery-products-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant autocomplete-product-search-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant search-grocery-products-by-upc-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-comparable-products-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-product-information-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant product-nutrition-label-widget-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant product-nutrition-label-image-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant visualize-product-nutrition-by-id-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant product-nutrition-by-id-image-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  /// Classify Grocery Product
  record classify-grocery-product-params {
    /// The display name of the returned category, supported is en_US (for American English) and en_GB (for British English).
    locale: option<classify-grocery-product-locale-enum>,
    plu-code: string,
    title: string,
    upc: string
  }
  classify-grocery-product: func(params: classify-grocery-product-params) -> result<classify-grocery-product-response, classify-grocery-product-error>;

  /// Classify Grocery Product Bulk
  record classify-grocery-product-bulk-params {
    /// The display name of the returned category, supported is en_US (for American English) and en_GB (for British English).
    locale: option<string>,
    body: list<classify-grocery-product-bulk-body-item>
  }
  classify-grocery-product-bulk: func(params: classify-grocery-product-bulk-params) -> result<list<classify-grocery-product-bulk-response-item>, classify-grocery-product-bulk-error>;

  /// Search Grocery Products
  record search-grocery-products-params {
    /// The (natural language) search query.
    query: option<string>,
    /// The minimum amount of calories the product must have.
    min-calories: option<f64>,
    /// The maximum amount of calories the product can have.
    max-calories: option<f64>,
    /// The minimum amount of carbohydrates in grams the product must have.
    min-carbs: option<f64>,
    /// The maximum amount of carbohydrates in grams the product can have.
    max-carbs: option<f64>,
    /// The minimum amount of protein in grams the product must have.
    min-protein: option<f64>,
    /// The maximum amount of protein in grams the product can have.
    max-protein: option<f64>,
    /// The minimum amount of fat in grams the product must have.
    min-fat: option<f64>,
    /// The maximum amount of fat in grams the product can have.
    max-fat: option<f64>,
    /// If set to true, you get more information about the products returned.
    add-product-information: option<bool>,
    /// The number of results to skip (between 0 and 900).
    offset: option<s32>,
    /// The maximum number of items to return (between 1 and 100). Defaults to 10.
    number: option<s32>
  }
  search-grocery-products: func(params: search-grocery-products-params) -> result<search-grocery-products-response, search-grocery-products-error>;

  /// Autocomplete Product Search
  record autocomplete-product-search-params {
    /// The (partial) search query.
    query: string,
    /// The number of results to return (between 1 and 25).
    number: option<s32>
  }
  autocomplete-product-search: func(params: autocomplete-product-search-params) -> result<autocomplete-product-search-response, autocomplete-product-search-error>;

  /// Search Grocery Products by UPC
  record search-grocery-products-by-upc-params {
    /// The product's UPC.
    upc: string
  }
  search-grocery-products-by-upc: func(params: search-grocery-products-by-upc-params) -> result<search-grocery-products-by-upc-response, search-grocery-products-by-upc-error>;

  /// Get Comparable Products
  record get-comparable-products-params {
    /// The UPC of the product for which you want to find comparable products.
    upc: string
  }
  get-comparable-products: func(params: get-comparable-products-params) -> result<get-comparable-products-response, get-comparable-products-error>;

  /// Get Product Information
  record get-product-information-params {
    /// The id of the packaged food.
    id: string
  }
  get-product-information: func(params: get-product-information-params) -> result<get-product-information-response, get-product-information-error>;

  /// Product Nutrition Label Widget
  record product-nutrition-label-widget-params {
    /// The product id.
    id: string,
    /// Whether the default CSS should be added to the response.
    default-css: option<bool>,
    /// Whether to show optional nutrients.
    show-optional-nutrients: option<bool>,
    /// Whether to show zero values.
    show-zero-values: option<bool>,
    /// Whether to show a list of ingredients.
    show-ingredients: option<bool>
  }
  product-nutrition-label-widget: func(params: product-nutrition-label-widget-params) -> result<string, product-nutrition-label-widget-error>;

  /// Product Nutrition Label Image
  record product-nutrition-label-image-params {
    /// The product id.
    id: string,
    /// Whether to show optional nutrients.
    show-optional-nutrients: option<bool>,
    /// Whether to show zero values.
    show-zero-values: option<bool>,
    /// Whether to show a list of ingredients.
    show-ingredients: option<bool>
  }
  product-nutrition-label-image: func(params: product-nutrition-label-image-params) -> result<product-nutrition-label-image-response, product-nutrition-label-image-error>;

  /// Product Nutrition by ID Widget
  record visualize-product-nutrition-by-id-params {
    /// The id of the product.
    id: string,
    /// Whether the default CSS should be added to the response.
    default-css: option<bool>,
    /// Accept header.
    accept: option<visualize-product-nutrition-by-id-accept-enum>
  }
  visualize-product-nutrition-by-id: func(params: visualize-product-nutrition-by-id-params) -> result<string, visualize-product-nutrition-by-id-error>;

  /// Product Nutrition by ID Image
  record product-nutrition-by-id-image-params {
    /// The id of the product.
    id: string
  }
  product-nutrition-by-id-image: func(params: product-nutrition-by-id-image-params) -> result<product-nutrition-by-id-image-response, product-nutrition-by-id-image-error>;

}

interface food {
  record search-restaurants-response {
    restaurants: option<list<search-restaurants-response-restaurants-item>>
  }

  record search-restaurants-response-restaurants-item {
    id: option<string>,
    address: option<search-restaurants-response-restaurants-item-address>,
    aggregated-rating-count: option<s32>,
    cuisines: option<list<string>>,
    delivery-enabled: option<bool>,
    description: option<string>,
    dollar-signs: option<s32>,
    food-photos: option<list<string>>,
    is-open: option<bool>,
    local-hours: option<search-restaurants-response-restaurants-item-local-hours>,
    logo-photos: option<list<string>>,
    miles: option<f64>,
    name: option<string>,
    offers-first-party-delivery: option<bool>,
    offers-third-party-delivery: option<bool>,
    phone-number: option<s32>,
    pickup-enabled: option<bool>,
    store-photos: option<list<search-restaurants-response-restaurants-item-store-photos-item>>,
    type-op: option<string>,
    weighted-rating-value: option<f64>
  }

  record search-restaurants-response-restaurants-item-address {
    city: option<string>,
    country: option<string>,
    lat: option<f64>,
    latitude: option<f64>,
    lon: option<f64>,
    longitude: option<f64>,
    state: option<string>,
    street-addr: option<string>,
    street-addr-v2: option<string>,
    zipcode: option<string>
  }

  record search-restaurants-response-restaurants-item-local-hours {
    delivery: option<search-restaurants-response-restaurants-item-local-hours-delivery>,
    dine-in: option<search-restaurants-response-restaurants-item-local-hours-dine-in>,
    operational: option<search-restaurants-response-restaurants-item-local-hours-operational>,
    pickup: option<search-restaurants-response-restaurants-item-local-hours-pickup>
  }

  record search-restaurants-response-restaurants-item-local-hours-delivery {
    friday: option<string>,
    monday: option<string>,
    saturday: option<string>,
    sunday: option<string>,
    thursday: option<string>,
    tuesday: option<string>,
    wednesday: option<string>
  }

  record search-restaurants-response-restaurants-item-local-hours-dine-in {
    friday: option<string>,
    monday: option<string>,
    saturday: option<string>,
    sunday: option<string>,
    thursday: option<string>,
    tuesday: option<string>,
    wednesday: option<string>
  }

  record search-restaurants-response-restaurants-item-local-hours-operational {
    friday: option<string>,
    monday: option<string>,
    saturday: option<string>,
    sunday: option<string>,
    thursday: option<string>,
    tuesday: option<string>,
    wednesday: option<string>
  }

  record search-restaurants-response-restaurants-item-local-hours-pickup {
    friday: option<string>,
    monday: option<string>,
    saturday: option<string>,
    sunday: option<string>,
    thursday: option<string>,
    tuesday: option<string>,
    wednesday: option<string>
  }

  record search-restaurants-response-restaurants-item-store-photos-item {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  variant search-restaurants-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  /// Search Restaurants
  record search-restaurants-params {
    /// The search query.
    query: option<string>,
    /// The latitude of the user's location.
    lat: option<f64>,
    /// The longitude of the user's location.".
    lng: option<f64>,
    /// The distance around the location in miles.
    distance: option<f64>,
    /// The user's budget for a meal in USD.
    budget: option<f64>,
    /// The cuisine of the restaurant.
    cuisine: option<string>,
    /// The minimum rating of the restaurant between 0 and 5.
    min-rating: option<f64>,
    /// Whether the restaurant must be open at the time of search.
    is-open: option<bool>,
    /// How to sort the results, one of the following 'cheapest', 'fastest', 'rating', 'distance' or the default 'relevance'.
    sort: option<string>,
    /// The page number of results.
    page: option<f64>
  }
  search-restaurants: func(params: search-restaurants-params) -> result<search-restaurants-response, search-restaurants-error>;

}

interface wine {
  record get-wine-description-response {
    wine-description: string
  }

  record get-dish-pairing-for-wine-response {
    pairings: list<string>,
    text: string
  }

  record get-wine-pairing-response {
    paired-wines: list<string>,
    pairing-text: string,
    product-matches: list<get-wine-pairing-response-product-matches-item>
  }

  record get-wine-pairing-response-product-matches-item {
    average-rating: f64,
    description: option<string>,
    id: s32,
    image-url: string,
    link: string,
    price: string,
    rating-count: s32,
    score: f64,
    title: string
  }

  record get-wine-recommendation-response {
    recommended-wines: list<get-wine-recommendation-response-recommended-wines-item>,
    total-found: s32
  }

  record get-wine-recommendation-response-recommended-wines-item {
    average-rating: f64,
    description: string,
    id: s32,
    image-url: string,
    link: string,
    price: string,
    rating-count: s32,
    score: f64,
    title: string
  }

  variant get-wine-description-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-dish-pairing-for-wine-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-wine-pairing-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-wine-recommendation-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  /// Wine Description
  record get-wine-description-params {
    /// The name of the wine that should be paired, e.g. "merlot", "riesling", or "malbec".
    wine: string
  }
  get-wine-description: func(params: get-wine-description-params) -> result<get-wine-description-response, get-wine-description-error>;

  /// Dish Pairing for Wine
  record get-dish-pairing-for-wine-params {
    /// The type of wine that should be paired, e.g. "merlot", "riesling", or "malbec".
    wine: string
  }
  get-dish-pairing-for-wine: func(params: get-dish-pairing-for-wine-params) -> result<get-dish-pairing-for-wine-response, get-dish-pairing-for-wine-error>;

  /// Wine Pairing
  record get-wine-pairing-params {
    /// The food to get a pairing for. This can be a dish ("steak"), an ingredient ("salmon"), or a cuisine ("italian").
    food: string,
    /// The maximum price for the specific wine recommendation in USD.
    max-price: option<f64>
  }
  get-wine-pairing: func(params: get-wine-pairing-params) -> result<get-wine-pairing-response, get-wine-pairing-error>;

  /// Wine Recommendation
  record get-wine-recommendation-params {
    /// The type of wine to get a specific product recommendation for.
    wine: string,
    /// The maximum price for the specific wine recommendation in USD.
    max-price: option<f64>,
    /// The minimum rating of the recommended wine between 0 and 1. For example, 0.8 equals 4 out of 5 stars.
    min-rating: option<f64>,
    /// The number of wine recommendations expected (between 1 and 100).
    number: option<f64>
  }
  get-wine-recommendation: func(params: get-wine-recommendation-params) -> result<get-wine-recommendation-response, get-wine-recommendation-error>;

}

interface meal-planning {
  record generate-meal-plan-response {
    meals: list<generate-meal-plan-response-meals-item>,
    nutrients: generate-meal-plan-response-nutrients
  }

  record generate-meal-plan-response-meals-item {
    id: s32,
    image-type: string,
    ready-in-minutes: s32,
    servings: f64,
    source-url: string,
    title: string
  }

  record generate-meal-plan-response-nutrients {
    calories: f64,
    carbohydrates: f64,
    fat: f64,
    protein: f64
  }

  record clear-meal-plan-day-response {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record add-to-meal-plan-body-value {
    ingredients: list<add-to-meal-plan-body-value-ingredients-item>
  }

  record add-to-meal-plan-body-value-ingredients-item {
    name: string
  }

  record add-to-meal-plan-response {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record delete-from-meal-plan-response {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record get-shopping-list-response {
    aisles: list<get-shopping-list-response-aisles-item>,
    cost: f64,
    end-date: f64,
    start-date: f64
  }

  record get-shopping-list-response-aisles-item {
    aisle: string,
    items: option<list<get-shopping-list-response-aisles-item-items-item>>
  }

  record get-shopping-list-response-aisles-item-items-item {
    aisle: string,
    cost: f64,
    id: s32,
    ingredient-id: s32,
    measures: option<get-shopping-list-response-aisles-item-items-item-measures>,
    name: string,
    pantry-item: bool
  }

  record get-shopping-list-response-aisles-item-items-item-measures {
    metric: get-shopping-list-response-aisles-item-items-item-measures-metric,
    original: get-shopping-list-response-aisles-item-items-item-measures-original,
    us: get-shopping-list-response-aisles-item-items-item-measures-us
  }

  record get-shopping-list-response-aisles-item-items-item-measures-metric {
    amount: f64,
    unit: string
  }

  record get-shopping-list-response-aisles-item-items-item-measures-original {
    amount: f64,
    unit: string
  }

  record get-shopping-list-response-aisles-item-items-item-measures-us {
    amount: f64,
    unit: string
  }

  record add-to-shopping-list-response {
    aisles: list<add-to-shopping-list-response-aisles-item>,
    cost: f64,
    end-date: f64,
    start-date: f64
  }

  record add-to-shopping-list-response-aisles-item {
    aisle: string,
    items: option<list<add-to-shopping-list-response-aisles-item-items-item>>
  }

  record add-to-shopping-list-response-aisles-item-items-item {
    aisle: string,
    cost: f64,
    id: s32,
    ingredient-id: s32,
    measures: option<add-to-shopping-list-response-aisles-item-items-item-measures>,
    name: string,
    pantry-item: bool
  }

  record add-to-shopping-list-response-aisles-item-items-item-measures {
    metric: add-to-shopping-list-response-aisles-item-items-item-measures-metric,
    original: add-to-shopping-list-response-aisles-item-items-item-measures-original,
    us: add-to-shopping-list-response-aisles-item-items-item-measures-us
  }

  record add-to-shopping-list-response-aisles-item-items-item-measures-metric {
    amount: f64,
    unit: string
  }

  record add-to-shopping-list-response-aisles-item-items-item-measures-original {
    amount: f64,
    unit: string
  }

  record add-to-shopping-list-response-aisles-item-items-item-measures-us {
    amount: f64,
    unit: string
  }

  record delete-from-shopping-list-response {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record generate-shopping-list-response {
    aisles: list<generate-shopping-list-response-aisles-item>,
    cost: f64,
    end-date: f64,
    start-date: f64
  }

  record generate-shopping-list-response-aisles-item {
    aisle: string,
    items: option<list<generate-shopping-list-response-aisles-item-items-item>>
  }

  record generate-shopping-list-response-aisles-item-items-item {
    aisle: string,
    cost: f64,
    id: s32,
    ingredient-id: s32,
    measures: option<generate-shopping-list-response-aisles-item-items-item-measures>,
    name: string,
    pantry-item: bool
  }

  record generate-shopping-list-response-aisles-item-items-item-measures {
    metric: generate-shopping-list-response-aisles-item-items-item-measures-metric,
    original: generate-shopping-list-response-aisles-item-items-item-measures-original,
    us: generate-shopping-list-response-aisles-item-items-item-measures-us
  }

  record generate-shopping-list-response-aisles-item-items-item-measures-metric {
    amount: f64,
    unit: string
  }

  record generate-shopping-list-response-aisles-item-items-item-measures-original {
    amount: f64,
    unit: string
  }

  record generate-shopping-list-response-aisles-item-items-item-measures-us {
    amount: f64,
    unit: string
  }

  record get-meal-plan-templates-response {
    templates: list<get-meal-plan-templates-response-templates-item>
  }

  record get-meal-plan-templates-response-templates-item {
    id: s32,
    name: string
  }

  record add-meal-plan-template-response {
    items: list<add-meal-plan-template-response-items-item>,
    name: string,
    publish-as-public: bool
  }

  record add-meal-plan-template-response-items-item {
    day: s32,
    position: s32,
    slot: s32,
    type-op: string,
    value: option<add-meal-plan-template-response-items-item-value>
  }

  record add-meal-plan-template-response-items-item-value {
    id: option<s32>,
    image-type: option<string>,
    servings: option<f64>,
    title: option<string>
  }

  record get-meal-plan-template-response {
    days: list<get-meal-plan-template-response-days-item>,
    id: s32,
    name: string
  }

  record get-meal-plan-template-response-days-item {
    day: string,
    items: option<list<get-meal-plan-template-response-days-item-items-item>>,
    nutrition-summary: option<get-meal-plan-template-response-days-item-nutrition-summary>,
    nutrition-summary-breakfast: option<get-meal-plan-template-response-days-item-nutrition-summary-breakfast>,
    nutrition-summary-dinner: option<get-meal-plan-template-response-days-item-nutrition-summary-dinner>,
    nutrition-summary-lunch: option<get-meal-plan-template-response-days-item-nutrition-summary-lunch>
  }

  record get-meal-plan-template-response-days-item-items-item {
    id: s32,
    position: s32,
    slot: s32,
    type-op: string,
    value: option<get-meal-plan-template-response-days-item-items-item-value>
  }

  record get-meal-plan-template-response-days-item-items-item-value {
    id: f64,
    image-type: string,
    title: string
  }

  record get-meal-plan-template-response-days-item-nutrition-summary {
    nutrients: list<get-meal-plan-template-response-days-item-nutrition-summary-nutrients-item>
  }

  record get-meal-plan-template-response-days-item-nutrition-summary-nutrients-item {
    amount: f64,
    name: string,
    percent-daily-needs: f64,
    unit: string
  }

  record get-meal-plan-template-response-days-item-nutrition-summary-breakfast {
    nutrients: list<get-meal-plan-template-response-days-item-nutrition-summary-breakfast-nutrients-item>
  }

  record get-meal-plan-template-response-days-item-nutrition-summary-breakfast-nutrients-item {
    amount: f64,
    name: string,
    percent-daily-needs: f64,
    unit: string
  }

  record get-meal-plan-template-response-days-item-nutrition-summary-dinner {
    nutrients: list<get-meal-plan-template-response-days-item-nutrition-summary-dinner-nutrients-item>
  }

  record get-meal-plan-template-response-days-item-nutrition-summary-dinner-nutrients-item {
    amount: f64,
    name: string,
    percent-daily-needs: f64,
    unit: string
  }

  record get-meal-plan-template-response-days-item-nutrition-summary-lunch {
    nutrients: list<get-meal-plan-template-response-days-item-nutrition-summary-lunch-nutrients-item>
  }

  record get-meal-plan-template-response-days-item-nutrition-summary-lunch-nutrients-item {
    amount: f64,
    name: string,
    percent-daily-needs: f64,
    unit: string
  }

  record delete-meal-plan-template-response {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record get-meal-plan-week-response {
    days: list<get-meal-plan-week-response-days-item>
  }

  record get-meal-plan-week-response-days-item {
    date: f64,
    day: string,
    items: option<list<get-meal-plan-week-response-days-item-items-item>>,
    nutrition-summary: option<get-meal-plan-week-response-days-item-nutrition-summary>,
    nutrition-summary-breakfast: option<get-meal-plan-week-response-days-item-nutrition-summary-breakfast>,
    nutrition-summary-dinner: option<get-meal-plan-week-response-days-item-nutrition-summary-dinner>,
    nutrition-summary-lunch: option<get-meal-plan-week-response-days-item-nutrition-summary-lunch>
  }

  record get-meal-plan-week-response-days-item-items-item {
    id: s32,
    position: s32,
    slot: s32,
    type-op: string,
    value: option<get-meal-plan-week-response-days-item-items-item-value>
  }

  record get-meal-plan-week-response-days-item-items-item-value {
    id: f64,
    image-type: string,
    servings: f64,
    title: string
  }

  record get-meal-plan-week-response-days-item-nutrition-summary {
    nutrients: list<get-meal-plan-week-response-days-item-nutrition-summary-nutrients-item>
  }

  record get-meal-plan-week-response-days-item-nutrition-summary-nutrients-item {
    amount: f64,
    name: string,
    percent-daily-needs: f64,
    unit: string
  }

  record get-meal-plan-week-response-days-item-nutrition-summary-breakfast {
    nutrients: list<get-meal-plan-week-response-days-item-nutrition-summary-breakfast-nutrients-item>
  }

  record get-meal-plan-week-response-days-item-nutrition-summary-breakfast-nutrients-item {
    amount: f64,
    name: string,
    percent-daily-needs: f64,
    unit: string
  }

  record get-meal-plan-week-response-days-item-nutrition-summary-dinner {
    nutrients: list<get-meal-plan-week-response-days-item-nutrition-summary-dinner-nutrients-item>
  }

  record get-meal-plan-week-response-days-item-nutrition-summary-dinner-nutrients-item {
    amount: f64,
    name: string,
    percent-daily-needs: f64,
    unit: string
  }

  record get-meal-plan-week-response-days-item-nutrition-summary-lunch {
    nutrients: list<get-meal-plan-week-response-days-item-nutrition-summary-lunch-nutrients-item>
  }

  record get-meal-plan-week-response-days-item-nutrition-summary-lunch-nutrients-item {
    amount: f64,
    name: string,
    percent-daily-needs: f64,
    unit: string
  }

  record connect-user-response {
    hash: string,
    username: string
  }

  variant generate-meal-plan-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant clear-meal-plan-day-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant add-to-meal-plan-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant delete-from-meal-plan-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-shopping-list-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant add-to-shopping-list-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant delete-from-shopping-list-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant generate-shopping-list-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-meal-plan-templates-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant add-meal-plan-template-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-meal-plan-template-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant delete-meal-plan-template-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant get-meal-plan-week-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  variant connect-user-error {
    unauthorized(string),
    forbidden(string),
    not-found(string),
    other(string)
  }

  /// Generate Meal Plan
  record generate-meal-plan-params {
    /// Either for one "day" or an entire "week".
    time-frame: option<string>,
    /// What is the caloric target for one day? The meal plan generator will try to get as close as possible to that goal.
    target-calories: option<f64>,
    /// Enter a diet that the meal plan has to adhere to. See a full list of supported diets.
    diet: option<string>,
    /// A comma-separated list of allergens or ingredients that must be excluded.
    exclude: option<string>
  }
  generate-meal-plan: func(params: generate-meal-plan-params) -> result<generate-meal-plan-response, generate-meal-plan-error>;

  /// Clear Meal Plan Day
  record clear-meal-plan-day-params {
    /// The username.
    username: string,
    /// The date in the format yyyy-mm-dd.
    date: string,
    /// The private hash for the username.
    hash: string,
    /// The date in the format yyyy-mm-dd.
    date-v2: string,
    /// The private hash for the username.
    hash-v2: string,
    /// The username.
    username-v2: string
  }
  clear-meal-plan-day: func(params: clear-meal-plan-day-params) -> result<clear-meal-plan-day-response, clear-meal-plan-day-error>;

  /// Add to Meal Plan
  record add-to-meal-plan-params {
    /// The username.
    username: string,
    /// The private hash for the username.
    hash: string,
    date: f64,
    position: s32,
    slot: s32,
    type-op: string,
    value: add-to-meal-plan-body-value
  }
  add-to-meal-plan: func(params: add-to-meal-plan-params) -> result<add-to-meal-plan-response, add-to-meal-plan-error>;

  /// Delete from Meal Plan
  record delete-from-meal-plan-params {
    /// The username.
    username: string,
    /// The shopping list item id.
    id: string,
    /// The private hash for the username.
    hash: string,
    /// The private hash for the username.
    hash-v2: string,
    /// The shopping list item id.
    id-v2: f64,
    /// The username.
    username-v2: string
  }
  delete-from-meal-plan: func(params: delete-from-meal-plan-params) -> result<delete-from-meal-plan-response, delete-from-meal-plan-error>;

  /// Get Shopping List
  record get-shopping-list-params {
    /// The username.
    username: string,
    /// The private hash for the username.
    hash: string
  }
  get-shopping-list: func(params: get-shopping-list-params) -> result<get-shopping-list-response, get-shopping-list-error>;

  /// Add to Shopping List
  record add-to-shopping-list-params {
    /// The username.
    username: string,
    /// The private hash for the username.
    hash: string,
    aisle: string,
    item: string,
    parse: bool
  }
  add-to-shopping-list: func(params: add-to-shopping-list-params) -> result<add-to-shopping-list-response, add-to-shopping-list-error>;

  /// Delete from Shopping List
  record delete-from-shopping-list-params {
    /// The username.
    username: string,
    /// The shopping list item id.
    id: string,
    /// The private hash for the username.
    hash: string,
    /// The private hash for the username.
    hash-v2: string,
    /// The shopping list item id.
    id-v2: f64,
    /// The username.
    username-v2: string
  }
  delete-from-shopping-list: func(params: delete-from-shopping-list-params) -> result<delete-from-shopping-list-response, delete-from-shopping-list-error>;

  /// Generate Shopping List
  record generate-shopping-list-params {
    /// The username.
    username: string,
    /// The start date in the format yyyy-mm-dd.
    start-date: string,
    /// The end date in the format yyyy-mm-dd.
    end-date: string,
    /// The private hash for the username.
    hash: string,
    /// The end date in the format yyyy-mm-dd.
    end-date-v2: string,
    /// The private hash for the username.
    hash-v2: string,
    /// The start date in the format yyyy-mm-dd.
    start-date-v2: string,
    /// The username.
    username-v2: string
  }
  generate-shopping-list: func(params: generate-shopping-list-params) -> result<generate-shopping-list-response, generate-shopping-list-error>;

  /// Get Meal Plan Templates
  record get-meal-plan-templates-params {
    /// The username.
    username: string,
    /// The private hash for the username.
    hash: string
  }
  get-meal-plan-templates: func(params: get-meal-plan-templates-params) -> result<get-meal-plan-templates-response, get-meal-plan-templates-error>;

  /// Add Meal Plan Template
  record add-meal-plan-template-params {
    /// The username.
    username: string,
    /// The private hash for the username.
    hash: string,
    /// The private hash for the username.
    hash-v2: string,
    /// The username.
    username-v2: string
  }
  add-meal-plan-template: func(params: add-meal-plan-template-params) -> result<add-meal-plan-template-response, add-meal-plan-template-error>;

  /// Get Meal Plan Template
  record get-meal-plan-template-params {
    /// The username.
    username: string,
    /// The shopping list item id.
    id: string,
    /// The private hash for the username.
    hash: string
  }
  get-meal-plan-template: func(params: get-meal-plan-template-params) -> result<get-meal-plan-template-response, get-meal-plan-template-error>;

  /// Delete Meal Plan Template
  record delete-meal-plan-template-params {
    /// The username.
    username: string,
    /// The shopping list item id.
    id: string,
    /// The private hash for the username.
    hash: string,
    /// The private hash for the username.
    hash-v2: string,
    /// The shopping list item id.
    id-v2: f64,
    /// The username.
    username-v2: string
  }
  delete-meal-plan-template: func(params: delete-meal-plan-template-params) -> result<delete-meal-plan-template-response, delete-meal-plan-template-error>;

  /// Get Meal Plan Week
  record get-meal-plan-week-params {
    /// The username.
    username: string,
    /// The start date of the meal planned week in the format yyyy-mm-dd.
    start-date: string,
    /// The private hash for the username.
    hash: string
  }
  get-meal-plan-week: func(params: get-meal-plan-week-params) -> result<get-meal-plan-week-response, get-meal-plan-week-error>;

  /// Connect User
  record connect-user-params {
    email: string,
    first-name: string,
    last-name: string,
    username: string
  }
  connect-user: func(params: connect-user-params) -> result<connect-user-response, connect-user-error>;

}
/// The component world: imports the secrets host capability
/// the runtime uses and exports the generated interfaces. The HTTP host
/// capability is imported by the `wstd` crate's bindings at build time.
world client {
  import wasmcloud:secrets/store@1.0.0;
  import wasmcloud:secrets/reveal@1.0.0;
  export misc;
  export ingredients;
  export recipes;
  export menu-items;
  export products;
  export food;
  export wine;
  export meal-planning;
}
