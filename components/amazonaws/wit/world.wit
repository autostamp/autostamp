/// ## Authentication
///
/// Reads credentials from the host via `wasmcloud:secrets`; operations never take a token.
/// Provision these named secrets:
///
/// | Secret name | Applied to each request as |
/// | --- | --- |
/// | `hmac` | header `Authorization` |
package autostamp:amazonaws@0.5.0;

interface aws-migration-hub {
  enum associate-created-artifact-x-amz-target-enum {
    aws-migration-hub-associate-created-artifact,
  }

  enum associate-discovered-resource-x-amz-target-enum {
    aws-migration-hub-associate-discovered-resource,
  }

  enum create-progress-update-stream-x-amz-target-enum {
    aws-migration-hub-create-progress-update-stream,
  }

  enum delete-progress-update-stream-x-amz-target-enum {
    aws-migration-hub-delete-progress-update-stream,
  }

  enum describe-application-state-x-amz-target-enum {
    aws-migration-hub-describe-application-state,
  }

  enum describe-migration-task-x-amz-target-enum {
    aws-migration-hub-describe-migration-task,
  }

  enum disassociate-created-artifact-x-amz-target-enum {
    aws-migration-hub-disassociate-created-artifact,
  }

  enum disassociate-discovered-resource-x-amz-target-enum {
    aws-migration-hub-disassociate-discovered-resource,
  }

  enum import-migration-task-x-amz-target-enum {
    aws-migration-hub-import-migration-task,
  }

  enum list-application-states-x-amz-target-enum {
    aws-migration-hub-list-application-states,
  }

  enum list-created-artifacts-x-amz-target-enum {
    aws-migration-hub-list-created-artifacts,
  }

  enum list-discovered-resources-x-amz-target-enum {
    aws-migration-hub-list-discovered-resources,
  }

  enum list-migration-tasks-x-amz-target-enum {
    aws-migration-hub-list-migration-tasks,
  }

  enum list-progress-update-streams-x-amz-target-enum {
    aws-migration-hub-list-progress-update-streams,
  }

  enum notify-application-state-x-amz-target-enum {
    aws-migration-hub-notify-application-state,
  }

  enum notify-migration-task-state-x-amz-target-enum {
    aws-migration-hub-notify-migration-task-state,
  }

  enum put-resource-attributes-x-amz-target-enum {
    aws-migration-hub-put-resource-attributes,
  }

  record associate-created-artifact-request-progress-update-stream {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record associate-created-artifact-request-migration-task-name {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record associate-created-artifact-request-created-artifact {
    name: associate-created-artifact-request-created-artifact-name,
    description: option<associate-created-artifact-request-created-artifact-description>
  }

  record associate-created-artifact-request-created-artifact-name {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record associate-created-artifact-request-created-artifact-description {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record associate-created-artifact-request-dry-run {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record associate-created-artifact-result {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record associate-discovered-resource-request-progress-update-stream {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record associate-discovered-resource-request-migration-task-name {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record associate-discovered-resource-request-discovered-resource {
    configuration-id: associate-discovered-resource-request-discovered-resource-configuration-id,
    description: option<associate-discovered-resource-request-discovered-resource-description>
  }

  record associate-discovered-resource-request-discovered-resource-configuration-id {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record associate-discovered-resource-request-discovered-resource-description {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record associate-discovered-resource-request-dry-run {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record associate-discovered-resource-result {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record create-progress-update-stream-request-progress-update-stream-name {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record create-progress-update-stream-request-dry-run {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record create-progress-update-stream-result {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record delete-progress-update-stream-request-progress-update-stream-name {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record delete-progress-update-stream-request-dry-run {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record delete-progress-update-stream-result {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record describe-application-state-request-application-id {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record describe-application-state-result {
    application-status: option<describe-application-state-result-application-status>,
    last-updated-time: option<describe-application-state-result-last-updated-time>
  }

  record describe-application-state-result-application-status {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record describe-application-state-result-last-updated-time {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record describe-migration-task-request-progress-update-stream {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record describe-migration-task-request-migration-task-name {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record describe-migration-task-result {
    migration-task: option<describe-migration-task-result-migration-task>
  }

  record describe-migration-task-result-migration-task {
    progress-update-stream: option<describe-migration-task-result-migration-task-progress-update-stream>,
    migration-task-name: option<describe-migration-task-result-migration-task-migration-task-name>,
    task: option<describe-migration-task-result-migration-task-task>,
    update-date-time: option<describe-migration-task-result-migration-task-update-date-time>,
    resource-attribute-list: option<describe-migration-task-result-migration-task-resource-attribute-list>
  }

  record describe-migration-task-result-migration-task-progress-update-stream {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record describe-migration-task-result-migration-task-migration-task-name {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record describe-migration-task-result-migration-task-task {
    status: describe-migration-task-result-migration-task-task-status,
    status-detail: option<describe-migration-task-result-migration-task-task-status-detail>,
    progress-percent: option<describe-migration-task-result-migration-task-task-progress-percent>
  }

  record describe-migration-task-result-migration-task-task-status {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record describe-migration-task-result-migration-task-task-status-detail {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record describe-migration-task-result-migration-task-task-progress-percent {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record describe-migration-task-result-migration-task-update-date-time {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record describe-migration-task-result-migration-task-resource-attribute-list {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record disassociate-created-artifact-request-progress-update-stream {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record disassociate-created-artifact-request-migration-task-name {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record disassociate-created-artifact-request-created-artifact-name {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record disassociate-created-artifact-request-dry-run {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record disassociate-created-artifact-result {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record disassociate-discovered-resource-request-progress-update-stream {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record disassociate-discovered-resource-request-migration-task-name {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record disassociate-discovered-resource-request-configuration-id {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record disassociate-discovered-resource-request-dry-run {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record disassociate-discovered-resource-result {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record import-migration-task-request-progress-update-stream {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record import-migration-task-request-migration-task-name {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record import-migration-task-request-dry-run {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record import-migration-task-result {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-application-states-request-application-ids {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-application-states-request-next-token {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-application-states-request-max-results {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-application-states-result {
    application-state-list: option<list-application-states-result-application-state-list>,
    next-token: option<list-application-states-result-next-token>
  }

  record list-application-states-result-application-state-list {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-application-states-result-next-token {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-created-artifacts-request-progress-update-stream {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-created-artifacts-request-migration-task-name {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-created-artifacts-request-next-token {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-created-artifacts-request-max-results {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-created-artifacts-result {
    next-token: option<list-created-artifacts-result-next-token>,
    created-artifact-list: option<list-created-artifacts-result-created-artifact-list>
  }

  record list-created-artifacts-result-next-token {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-created-artifacts-result-created-artifact-list {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-discovered-resources-request-progress-update-stream {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-discovered-resources-request-migration-task-name {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-discovered-resources-request-next-token {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-discovered-resources-request-max-results {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-discovered-resources-result {
    next-token: option<list-discovered-resources-result-next-token>,
    discovered-resource-list: option<list-discovered-resources-result-discovered-resource-list>
  }

  record list-discovered-resources-result-next-token {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-discovered-resources-result-discovered-resource-list {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-migration-tasks-request-next-token {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-migration-tasks-request-max-results {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-migration-tasks-request-resource-name {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-migration-tasks-result {
    next-token: option<list-migration-tasks-result-next-token>,
    migration-task-summary-list: option<list-migration-tasks-result-migration-task-summary-list>
  }

  record list-migration-tasks-result-next-token {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-migration-tasks-result-migration-task-summary-list {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-progress-update-streams-request-next-token {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-progress-update-streams-request-max-results {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-progress-update-streams-result {
    progress-update-stream-summary-list: option<list-progress-update-streams-result-progress-update-stream-summary-list>,
    next-token: option<list-progress-update-streams-result-next-token>
  }

  record list-progress-update-streams-result-progress-update-stream-summary-list {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record list-progress-update-streams-result-next-token {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record notify-application-state-request-application-id {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record notify-application-state-request-status {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record notify-application-state-request-update-date-time {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record notify-application-state-request-dry-run {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record notify-application-state-result {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record notify-migration-task-state-request-progress-update-stream {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record notify-migration-task-state-request-migration-task-name {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record notify-migration-task-state-request-task {
    status: notify-migration-task-state-request-task-status,
    status-detail: option<notify-migration-task-state-request-task-status-detail>,
    progress-percent: option<notify-migration-task-state-request-task-progress-percent>
  }

  record notify-migration-task-state-request-task-status {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record notify-migration-task-state-request-task-status-detail {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record notify-migration-task-state-request-task-progress-percent {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record notify-migration-task-state-request-update-date-time {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record notify-migration-task-state-request-next-update-seconds {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record notify-migration-task-state-request-dry-run {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record notify-migration-task-state-result {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record put-resource-attributes-request-progress-update-stream {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record put-resource-attributes-request-migration-task-name {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record put-resource-attributes-request-resource-attribute-list {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record put-resource-attributes-request-dry-run {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  record put-resource-attributes-result {
    /// JSON-encoded free-form object payload
    data: option<string>
  }

  variant associate-created-artifact-error {
    status-v480(string),
    status-v481(string),
    status-v482(string),
    status-v483(string),
    status-v484(string),
    status-v485(string),
    status-v486(string),
    status-v487(string),
    status-v488(string),
    other(string)
  }

  variant associate-discovered-resource-error {
    status-v480(string),
    status-v481(string),
    status-v482(string),
    status-v483(string),
    status-v484(string),
    status-v485(string),
    status-v486(string),
    status-v487(string),
    status-v488(string),
    status-v489(string),
    other(string)
  }

  variant create-progress-update-stream-error {
    status-v480(string),
    status-v481(string),
    status-v482(string),
    status-v483(string),
    status-v484(string),
    status-v485(string),
    status-v486(string),
    status-v487(string),
    other(string)
  }

  variant delete-progress-update-stream-error {
    status-v480(string),
    status-v481(string),
    status-v482(string),
    status-v483(string),
    status-v484(string),
    status-v485(string),
    status-v486(string),
    status-v487(string),
    status-v488(string),
    other(string)
  }

  variant describe-application-state-error {
    status-v480(string),
    status-v481(string),
    status-v482(string),
    status-v483(string),
    status-v484(string),
    status-v485(string),
    status-v486(string),
    status-v487(string),
    other(string)
  }

  variant describe-migration-task-error {
    status-v480(string),
    status-v481(string),
    status-v482(string),
    status-v483(string),
    status-v484(string),
    status-v485(string),
    status-v486(string),
    other(string)
  }

  variant disassociate-created-artifact-error {
    status-v480(string),
    status-v481(string),
    status-v482(string),
    status-v483(string),
    status-v484(string),
    status-v485(string),
    status-v486(string),
    status-v487(string),
    status-v488(string),
    other(string)
  }

  variant disassociate-discovered-resource-error {
    status-v480(string),
    status-v481(string),
    status-v482(string),
    status-v483(string),
    status-v484(string),
    status-v485(string),
    status-v486(string),
    status-v487(string),
    status-v488(string),
    other(string)
  }

  variant import-migration-task-error {
    status-v480(string),
    status-v481(string),
    status-v482(string),
    status-v483(string),
    status-v484(string),
    status-v485(string),
    status-v486(string),
    status-v487(string),
    status-v488(string),
    other(string)
  }

  variant list-application-states-error {
    status-v480(string),
    status-v481(string),
    status-v482(string),
    status-v483(string),
    status-v484(string),
    status-v485(string),
    other(string)
  }

  variant list-created-artifacts-error {
    status-v480(string),
    status-v481(string),
    status-v482(string),
    status-v483(string),
    status-v484(string),
    status-v485(string),
    status-v486(string),
    other(string)
  }

  variant list-discovered-resources-error {
    status-v480(string),
    status-v481(string),
    status-v482(string),
    status-v483(string),
    status-v484(string),
    status-v485(string),
    status-v486(string),
    other(string)
  }

  variant list-migration-tasks-error {
    status-v480(string),
    status-v481(string),
    status-v482(string),
    status-v483(string),
    status-v484(string),
    status-v485(string),
    status-v486(string),
    status-v487(string),
    other(string)
  }

  variant list-progress-update-streams-error {
    status-v480(string),
    status-v481(string),
    status-v482(string),
    status-v483(string),
    status-v484(string),
    status-v485(string),
    other(string)
  }

  variant notify-application-state-error {
    status-v480(string),
    status-v481(string),
    status-v482(string),
    status-v483(string),
    status-v484(string),
    status-v485(string),
    status-v486(string),
    status-v487(string),
    status-v488(string),
    status-v489(string),
    other(string)
  }

  variant notify-migration-task-state-error {
    status-v480(string),
    status-v481(string),
    status-v482(string),
    status-v483(string),
    status-v484(string),
    status-v485(string),
    status-v486(string),
    status-v487(string),
    status-v488(string),
    other(string)
  }

  variant put-resource-attributes-error {
    status-v480(string),
    status-v481(string),
    status-v482(string),
    status-v483(string),
    status-v484(string),
    status-v485(string),
    status-v486(string),
    status-v487(string),
    status-v488(string),
    other(string)
  }

  record associate-created-artifact-params {
    x-amz-content-sha256: option<string>,
    x-amz-date: option<string>,
    x-amz-algorithm: option<string>,
    x-amz-credential: option<string>,
    x-amz-security-token: option<string>,
    x-amz-signature: option<string>,
    x-amz-signed-headers: option<string>,
    x-amz-target: associate-created-artifact-x-amz-target-enum,
    progress-update-stream: associate-created-artifact-request-progress-update-stream,
    migration-task-name: associate-created-artifact-request-migration-task-name,
    created-artifact: associate-created-artifact-request-created-artifact,
    dry-run: option<associate-created-artifact-request-dry-run>
  }
  associate-created-artifact: func(params: associate-created-artifact-params) -> result<associate-created-artifact-result, associate-created-artifact-error>;

  record associate-discovered-resource-params {
    x-amz-content-sha256: option<string>,
    x-amz-date: option<string>,
    x-amz-algorithm: option<string>,
    x-amz-credential: option<string>,
    x-amz-security-token: option<string>,
    x-amz-signature: option<string>,
    x-amz-signed-headers: option<string>,
    x-amz-target: associate-discovered-resource-x-amz-target-enum,
    progress-update-stream: associate-discovered-resource-request-progress-update-stream,
    migration-task-name: associate-discovered-resource-request-migration-task-name,
    discovered-resource: associate-discovered-resource-request-discovered-resource,
    dry-run: option<associate-discovered-resource-request-dry-run>
  }
  associate-discovered-resource: func(params: associate-discovered-resource-params) -> result<associate-discovered-resource-result, associate-discovered-resource-error>;

  record create-progress-update-stream-params {
    x-amz-content-sha256: option<string>,
    x-amz-date: option<string>,
    x-amz-algorithm: option<string>,
    x-amz-credential: option<string>,
    x-amz-security-token: option<string>,
    x-amz-signature: option<string>,
    x-amz-signed-headers: option<string>,
    x-amz-target: create-progress-update-stream-x-amz-target-enum,
    progress-update-stream-name: create-progress-update-stream-request-progress-update-stream-name,
    dry-run: option<create-progress-update-stream-request-dry-run>
  }
  create-progress-update-stream: func(params: create-progress-update-stream-params) -> result<create-progress-update-stream-result, create-progress-update-stream-error>;

  record delete-progress-update-stream-params {
    x-amz-content-sha256: option<string>,
    x-amz-date: option<string>,
    x-amz-algorithm: option<string>,
    x-amz-credential: option<string>,
    x-amz-security-token: option<string>,
    x-amz-signature: option<string>,
    x-amz-signed-headers: option<string>,
    x-amz-target: delete-progress-update-stream-x-amz-target-enum,
    progress-update-stream-name: delete-progress-update-stream-request-progress-update-stream-name,
    dry-run: option<delete-progress-update-stream-request-dry-run>
  }
  delete-progress-update-stream: func(params: delete-progress-update-stream-params) -> result<delete-progress-update-stream-result, delete-progress-update-stream-error>;

  record describe-application-state-params {
    x-amz-content-sha256: option<string>,
    x-amz-date: option<string>,
    x-amz-algorithm: option<string>,
    x-amz-credential: option<string>,
    x-amz-security-token: option<string>,
    x-amz-signature: option<string>,
    x-amz-signed-headers: option<string>,
    x-amz-target: describe-application-state-x-amz-target-enum,
    application-id: describe-application-state-request-application-id
  }
  describe-application-state: func(params: describe-application-state-params) -> result<describe-application-state-result, describe-application-state-error>;

  record describe-migration-task-params {
    x-amz-content-sha256: option<string>,
    x-amz-date: option<string>,
    x-amz-algorithm: option<string>,
    x-amz-credential: option<string>,
    x-amz-security-token: option<string>,
    x-amz-signature: option<string>,
    x-amz-signed-headers: option<string>,
    x-amz-target: describe-migration-task-x-amz-target-enum,
    progress-update-stream: describe-migration-task-request-progress-update-stream,
    migration-task-name: describe-migration-task-request-migration-task-name
  }
  describe-migration-task: func(params: describe-migration-task-params) -> result<describe-migration-task-result, describe-migration-task-error>;

  record disassociate-created-artifact-params {
    x-amz-content-sha256: option<string>,
    x-amz-date: option<string>,
    x-amz-algorithm: option<string>,
    x-amz-credential: option<string>,
    x-amz-security-token: option<string>,
    x-amz-signature: option<string>,
    x-amz-signed-headers: option<string>,
    x-amz-target: disassociate-created-artifact-x-amz-target-enum,
    progress-update-stream: disassociate-created-artifact-request-progress-update-stream,
    migration-task-name: disassociate-created-artifact-request-migration-task-name,
    created-artifact-name: disassociate-created-artifact-request-created-artifact-name,
    dry-run: option<disassociate-created-artifact-request-dry-run>
  }
  disassociate-created-artifact: func(params: disassociate-created-artifact-params) -> result<disassociate-created-artifact-result, disassociate-created-artifact-error>;

  record disassociate-discovered-resource-params {
    x-amz-content-sha256: option<string>,
    x-amz-date: option<string>,
    x-amz-algorithm: option<string>,
    x-amz-credential: option<string>,
    x-amz-security-token: option<string>,
    x-amz-signature: option<string>,
    x-amz-signed-headers: option<string>,
    x-amz-target: disassociate-discovered-resource-x-amz-target-enum,
    progress-update-stream: disassociate-discovered-resource-request-progress-update-stream,
    migration-task-name: disassociate-discovered-resource-request-migration-task-name,
    configuration-id: disassociate-discovered-resource-request-configuration-id,
    dry-run: option<disassociate-discovered-resource-request-dry-run>
  }
  disassociate-discovered-resource: func(params: disassociate-discovered-resource-params) -> result<disassociate-discovered-resource-result, disassociate-discovered-resource-error>;

  record import-migration-task-params {
    x-amz-content-sha256: option<string>,
    x-amz-date: option<string>,
    x-amz-algorithm: option<string>,
    x-amz-credential: option<string>,
    x-amz-security-token: option<string>,
    x-amz-signature: option<string>,
    x-amz-signed-headers: option<string>,
    x-amz-target: import-migration-task-x-amz-target-enum,
    progress-update-stream: import-migration-task-request-progress-update-stream,
    migration-task-name: import-migration-task-request-migration-task-name,
    dry-run: option<import-migration-task-request-dry-run>
  }
  import-migration-task: func(params: import-migration-task-params) -> result<import-migration-task-result, import-migration-task-error>;

  record list-application-states-params {
    x-amz-content-sha256: option<string>,
    x-amz-date: option<string>,
    x-amz-algorithm: option<string>,
    x-amz-credential: option<string>,
    x-amz-security-token: option<string>,
    x-amz-signature: option<string>,
    x-amz-signed-headers: option<string>,
    /// Pagination limit
    max-results: option<string>,
    /// Pagination token
    next-token: option<string>,
    x-amz-target: list-application-states-x-amz-target-enum,
    application-ids: option<list-application-states-request-application-ids>,
    next-token-v2: option<list-application-states-request-next-token>,
    max-results-v2: option<list-application-states-request-max-results>
  }
  list-application-states: func(params: list-application-states-params) -> result<list-application-states-result, list-application-states-error>;

  record list-created-artifacts-params {
    x-amz-content-sha256: option<string>,
    x-amz-date: option<string>,
    x-amz-algorithm: option<string>,
    x-amz-credential: option<string>,
    x-amz-security-token: option<string>,
    x-amz-signature: option<string>,
    x-amz-signed-headers: option<string>,
    /// Pagination limit
    max-results: option<string>,
    /// Pagination token
    next-token: option<string>,
    x-amz-target: list-created-artifacts-x-amz-target-enum,
    progress-update-stream: list-created-artifacts-request-progress-update-stream,
    migration-task-name: list-created-artifacts-request-migration-task-name,
    next-token-v2: option<list-created-artifacts-request-next-token>,
    max-results-v2: option<list-created-artifacts-request-max-results>
  }
  list-created-artifacts: func(params: list-created-artifacts-params) -> result<list-created-artifacts-result, list-created-artifacts-error>;

  record list-discovered-resources-params {
    x-amz-content-sha256: option<string>,
    x-amz-date: option<string>,
    x-amz-algorithm: option<string>,
    x-amz-credential: option<string>,
    x-amz-security-token: option<string>,
    x-amz-signature: option<string>,
    x-amz-signed-headers: option<string>,
    /// Pagination limit
    max-results: option<string>,
    /// Pagination token
    next-token: option<string>,
    x-amz-target: list-discovered-resources-x-amz-target-enum,
    progress-update-stream: list-discovered-resources-request-progress-update-stream,
    migration-task-name: list-discovered-resources-request-migration-task-name,
    next-token-v2: option<list-discovered-resources-request-next-token>,
    max-results-v2: option<list-discovered-resources-request-max-results>
  }
  list-discovered-resources: func(params: list-discovered-resources-params) -> result<list-discovered-resources-result, list-discovered-resources-error>;

  record list-migration-tasks-params {
    x-amz-content-sha256: option<string>,
    x-amz-date: option<string>,
    x-amz-algorithm: option<string>,
    x-amz-credential: option<string>,
    x-amz-security-token: option<string>,
    x-amz-signature: option<string>,
    x-amz-signed-headers: option<string>,
    /// Pagination limit
    max-results: option<string>,
    /// Pagination token
    next-token: option<string>,
    x-amz-target: list-migration-tasks-x-amz-target-enum,
    next-token-v2: option<list-migration-tasks-request-next-token>,
    max-results-v2: option<list-migration-tasks-request-max-results>,
    resource-name: option<list-migration-tasks-request-resource-name>
  }
  list-migration-tasks: func(params: list-migration-tasks-params) -> result<list-migration-tasks-result, list-migration-tasks-error>;

  record list-progress-update-streams-params {
    x-amz-content-sha256: option<string>,
    x-amz-date: option<string>,
    x-amz-algorithm: option<string>,
    x-amz-credential: option<string>,
    x-amz-security-token: option<string>,
    x-amz-signature: option<string>,
    x-amz-signed-headers: option<string>,
    /// Pagination limit
    max-results: option<string>,
    /// Pagination token
    next-token: option<string>,
    x-amz-target: list-progress-update-streams-x-amz-target-enum,
    next-token-v2: option<list-progress-update-streams-request-next-token>,
    max-results-v2: option<list-progress-update-streams-request-max-results>
  }
  list-progress-update-streams: func(params: list-progress-update-streams-params) -> result<list-progress-update-streams-result, list-progress-update-streams-error>;

  record notify-application-state-params {
    x-amz-content-sha256: option<string>,
    x-amz-date: option<string>,
    x-amz-algorithm: option<string>,
    x-amz-credential: option<string>,
    x-amz-security-token: option<string>,
    x-amz-signature: option<string>,
    x-amz-signed-headers: option<string>,
    x-amz-target: notify-application-state-x-amz-target-enum,
    application-id: notify-application-state-request-application-id,
    status: notify-application-state-request-status,
    update-date-time: option<notify-application-state-request-update-date-time>,
    dry-run: option<notify-application-state-request-dry-run>
  }
  notify-application-state: func(params: notify-application-state-params) -> result<notify-application-state-result, notify-application-state-error>;

  record notify-migration-task-state-params {
    x-amz-content-sha256: option<string>,
    x-amz-date: option<string>,
    x-amz-algorithm: option<string>,
    x-amz-credential: option<string>,
    x-amz-security-token: option<string>,
    x-amz-signature: option<string>,
    x-amz-signed-headers: option<string>,
    x-amz-target: notify-migration-task-state-x-amz-target-enum,
    progress-update-stream: notify-migration-task-state-request-progress-update-stream,
    migration-task-name: notify-migration-task-state-request-migration-task-name,
    task: notify-migration-task-state-request-task,
    update-date-time: notify-migration-task-state-request-update-date-time,
    next-update-seconds: notify-migration-task-state-request-next-update-seconds,
    dry-run: option<notify-migration-task-state-request-dry-run>
  }
  notify-migration-task-state: func(params: notify-migration-task-state-params) -> result<notify-migration-task-state-result, notify-migration-task-state-error>;

  record put-resource-attributes-params {
    x-amz-content-sha256: option<string>,
    x-amz-date: option<string>,
    x-amz-algorithm: option<string>,
    x-amz-credential: option<string>,
    x-amz-security-token: option<string>,
    x-amz-signature: option<string>,
    x-amz-signed-headers: option<string>,
    x-amz-target: put-resource-attributes-x-amz-target-enum,
    progress-update-stream: put-resource-attributes-request-progress-update-stream,
    migration-task-name: put-resource-attributes-request-migration-task-name,
    resource-attribute-list: put-resource-attributes-request-resource-attribute-list,
    dry-run: option<put-resource-attributes-request-dry-run>
  }
  put-resource-attributes: func(params: put-resource-attributes-params) -> result<put-resource-attributes-result, put-resource-attributes-error>;

}

/// The component world: imports the secrets host capability
/// the runtime uses and exports the generated interfaces. The HTTP host
/// capability is imported by the `wstd` crate's bindings at build time.
world client {
  import wasmcloud:secrets/store@1.0.0;
  import wasmcloud:secrets/reveal@1.0.0;
  export aws-migration-hub;
}
