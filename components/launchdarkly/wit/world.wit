package autostamp:launchdarkly@0.3.0;

interface root {
  get-root: func() -> result<string, string>;

}


interface relay-proxy-configurations {
  /// Returns a list of relay proxy configurations in the account.
  get-relay-proxy-configs: func() -> result<string, string>;

  /// Create a new relay proxy config.
  post-relay-auto-config: func() -> result<string, string>;

  /// Get a single relay proxy configuration by ID.
  record get-relay-proxy-config-params {
    /// The relay proxy configuration ID
    id: string
  }
  get-relay-proxy-config: func(params: get-relay-proxy-config-params) -> result<string, string>;

  /// Modify a relay proxy configuration by ID.
  record patch-relay-proxy-config-params {
    /// The relay proxy configuration ID
    id: string
  }
  patch-relay-proxy-config: func(params: patch-relay-proxy-config-params) -> result<string, string>;

  /// Delete a relay proxy configuration by ID.
  record delete-relay-proxy-config-params {
    /// The relay proxy configuration ID
    id: string
  }
  delete-relay-proxy-config: func(params: delete-relay-proxy-config-params) -> result<string, string>;

  /// Reset a relay proxy configuration's secret key with an optional expiry time for the old key.
  record reset-relay-proxy-config-params {
    /// The relay proxy configuration ID
    id: string,
    /// An expiration time for the old relay proxy configuration key, expressed as a Unix epoch time in milliseconds. By default, the relay proxy configuration will expire immediately
    expiry: option<s64>
  }
  reset-relay-proxy-config: func(params: reset-relay-proxy-config-params) -> result<string, string>;

}

interface audit-log {
  /// Get a list of all audit log entries. The query parameters allow you to restrict the returned results by date ranges, resource specifiers, or a full-text search query.
  record get-audit-log-entries-params {
    /// A timestamp filter, expressed as a Unix epoch time in milliseconds. All entries returned will have before this timestamp.
    before: option<s64>,
    /// A timestamp filter, expressed as a Unix epoch time in milliseconds. All entries returned will have occurred after this timestamp.
    after: option<s64>,
    /// Text to search for. You can search for the full or partial name of the resource involved or full or partial email address of the member who made the change.
    q: option<string>,
    /// A limit on the number of audit log entries to be returned, between 1 and 20.
    limit: option<f64>,
    /// A resource specifier, allowing you to filter audit log listings by resource.
    spec: option<string>
  }
  get-audit-log-entries: func(params: get-audit-log-entries-params) -> result<string, string>;

  /// Use this endpoint to fetch a single audit log entry by its resouce ID.
  record get-audit-log-entry-params {
    /// The resource ID.
    resource-id: string
  }
  get-audit-log-entry: func(params: get-audit-log-entry-params) -> result<string, string>;

}

interface data-export-destinations {
  /// Returns a list of all data export destinations.
  get-destinations: func() -> result<string, string>;

  /// Create a new data export destination
  record post-destination-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string
  }
  post-destination: func(params: post-destination-params) -> result<string, string>;

  /// Get a single data export destination by ID
  record get-destination-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The data export destination ID.
    destination-id: string
  }
  get-destination: func(params: get-destination-params) -> result<string, string>;

  /// Perform a partial update to a data export destination.
  record patch-destination-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The data export destination ID.
    destination-id: string
  }
  patch-destination: func(params: patch-destination-params) -> result<string, string>;

  /// Get a single data export destination by ID
  record delete-destination-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The data export destination ID.
    destination-id: string
  }
  delete-destination: func(params: delete-destination-params) -> result<string, string>;

}

interface feature-flags {
  /// Get the status for a particular feature flag across environments
  record get-feature-flag-status-across-environments-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The feature flag's key. The key identifies the flag in your code.
    feature-flag-key: string
  }
  get-feature-flag-status-across-environments: func(params: get-feature-flag-status-across-environments-params) -> result<string, string>;

  /// Get a list of statuses for all feature flags. The status includes the last time the feature flag was requested, as well as the state of the flag.
  record get-feature-flag-statuses-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string
  }
  get-feature-flag-statuses: func(params: get-feature-flag-statuses-params) -> result<string, string>;

  /// Get the status for a particular feature flag.
  record get-feature-flag-status-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The feature flag's key. The key identifies the flag in your code.
    feature-flag-key: string
  }
  get-feature-flag-status: func(params: get-feature-flag-status-params) -> result<string, string>;

  /// Get a list of all features in the given project.
  record get-feature-flags-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// By default, each feature will include configurations for each environment. You can filter environments with the env query parameter. For example, setting env=["production"] will restrict the returned configurations to just your production environment.
    env: option<list<string>>,
    /// By default in api version >= 1, flags will _not_ include their list of prerequisites, targets or rules.  Set summary=0 to include these fields for each flag returned.
    summary: option<bool>,
    /// When set to 1, only archived flags will be included in the list of flags returned.  By default, archived flags are not included in the list of flags.
    archived: option<bool>,
    /// The number of objects to return. Defaults to -1, which returns everything.
    limit: option<f64>,
    /// Where to start in the list. This is for use with pagination. For example, an offset of 10 would skip the first 10 items and then return the next limit items.
    offset: option<f64>,
    /// A comma-separated list of filters. Each filter is of the form field:value.
    filter: option<string>,
    /// A comma-separated list of fields to sort by. A field prefixed by a - will be sorted in descending order.
    sort: option<string>,
    /// Filter by tag. A tag can be used to group flags across projects.
    tag: option<string>
  }
  get-feature-flags: func(params: get-feature-flags-params) -> result<string, string>;

  /// Creates a new feature flag.
  record post-feature-flag-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The key of the feature flag to be cloned. The key identifies the flag in your code.  For example, setting clone=flagKey will copy the full targeting configuration for all environments (including on/off state) from the original flag to the new flag.
    clone: option<string>
  }
  post-feature-flag: func(params: post-feature-flag-params) -> result<string, string>;

  /// Get dependent flags for the flag in the environment specified in path parameters
  record get-flags-project-key-environment-key-feature-flag-key-dependent-flags-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The feature flag's key. The key identifies the flag in your code.
    feature-flag-key: string
  }
  get-flags-project-key-environment-key-feature-flag-key-dependent-flags: func(params: get-flags-project-key-environment-key-feature-flag-key-dependent-flags-params) -> result<string, string>;

  /// Get a single feature flag by key.
  record get-feature-flag-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The feature flag's key. The key identifies the flag in your code.
    feature-flag-key: string,
    /// By default, each feature will include configurations for each environment. You can filter environments with the env query parameter. For example, setting env=["production"] will restrict the returned configurations to just your production environment.
    env: option<list<string>>
  }
  get-feature-flag: func(params: get-feature-flag-params) -> result<string, string>;

  /// Perform a partial update to a feature.
  record patch-feature-flag-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The feature flag's key. The key identifies the flag in your code.
    feature-flag-key: string
  }
  patch-feature-flag: func(params: patch-feature-flag-params) -> result<string, string>;

  /// Delete a feature flag in all environments. Be careful-- only delete feature flags that are no longer being used by your application.
  record delete-feature-flag-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The feature flag's key. The key identifies the flag in your code.
    feature-flag-key: string
  }
  delete-feature-flag: func(params: delete-feature-flag-params) -> result<string, string>;

  /// Copies the feature flag configuration from one environment to the same feature flag in another environment.
  record copy-feature-flag-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The feature flag's key. The key identifies the flag in your code.
    feature-flag-key: string
  }
  copy-feature-flag: func(params: copy-feature-flag-params) -> result<string, string>;

  /// Get dependent flags across all environments for the flag specified in the path parameters
  record get-flags-project-key-feature-flag-key-dependent-flags-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The feature flag's key. The key identifies the flag in your code.
    feature-flag-key: string
  }
  get-flags-project-key-feature-flag-key-dependent-flags: func(params: get-flags-project-key-feature-flag-key-dependent-flags-params) -> result<string, string>;

  /// Get expiring user targets for feature flag
  record get-expiring-user-targets-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The feature flag's key. The key identifies the flag in your code.
    feature-flag-key: string
  }
  get-expiring-user-targets: func(params: get-expiring-user-targets-params) -> result<string, string>;

  /// Update, add, or delete expiring user targets on feature flag
  record patch-expiring-user-targets-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The feature flag's key. The key identifies the flag in your code.
    feature-flag-key: string
  }
  patch-expiring-user-targets: func(params: patch-expiring-user-targets-params) -> result<string, string>;

  /// Get all approval requests for a feature flag config
  record get-approval-requests-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The feature flag's key. The key identifies the flag in your code.
    feature-flag-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string
  }
  get-approval-requests: func(params: get-approval-requests-params) -> result<string, string>;

  /// Get a single approval request for a feature flag config
  record get-approval-request-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The feature flag's key. The key identifies the flag in your code.
    feature-flag-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The approval request ID
    approval-request-id: string
  }
  get-approval-request: func(params: get-approval-request-params) -> result<string, string>;

  /// Create an approval request for a feature flag config
  record post-approval-request-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The feature flag's key. The key identifies the flag in your code.
    feature-flag-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The approval request ID
    approval-request-id: string
  }
  post-approval-request: func(params: post-approval-request-params) -> result<string, string>;

  /// Delete an approval request for a feature flag config
  record delete-approval-request-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The feature flag's key. The key identifies the flag in your code.
    feature-flag-key: string,
    /// The approval request ID
    approval-request-id: string
  }
  delete-approval-request: func(params: delete-approval-request-params) -> result<string, string>;

  /// Apply approval request for a feature flag config
  record post-apply-approval-request-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The feature flag's key. The key identifies the flag in your code.
    feature-flag-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The approval request ID
    approval-request-id: string
  }
  post-apply-approval-request: func(params: post-apply-approval-request-params) -> result<string, string>;

  /// Review approval request for a feature flag config
  record post-review-approval-request-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The feature flag's key. The key identifies the flag in your code.
    feature-flag-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The approval request ID
    approval-request-id: string
  }
  post-review-approval-request: func(params: post-review-approval-request-params) -> result<string, string>;

  /// Get all scheduled workflows for a feature flag by key.
  record get-flag-config-scheduled-changes-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The feature flag's key. The key identifies the flag in your code.
    feature-flag-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string
  }
  get-flag-config-scheduled-changes: func(params: get-flag-config-scheduled-changes-params) -> result<string, string>;

  /// Creates a new scheduled change for a feature flag.
  record post-flag-config-scheduled-changes-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The feature flag's key. The key identifies the flag in your code.
    feature-flag-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string
  }
  post-flag-config-scheduled-changes: func(params: post-flag-config-scheduled-changes-params) -> result<string, string>;

  /// Lists conflicts between the given instructions and any existing scheduled changes for the feature flag. The actual HTTP verb should be REPORT, not POST.
  record get-flag-config-scheduled-changes-conflicts-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The feature flag's key. The key identifies the flag in your code.
    feature-flag-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string
  }
  get-flag-config-scheduled-changes-conflicts: func(params: get-flag-config-scheduled-changes-conflicts-params) -> result<string, string>;

  /// Get a scheduled change on a feature flag by id.
  record get-flag-config-scheduled-change-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The feature flag's key. The key identifies the flag in your code.
    feature-flag-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The id of the scheduled change
    scheduled-change-id: string
  }
  get-flag-config-scheduled-change: func(params: get-flag-config-scheduled-change-params) -> result<string, string>;

  /// Updates an existing scheduled-change on a feature flag in an environment.
  record patch-flag-config-scheduled-change-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The feature flag's key. The key identifies the flag in your code.
    feature-flag-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The id of the scheduled change
    scheduled-change-id: string
  }
  patch-flag-config-scheduled-change: func(params: patch-flag-config-scheduled-change-params) -> result<string, string>;

  /// Delete a scheduled change on a feature flag in an environment.
  record delete-flag-config-scheduled-changes-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The feature flag's key. The key identifies the flag in your code.
    feature-flag-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The id of the scheduled change
    scheduled-change-id: string
  }
  delete-flag-config-scheduled-changes: func(params: delete-flag-config-scheduled-changes-params) -> result<string, string>;

}

interface integrations {
  /// Get a list of all configured audit log event integrations associated with this account.
  get-integrations: func() -> result<string, string>;

  /// Get a list of all configured integrations of a given kind.
  record get-integration-subscriptions-params {
    /// The key used to specify the integration kind.
    integration-key: string
  }
  get-integration-subscriptions: func(params: get-integration-subscriptions-params) -> result<string, string>;

  /// Create a new integration subscription of a given kind.
  record post-integration-subscription-params {
    /// The key used to specify the integration kind.
    integration-key: string
  }
  post-integration-subscription: func(params: post-integration-subscription-params) -> result<string, string>;

  /// Get a single integration subscription by ID.
  record get-integration-subscription-params {
    /// The key used to specify the integration kind.
    integration-key: string,
    /// The integration ID.
    integration-id: string
  }
  get-integration-subscription: func(params: get-integration-subscription-params) -> result<string, string>;

  /// Modify an integration subscription by ID.
  record patch-integration-subscription-params {
    /// The key used to specify the integration kind.
    integration-key: string,
    /// The integration ID.
    integration-id: string
  }
  patch-integration-subscription: func(params: patch-integration-subscription-params) -> result<string, string>;

  /// Delete an integration subscription by ID.
  record delete-integration-subscription-params {
    /// The key used to specify the integration kind.
    integration-key: string,
    /// The integration ID.
    integration-id: string
  }
  delete-integration-subscription: func(params: delete-integration-subscription-params) -> result<string, string>;

}

interface team-members {
  /// Returns a list of all members in the account.
  record get-members-params {
    /// The number of objects to return. Defaults to -1, which returns everything.
    limit: option<f64>,
    /// Where to start in the list. This is for use with pagination. For example, an offset of 10 would skip the first 10 items and then return the next limit items.
    offset: option<f64>,
    /// A comma-separated list of filters. Each filter is of the form field:value.
    filter: option<string>,
    /// A comma-separated list of fields to sort by. A field prefixed by a - will be sorted in descending order.
    sort: option<string>
  }
  get-members: func(params: get-members-params) -> result<string, string>;

  /// Invite new members.
  post-members: func() -> result<string, string>;

  /// Get the current team member associated with the token
  get-me: func() -> result<string, string>;

  /// Get a single team member by ID.
  record get-member-params {
    /// The member ID.
    member-id: string
  }
  get-member: func(params: get-member-params) -> result<string, string>;

  /// Modify a team member by ID.
  record patch-member-params {
    /// The member ID.
    member-id: string
  }
  patch-member: func(params: patch-member-params) -> result<string, string>;

  /// Delete a team member by ID.
  record delete-member-params {
    /// The member ID.
    member-id: string
  }
  delete-member: func(params: delete-member-params) -> result<string, string>;

}

interface projects {
  /// Returns a list of all projects in the account.
  get-projects: func() -> result<string, string>;

  /// Create a new project with the given key and name.
  post-project: func() -> result<string, string>;

  /// Fetch a single project by key.
  record get-project-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string
  }
  get-project: func(params: get-project-params) -> result<string, string>;

  /// Modify a project by ID.
  record patch-project-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string
  }
  patch-project: func(params: patch-project-params) -> result<string, string>;

  /// Delete a project by key. Caution-- deleting a project will delete all associated environments and feature flags. You cannot delete the last project in an account.
  record delete-project-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string
  }
  delete-project: func(params: delete-project-params) -> result<string, string>;

}

interface environments {
  /// Create a new environment in a specified project with a given name, key, and swatch color.
  record post-environment-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string
  }
  post-environment: func(params: post-environment-params) -> result<string, string>;

  /// Get an environment given a project and key.
  record get-environment-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string
  }
  get-environment: func(params: get-environment-params) -> result<string, string>;

  /// Modify an environment by ID. If you try to patch the environment by setting both required and requiredApprovalTags, it will result in an error. Users can specify either required approvals for all flags in an environment or those with specific tags, but not both. Only customers on an Enterprise plan can require approval for flag updates with either mechanism.
  record patch-environment-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string
  }
  patch-environment: func(params: patch-environment-params) -> result<string, string>;

  /// Delete an environment in a specific project.
  record delete-environment-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string
  }
  delete-environment: func(params: delete-environment-params) -> result<string, string>;

  /// Reset an environment's SDK key with an optional expiry time for the old key.
  record reset-environment-sdk-key-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// An expiration time for the old environment SDK key, expressed as a Unix epoch time in milliseconds. By default, the key will expire immediately.
    expiry: option<s64>
  }
  reset-environment-sdk-key: func(params: reset-environment-sdk-key-params) -> result<string, string>;

  /// Reset an environment's mobile key. The optional expiry for the old key is deprecated for this endpoint, so the old key will always expire immediately.
  record reset-environment-mobile-key-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The expiry parameter is deprecated for this endpoint, so the old mobile key will always expire immediately. This parameter will be removed in an upcoming major API client version.
    expiry: option<s64>
  }
  reset-environment-mobile-key: func(params: reset-environment-mobile-key-params) -> result<string, string>;

}

interface custom-roles {
  /// Return a complete list of custom roles.
  get-custom-roles: func() -> result<string, string>;

  /// Create a new custom role.
  post-custom-role: func() -> result<string, string>;

  /// Get one custom role by key.
  record get-custom-role-params {
    /// The custom role key.
    custom-role-key: string
  }
  get-custom-role: func(params: get-custom-role-params) -> result<string, string>;

  /// Modify a custom role by key.
  record patch-custom-role-params {
    /// The custom role key.
    custom-role-key: string
  }
  patch-custom-role: func(params: patch-custom-role-params) -> result<string, string>;

  /// Delete a custom role by key.
  record delete-custom-role-params {
    /// The custom role key.
    custom-role-key: string
  }
  delete-custom-role: func(params: delete-custom-role-params) -> result<string, string>;

}

interface user-segments {
  /// Get a list of all user segments in the given project.
  record get-user-segments-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// Filter by tag. A tag can be used to group flags across projects.
    tag: option<string>
  }
  get-user-segments: func(params: get-user-segments-params) -> result<string, string>;

  /// Creates a new user segment.
  record post-user-segment-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string
  }
  post-user-segment: func(params: post-user-segment-params) -> result<string, string>;

  /// Get a single user segment by key.
  record get-user-segment-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The user segment's key. The key identifies the user segment in your code.
    user-segment-key: string
  }
  get-user-segment: func(params: get-user-segment-params) -> result<string, string>;

  /// Perform a partial update to a user segment.
  record patch-user-segment-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The user segment's key. The key identifies the user segment in your code.
    user-segment-key: string
  }
  patch-user-segment: func(params: patch-user-segment-params) -> result<string, string>;

  /// Delete a user segment.
  record delete-user-segment-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The user segment's key. The key identifies the user segment in your code.
    user-segment-key: string
  }
  delete-user-segment: func(params: delete-user-segment-params) -> result<string, string>;

  /// Update targets included or excluded in a big segment
  record update-big-segment-targets-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The user segment's key. The key identifies the user segment in your code.
    user-segment-key: string
  }
  update-big-segment-targets: func(params: update-big-segment-targets-params) -> result<string, string>;

  /// Get expiring user targets for user segment
  record get-expiring-user-targets-on-segment-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The user segment's key. The key identifies the user segment in your code.
    user-segment-key: string
  }
  get-expiring-user-targets-on-segment: func(params: get-expiring-user-targets-on-segment-params) -> result<string, string>;

  /// Update, add, or delete expiring user targets on user segment
  record patch-expiring-user-targets-on-segment-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The user segment's key. The key identifies the user segment in your code.
    user-segment-key: string
  }
  patch-expiring-user-targets-on-segment: func(params: patch-expiring-user-targets-on-segment-params) -> result<string, string>;

}

interface access-tokens {
  /// Returns a list of tokens in the account.
  record get-tokens-params {
    /// If set to true, and the authentication access token has the "Admin" role, personal access tokens for all members will be retrieved.
    show-all: option<bool>
  }
  get-tokens: func(params: get-tokens-params) -> result<string, string>;

  /// Create a new token.
  post-token: func() -> result<string, string>;

  /// Get a single access token by ID.
  record get-token-params {
    /// The access token ID.
    token-id: string
  }
  get-token: func(params: get-token-params) -> result<string, string>;

  /// Modify an access token by ID.
  record patch-token-params {
    /// The access token ID.
    token-id: string
  }
  patch-token: func(params: patch-token-params) -> result<string, string>;

  /// Delete an access token by ID.
  record delete-token-params {
    /// The access token ID.
    token-id: string
  }
  delete-token: func(params: delete-token-params) -> result<string, string>;

  /// Reset an access token's secret key with an optional expiry time for the old key.
  record reset-token-params {
    /// The access token ID.
    token-id: string,
    /// An expiration time for the old token key, expressed as a Unix epoch time in milliseconds. By default, the token will expire immediately.
    expiry: option<s64>
  }
  reset-token: func(params: reset-token-params) -> result<string, string>;

}

interface customer-metrics {
  /// Returns of the usage endpoints available.
  get-usage: func() -> result<string, string>;

  /// Get events usage by event id and the feature flag key.
  record get-evaluations-params {
    /// The environment id for the flag evaluations in question.
    env-id: string,
    /// The key of the flag we want metrics for.
    flag-key: string
  }
  get-evaluations: func(params: get-evaluations-params) -> result<string, string>;

  /// Get events usage endpoints.
  get-events: func() -> result<string, string>;

  /// Get events usage by event type.
  record get-event-params {
    /// The type of event we would like to track.
    type-op: string
  }
  get-event: func(params: get-event-params) -> result<string, string>;

  /// Get monthly active user data.
  get-mau: func() -> result<string, string>;

  /// Get monthly active user data by category.
  get-mau-by-category: func() -> result<string, string>;

  /// Returns a list of all streams.
  get-streams: func() -> result<string, string>;

  /// Get a stream endpoint and return timeseries data.
  record get-stream-params {
    /// The source of where the stream comes from.
    source: string
  }
  get-stream: func(params: get-stream-params) -> result<string, string>;

  /// Get a stream timeseries data by source show sdk version metadata.
  record get-stream-by-sdk-params {
    /// The source of where the stream comes from.
    source: string
  }
  get-stream-by-sdk: func(params: get-stream-by-sdk-params) -> result<string, string>;

  /// Get a stream timeseries data by source and show all sdk version associated.
  record get-stream-sdk-version-params {
    /// The source of where the stream comes from.
    source: string
  }
  get-stream-sdk-version: func(params: get-stream-sdk-version-params) -> result<string, string>;

}

interface users {
  /// Search users in LaunchDarkly based on their last active date, or a search query. It should not be used to enumerate all users in LaunchDarkly-- use the List users API resource.
  record get-search-users-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// Search query.
    q: option<string>,
    /// Pagination limit.
    limit: option<s32>,
    /// Specifies the first item to return in the collection.
    offset: option<s32>,
    /// A timestamp filter, expressed as a Unix epoch time in milliseconds. All entries returned will have occurred after this timestamp.
    after: option<s64>
  }
  get-search-users: func(params: get-search-users-params) -> result<string, string>;

  /// List all users in the environment. Includes the total count of users. In each page, there will be up to 'limit' users returned (default 20). This is useful for exporting all users in the system for further analysis. Paginated collections will include a next link containing a URL with the next set of elements in the collection.
  record get-users-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// Pagination limit.
    limit: option<s32>,
    /// This parameter is required when following "next" links.
    h: option<string>,
    /// This parameter is required when following "next" links.
    scroll-id: option<string>
  }
  get-users: func(params: get-users-params) -> result<string, string>;

  /// Get a user by key.
  record get-user-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The user's key.
    user-key: string
  }
  get-user: func(params: get-user-params) -> result<string, string>;

  /// Delete a user by ID.
  record delete-user-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The user's key.
    user-key: string
  }
  delete-user: func(params: delete-user-params) -> result<string, string>;

}

interface user-settings {
  /// Fetch a single flag setting for a user by key.
  record get-user-flag-settings-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The user's key.
    user-key: string
  }
  get-user-flag-settings: func(params: get-user-flag-settings-params) -> result<string, string>;

  /// Fetch a single flag setting for a user by key.
  record get-user-flag-setting-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The user's key.
    user-key: string,
    /// The feature flag's key. The key identifies the flag in your code.
    feature-flag-key: string
  }
  get-user-flag-setting: func(params: get-user-flag-setting-params) -> result<string, string>;

  /// Specifically enable or disable a feature flag for a user based on their key.
  record put-flag-setting-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The user's key.
    user-key: string,
    /// The feature flag's key. The key identifies the flag in your code.
    feature-flag-key: string
  }
  put-flag-setting: func(params: put-flag-setting-params) -> result<string, string>;

  /// Get expiring dates on flags for user
  record get-expiring-user-targets-for-user-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The user's key.
    user-key: string
  }
  get-expiring-user-targets-for-user: func(params: get-expiring-user-targets-for-user-params) -> result<string, string>;

  /// Update, add, or delete expiring user targets for a single user on all flags
  record patch-expiring-user-targets-for-flags-params {
    /// The project key, used to tie the flags together under one project so they can be managed together.
    project-key: string,
    /// The environment key, used to tie together flag configuration and users under one environment so they can be managed together.
    environment-key: string,
    /// The user's key.
    user-key: string
  }
  patch-expiring-user-targets-for-flags: func(params: patch-expiring-user-targets-for-flags-params) -> result<string, string>;

}

interface webhooks {
  /// Fetch a list of all webhooks.
  get-webhooks: func() -> result<string, string>;

  /// Create a webhook.
  post-webhook: func() -> result<string, string>;

  /// Get a webhook by ID.
  record get-webhook-params {
    /// The resource ID.
    resource-id: string
  }
  get-webhook: func(params: get-webhook-params) -> result<string, string>;

  /// Modify a webhook by ID.
  record patch-webhook-params {
    /// The resource ID.
    resource-id: string
  }
  patch-webhook: func(params: patch-webhook-params) -> result<string, string>;

  /// Delete a webhook by ID.
  record delete-webhook-params {
    /// The resource ID.
    resource-id: string
  }
  delete-webhook: func(params: delete-webhook-params) -> result<string, string>;

}
/// The component world: imports the secrets host capability
/// the runtime uses and exports the generated interfaces. The HTTP host
/// capability is imported by the `wstd` crate's bindings at build time.
world client {
  import wasmcloud:secrets/store@1.0.0;
  import wasmcloud:secrets/reveal@1.0.0;
  export root;
  export relay-proxy-configurations;
  export audit-log;
  export data-export-destinations;
  export feature-flags;
  export integrations;
  export team-members;
  export projects;
  export environments;
  export custom-roles;
  export user-segments;
  export access-tokens;
  export customer-metrics;
  export users;
  export user-settings;
  export webhooks;
}
